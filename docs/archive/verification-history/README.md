# Historical Verification Reports

This directory stores stale generated verification output for audit history only.
Do not treat files here as current pass/fail truth.

Current verification truth lives in the root `PROBLEMS.md` snapshot until a newer
repo-level verification report explicitly replaces it.

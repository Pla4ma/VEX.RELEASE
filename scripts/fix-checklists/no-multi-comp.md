# no-multi-comp — manual fix checklist

Diagnostics found: **56**.

Estimated human time: 14-28 minutes.

## Per-file fixes

### src/components/ui/Skeleton.tsx

- L92: This file declares several components, so each component is harder to find, test, and change.
- L144: This file declares several components, so each component is harder to find, test, and change.

### src/features/ai-coach/components/primitives/error-state.tsx

- L139: This file declares several components, so each component is harder to find, test, and change.
- L151: This file declares several components, so each component is harder to find, test, and change.

### src/features/ai-coach/components/primitives/progress-indicators.tsx

- L75: This file declares several components, so each component is harder to find, test, and change.
- L113: This file declares several components, so each component is harder to find, test, and change.

### src/features/ai-coach/components/primitives/progress-state.tsx

- L80: This file declares several components, so each component is harder to find, test, and change.
- L117: This file declares several components, so each component is harder to find, test, and change.

### src/features/companion/components/companion-evolution-effects.tsx

- L42: This file declares several components, so each component is harder to find, test, and change.
- L84: This file declares several components, so each component is harder to find, test, and change.

### src/features/content-study/components/SkeletonCards.tsx

- L97: This file declares several components, so each component is harder to find, test, and change.
- L133: This file declares several components, so each component is harder to find, test, and change.

### src/features/content-study/screens/ContentReviewScreen.helpers.tsx

- L62: This file declares several components, so each component is harder to find, test, and change.
- L163: This file declares several components, so each component is harder to find, test, and change.

### src/features/home-spine/components/BossPreviewCard.subcomponents.tsx

- L42: This file declares several components, so each component is harder to find, test, and change.
- L122: This file declares several components, so each component is harder to find, test, and change.

### src/features/home-spine/components/GreetingHeaderParts.tsx

- L45: This file declares several components, so each component is harder to find, test, and change.
- L73: This file declares several components, so each component is harder to find, test, and change.

### src/features/home-spine/components/TodaysChallengesStates.tsx

- L48: This file declares several components, so each component is harder to find, test, and change.
- L71: This file declares several components, so each component is harder to find, test, and change.

### src/features/home-spine/components/streak-widget-display.tsx

- L46: This file declares several components, so each component is harder to find, test, and change.
- L90: This file declares several components, so each component is harder to find, test, and change.

### src/features/monthly-report/components/report-content-helpers.tsx

- L43: This file declares several components, so each component is harder to find, test, and change.
- L72: This file declares several components, so each component is harder to find, test, and change.

### src/features/progression/components/level-up-subcomponents.tsx

- L51: This file declares several components, so each component is harder to find, test, and change.
- L91: This file declares several components, so each component is harder to find, test, and change.

### src/features/project-focus/components/ProjectHomeCardStates.tsx

- L42: This file declares several components, so each component is harder to find, test, and change.
- L84: This file declares several components, so each component is harder to find, test, and change.

### src/features/session-runtime/components/BossDamagePreview.styles.tsx

- L46: This file declares several components, so each component is harder to find, test, and change.
- L101: This file declares several components, so each component is harder to find, test, and change.

### src/features/session-runtime/components/QualityIndicator-helpers.tsx

- L99: This file declares several components, so each component is harder to find, test, and change.
- L126: This file declares several components, so each component is harder to find, test, and change.

### src/features/session-runtime/components/SquadSyncStates.tsx

- L23: This file declares several components, so each component is harder to find, test, and change.
- L42: This file declares several components, so each component is harder to find, test, and change.

### src/features/streaks/components/ComebackQuestSubcomponents.tsx

- L94: This file declares several components, so each component is harder to find, test, and change.
- L125: This file declares several components, so each component is harder to find, test, and change.

### src/features/streaks/components/streakGamblePrompt/OutcomeViews.tsx

- L63: This file declares several components, so each component is harder to find, test, and change.
- L127: This file declares several components, so each component is harder to find, test, and change.

### src/screens/home/HomeHero.helpers.tsx

- L62: This file declares several components, so each component is harder to find, test, and change.
- L87: This file declares several components, so each component is harder to find, test, and change.

### src/screens/notifications/NotificationComponents.tsx

- L63: This file declares several components, so each component is harder to find, test, and change.
- L158: This file declares several components, so each component is harder to find, test, and change.

### src/screens/paywall/PaywallHero.defaults.tsx

- L106: This file declares several components, so each component is harder to find, test, and change.
- L167: This file declares several components, so each component is harder to find, test, and change.

### src/screens/paywall/PaywallHero.lanes.tsx

- L34: This file declares several components, so each component is harder to find, test, and change.
- L76: This file declares several components, so each component is harder to find, test, and change.

### src/screens/profile/AchievementCategorySection.tsx

- L109: This file declares several components, so each component is harder to find, test, and change.
- L125: This file declares several components, so each component is harder to find, test, and change.

### src/screens/session/components/FirstSessionOptionalCards.tsx

- L31: This file declares several components, so each component is harder to find, test, and change.
- L59: This file declares several components, so each component is harder to find, test, and change.

### src/session/components/BossDamagePreview.styles.tsx

- L45: This file declares several components, so each component is harder to find, test, and change.
- L99: This file declares several components, so each component is harder to find, test, and change.

### src/session/components/QualityIndicator-helpers.tsx

- L98: This file declares several components, so each component is harder to find, test, and change.
- L125: This file declares several components, so each component is harder to find, test, and change.

### src/session/components/SquadSyncStates.tsx

- L23: This file declares several components, so each component is harder to find, test, and change.
- L42: This file declares several components, so each component is harder to find, test, and change.

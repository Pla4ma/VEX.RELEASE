# rn-no-scrollview-mapped-list — manual fix checklist

Diagnostics found: **16**.

Estimated human time: 4-8 minutes.

## Per-file fixes

### src/features/analytics/components/MetricSelector.tsx

- L130: Your users get slow scrolling when <ScrollView> with items.

### src/features/analytics/components/TimeRangeFilter.tsx

- L48: Your users get slow scrolling when <ScrollView> with items.

### src/features/challenges/components/ChallengeHub.skeleton.tsx

- L43: Your users get slow scrolling when <ScrollView> with items.

### src/features/challenges/components/ChallengeHub.tsx

- L95: Your users get slow scrolling when <ScrollView> with items.

### src/features/content-study/components/QuizPanel.tsx

- L113: Your users get slow scrolling when <ScrollView> with items.

### src/features/content-study/components/StudyTaskList.tsx

- L93: Your users get slow scrolling when <ScrollView> with items.

### src/features/progression/first-week-pacing/DeeperModePrompt.tsx

- L68: Your users get slow scrolling when <ScrollView> with items.

### src/features/session-runtime/components/SessionHistory.tsx

- L90: Your users get slow scrolling when <ScrollView> with items.
- L118: Your users get slow scrolling when <ScrollView> with items.

### src/features/session-runtime/components/SessionPresets.tsx

- L54: Your users get slow scrolling when <ScrollView> with items.

### src/features/settings/components/SettingsCategoryTabs.tsx

- L30: Your users get slow scrolling when <ScrollView> with items.

### src/screens/home/FocusScreen.tsx

- L85: Your users get slow scrolling when <ScrollView> with items.

### src/screens/settings/NotificationSettingsScreen.tsx

- L90: Your users get slow scrolling when <ScrollView> with items.

### src/session/components/SessionHistory.tsx

- L90: Your users get slow scrolling when <ScrollView> with items.
- L118: Your users get slow scrolling when <ScrollView> with items.

### src/session/components/SessionPresets.tsx

- L54: Your users get slow scrolling when <ScrollView> with items.

# rn-prefer-expo-image — manual fix checklist

Diagnostics found: **11**.

Estimated human time: 3-6 minutes.

## Per-file fixes

### src/components/Banner.tsx

- L7: Your users watch images reload often because Image from react-native has no caching.

### src/components/glass/RealisticModeOrb.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/components/glass/VexAssetImage.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/features/session-runtime/components/SquadMemberIndicator.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/screens/auth/components/ethereal/EtherealSkyBackground.tsx

- L2: Your users watch images reload often because ImageBackground from react-native has no caching.

### src/screens/home/components/Day0Mascot.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/screens/onboarding/components/ethereal/PngMascotRenderer.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/screens/session/SessionQuickStartCard.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/screens/session/components/SessionSmartPickCard.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/session/components/SquadMemberIndicator.tsx

- L2: Your users watch images reload often because Image from react-native has no caching.

### src/shared/ui/liquid-glass/LiquidGlassScreen.tsx

- L2: Your users watch images reload often because ImageBackground from react-native has no caching.

# no-inline-exhaustive-style — manual fix checklist

Diagnostics found: **60**.

Estimated human time: 15-30 minutes.

## Per-file fixes

### .agents/skills/remotion-video-creation/rules/assets/text-animations-word-highlight.tsx

- L49: This inline style has 10 properties, which is hard to read & rebuilds every render.

### src/animation/Particle.tsx

- L40: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/components/EmptyState.tsx

- L44: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/components/UnlockRequirementRow.tsx

- L23: This inline style has 10 properties, which is hard to read & rebuilds every render.

### src/components/glass/GlassSurface.overlays.tsx

- L57: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/components/glass/LiquidProgressBar.tsx

- L30: This inline style has 11 properties, which is hard to read & rebuilds every render.

### src/components/states/ErrorState.tsx

- L80: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/components/ui/StepIndicator.tsx

- L48: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/errors/ErrorFallback.tsx

- L70: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/ai-coach/components/CoachSessionBanner.tsx

- L104: This inline style has 14 properties, which is hard to read & rebuilds every render.

### src/features/ai-coach/components/DailyQuestCard.tsx

- L36: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/capture/components/CaptureSheet.tsx

- L53: This inline style has 17 properties, which is hard to read & rebuilds every render.

### src/features/content-study/components/TextPasteInput.tsx

- L67: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/home-spine/components/DayCheckRow.tsx

- L33: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/features/home-spine/components/GreetingHeader.tsx

- L135: This inline style has 11 properties, which is hard to read & rebuilds every render.

### src/features/mode-native/components/ModeQuickContract.tsx

- L98: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/features/onboarding/components/TooltipBubble.tsx

- L111: This inline style has 16 properties, which is hard to read & rebuilds every render.

### src/features/rescue-mode/components/RescueBanner.tsx

- L60: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/session-runtime/components/CompanionSessionLayer.tsx

- L37: This inline style has 9 properties, which is hard to read & rebuilds every render.
- L63: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/session-runtime/components/CreativeMoodLogger.tsx

- L52: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/session-start/components/AdaptiveDifficultyBanner.tsx

- L74: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/session-start/components/EmptyStakesMessage.tsx

- L12: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/features/session-start/components/StakeCard.tsx

- L65: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/navigation/components/VexTabBar.tsx

- L82: This inline style has 9 properties, which is hard to read & rebuilds every render.
- L112: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L126: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/navigation/components/VexTabBarGlassDecoration.tsx

- L25: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L38: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L51: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/screens/auth/components/LiquidMonogram.tsx

- L36: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/screens/auth/components/VexAuroraCanvas.tsx

- L31: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L45: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L72: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/screens/auth/components/VexLetter.tsx

- L61: This inline style has 9 properties, which is hard to read & rebuilds every render.
- L76: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/auth/components/VexPrimaryButton.tsx

- L54: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/home/components/GlassSettingsButton.tsx

- L25: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/home/containers/HomeTopBar.tsx

- L64: This inline style has 11 properties, which is hard to read & rebuilds every render.

### src/screens/home/containers/NotificationButton.tsx

- L19: This inline style has 10 properties, which is hard to read & rebuilds every render.

### src/screens/onboarding/components/ElementCard.tsx

- L54: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L100: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/profile/MasteryHeader.tsx

- L41: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/profile/components/ProfileGlassTabs.tsx

- L29: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/session/components/ActiveSessionControlDock.tsx

- L84: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L103: This inline style has 9 properties, which is hard to read & rebuilds every render.
- L153: This inline style has 10 properties, which is hard to read & rebuilds every render.

### src/screens/session/components/BossCombatHUDView.tsx

- L49: This inline style has 10 properties, which is hard to read & rebuilds every render.
- L64: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/session/components/SessionReflectionSheet.tsx

- L61: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L87: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/screens/session/components/VexControlDock.tsx

- L119: This inline style has 11 properties, which is hard to read & rebuilds every render.

### src/screens/settings/CoachSettingsScreen.tsx

- L137: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/screens/settings/ColorSchemeToggle.tsx

- L60: This inline style has 9 properties, which is hard to read & rebuilds every render.

### src/screens/settings/PasswordChangeSection.tsx

- L131: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/session/components/CompanionSessionLayer.tsx

- L37: This inline style has 9 properties, which is hard to read & rebuilds every render.
- L63: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/session/components/CreativeMoodLogger.tsx

- L52: This inline style has 8 properties, which is hard to read & rebuilds every render.

### src/shared/ui/liquid-glass/LiquidGlassScreen.tsx

- L49: This inline style has 8 properties, which is hard to read & rebuilds every render.
- L62: This inline style has 8 properties, which is hard to read & rebuilds every render.

perf: hoist Intl.DateTimeFormat, dedupe with Set, replace .sort()[0] with reduce

- Hoist DateTimeFormat to module scope where possible
- Add timezone-aware formatter cache for per-timezone formatters
- Replace .indexOf/.includes inside loops with Set lookups
- Replace .sort()[0] with .reduce() for min/max selection
- Extract helper functions from components to module scope
  (prefer-module-scope-pure-function)
- Destructured useQuery results in useActiveIntervention
- Extracted renderItem to renderPersonalBest (module scope)

Score 68, 0 errors, 799 warnings

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>

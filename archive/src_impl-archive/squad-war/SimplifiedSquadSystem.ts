import{eventBus}from'../../events'; export interface SimplifiedSquad{id:string;name:string;description:string;avatarUrl:string|null;createdAt:number;createdByUserId:string;members:SquadMember[];maxMembers:number;weeklyGoal:WeeklyGoal;streak:SquadStreak;recentActivity:SquadActivity[];activeBossEncounter:SquadBossEncounter|null;isPublic:boolean;joinCode:string|null;}export interface SquadMember{userId:string;userName:string;avatarUrl:string|null;role:'LEADER'|'MEMBER';joinedAt:number;weeklyFocusMinutes:number;lastSessionAt:number|null;streakContributing:boolean;isActive:boolean;}export interface WeeklyGoal{targetFocusHours:number;currentTotalHours:number;weekStartsAt:number;weekEndsAt:number;achieved:boolean;}export interface SquadStreak{currentWeeks:number;longestWeeks:number;lastAchievedAt:number|null;requiresAllMembers:boolean;}export interface SquadActivity{id:string;userId:string;userName:string;type:'SESSION_COMPLETE'|'BOSS_DAMAGE'|'STREAK_MILESTONE'|'GOAL_ACHIEVED';description:string;timestamp:number;metadata?:{duration?:number;damage?:number;streakDays?:number;};}export interface SquadBossEncounter{encounterId:string;bossId:string;bossName:string;startedAt:number;expiresAt:number;healthRemaining:number;maxHealth:number;participantDamage:Record<string,number>;status:'ACTIVE'|'DEFEATED'|'EXPIRED';}const squads = new Map<string,SimplifiedSquad>(); export function createSquad(creatorId:string,creatorName:string,name:string,description:string,isPublic:boolean = false):SimplifiedSquad{const now = Date.now(); const weekStart = getWeekStart(now); const squad:SimplifiedSquad = {id:`squad-${now}-${Math.random().toString(36).substr(2,9)}`,name,description,avatarUrl:null,createdAt:now,createdByUserId:creatorId,members:[{userId:creatorId,userName:creatorName,avatarUrl:null,role:'LEADER',joinedAt:now,weeklyFocusMinutes:0,lastSessionAt:null,streakContributing:false,isActive:true}],maxMembers:10,weeklyGoal:{targetFocusHours:10,currentTotalHours:0,weekStartsAt:weekStart,weekEndsAt:weekStart + 7 * 24 * 60 * 60 * 1000,achieved:false},streak:{currentWeeks:0,longestWeeks:0,lastAchievedAt:null,requiresAllMembers:true},recentActivity:[],activeBossEncounter:null,isPublic,joinCode:isPublic ? null : generateJoinCode()}; squads.set(squad.id,squad); eventBus.publish('squad:created',{squadId:squad.id,creatorId,name}); return squad;}function getWeekStart(timestamp:number):number{const date = new Date(timestamp); const day = date.getDay(); const diff = date.getDate() - day; const start = new Date(date.setDate(diff)); start.setHours(0,0,0,0); return start.getTime();}function generateJoinCode():string{return Math.random().toString(36).substring(2,8).toUpperCase();}export function joinSquad(squadId:string,userId:string,userName:string,joinCode?:string):{success:boolean;squad?:SimplifiedSquad;error?:string;}{const squad = squads.get(squadId); if(!squad){return{success:false,error:'Squad not found'};}if(!squad.isPublic && squad.joinCode !== joinCode){return{success:false,error:'Invalid join code'};}if(squad.members.length >= squad.maxMembers){return{success:false,error:'Squad is full (max 10 members)'};}if(squad.members.some(m=>m.userId === userId)){return{success:false,error:'Already a member'};}squad.members.push({userId,userName,avatarUrl:null,role:'MEMBER',joinedAt:Date.now(),weeklyFocusMinutes:0,lastSessionAt:null,streakContributing:false,isActive:true}); squad.weeklyGoal.targetFocusHours = Math.round(squad.members.length * 8); eventBus.publish('squad:member_joined',{squadId,userId,role:'member',userName,memberCount:squad.members.length}); return{success:true,squad};}export function leaveSquad(squadId:string,userId:string):boolean{const squad = squads.get(squadId); if(!squad){return false;}const memberIndex = squad.members.findIndex(m=>m.userId === userId); if(memberIndex === -1){return false;}const wasLeader = squad.members[memberIndex].role === 'LEADER'; squad.members.splice(memberIndex,1); if(wasLeader && squad.members.length > 0){squad.members[0].role = 'LEADER';}if(squad.members.length === 0){squads.delete(squadId); eventBus.publish('squad:disbanded',{squadId,userId,memberCount:0}); return true;}squad.weeklyGoal.targetFocusHours = Math.round(squad.members.length * 8); eventBus.publish('squad:member_left',{squadId,userId,wasFounder:userId === (squad as any).founderId,memberCount:squad.members.length}); return true;}export function recordMemberActivity(squadId:string,userId:string,durationMinutes:number):SimplifiedSquad{const squad = squads.get(squadId); if(!squad){throw new Error('Squad not found');}const member = squad.members.find(m=>m.userId === userId); if(!member){throw new Error('Not a squad member');}member.weeklyFocusMinutes += durationMinutes; member.lastSessionAt = Date.now(); member.isActive = true; member.streakContributing = true; const totalMinutes = squad.members.reduce((sum,m)=>sum + m.weeklyFocusMinutes,0); squad.weeklyGoal.currentTotalHours = Math.round(totalMinutes / 60 * 10) / 10; squad.weeklyGoal.achieved = squad.weeklyGoal.currentTotalHours >= squad.weeklyGoal.targetFocusHours; addActivity(squad,{id:`act-${Date.now()}`,userId,userName:member.userName,type:'SESSION_COMPLETE',description:`${member.userName} focused for ${Math.round(durationMinutes)} minutes`,timestamp:Date.now(),metadata:{duration:durationMinutes}}); if(squad.weeklyGoal.achieved && !squad.streak.lastAchievedAt){handleGoalAchieved(squad);}return squad;}function handleGoalAchieved(squad:SimplifiedSquad):void{squad.streak.currentWeeks += 1; squad.streak.lastAchievedAt = Date.now(); if(squad.streak.currentWeeks > squad.streak.longestWeeks){squad.streak.longestWeeks = squad.streak.currentWeeks;}addActivity(squad,{id:`act-${Date.now()}-goal`,userId:'system',userName:'Squad',type:'GOAL_ACHIEVED',description:`Weekly goal achieved! ${squad.streak.currentWeeks} week streak!`,timestamp:Date.now()}); eventBus.publish('squad:goal_achieved',{squadId:squad.id,goalId:`weekly-${squad.id}`,goalType:'weekly',achievedBy:squad.members.map(m=>m.userId)});}export function resetWeeklyProgress(squadId:string):SimplifiedSquad{const squad = squads.get(squadId); if(!squad){throw new Error('Squad not found');}const now = Date.now(); const weekStart = getWeekStart(now); const allMembersActive = squad.members.every(m=>m.streakContributing); if(!allMembersActive){squad.streak.currentWeeks = 0; eventBus.publish('squad:streak_broken',{squadId:squad.id,userId:'system',previousStreak:squad.streak.currentWeeks});}for(const member of squad.members){member.weeklyFocusMinutes = 0; member.streakContributing = false; member.isActive = member.lastSessionAt ? now - member.lastSessionAt < 7 * 24 * 60 * 60 * 1000 : false;}squad.weeklyGoal = {targetFocusHours:Math.round(squad.members.length * 8),currentTotalHours:0,weekStartsAt:weekStart,weekEndsAt:weekStart + 7 * 24 * 60 * 60 * 1000,achieved:false}; return squad;}function addActivity(squad:SimplifiedSquad,activity:SquadActivity):void{squad.recentActivity.unshift(activity); if(squad.recentActivity.length > 50){squad.recentActivity = squad.recentActivity.slice(0,50);}const sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000; squad.recentActivity = squad.recentActivity.filter(a=>a.timestamp > sevenDaysAgo);}export function getActivityFeed(squadId:string,limit:number = 20):SquadActivity[]{const squad = squads.get(squadId); if(!squad){return[];}return squad.recentActivity.slice(0,limit);}export function recordBossDamageActivity(squadId:string,userId:string,damage:number):void{const squad = squads.get(squadId); if(!squad){return;}const member = squad.members.find(m=>m.userId === userId); if(!member){return;}addActivity(squad,{id:`act-${Date.now()}`,userId,userName:member.userName,type:'BOSS_DAMAGE',description:`${member.userName} dealt ${damage} damage to the boss`,timestamp:Date.now(),metadata:{damage}});}export function startSquadBoss(squadId:string,bossId:string,bossName:string,baseHealth:number):{success:boolean;encounter?:SquadBossEncounter;error?:string;}{const squad = squads.get(squadId); if(!squad){return{success:false,error:'Squad not found'};}if(squad.activeBossEncounter?.status === 'ACTIVE'){return{success:false,error:'Squad already has an active boss'};}const now = Date.now(); const scaledHealth = Math.floor(baseHealth * (1 + squad.members.length * 0.2)); const encounter:SquadBossEncounter = {encounterId:`encounter-${now}`,bossId,bossName,startedAt:now,expiresAt:now + 48 * 60 * 60 * 1000,healthRemaining:scaledHealth,maxHealth:scaledHealth,participantDamage:{},status:'ACTIVE'}; squad.activeBossEncounter = encounter; addActivity(squad,{id:`act-${now}-boss`,userId:'system',userName:'Squad',type:'BOSS_DAMAGE',description:`Squad boss ${bossName} has appeared! Defeat it together!`,timestamp:now}); eventBus.publish('squad:boss_started',{squadId,encounterId:encounter.encounterId,bossId,bossName,memberCount:squad.members.length}); return{success:true,encounter};}export function damageSquadBoss(squadId:string,userId:string,damage:number):{success:boolean;isDefeated:boolean;encounter?:SquadBossEncounter;}{const squad = squads.get(squadId); if(!squad || !squad.activeBossEncounter){return{success:false,isDefeated:false};}const encounter = squad.activeBossEncounter; if(encounter.status !== 'ACTIVE'){return{success:false,isDefeated:false};}encounter.healthRemaining = Math.max(0,encounter.healthRemaining - damage); encounter.participantDamage[userId] = (encounter.participantDamage[userId] || 0) + damage; const isDefeated = encounter.healthRemaining === 0; if(isDefeated){encounter.status = 'DEFEATED'; handleBossDefeated(squad,encounter);}return{success:true,isDefeated,encounter};}function handleBossDefeated(squad:SimplifiedSquad,encounter:SquadBossEncounter):void{addActivity(squad,{id:`act-${Date.now()}-victory`,userId:'system',userName:'Squad',type:'BOSS_DAMAGE',description:`${encounter.bossName} defeated by the squad! Great teamwork!`,timestamp:Date.now()}); eventBus.publish('squad:boss_defeated',{squadId:squad.id,encounterId:encounter.encounterId,bossId:encounter.encounterId,bossName:encounter.bossName,totalDamage:encounter.maxHealth || 1000}as any);}export function discoverPublicSquads(limit:number = 20,excludeFull:boolean = true):SimplifiedSquad[]{const allSquads = Array.from(squads.values()); return allSquads.filter(s=>s.isPublic && (!excludeFull || s.members.length < s.maxMembers)).sort((a,b)=>b.members.length - a.members.length).slice(0,limit);}export function getUserSquads(userId:string):SimplifiedSquad[]{return Array.from(squads.values()).filter(s=>s.members.some(m=>m.userId === userId));}export function getSquad(squadId:string):SimplifiedSquad|null{return squads.get(squadId) || null;}export interface SquadSummary{id:string;name:string;memberCount:number;maxMembers:number;isFull:boolean;streakWeeks:number;weeklyProgress:number;recentActivityCount:number;hasActiveBoss:boolean;isMember:boolean;userRole?:'LEADER'|'MEMBER';}export function getSquadSummary(squadId:string,userId:string):SquadSummary|null{const squad = squads.get(squadId); if(!squad){return null;}const member = squad.members.find(m=>m.userId === userId); const progress = squad.weeklyGoal.targetFocusHours > 0 ? Math.min(100,squad.weeklyGoal.currentTotalHours / squad.weeklyGoal.targetFocusHours * 100) : 0; return{id:squad.id,name:squad.name,memberCount:squad.members.length,maxMembers:squad.maxMembers,isFull:squad.members.length >= squad.maxMembers,streakWeeks:squad.streak.currentWeeks,weeklyProgress:Math.round(progress),recentActivityCount:squad.recentActivity.length,hasActiveBoss:squad.activeBossEncounter?.status === 'ACTIVE',isMember:!!member,userRole:member?.role};}

# Archived Session Story Dead Components

Archived during Phase 0 hygiene because `StoryCard.tsx` was not imported by live source and depended on an already archived transformation error-boundary module.

Replacement path: use the production post-session story flow under `src/features/session-story/screens/PostSessionStoryScreen.tsx` and shared screen error-boundary components.

Phase 0 archive for code that stayed under `src` but was not imported by the active app flow or depended on archived systems.

The active replacements are:
- Session boss UI: `src/screens/session/components/BossCombatHUD.tsx` and `src/features/boss/components`.
- Retention: `src/features/emotion-retention`.
- AI coach live surfaces: `src/features/ai-coach/components/CoachScreen.tsx`, `CoachInterventionBanner.tsx`, and `CoachSessionBanner.tsx`.

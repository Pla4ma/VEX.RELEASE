import{useMemo,useCallback}from'react'; import{useActiveBoss as useActiveBossBase}from'../hooks'; export interface DamageCalculation{baseDamage:number;purityMultiplier:number;streakMultiplier:number;totalDamage:number;damagePerMinute:number;}export interface KillEstimate{willDefeat:boolean;sessionsRemaining:number;minutesRemaining:number;percentDamage:number;}export interface ActiveBossState{encounter:{id:string;bossId:string;bossName:string;bossAvatarUrl:string|null;healthRemaining:number;maxHealth:number;percentHealthRemaining:number;tier:'COMMON'|'UNCOMMON'|'RARE'|'EPIC'|'LEGENDARY';}|null;damageThisSession:DamageCalculation;estimatedKill:KillEstimate;combatState:'ENCOUNTER_START'|'COMBAT_ACTIVE'|'BOSS_RAGE'|'NEAR_DEATH'|'VICTORY';showDamageFlash:boolean;recentDamage:number;isLoading:boolean;error:Error|null;actions:{calculateDamage:(elapsedMinutes:number,purityScore:number)=>DamageCalculation;clearDamageFlash:()=>void;refresh:()=>void;};}function calculateBaseDamage(minutes:number):number{const baseRate = 6.67; const bonus = Math.max(0,(minutes - 15) * 0.5); return Math.floor(minutes * baseRate + bonus);}function calculatePurityMultiplier(purityScore:number):number{const minMultiplier = 0.5; const maxMultiplier = 2.0; return minMultiplier + purityScore / 100 * (maxMultiplier - minMultiplier);}export function useActiveBoss(userId:string|null,sessionId:string|null,sessionMetrics?:{elapsedMinutes:number;purityScore:number;streakDays:number;}):ActiveBossState{const{data:encounter,isLoading,error,refetch} = useActiveBossBase(userId ?? ''); const calculateDamage = useCallback((elapsedMinutes:number,purityScore:number):DamageCalculation=>{const baseDamage = calculateBaseDamage(elapsedMinutes); const purityMultiplier = calculatePurityMultiplier(purityScore); const streakMultiplier = 1 + (sessionMetrics?.streakDays ?? 0) * 0.1; const totalDamage = Math.floor(baseDamage * purityMultiplier * streakMultiplier); return{baseDamage,purityMultiplier,streakMultiplier,totalDamage,damagePerMinute:totalDamage / Math.max(1,elapsedMinutes)};},[sessionMetrics?.streakDays]); const damageThisSession = useMemo(()=>{const elapsedMinutes = sessionMetrics?.elapsedMinutes ?? 0; const purityScore = sessionMetrics?.purityScore ?? 80; return calculateDamage(elapsedMinutes,purityScore);},[sessionMetrics,calculateDamage]); const estimatedKill = useMemo(():KillEstimate=>{if(!encounter){return{willDefeat:false,sessionsRemaining:0,minutesRemaining:0,percentDamage:0};}const currentDamage = damageThisSession.totalDamage; const healthRemaining = encounter.healthRemaining; const willDefeat = currentDamage >= healthRemaining; const percentDamage = Math.min(100,currentDamage / encounter.maxHealth * 100); if(willDefeat){return{willDefeat:true,sessionsRemaining:1,minutesRemaining:sessionMetrics?.elapsedMinutes ?? 0,percentDamage};}const sessionsRemaining = currentDamage > 0 ? Math.ceil(healthRemaining / currentDamage) : 999; const minutesRemaining = sessionsRemaining * (sessionMetrics?.elapsedMinutes ?? 30); return{willDefeat:false,sessionsRemaining,minutesRemaining,percentDamage};},[encounter,damageThisSession,sessionMetrics?.elapsedMinutes]); const combatState = useMemo(():ActiveBossState['combatState']=>{if(!encounter){return'ENCOUNTER_START';}const percentHealth = encounter.percentHealthRemaining; if(percentHealth <= 0){return'VICTORY';}if(percentHealth <= 10){return'NEAR_DEATH';}if(percentHealth <= 25){return'BOSS_RAGE';}return'COMBAT_ACTIVE';},[encounter]); const showDamageFlash = false; const recentDamage = 0; return{encounter:encounter ? {id:encounter.id,bossId:encounter.bossId,bossName:encounter.bossName,bossAvatarUrl:encounter.bossAvatarUrl,healthRemaining:encounter.healthRemaining,maxHealth:encounter.maxHealth,percentHealthRemaining:encounter.percentHealthRemaining,tier:'COMMON'} : null,damageThisSession,estimatedKill,combatState,showDamageFlash,recentDamage,isLoading,error:error || null,actions:{calculateDamage,clearDamageFlash:()=>{},refresh:()=>{void (refetch)();}}};}export default useActiveBoss;

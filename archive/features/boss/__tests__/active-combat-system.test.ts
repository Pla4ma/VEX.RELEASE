import{describe,it,expect,beforeEach}from'@jest/globals'; import{executeCombatAbility,calculateBossPhase,getPhaseMultiplier,selectAttackPattern,resolveAttackPattern,checkEncounterEnd,COMBAT_ABILITIES,type ActiveEncounter}from'../active-combat-system'; jest.mock('../../../events',()=>({eventBus:{publish:jest.fn()}})); describe('ActiveCombatSystem',()=>{const mockEncounter:ActiveEncounter = {id:'encounter-123',userId:'user-123',bossId:'boss-1',bossName:'Test Boss',bossAvatarUrl:null,bossMaxHealth:100,bossCurrentHealth:100,currentPhase:'CALM',currentAttackPattern:null,attackPatternStartedAt:null,attackPatternDurationMs:30000,userMaxFocusEnergy:100,userCurrentFocusEnergy:100,userFocusEnergyRegenRate:1,availableAbilities:COMBAT_ABILITIES,abilityCooldowns:{},encounterStartedAt:Date.now(),expiresAt:Date.now() + 3600000,lastUserActionAt:null,sessionCount:1,totalDamageDealt:0,attacksDodged:0,attacksHit:0,status:'ACTIVE'}; describe('executeCombatAbility',()=>{it('Focus Strike ability deals correct base damage',()=>{const result = executeCombatAbility(mockEncounter,'focus_strike',0); expect(result.success).toBe(true); expect(result.damageDealt).toBe(15); expect(result.energyConsumed).toBe(20); expect(result.message).toContain('Focus Strike dealt 15 damage');}); it('Deep Dive ability requires purity/streak >= 3',()=>{const resultLowStreak = executeCombatAbility(mockEncounter,'deep_dive',2); expect(resultLowStreak.success).toBe(false); expect(resultLowStreak.message).toContain('Requires 3 day streak'); const resultHighStreak = executeCombatAbility(mockEncounter,'deep_dive',3); expect(resultHighStreak.success).toBe(true); expect(resultHighStreak.damageDealt).toBe(35);}); it('Flow State ability requires streak >= 7',()=>{const resultLow = executeCombatAbility(mockEncounter,'flow_state',5); expect(resultLow.success).toBe(false); expect(resultLow.message).toContain('Requires 7 day streak'); const resultHigh = executeCombatAbility(mockEncounter,'flow_state',7); expect(resultHigh.success).toBe(true); expect(resultHigh.damageDealt).toBe(60);}); it('Zen Strike requires streak >= 14',()=>{const resultLow = executeCombatAbility(mockEncounter,'zen_strike',10); expect(resultLow.success).toBe(false); expect(resultLow.message).toContain('Requires 14 day streak'); const resultHigh = executeCombatAbility(mockEncounter,'zen_strike',14); expect(resultHigh.success).toBe(true); expect(resultHigh.damageDealt).toBe(150);}); it('Ability cooldown prevents double-activation',()=>{const now = Date.now(); const encounterWithCooldown = {...mockEncounter,abilityCooldowns:{focus_strike:now + 5000}}; const result = executeCombatAbility(encounterWithCooldown,'focus_strike',0,now); expect(result.success).toBe(false); expect(result.message).toBe('Ability on cooldown');}); it('Insufficient energy blocks ability use',()=>{const lowEnergyEncounter = {...mockEncounter,userCurrentFocusEnergy:10}; const result = executeCombatAbility(lowEnergyEncounter,'focus_strike',0); expect(result.success).toBe(false); expect(result.message).toBe('Not enough Focus Energy!');}); it('Phase multiplier increases damage in ENRAGED phase',()=>{const enragedEncounter = {...mockEncounter,currentPhase:'ENRAGED'}; const result = executeCombatAbility(enragedEncounter,'focus_strike',0); expect(result.damageDealt).toBe(22); expect(result.newPhase).toBe('ENRAGED');}); it('Phase transitions from CALM to AGITATED at 70% health',()=>{const encounter = {...mockEncounter,bossCurrentHealth:69}; const result = executeCombatAbility(encounter,'focus_strike',0); expect(result.newPhase).toBe('AGITATED');}); it('Phase transitions to ENRAGED at 40% health',()=>{const encounter = {...mockEncounter,bossCurrentHealth:39}; const result = executeCombatAbility(encounter,'focus_strike',0); expect(result.newPhase).toBe('ENRAGED');}); it('Phase transitions to DESPERATE at 15% health',()=>{const encounter = {...mockEncounter,bossCurrentHealth:14}; const result = executeCombatAbility(encounter,'focus_strike',0); expect(result.newPhase).toBe('DESPERATE');}); it('Combo bonus applies for rapid successive attacks',()=>{const now = Date.now(); const encounterWithRecentAction = {...mockEncounter,lastUserActionAt:now - 5000}; const result = executeCombatAbility(encounterWithRecentAction,'focus_strike',0,now); expect(result.comboBonus).toBe(0.2);}); it('No combo bonus for slow attacks (>10s apart)',()=>{const now = Date.now(); const encounterWithOldAction = {...mockEncounter,lastUserActionAt:now - 15000}; const result = executeCombatAbility(encounterWithOldAction,'focus_strike',0,now); expect(result.comboBonus).toBe(0);});}); describe('calculateBossPhase',()=>{it('returns CALM at full health',()=>{const phase = calculateBossPhase(100,100,0,3600000); expect(phase).toBe('CALM');}); it('returns AGITATED at 50% health',()=>{const phase = calculateBossPhase(50,100,0,3600000); expect(phase).toBe('AGITATED');}); it('returns ENRAGED at 25% health',()=>{const phase = calculateBossPhase(25,100,0,3600000); expect(phase).toBe('ENRAGED');}); it('returns DESPERATE at 10% health',()=>{const phase = calculateBossPhase(10,100,0,3600000); expect(phase).toBe('DESPERATE');}); it('returns ENRAGED when time is over 80% elapsed',()=>{const phase = calculateBossPhase(100,100,2900000,3600000); expect(phase).toBe('ENRAGED');});}); describe('getPhaseMultiplier',()=>{it('returns 1.0 for CALM',()=>{expect(getPhaseMultiplier('CALM')).toBe(1.0);}); it('returns 1.2 for AGITATED',()=>{expect(getPhaseMultiplier('AGITATED')).toBe(1.2);}); it('returns 1.5 for ENRAGED',()=>{expect(getPhaseMultiplier('ENRAGED')).toBe(1.5);}); it('returns 2.0 for DESPERATE',()=>{expect(getPhaseMultiplier('DESPERATE')).toBe(2.0);});}); describe('resolveAttackPattern',()=>{it('Distraction Wave is dodged by completing sprint',()=>{const encounter = {...mockEncounter,currentAttackPattern:'DISTRACTION_WAVE',attackPatternStartedAt:Date.now() - 35000}; const result = resolveAttackPattern(encounter,true,false); expect(result.dodged).toBe(true); expect(result.hit).toBe(false);}); it('Distraction Wave hits if sprint not completed',()=>{const encounter = {...mockEncounter,currentAttackPattern:'DISTRACTION_WAVE',attackPatternStartedAt:Date.now() - 35000}; const result = resolveAttackPattern(encounter,false,false); expect(result.dodged).toBe(false); expect(result.hit).toBe(true); expect(result.damageTaken).toBe(15);}); it('Procrastination Beam is dodged by not pausing',()=>{const encounter = {...mockEncounter,currentAttackPattern:'PROCRASTINATION_BEAM',attackPatternStartedAt:Date.now() - 65000}; const result = resolveAttackPattern(encounter,false,true); expect(result.dodged).toBe(true);}); it('No damage if attack pattern still active',()=>{const encounter = {...mockEncounter,currentAttackPattern:'DISTRACTION_WAVE',attackPatternStartedAt:Date.now() - 10000}; const result = resolveAttackPattern(encounter,false,false); expect(result.hit).toBe(false); expect(result.dodged).toBe(false);});}); describe('checkEncounterEnd',()=>{it('returns victory when boss health is 0',()=>{const encounter = {...mockEncounter,bossCurrentHealth:0}; const result = checkEncounterEnd(encounter); expect(result.ended).toBe(true); expect(result.victory).toBe(true); expect(result.reason).toBe('DEFEATED'); expect(result.rewards).toContainEqual({type:'XP',amount:expect.any(Number)}); expect(result.rewards).toContainEqual({type:'COINS',amount:expect.any(Number)});}); it('returns timeout when expired',()=>{const encounter = {...mockEncounter,expiresAt:Date.now() - 1000}; const result = checkEncounterEnd(encounter); expect(result.ended).toBe(true); expect(result.victory).toBe(false); expect(result.reason).toBe('TIMED_OUT');}); it('returns defeat when focus energy depleted',()=>{const encounter = {...mockEncounter,userCurrentFocusEnergy:0}; const result = checkEncounterEnd(encounter); expect(result.ended).toBe(true); expect(result.victory).toBe(false); expect(result.reason).toBe('ABANDONED');}); it('returns active when encounter ongoing',()=>{const result = checkEncounterEnd(mockEncounter); expect(result.ended).toBe(false); expect(result.victory).toBe(false); expect(result.reason).toBe('ACTIVE');}); it('awards gem bonus for dodging 3+ attacks',()=>{const encounter = {...mockEncounter,bossCurrentHealth:0,attacksDodged:3}; const result = checkEncounterEnd(encounter); const gemReward = result.rewards.find(r=>r.type === 'GEMS'); expect(gemReward).toEqual({type:'GEMS',amount:10});});}); describe('COMBAT_ABILITIES database',()=>{it('has 6 combat abilities',()=>{expect(COMBAT_ABILITIES).toHaveLength(6);}); it('all abilities have required fields',()=>{for(const ability of COMBAT_ABILITIES){expect(ability.id).toBeDefined(); expect(ability.name).toBeDefined(); expect(ability.baseDamage).toBeGreaterThan(0); expect(ability.focusEnergyCost).toBeGreaterThanOrEqual(0); expect(ability.cooldownSeconds).toBeGreaterThanOrEqual(0); expect(ability.icon).toBeDefined();}}); it('abilities have escalating requirements',()=>{const focusStrike = COMBAT_ABILITIES.find(a=>a.id === 'focus_strike')!; const deepDive = COMBAT_ABILITIES.find(a=>a.id === 'deep_dive')!; const flowState = COMBAT_ABILITIES.find(a=>a.id === 'flow_state')!; const zenStrike = COMBAT_ABILITIES.find(a=>a.id === 'zen_strike')!; expect(focusStrike.requiresStreak).toBe(0); expect(deepDive.requiresStreak).toBeGreaterThan(focusStrike.requiresStreak); expect(flowState.requiresStreak).toBeGreaterThan(deepDive.requiresStreak); expect(zenStrike.requiresStreak).toBeGreaterThan(flowState.requiresStreak);});});});

import{describe,it,expect,beforeEach,afterEach,jest}from'@jest/globals';import{PerformanceGate,performanceGate,PRODUCTION_TARGETS,DEVELOPMENT_TARGETS,type PerformanceGateResult,type PerformanceTargets}from'../PerformanceGate';const mockPerformanceMonitor={start:jest.fn(),stop:jest.fn(),getMetrics:jest.fn(()=>({fps:60,avgFps:58,jankFrames:2,memoryUsage:120,jsHeapSize:50,longTasks:1,timestamp:Date.now()})),onJank:jest.fn()};const mockFetch=jest.fn();Object.defineProperty(global,'fetch',{value:mockFetch,writable:true});const mockRequestAnimationFrame=jest.fn();Object.defineProperty(global,'requestAnimationFrame',{value:mockRequestAnimationFrame,writable:true});describe('PerformanceGate',()=>{let gate:PerformanceGate;beforeEach(()=>{jest.clearAllMocks();gate=performanceGate;mockFetch.mockClear();mockRequestAnimationFrame.mockClear();});afterEach(()=>{gate.cleanup();});describe('Configuration Management',()=>{it('should use production targets by default',()=>{const targets=gate.getTargets();expect(targets.minFps).toBe(PRODUCTION_TARGETS.minFps);expect(targets.targetFps).toBe(PRODUCTION_TARGETS.targetFps);expect(targets.maxMemoryMb).toBe(PRODUCTION_TARGETS.maxMemoryMb);});it('should allow custom target configuration',()=>{const customTargets:Partial<PerformanceTargets>={minFps:45,targetFps:120};gate.setTargets(customTargets);const targets=gate.getTargets();expect(targets.minFps).toBe(45);expect(targets.targetFps).toBe(120);expect(targets.maxMemoryMb).toBe(PRODUCTION_TARGETS.maxMemoryMb);});it('should update targets dynamically',()=>{const initialTargets=gate.getTargets();gate.setTargets({minFps:50});const updatedTargets=gate.getTargets();expect(updatedTargets.minFps).toBe(50);expect(updatedTargets.targetFps).toBe(initialTargets.targetFps);});});describe('Performance Gate Evaluation',()=>{it('should pass with good performance metrics',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:60,avgFps:58,jankFrames:1,memoryUsage:100,jsHeapSize:40,longTasks:0,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(true);expect(result.score).toBeGreaterThanOrEqual(90);expect(result.metrics.fps.passed).toBe(true);expect(result.metrics.memory.passed).toBe(true);expect(result.issues).toHaveLength(0);});it('should fail with low FPS',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:25,avgFps:28,jankFrames:15,memoryUsage:80,jsHeapSize:35,longTasks:2,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(false);expect(result.score).toBeLessThan(80);expect(result.metrics.fps.passed).toBe(false);expect(result.issues.some(issue=>issue.category==='fps')).toBe(true);});it('should fail with high memory usage',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:55,avgFps:52,jankFrames:3,memoryUsage:200,jsHeapSize:60,longTasks:1,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(false);expect(result.metrics.memory.passed).toBe(false);expect(result.issues.some(issue=>issue.category==='memory')).toBe(true);});it('should detect animation performance issues',async()=>{gate.setBundleSize(500);const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(false);expect(result.metrics.bundle.passed).toBe(false);expect(result.issues.some(issue=>issue.category==='bundle')).toBe(true);});it('should detect network performance issues',async()=>{mockFetch.mockImplementation(async()=>{await new Promise(resolve=>setTimeout(resolve,500));return{ok:true};});await (globalThis.fetch)('https://api.example.com/data');const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(false);expect(result.metrics.network.passed).toBe(false);expect(result.issues.some(issue=>issue.category==='network')).toBe(true);});it('should provide comprehensive performance report',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:30,avgFps:25,jankFrames:10,memoryUsage:180,jsHeapSize:70,longTasks:4,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();const report=gate.generateReport(result);expect(report).toContain('# Performance Gate Report');expect(report).toContain('**Overall Status: ❌ FAILED**');expect(report).toContain('### FPS');expect(report).toContain('### Memory');expect(report).toContain('## Issues Found');expect(report).toContain('## General Recommendations');});});describe('Performance Metrics Collection',()=>{it('should collect FPS metrics correctly',()=>{gate.setTargets(PRODUCTION_TARGETS);(gate as any).initializeMetricsCollection();const frameCallback=mockRequestAnimationFrame.mock.calls[0]?.[0];if(frameCallback){frameCallback(performance.now());frameCallback(performance.now()+16);frameCallback(performance.now()+33);}const metrics=gate.getDiagnosticInfo().currentMetrics;expect(metrics.fps).toBeGreaterThan(50);expect(metrics.jankFrames).toBeLessThan(5);});it('should track memory usage',()=>{gate.setTargets(PRODUCTION_TARGETS);(gate as any).initializeMetricsCollection();const metrics=gate.getDiagnosticInfo().currentMetrics;expect(metrics.memoryUsage).toBeGreaterThanOrEqual(0);expect(typeof metrics.jsHeapSize).toBe('number');});it('should monitor network requests',async()=>{gate.setTargets(PRODUCTION_TARGETS);(gate as any).initializeMetricsCollection();mockFetch.mockResolvedValue({data:'test'});await (globalThis.fetch)('https://api.example.com/test');const diagnostics=gate.getDiagnosticInfo();expect(diagnostics.isMonitoring).toBe(true);});it('should provide diagnostic information',()=>{const diagnostics=gate.getDiagnosticInfo();expect(diagnostics).toHaveProperty('targets');expect(diagnostics).toHaveProperty('currentMetrics');expect(diagnostics).toHaveProperty('isMonitoring');expect(diagnostics).toHaveProperty('activeAnimations');expect(diagnostics).toHaveProperty('registrySize');});});describe('Performance Thresholds',()=>{it('should use production targets in production',()=>{const productionGate=PerformanceGate.getInstance();const targets=productionGate.getTargets();expect(targets.minFps).toBe(30);expect(targets.maxMemoryMb).toBe(150);});it('should use development targets in development',()=>{const devGate=PerformanceGate.getInstance();const targets=devGate.getTargets();expect(targets.minFps).toBe(30);expect(targets.maxMemoryMb).toBe(200);});});describe('Performance Recommendations',()=>{it('should generate FPS-specific recommendations',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:20,avgFps:18,jankFrames:25,memoryUsage:100,jsHeapSize:40,longTasks:2,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();expect(result.recommendations).toContain('Optimize rendering pipeline');expect(result.recommendations).toContain('Reduce complexity');});it('should generate memory-specific recommendations',async()=>{mockPerformanceMonitor.getMetrics.mockReturnValue({fps:55,avgFps:52,jankFrames:3,memoryUsage:250,jsHeapSize:80,longTasks:1,timestamp:Date.now()});const result=await gate.evaluatePerformanceGate();expect(result.recommendations).toContain('memory pooling');expect(result.recommendations).toContain('optimize data structures');});it('should generate network-specific recommendations',async()=>{mockFetch.mockImplementation(async()=>{await new Promise(resolve=>setTimeout(resolve,1000));return{ok:true};});await (globalThis.fetch)('https://api.example.com/slow');const result=await gate.evaluatePerformanceGate();expect(result.recommendations).toContain('request caching');expect(result.recommendations).toContain('optimize API calls');});});describe('Error Handling',()=>{it('should handle performance monitor errors gracefully',async()=>{mockPerformanceMonitor.getMetrics.mockImplementation(()=>{throw new Error('Performance monitor error');});const result=await gate.evaluatePerformanceGate();expect(result.passed).toBe(false);expect(result.score).toBeLessThan(50);expect(result.issues.length).toBeGreaterThan(0);});it('should provide fallback metrics on error',async()=>{mockPerformanceMonitor.getMetrics.mockImplementation(()=>{throw new Error('Performance monitor error');});const diagnostics=gate.getDiagnosticInfo();expect(diagnostics.currentMetrics).toBeDefined();expect(diagnostics.currentMetrics.fps).toBe(0);expect(diagnostics.currentMetrics.memoryUsage).toBe(0);});});describe('Integration with Performance Monitor',()=>{it('should integrate with existing performance monitoring',()=>{const diagnostics=gate.getDiagnosticInfo();expect(diagnostics.isMonitoring).toBe(true);expect(diagnostics.targets).toBeDefined();expect(diagnostics.currentMetrics).toBeDefined();});it('should cleanup resources properly',()=>{(gate as any).initializeMetricsCollection();gate.cleanup();const diagnostics=gate.getDiagnosticInfo();expect(diagnostics.isMonitoring).toBe(false);});});});

export{validateEmail,validatePassword,validateUsername,validateName,validatePhone,validateDateOfBirth,validateUserRegistration,validateUserProfile,validateUserPermissions,validateUserId,type UserValidationResult,type UserRegistrationData,type UserProfileData,type UserPermissionData}from'./userValidation';export{validateLoginCredentials,validateSession,validateToken,validatePasswordReset,validateTwoFactor,validateRateLimit,validateSecurityHeaders,validateIPAddress,type AuthValidationResult,type LoginCredentials,type SessionData,type TokenData,type PasswordResetData,type TwoFactorData}from'./authValidation';export{validateString,validateNumber,validateBoolean,validateDate,validateArray,validateObject,validateEmail as validateEmailData,validateURL as validateURLData,validateUUID,validateJSON,validatePhoneNumber,validateCreditCard,type DataValidationResult,type ValidationRule,type ValidationSchema}from'./dataValidation';export{validateHTTPMethod,validateURL as validateURLAPI,validateHeaders,validateRequestBody,validateQueryParams,validatePathParams,validateAPIRequest,validateAPIResponse,validateRateLimit as validateAPIRateLimit,type APIValidationResult,type APIRequest,type APIResponse,type APIEndpoint}from'./apiValidation';export{validateFormField,validateForm,validateFormState,validateFormSubmission,validateFieldDependencies,validateFormProgress,validateMultiStepForm,validateDynamicForm,type FormValidationResult,type FormField,type FormConfig}from'./formValidation';export{validateXSS,validateSQLInjection,validateCSRF,sanitizeInput,validateFileUpload,validateRateLimit as validateSecurityRateLimit,validateIPAddressSecurity,validateSecurity,type SecurityValidationResult,type SecurityConfig}from'./securityValidation';export{validateECommerceOrder,validateFinancialTransaction,validateHealthcareAppointment,validateRealEstateListing,validateEducationEnrollment,validateBusinessRules,type BusinessValidationResult,type BusinessRule,type BusinessContext}from'./businessValidation';export{validateResponseTime,validateThroughput,validateCPUUsage,validateMemoryUsage,validateDiskUsage,validateNetworkLatency,validateErrorRate,validateAvailability,validatePerformance,defaultThresholds,type PerformanceValidationResult,type PerformanceMetrics,type PerformanceThresholds}from'./performanceValidation';export class ValidationEngine{static validateAll(data:DynamicValue,options:{user?:boolean;auth?:boolean;data?:boolean;api?:boolean;form?:boolean;security?:boolean;business?:boolean;performance?:boolean;}={}):{isValid:boolean;errors:string[];warnings:string[];results:DynamicValue;}{const allErrors:string[]=[];const allWarnings:string[]=[];const results:DynamicValue={};try{if(options.user){results.user={isValid:true,errors:[],warnings:[]};}if(options.auth){results.auth={isValid:true,errors:[],warnings:[]};}if(options.data){results.data={isValid:true,errors:[],warnings:[]};}if(options.api){results.api={isValid:true,errors:[],warnings:[]};}if(options.form){results.form={isValid:true,errors:[],warnings:[]};}if(options.security){results.security={isValid:true,errors:[],warnings:[]};allErrors.push(...results.security.errors);allWarnings.push(...results.security.warnings);}if(options.business){results.business={isValid:true,errors:[],warnings:[]};}if(options.performance){results.performance={isValid:true,errors:[],warnings:[]};}}catch(error:DynamicValue){allErrors.push(`Validation engine error: ${error}`);}return{isValid:allErrors.length===0,errors:allErrors,warnings:allWarnings,...results};}static createPipeline(steps:Array<{name:string;validator:(data:DynamicValue)=>DynamicValue;required?:boolean;}>){return{validate:(data:DynamicValue)=>{const results:DynamicValue={};const allErrors:string[]=[];const allWarnings:string[]=[];for(const step of steps){try{const result=step.validator(data);results[step.name]=result;if(result.errors){allErrors.push(...result.errors);}if(result.warnings){allWarnings.push(...result.warnings);}if(step.required&&!result.isValid){allErrors.push(`Required validation step '${step.name}' failed`);}}catch(error){allErrors.push(`Validation step '${step.name}' error: ${error}`);if(step.required){break;}}}return{isValid:allErrors.length===0,errors:allErrors,warnings:allWarnings,results};}};}static async validateWithRetry(validator:(data:DynamicValue)=>DynamicValue,data:DynamicValue,maxRetries:number=3,delay:number=1000):Promise<DynamicValue>{let lastError:DynamicValue;for(let attempt=1;attempt<=maxRetries;attempt++){try{return validator(data);}catch(error){lastError=error;if(attempt<maxRetries){await new Promise(resolve=>setTimeout(resolve,delay*attempt));}}}throw lastError;}private static validationCache=new Map<string,{result:DynamicValue;timestamp:number;}>();static validateWithCache(key:string,validator:(data:DynamicValue)=>DynamicValue,data:DynamicValue,ttl:number=300000):DynamicValue{const cached=this.validationCache.get(key);if(cached&&Date.now()-cached.timestamp<ttl){return cached.result;}const result=validator(data);this.validationCache.set(key,{result,timestamp:Date.now()});return result;}static clearCache():void{this.validationCache.clear();}static getCacheStats():{size:number;entries:Array<{key:string;age:number;}>;}{const entries=Array.from(this.validationCache.entries()).map(([key,value])=>({key,age:Date.now()-value.timestamp}));return{size:this.validationCache.size,entries};}}export const VALIDATION_CONSTANTS={MAX_STRING_LENGTH:1000,MAX_ARRAY_LENGTH:1000,MAX_OBJECT_DEPTH:10,MAX_FILE_SIZE:10485760,MAX_EMAIL_LENGTH:254,MAX_URL_LENGTH:2048,MAX_PHONE_LENGTH:20,MIN_PASSWORD_LENGTH:8,MAX_PASSWORD_LENGTH:128,DEFAULT_RATE_LIMIT:100,DEFAULT_TIMEOUT:30000}as const;export const VALIDATION_ERROR_CODES={REQUIRED:'REQUIRED',INVALID_TYPE:'INVALID_TYPE',INVALID_FORMAT:'INVALID_FORMAT',TOO_SHORT:'TOO_SHORT',TOO_LONG:'TOO_LONG',INVALID_RANGE:'INVALID_RANGE',PATTERN_MISMATCH:'PATTERN_MISMATCH',SECURITY_THREAT:'SECURITY_THREAT',BUSINESS_RULE_VIOLATION:'BUSINESS_RULE_VIOLATION',PERFORMANCE_ISSUE:'PERFORMANCE_ISSUE'}as const;export const ValidationUtils={isEmpty(value:DynamicValue):boolean{return value===null||value===undefined||value===''||Array.isArray(value)&&value.length===0||typeof value==='object'&&Object.keys(value).length===0;},sanitizeString(str:string):string{return str.trim().replace(/\s+/g,' ');},hasOnlyAllowedChars(str:string,allowed:RegExp):boolean{return allowed.test(str);},formatResult(isValid:boolean,errors:string[]=[],warnings:string[]=[]):DynamicValue{return{isValid,errors,warnings,timestamp:new Date().toISOString()};},mergeResults(...results:DynamicValue[]):DynamicValue{const merged={isValid:true,errors:[]as string[],warnings:[]as string[]};for(const result of results){if(!result.isValid){merged.isValid=false;}if(result.errors){merged.errors.push(...result.errors);}if(result.warnings){merged.warnings.push(...result.warnings);}}return merged;},createError(message:string,context?:string,code?:string):string{const parts=[message];if(context){parts.push(`(${context})`);}if(code){parts.push(`[${code}]`);}return parts.join(' ');},async validateAsync<T>(validator:(data:T)=>Promise<boolean>,data:T,timeout:number=5000):Promise<boolean>{return Promise.race([validator(data),new Promise<boolean>((_,reject)=>setTimeout(()=>reject(new Error('Validation timeout')),timeout))]);}};

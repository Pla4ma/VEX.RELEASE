export interface APIValidationResult{isValid:boolean;errors:string[];warnings:string[];sanitizedData?:unknown;securityLevel:'low'|'medium'|'high';}export interface APIRequest{method:string;url:string;headers?:Record<string,string>;body?:unknown;params?:Record<string,string>;query?:Record<string,string>;}export interface APIResponse{status:number;headers?:Record<string,string>;body?:unknown;duration?:number;}export interface APIEndpoint{method:string;path:string;authRequired?:boolean;rateLimit?:number;maxBodySize?:number;allowedHeaders?:string[];requiredParams?:string[];validationSchema?:unknown;}export const validateHTTPMethod=(method:string):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(!method||typeof method!=='string'){errors.push('HTTP method is required and must be a string');return{isValid:false,errors,warnings,securityLevel};}const validMethods=['GET','POST','PUT','PATCH','DELETE','HEAD','OPTIONS'];const upperMethod=method.toUpperCase();if(!validMethods.includes(upperMethod)){errors.push(`Invalid HTTP method: ${method}`);return{isValid:false,errors,warnings,securityLevel};}if(['POST','PUT','PATCH','DELETE'].includes(upperMethod)){securityLevel='high';}else if(['GET','HEAD'].includes(upperMethod)){securityLevel='medium';}else{securityLevel='low';}return{isValid:true,errors,warnings,securityLevel};};export const validateURL=(url:string,allowedDomains?:string[]):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(!url||typeof url!=='string'){errors.push('URL is required and must be a string');return{isValid:false,errors,warnings,securityLevel};}try{const parsedURL=new URL(url);const allowedProtocols=['http:','https:'];if(!allowedProtocols.includes(parsedURL.protocol)){errors.push(`Protocol not allowed: ${parsedURL.protocol}`);securityLevel='low';}if(parsedURL.protocol!=='https:'){warnings.push('Consider using HTTPS for better security');securityLevel='low';}if(allowedDomains&&allowedDomains.length>0){if(!allowedDomains.includes(parsedURL.hostname)){errors.push(`Domain not allowed: ${parsedURL.hostname}`);securityLevel='low';}}const suspiciousDomains=['localhost','127.0.0.1','0.0.0.0'];if(suspiciousDomains.includes(parsedURL.hostname)){warnings.push('Using local development domain');securityLevel='low';}if(parsedURL.pathname.length>2048){errors.push('URL path is too long (maximum 2048 characters)');}if(parsedURL.pathname.includes('../')||parsedURL.pathname.includes('..\\')){errors.push('Path traversal detected in URL');securityLevel='low';}if(parsedURL.search.length>4096){errors.push('Query string is too long (maximum 4096 characters)');}const sqlPatterns=['union','select','insert','update','delete','drop'];const queryString=parsedURL.search.toLowerCase();if(sqlPatterns.some(pattern=>queryString.includes(pattern))){errors.push('Potential SQL injection detected in query parameters');securityLevel='low';}}catch(error){errors.push('Invalid URL format');}return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validateHeaders=(headers:Record<string,string>,allowedHeaders?:string[]):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(!headers||typeof headers!=='object'){errors.push('Headers must be an object');return{isValid:false,errors,warnings,securityLevel};}if(allowedHeaders&&allowedHeaders.length>0){for(const headerName of Object.keys(headers)){if(!allowedHeaders.includes(headerName.toLowerCase())){warnings.push(`Header not allowed: ${headerName}`);}}}const contentType=headers['content-type']||headers['Content-Type'];if(contentType){const validContentTypes=['application/json','application/xml','text/plain','multipart/form-data','application/x-www-form-urlencoded'];const mainType=contentType.split(';')[0].toLowerCase();if(!validContentTypes.some(type=>mainType.includes(type))){warnings.push(`Unusual Content-Type: ${contentType}`);}}const authHeader=headers.authorization||headers.Authorization;if(authHeader){const authParts=authHeader.split(' ');if(authParts.length!==2){errors.push('Invalid Authorization header format');securityLevel='low';}else{const[scheme,credentials]=authParts;if(scheme!=='Bearer'&&scheme!=='Basic'){warnings.push(`Unusual authentication scheme: ${scheme}`);}if(scheme==='Bearer'&&credentials.length<20){warnings.push('Bearer token appears too short');securityLevel='low';}if(scheme==='Basic'){try{const decoded=atob(credentials);const[username,password]=decoded.split(':');if(!username||!password){errors.push('Invalid Basic authentication credentials');securityLevel='low';}}catch{errors.push('Invalid Basic authentication encoding');securityLevel='low';}}}}const securityHeaders=['x-frame-options','x-content-type-options','x-xss-protection','strict-transport-security'];const missingSecurityHeaders=securityHeaders.filter(header=>!(headers[header]||headers[header.toLowerCase()]));if(missingSecurityHeaders.length>0){warnings.push(`Missing security headers: ${missingSecurityHeaders.join(', ')}`);securityLevel='low';}const headerSize=JSON.stringify(headers).length;if(headerSize>8192){errors.push('Headers too large (maximum 8KB)');}return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validateRequestBody=(body:unknown,contentType?:string,maxSize?:number):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(body===null||body===undefined){return{isValid:true,errors,warnings,securityLevel};}const bodySize=JSON.stringify(body).length;const maxAllowedSize=maxSize||1048576;if(bodySize>maxAllowedSize){errors.push(`Request body too large (maximum ${maxAllowedSize} bytes)`);return{isValid:false,errors,warnings,securityLevel};}if(contentType){const mainType=contentType.split(';')[0].toLowerCase();if(mainType.includes('application/json')){if(typeof body!=='object'){errors.push('Body must be an object for JSON content type');}else{const maxDepth=10;let currentDepth=0;const checkDepth=(obj:unknown,depth:number):boolean=>{if(depth>maxDepth){return false;}if(typeof obj!=='object'||obj===null){return true;}currentDepth=Math.max(currentDepth,depth);return Object.values(obj).every(value=>checkDepth(value,depth+1));};if(!checkDepth(body,0)){errors.push('Request body nesting too deep (maximum 10 levels)');}if(currentDepth>5){warnings.push('Request body has deep nesting');}}}}const bodyString=JSON.stringify(body).toLowerCase();const sqlPatterns=['union select','select from','insert into','update set','delete from','drop table','exec(','script>','javascript:','<script'];for(const pattern of sqlPatterns){if(bodyString.includes(pattern)){errors.push(`Potential injection detected: ${pattern}`);securityLevel='low';break;}}const checkLargeArrays=(obj:unknown,path:string=''):void=>{if(Array.isArray(obj)){if(obj.length>1000){warnings.push(`Large array detected at ${path}: ${obj.length} items`);}}else if(typeof obj==='object'&&obj!==null){for(const[key,value]of Object.entries(obj)){checkLargeArrays(value,path?`${path}.${key}`:key);}}};checkLargeArrays(body);return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validateQueryParams=(params:Record<string,string>,allowedParams?:string[]):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(!params||typeof params!=='object'){errors.push('Query parameters must be an object');return{isValid:false,errors,warnings,securityLevel};}if(allowedParams&&allowedParams.length>0){for(const paramName of Object.keys(params)){if(!allowedParams.includes(paramName)){warnings.push(`Parameter not allowed: ${paramName}`);}}}for(const[key,value]of Object.entries(params)){if(key.length>100){errors.push(`Parameter name too long: ${key}`);}if(value.length>1000){errors.push(`Parameter value too long: ${key}`);}const sqlPatterns=['union','select','insert','update','delete','drop'];const lowerValue=value.toLowerCase();if(sqlPatterns.some(pattern=>lowerValue.includes(pattern))){errors.push(`Potential SQL injection in parameter: ${key}`);securityLevel='low';}const xssPatterns=['<script','javascript:','onload=','onerror='];if(xssPatterns.some(pattern=>lowerValue.includes(pattern))){errors.push(`Potential XSS in parameter: ${key}`);securityLevel='low';}}const paramCount=Object.keys(params).length;if(paramCount>50){warnings.push(`Too many query parameters: ${paramCount}`);}return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validatePathParams=(params:Record<string,string>,requiredParams?:string[]):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(!params||typeof params!=='object'){errors.push('Path parameters must be an object');return{isValid:false,errors,warnings,securityLevel};}if(requiredParams&&requiredParams.length>0){for(const requiredParam of requiredParams){if(!params[requiredParam]){errors.push(`Required path parameter missing: ${requiredParam}`);}}}for(const[key,value]of Object.entries(params)){if(key.length>50){errors.push(`Path parameter name too long: ${key}`);}if(value.length>200){errors.push(`Path parameter value too long: ${key}`);}const validPattern=/^[a-zA-Z0-9_-]+$/;if(!validPattern.test(value)){errors.push(`Invalid characters in path parameter: ${key}`);securityLevel='low';}if(value.includes('../')||value.includes('..\\')){errors.push(`Path traversal detected in parameter: ${key}`);securityLevel='low';}}return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validateAPIRequest=(request:APIRequest,endpoint:APIEndpoint):APIValidationResult=>{const allErrors:string[]=[];const allWarnings:string[]=[];let overallSecurityLevel:'low'|'medium'|'high'='medium';const methodValidation=validateHTTPMethod(request.method);allErrors.push(...methodValidation.errors);allWarnings.push(...methodValidation.warnings);const urlValidation=validateURL(request.url);allErrors.push(...urlValidation.errors);allWarnings.push(...urlValidation.warnings);if(request.headers){const headersValidation=validateHeaders(request.headers,endpoint.allowedHeaders);allErrors.push(...headersValidation.errors);allWarnings.push(...headersValidation.warnings);}if(request.query){const queryValidation=validateQueryParams(request.query);allErrors.push(...queryValidation.errors);allWarnings.push(...queryValidation.warnings);}if(request.params){const pathValidation=validatePathParams(request.params,endpoint.requiredParams);allErrors.push(...pathValidation.errors);allWarnings.push(...pathValidation.warnings);}if(request.body&&['POST','PUT','PATCH'].includes(request.method.toUpperCase())){const contentType=request.headers?.['content-type'];const bodyValidation=validateRequestBody(request.body,contentType,endpoint.maxBodySize);allErrors.push(...bodyValidation.errors);allWarnings.push(...bodyValidation.warnings);}else if(request.body&&!['POST','PUT','PATCH'].includes(request.method.toUpperCase())){allErrors.push('Request body not allowed for this method');}if(endpoint.authRequired){const authHeader=request.headers?.authorization||request.headers?.Authorization;if(!authHeader){allErrors.push('Authentication required for this endpoint');overallSecurityLevel='low';}}if(endpoint.rateLimit&&endpoint.rateLimit>0){allWarnings.push(`Rate limit applies: ${endpoint.rateLimit} requests`);}if(allErrors.some(error=>error.includes('injection')||error.includes('traversal'))){overallSecurityLevel='low';}else if(allWarnings.length>5){overallSecurityLevel='medium';}else{overallSecurityLevel='high';}return{isValid:allErrors.length===0,errors:allErrors,warnings:allWarnings,securityLevel:overallSecurityLevel};};export const validateAPIResponse=(response:APIResponse,expectedStatus?:number[]):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(expectedStatus&&expectedStatus.length>0){if(!expectedStatus.includes(response.status)){warnings.push(`Unexpected status code: ${response.status}`);}}if(response.body){const responseSize=JSON.stringify(response.body).length;if(responseSize>10485760){errors.push('Response body too large (maximum 10MB)');}const sensitiveFields=['password','token','secret','key','credential'];const bodyString=JSON.stringify(response.body).toLowerCase();for(const field of sensitiveFields){if(bodyString.includes(field)){warnings.push(`Potential sensitive data exposure: ${field}`);securityLevel='low';}}}if(response.duration){if(response.duration>30000){warnings.push(`Slow response: ${response.duration}ms`);}}const securityHeaders=['x-content-type-options','x-frame-options','x-xss-protection'];if(response.headers){const missingHeaders=securityHeaders.filter(header=>!(response.headers![header]||response.headers![header.toLowerCase()]));if(missingHeaders.length>0){warnings.push(`Missing security headers in response: ${missingHeaders.join(', ')}`);securityLevel='medium';}}return{isValid:errors.length===0,errors,warnings,securityLevel};};export const validateRateLimit=(currentRequests:number,windowMs:number,maxRequests:number,identifier:string):APIValidationResult=>{const errors:string[]=[];const warnings:string[]=[];let securityLevel:'low'|'medium'|'high'='medium';if(currentRequests>=maxRequests){errors.push(`Rate limit exceeded for ${identifier}: ${currentRequests}/${maxRequests}`);securityLevel='low';}else if(currentRequests>=maxRequests*0.8){warnings.push(`Approaching rate limit for ${identifier}: ${currentRequests}/${maxRequests}`);securityLevel='medium';}if(currentRequests>maxRequests*2){errors.push(`Suspicious activity detected for ${identifier}: ${currentRequests} requests`);securityLevel='low';}return{isValid:errors.length===0,errors,warnings,securityLevel};};

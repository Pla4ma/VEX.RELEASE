import{PaywallVerification,paywallVerification}from'../PaywallVerification';import{RevenueCatService,revenueCatService}from'../../shared/monetization/revenuecat-service';import type{PaywallVerificationResult,PaywallIssue,ProductCatalogIssue,PurchaseFlowIssue,SubscriptionIssue,ReceiptValidationIssue}from'../PaywallVerification';jest.mock('../../shared/monetization/revenuecat-service',()=>({revenueCatService:{initialize:jest.fn(),getStatus:jest.fn(),getOfferings:jest.fn(),purchasePackage:jest.fn(),getCustomerInfo:jest.fn(),getAnalyticsEvents:jest.fn()}}));const mockRevenueCatService=revenueCatService as jest.Mocked<RevenueCatService>;describe('PaywallVerification',()=>{let verification:PaywallVerification;beforeEach(()=>{verification=PaywallVerification.getInstance();jest.clearAllMocks();});describe('Singleton Pattern',()=>{it('should return the same instance',()=>{const instance1=PaywallVerification.getInstance();const instance2=PaywallVerification.getInstance();expect(instance1).toBe(instance2);});it('should export singleton instance',()=>{expect(paywallVerification).toBeInstanceOf(PaywallVerification);});});describe('Product Catalog Verification',()=>{it('should verify valid product catalog',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-monthly',displayName:'Premium Monthly',description:'Monthly premium subscription',price:9.99,currencyCode:'USD',type:'subscription',pricingPhases:[{price:9.99,billingPeriod:'P1M',recurrenceMode:'FINITE_RECURRING'}]}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifyProductCatalog();expect(result.valid).toBe(true);expect(result.issues).toHaveLength(0);expect(result.warnings).toHaveLength(0);});it('should detect missing product identifier',async()=>{const mockOfferings={success:true,offerings:{current:[{displayName:'Premium Monthly',description:'Monthly premium subscription',price:9.99,currencyCode:'USD',type:'subscription'}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifyProductCatalog();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'missing-identifier-undefined',category:'product-catalog',severity:'critical',message:'Product missing required identifier'}));});it('should detect invalid pricing',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-monthly',displayName:'Premium Monthly',description:'Monthly premium subscription',price:-5.99,currencyCode:'USD',type:'subscription'}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifyProductCatalog();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'invalid-price-premium-monthly',category:'product-catalog',severity:'critical',message:'Product has invalid price: -5.99'}));});it('should detect prices that are too low',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-monthly',displayName:'Premium Monthly',description:'Monthly premium subscription',price:0.50,currencyCode:'USD',type:'subscription'}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifyProductCatalog();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'price-too-low-premium-monthly',category:'product-catalog',severity:'major',message:'Product price too low: $0.5'}));});it('should handle catalog verification errors',async()=>{mockRevenueCatService.getOfferings.mockRejectedValue(new Error('Network error'));const result=await verification.verifyProductCatalog();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'verification-failed',category:'product-catalog',severity:'critical'}));});});describe('Purchase Flow Verification',()=>{it('should verify successful purchase flow',async()=>{const mockPurchaseResult={success:true,transactionId:'test-transaction-123',productIdentifier:'premium-monthly',receipt:{transactionId:'test-transaction-123',originalTransactionIdentifier:'test-transaction-123',productId:'premium-monthly',productIdentifier:'premium-monthly',purchaseDate:'2024-01-01T00:00:00.000Z',acknowledged:true,acknowledgedDate:'2024-01-01T00:00:00.000Z'},purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'};mockRevenueCatService.purchasePackage.mockResolvedValue(mockPurchaseResult);const result=await verification.verifyPurchaseFlow();expect(result.valid).toBe(true);expect(result.issues).toHaveLength(0);});it('should detect missing transaction ID',async()=>{const mockPurchaseResult={success:true,productIdentifier:'premium-monthly',receipt:{originalTransactionIdentifier:'test-transaction-123',productId:'premium-monthly',productIdentifier:'premium-monthly',purchaseDate:'2024-01-01T00:00:00.000Z',acknowledged:true,acknowledgedDate:'2024-01-01T00:00:00.000Z'},purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'};mockRevenueCatService.purchasePackage.mockResolvedValue(mockPurchaseResult);const result=await verification.verifyPurchaseFlow();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'missing-transaction-id',category:'purchase-flow',severity:'critical',message:'Purchase result missing transaction ID'}));});it('should detect missing receipt',async()=>{const mockPurchaseResult={success:true,transactionId:'test-transaction-123',productIdentifier:'premium-monthly',purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'};mockRevenueCatService.purchasePackage.mockResolvedValue(mockPurchaseResult);const result=await verification.verifyPurchaseFlow();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'missing-receipt',category:'receipt',severity:'critical',message:'Purchase result missing receipt'}));});it('should handle purchase flow verification errors',async()=>{mockRevenueCatService.purchasePackage.mockRejectedValue(new Error('Purchase failed'));const result=await verification.verifyPurchaseFlow();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'purchase-flow-failed',category:'purchase-flow',severity:'critical'}));});});describe('Subscription Management Verification',()=>{it('should verify valid subscription management',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-monthly',displayName:'Premium Monthly',description:'Monthly premium subscription',price:9.99,currencyCode:'USD',type:'subscription',subscriptionDuration:30,pricingPhases:[{price:9.99,billingPeriod:'P1M',recurrenceMode:'FINITE_RECURRING'}]}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifySubscriptionManagement();expect(result.valid).toBe(true);expect(result.issues).toHaveLength(0);});it('should detect subscription duration too short',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-weekly',displayName:'Premium Weekly',description:'Weekly premium subscription',price:2.99,currencyCode:'USD',type:'subscription',subscriptionDuration:7,pricingPhases:[{price:2.99,billingPeriod:'P1W',recurrenceMode:'FINITE_RECURRING'}]}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifySubscriptionManagement();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'subscription-duration-too-short',category:'subscription',severity:'major',message:'Subscription duration too short: 7 days'}));});it('should detect invalid subscription type',async()=>{const mockOfferings={success:true,offerings:{current:[{identifier:'premium-custom',displayName:'Premium Custom',description:'Custom premium subscription',price:14.99,currencyCode:'USD',type:'custom',subscriptionDuration:30}]}};mockRevenueCatService.getOfferings.mockResolvedValue(mockOfferings);const result=await verification.verifySubscriptionManagement();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'invalid-subscription-type',category:'subscription',severity:'critical',message:'Invalid subscription type: custom'}));});it('should handle subscription management verification errors',async()=>{mockRevenueCatService.getOfferings.mockRejectedValue(new Error('Network error'));const result=await verification.verifySubscriptionManagement();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'subscription-management-failed',category:'subscription',severity:'critical'}));});});describe('Receipt Validation Verification',()=>{it('should verify valid receipt validation',async()=>{mockRevenueCatService.getStatus.mockReturnValue('ready');const result=await verification.verifyReceiptValidation();expect(result.valid).toBe(true);expect(result.issues).toHaveLength(0);});it('should detect missing transaction ID in receipt',async()=>{mockRevenueCatService.getStatus.mockReturnValue('ready');const result=await verification.verifyReceiptValidation();expect(result).toBeDefined();});it('should handle receipt validation verification errors',async()=>{mockRevenueCatService.getStatus.mockRejectedValue(new Error('Receipt validation failed'));const result=await verification.verifyReceiptValidation();expect(result.valid).toBe(false);expect(result.issues).toContainEqual(expect.objectContaining({id:'receipt-validation-failed',category:'receipt',severity:'critical'}));});});describe('Analytics Integration Verification',()=>{it('should verify valid analytics integration',async()=>{mockRevenueCatService.getStatus.mockReturnValue('ready');mockRevenueCatService.getAnalyticsEvents.mockReturnValue(['purchase-completed','purchase-started','purchase-failed','subscription-started']);const result=await verification.verifyAnalyticsIntegration();expect(result.valid).toBe(true);expect(result.issues).toHaveLength(0);});it('should detect RevenueCat service not ready',async()=>{mockRevenueCatService.getStatus.mockReturnValue('initializing');const result=await verification.verifyAnalyticsIntegration();expect(result.valid).toBe(false);expect(result.issues).toContain('RevenueCat service not ready for analytics integration');});it('should detect missing analytics events',async()=>{mockRevenueCatService.getStatus.mockReturnValue('ready');mockRevenueCatService.getAnalyticsEvents.mockReturnValue([]);const result=await verification.verifyAnalyticsIntegration();expect(result.valid).toBe(true);expect(result.warnings).toContain('No analytics events detected for purchase tracking');});it('should handle analytics integration verification errors',async()=>{mockRevenueCatService.getStatus.mockRejectedValue(new Error('Analytics error'));const result=await verification.verifyAnalyticsIntegration();expect(result.valid).toBe(false);expect(result.issues).toContain('Analytics error');});});describe('Compliance Verification',()=>{it('should verify GDPR compliance',async()=>{mockRevenueCatService.getAnalyticsEvents.mockReturnValue(['purchase-completed','purchase-started']);const result=await verification.verifyCompliance();expect(result.valid).toBe(true);expect(result.gdprCompliant).toBe(true);expect(result.issues).toHaveLength(0);});it('should detect missing data collection points',async()=>{mockRevenueCatService.getAnalyticsEvents.mockReturnValue([]);const result=await verification.verifyCompliance();expect(result.valid).toBe(true);expect(result.warnings).toContain('No data collection points found for analytics');});it('should handle compliance verification errors',async()=>{mockRevenueCatService.getAnalyticsEvents.mockRejectedValue(new Error('Compliance error'));const result=await verification.verifyCompliance();expect(result.valid).toBe(false);expect(result.issues).toContain('Compliance error');});});describe('Full Verification',()=>{it('should perform complete verification with all checks passing',async()=>{mockRevenueCatService.getOfferings.mockResolvedValue({success:true,offerings:{current:[{identifier:'premium-monthly',displayName:'Premium Monthly',description:'Monthly premium subscription',price:9.99,currencyCode:'USD',type:'subscription',subscriptionDuration:30,pricingPhases:[{price:9.99,billingPeriod:'P1M',recurrenceMode:'FINITE_RECURRING'}]}]}});mockRevenueCatService.purchasePackage.mockResolvedValue({success:true,transactionId:'test-transaction-123',productIdentifier:'premium-monthly',receipt:{transactionId:'test-transaction-123',originalTransactionIdentifier:'test-transaction-123',productId:'premium-monthly',productIdentifier:'premium-monthly',purchaseDate:'2024-01-01T00:00:00.000Z',acknowledged:true,acknowledgedDate:'2024-01-01T00:00:00.000Z'},purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'});mockRevenueCatService.getStatus.mockReturnValue('ready');mockRevenueCatService.getAnalyticsEvents.mockReturnValue(['purchase-completed','purchase-started','purchase-failed','subscription-started']);const result=await verification.performFullVerification();expect(result.passed).toBe(true);expect(result.score).toBeGreaterThanOrEqual(80);expect(result.issues).toHaveLength(0);expect(result.timestamp).toBeGreaterThan(0);});it('should calculate correct score based on issues',async()=>{mockRevenueCatService.getOfferings.mockResolvedValue({success:true,offerings:{current:[{displayName:'Premium Monthly',description:'Monthly premium subscription',price:-5.99,currencyCode:'USD',type:'subscription'}]}});mockRevenueCatService.purchasePackage.mockResolvedValue({success:true,productIdentifier:'premium-monthly',purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'});mockRevenueCatService.getStatus.mockReturnValue('initializing');mockRevenueCatService.getAnalyticsEvents.mockReturnValue([]);const result=await verification.performFullVerification();expect(result.passed).toBe(false);expect(result.score).toBeLessThan(80);expect(result.issues.length).toBeGreaterThan(0);});it('should generate appropriate recommendations',async()=>{mockRevenueCatService.getOfferings.mockResolvedValue({success:true,offerings:{current:[{displayName:'Premium Monthly',description:'Monthly premium subscription',price:-5.99,currencyCode:'USD',type:'subscription'}]}});mockRevenueCatService.purchasePackage.mockResolvedValue({success:true,productIdentifier:'premium-monthly',purchaseToken:'test-purchase-token',purchaseDate:'2024-01-01'});mockRevenueCatService.getStatus.mockReturnValue('initializing');mockRevenueCatService.getAnalyticsEvents.mockReturnValue([]);const result=await verification.performFullVerification();expect(result.recommendations).toContain('Review product catalog structure and ensure all required fields are present');expect(result.recommendations).toContain('Test purchase flow thoroughly with various scenarios');expect(result.recommendations).toContain('Ensure all purchase events are properly tracked');});});describe('Error Handling',()=>{it('should handle service not initialized',async()=>{mockRevenueCatService.getStatus.mockReturnValue('uninitialized');const result=await verification.performFullVerification();expect(result.passed).toBe(false);expect(result.score).toBeLessThan(80);});it('should handle network errors gracefully',async()=>{mockRevenueCatService.getOfferings.mockRejectedValue(new Error('Network unavailable'));mockRevenueCatService.purchasePackage.mockRejectedValue(new Error('Network unavailable'));mockRevenueCatService.getStatus.mockRejectedValue(new Error('Network unavailable'));mockRevenueCatService.getAnalyticsEvents.mockRejectedValue(new Error('Network unavailable'));const result=await verification.performFullVerification();expect(result.passed).toBe(false);expect(result.issues.length).toBeGreaterThan(0);});});});

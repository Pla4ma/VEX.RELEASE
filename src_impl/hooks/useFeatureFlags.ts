import{useState,useEffect,useCallback}from'react';import{FeatureFlagService,FeatureFlag,FeatureFlagValue}from'../features/FeatureFlagService';import{eventBus}from'../events';import{createDebugger}from'../utils/debug';const debug=createDebugger('hooks:feature-flags');export interface FeatureFlagsState{flags:Record<string,FeatureFlag>;loading:boolean;error:Error|null;initialized:boolean;}export interface UseFeatureFlagsReturn extends FeatureFlagsState{isEnabled:(key:string)=>boolean;get:(key:string)=>FeatureFlagValue|null;setOverride:(key:string,value:FeatureFlagValue)=>Promise<void>;clearOverride:(key:string)=>Promise<void>;refresh:()=>Promise<void>;}export interface UseFeatureFlagReturn{enabled:boolean;value:FeatureFlagValue|null;loading:boolean;}let featureFlagService:FeatureFlagService|null=null;function getFeatureFlagService():FeatureFlagService{if(!featureFlagService){featureFlagService=new FeatureFlagService();}return featureFlagService;}export function useFeatureFlags():UseFeatureFlagsReturn{const service=getFeatureFlagService();const[state,setState]=useState<FeatureFlagsState>({flags:{},loading:true,error:null,initialized:false});useEffect(()=>{let mounted=true;async function init(){try{if(!service){return;}await service.initialize();if(mounted){setState({flags:service.getAll(),loading:false,error:null,initialized:true});}}catch(error){debug.error('Failed to initialize feature flags',error as Error);if(mounted){setState(prev=>({...prev,loading:false,error:error as Error,initialized:false}));}}}init();return()=>{mounted=false;};},[service]);useEffect(()=>{if(!state.initialized){return;}const unsubscribe=eventBus.subscribe('feature:updated',()=>{setState(prev=>({...prev,flags:service.getAll()}));});return unsubscribe;},[state.initialized,service]);const isEnabled=useCallback((key:string):boolean=>{return service.isEnabled(key);},[service]);const get=useCallback((key:string):FeatureFlagValue|null=>{return service.get(key,false as FeatureFlagValue);},[service]);const setOverride=useCallback(async(key:string,value:FeatureFlagValue):Promise<void>=>{await service.setOverride(key,value);setState(prev=>({...prev,flags:service.getAll()}));},[service]);const clearOverride=useCallback(async(key:string):Promise<void>=>{await service.clearOverride(key);setState(prev=>({...prev,flags:service.getAll()}));},[service]);const refresh=useCallback(async():Promise<void>=>{setState(prev=>({...prev,loading:true,error:null}));try{await service.fetchRemote();setState({flags:service.getAll(),loading:false,error:null,initialized:true});}catch(error){debug.error('Failed to refresh feature flags',error as Error);setState(prev=>({...prev,loading:false,error:error as Error}));}},[service]);return{...state,isEnabled,get,setOverride,clearOverride,refresh};}export function useFeatureFlag(key:string):UseFeatureFlagReturn{const service=getFeatureFlagService();const[state,setState]=useState<UseFeatureFlagReturn>({enabled:false,value:null,loading:true});useEffect(()=>{let mounted=true;async function init(){try{await service.initialize();if(mounted){setState({enabled:service.isEnabled(key),value:service.get(key,false as FeatureFlagValue),loading:false});}}catch(error){if(mounted){setState({enabled:false,value:false,loading:false});}}}init();const unsubscribe=eventBus.subscribe('feature:updated',(payload:{key:string;})=>{if(payload.key===key){setState({enabled:service.isEnabled(key),value:service.get(key,false as FeatureFlagValue),loading:false});}});return()=>{mounted=false;unsubscribe();};},[key,service]);return state;}

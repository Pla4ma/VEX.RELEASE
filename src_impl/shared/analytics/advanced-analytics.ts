import{capture}from'./index'; export interface FunnelStep{name:string;event:string;count:number;conversionRate:number;dropOffRate:number;}export interface Funnel{id:string;name:string;steps:FunnelStep[];totalConversionRate:number;averageTimeToConvert:number;}const ONBOARDING_FUNNEL_STEPS = [{name:'App Open',event:'app_opened'},{name:'Started Onboarding',event:'onboarding_started'},{name:'Completed Profile',event:'profile_completed'},{name:'First Session Started',event:'session_started'},{name:'First Session Completed',event:'session_completed'},{name:'Joined Squad',event:'squad_joined'}]; export function trackFunnelEvent(funnelId:string,stepName:string,userId:string,metadata?:Record<string,unknown>):void{capture('funnel_step_completed',{funnel_id:funnelId,step_name:stepName,user_id:userId,...metadata}as Record<string,unknown>);}export function calculateFunnelMetrics(funnelId:string,stepCounts:number[]):Funnel{const steps:FunnelStep[] = ONBOARDING_FUNNEL_STEPS.map((step,index)=>{const count = stepCounts[index] || 0; const prevCount = index > 0 ? stepCounts[index - 1] : count; const conversionRate = prevCount > 0 ? count / prevCount * 100 : 0; const dropOffRate = 100 - conversionRate; return{name:step.name,event:step.event,count,conversionRate,dropOffRate};}); const totalConversionRate = stepCounts[0] > 0 ? stepCounts[stepCounts.length - 1] / stepCounts[0] * 100 : 0; return{id:funnelId,name:'Onboarding Funnel',steps,totalConversionRate,averageTimeToConvert:0};}export interface Cohort{id:string;startDate:string;size:number;retention:{day1:number;day7:number;day30:number;day90:number;};ltv:number;}export interface RetentionData{cohorts:Cohort[];averageRetention:{day1:number;day7:number;day30:number;day90:number;};}export function calculateCohortRetention(cohortUsers:string[],activeUsersByDay:Map<string,Set<string>>):Cohort['retention']{if(cohortUsers.length === 0){return{day1:0,day7:0,day30:0,day90:0};}const cohortSet = new Set(cohortUsers); const baseSize = cohortUsers.length; const calculateRetention = (day:number):number=>{const activeUsers = activeUsersByDay.get(`day${day}`) || new Set(); const retained = [...activeUsers].filter(u=>cohortSet.has(u)).length; return retained / baseSize * 100;}; return{day1:calculateRetention(1),day7:calculateRetention(7),day30:calculateRetention(30),day90:calculateRetention(90)};}export function trackRetentionEvent(userId:string,day:number,isActive:boolean):void{capture('retention_event',{user_id:userId,day,is_active:isActive});}export interface RevenueMetrics{totalRevenue:number;arpu:number;arppu:number;conversionRate:number;ltv:{average:number;median:number;p90:number;};mrr:number;churnRate:number;}export interface PurchaseEvent{userId:string;productId:string;amount:number;currency:string;timestamp:number;isSubscription:boolean;subscriptionPeriod?:'monthly'|'yearly';}export function trackPurchase(event:PurchaseEvent):void{capture('purchase_completed',{user_id:event.userId,product_id:event.productId,amount:event.amount,currency:event.currency,is_subscription:event.isSubscription,subscription_period:event.subscriptionPeriod}as Record<string,unknown>); if(event.isSubscription){capture('subscription_started',{user_id:event.userId,product_id:event.productId,period:event.subscriptionPeriod});}}export function trackSubscriptionCancellation(userId:string,productId:string,reason?:string):void{capture('subscription_cancelled',{user_id:userId,product_id:productId,reason});}export function calculateRevenueMetrics(purchases:PurchaseEvent[],totalUsers:number,payingUsers:string[]):RevenueMetrics{const totalRevenue = purchases.reduce((sum,p)=>sum + p.amount,0); const uniquePayingUsers = new Set(payingUsers).size; const userRevenue = new Map<string,number>(); for(const purchase of purchases){const current = userRevenue.get(purchase.userId) || 0; userRevenue.set(purchase.userId,current + purchase.amount);}const ltvValues = [...userRevenue.values()].sort((a,b)=>a - b); const averageLtv = ltvValues.reduce((sum,v)=>sum + v,0) / (ltvValues.length || 1); const medianLtv = ltvValues[Math.floor(ltvValues.length / 2)] || 0; const p90Ltv = ltvValues[Math.floor(ltvValues.length * 0.9)] || 0; const mrr = purchases.filter(p=>p.isSubscription).reduce((sum,p)=>{if(p.subscriptionPeriod === 'yearly'){return sum + p.amount / 12;}return sum + p.amount;},0); return{totalRevenue,arpu:totalRevenue / (totalUsers || 1),arppu:totalRevenue / (uniquePayingUsers || 1),conversionRate:uniquePayingUsers / (totalUsers || 1) * 100,ltv:{average:averageLtv,median:medianLtv,p90:p90Ltv},mrr,churnRate:0};}export interface MetricAlert{id:string;metric:string;threshold:number;operator:'>'|'<'|'=';currentValue:number;severity:'WARNING'|'CRITICAL';triggeredAt:number;}export interface AlertRule{id:string;name:string;metric:string;threshold:number;operator:'>'|'<'|'=';duration:number;severity:'WARNING'|'CRITICAL';notifyChannels:string[];}const DEFAULT_ALERT_RULES:AlertRule[] = [{id:'alert-d1-retention-drop',name:'D1 Retention Drop',metric:'retention.day1',threshold:10,operator:'<',duration:60,severity:'CRITICAL',notifyChannels:['email','slack']},{id:'alert-session-crash-rate',name:'Session Crash Rate High',metric:'crash.session_rate',threshold:1,operator:'>',duration:30,severity:'WARNING',notifyChannels:['email']},{id:'alert-revenue-drop',name:'Daily Revenue Drop',metric:'revenue.daily',threshold:20,operator:'<',duration:120,severity:'CRITICAL',notifyChannels:['email','slack','pagerduty']}]; export function checkAlerts(currentMetrics:Record<string,number>,rules:AlertRule[] = DEFAULT_ALERT_RULES):MetricAlert[]{const triggered:MetricAlert[] = []; for(const rule of rules){const currentValue = currentMetrics[rule.metric]; if(currentValue === undefined){continue;}let isTriggered = false; switch(rule.operator){case'>':isTriggered = currentValue > rule.threshold; break; case'<':isTriggered = currentValue < rule.threshold; break; case'=':isTriggered = currentValue === rule.threshold; break;}if(isTriggered){triggered.push({id:`alert-${Date.now()}-${rule.id}`,metric:rule.metric,threshold:rule.threshold,operator:rule.operator,currentValue,severity:rule.severity,triggeredAt:Date.now()}); capture('alert_triggered',{rule_id:rule.id,metric:rule.metric,current_value:currentValue,threshold:rule.threshold,severity:rule.severity}as Record<string,unknown>);}}return triggered;}export function createDashboardData(_timeRange:'1d'|'7d'|'30d'|'90d'):{revenue:RevenueMetrics;retention:RetentionData;funnels:Funnel[];alerts:MetricAlert[];}{return{revenue:{totalRevenue:0,arpu:0,arppu:0,conversionRate:0,ltv:{average:0,median:0,p90:0},mrr:0,churnRate:0},retention:{cohorts:[],averageRetention:{day1:0,day7:0,day30:0,day90:0}},funnels:[],alerts:[]};}

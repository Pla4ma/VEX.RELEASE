import React,{useMemo}from'react';import{View,ViewStyle}from'react-native';import Animated,{FadeIn,FadeInUp,FadeInDown,FadeInLeft,FadeInRight,useReducedMotion,Easing}from'react-native-reanimated';import{createSheet}from'@/shared/ui/create-sheet';export type EnterDirection='up'|'down'|'left'|'right'|'fade';export type EnterSpeed='instant'|'fast'|'normal'|'slow';export interface EnterAnimationProps{children:React.ReactNode;direction?:EnterDirection;speed?:EnterSpeed;delay?:number;distance?:number;style?:ViewStyle;enabled?:boolean;}export interface StaggeredEnterProps{children:React.ReactNode[];direction?:EnterDirection;speed?:EnterSpeed;staggerDelay?:number;initialDelay?:number;distance?:number;containerStyle?:ViewStyle;}const SPEED_CONFIGS:Record<EnterSpeed,{duration:number;easing:typeof Easing.ease;}>={instant:{duration:0,easing:Easing.ease},fast:{duration:200,easing:Easing.out(Easing.quad)},normal:{duration:350,easing:Easing.out(Easing.cubic)},slow:{duration:500,easing:Easing.out(Easing.cubic)}};const getEnterAnimation=(direction:EnterDirection,speed:EnterSpeed,delay:number,distance:number,reducedMotion:boolean)=>{const config=SPEED_CONFIGS[speed];if(reducedMotion||speed==='instant'){return FadeIn.duration(0).delay(0);}const baseDelay=delay;switch(direction){case'up':return FadeInUp.duration(config.duration).delay(baseDelay).easing(config.easing);case'down':return FadeInDown.duration(config.duration).delay(baseDelay).easing(config.easing);case'left':return FadeInLeft.duration(config.duration).delay(baseDelay).easing(config.easing);case'right':return FadeInRight.duration(config.duration).delay(baseDelay).easing(config.easing);case'fade':default:return FadeIn.duration(config.duration).delay(baseDelay).easing(config.easing);}};export const EnterAnimation:React.FC<EnterAnimationProps>=({children,direction='up',speed='normal',delay=0,style,enabled=true})=>{const reducedMotion=useReducedMotion();const entering=useMemo(()=>{if(!enabled){return undefined;}return getEnterAnimation(direction,speed,delay,20,reducedMotion??false);},[direction,speed,delay,enabled,reducedMotion]);return<Animated.View entering={entering}style={style}>
      {children}
    </Animated.View>;};export const StaggeredEnter:React.FC<StaggeredEnterProps>=({children,direction='up',speed='normal',staggerDelay=40,initialDelay=0,containerStyle})=>{const reducedMotion=useReducedMotion();const animatedChildren=useMemo(()=>{return React.Children.map(children,(child,index)=>{if(!React.isValidElement(child)){return child;}const delay=initialDelay+index*staggerDelay;const entering=getEnterAnimation(direction,speed,delay,20,reducedMotion??false);return<Animated.View key={child.key||index}entering={entering}style={styles.staggerItem}>
          {child}
        </Animated.View>;});},[children,direction,speed,staggerDelay,initialDelay,reducedMotion]);return<View style={[styles.staggerContainer,containerStyle]}>
      {animatedChildren}
    </View>;};export interface CardEnterAnimationProps{children:React.ReactNode;index?:number;total?:number;style?:ViewStyle;}export const CardEnterAnimation:React.FC<CardEnterAnimationProps>=({children,index=0,total:_total=1,style})=>{const reducedMotion=useReducedMotion();const delay=Math.min(index*80,400);const entering=useMemo(()=>{return getEnterAnimation('up','normal',delay,20,reducedMotion??false);},[delay,reducedMotion]);return<Animated.View entering={entering}style={style}>
      {children}
    </Animated.View>;};export const ScreenEnterAnimation:React.FC<{children:React.ReactNode;style?:ViewStyle;}>=({children,style})=>{const reducedMotion=useReducedMotion();const entering=useMemo(()=>{return getEnterAnimation('fade','normal',0,0,reducedMotion??false);},[reducedMotion]);return<Animated.View entering={entering}style={[styles.screenContainer,style]}>
      {children}
    </Animated.View>;};export const HeroEnterAnimation:React.FC<{children:React.ReactNode;delay?:number;style?:ViewStyle;}>=({children,delay=0,style})=>{const reducedMotion=useReducedMotion();const entering=useMemo(()=>{return getEnterAnimation('up','slow',delay,30,reducedMotion??false);},[delay,reducedMotion]);return<Animated.View entering={entering}style={style}>
      {children}
    </Animated.View>;};const styles=createSheet({staggerContainer:{gap:0},staggerItem:{},screenContainer:{flex:1}});export default EnterAnimation;

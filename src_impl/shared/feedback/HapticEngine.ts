const Haptics=require('expo-haptics');import{createDebugger}from'@/utils/debug';import{Platform}from'react-native';const debug=createDebugger('feedback:haptics');export type HapticIntensity='light'|'medium'|'heavy';export type HapticContext='success'|'error'|'warning'|'neutral'|'selection'|'impact';interface HapticConfig{enabled:boolean;respectBattery:boolean;respectSystemPreference:boolean;intensity:HapticIntensity;}interface HapticPattern{type:'notification'|'impact'|'custom';style?:string;delays?:number[];}const DEFAULT_CONFIG:HapticConfig={enabled:true,respectBattery:true,respectSystemPreference:true,intensity:'medium'};const PATTERNS:Record<HapticContext,HapticPattern>={success:{type:'notification',style:Haptics?.NotificationFeedbackType?.Success??'success'},error:{type:'notification',style:Haptics?.NotificationFeedbackType?.Error??'error'},warning:{type:'notification',style:Haptics?.NotificationFeedbackType?.Warning??'warning'},neutral:{type:'impact',style:Haptics?.ImpactFeedbackStyle?.Light??'light'},selection:{type:'impact',style:Haptics?.ImpactFeedbackStyle?.Light??'light'},impact:{type:'impact',style:Haptics?.ImpactFeedbackStyle?.Medium??'medium'}};class HapticEngine{private config:HapticConfig=DEFAULT_CONFIG;private batteryLevel:number=1.0;private lastTriggerTime:number=0;private cooldownMs:number=50;constructor(){this.loadConfig();}private async loadConfig():Promise<void>{}setBatteryLevel(level:number):void{this.batteryLevel=Math.max(0,Math.min(1,level));}private shouldTrigger():boolean{if(!this.config.enabled){return false;}if(Platform.OS==='web'){return false;}if(this.config.respectBattery&&this.batteryLevel<0.1){return false;}const now=Date.now();if(now-this.lastTriggerTime<this.cooldownMs){return false;}return true;}async trigger(context:HapticContext,intensity?:HapticIntensity):Promise<void>{if(!this.shouldTrigger()){return;}const pattern=PATTERNS[context];const targetIntensity=intensity||this.config.intensity;try{this.lastTriggerTime=Date.now();switch(pattern.type){case'notification':await Haptics?.notificationAsync?.(pattern.style);break;case'impact':const style=this.getImpactStyle(targetIntensity);await Haptics?.impactAsync?.(style);break;}debug.info('Haptic triggered: %s (%s)',context,targetIntensity);}catch(error){debug.warn('Haptic failed: %s',error instanceof Error?error.message:String(error));}}private getImpactStyle(intensity:HapticIntensity):string{const styles=Haptics?.ImpactFeedbackStyle;switch(intensity){case'light':return styles?.Light??'light';case'heavy':return styles?.Heavy??'heavy';case'medium':default:return styles?.Medium??'medium';}}async success(intensity?:HapticIntensity):Promise<void>{await this.trigger('success',intensity);}async error(intensity?:HapticIntensity):Promise<void>{await this.trigger('error',intensity);}async warning(intensity?:HapticIntensity):Promise<void>{await this.trigger('warning',intensity);}async selection():Promise<void>{await this.trigger('selection','light');}async impact(intensity?:HapticIntensity):Promise<void>{await this.trigger('impact',intensity);}async doubleTap(intensity:HapticIntensity='light'):Promise<void>{if(!this.shouldTrigger()){return;}const style=this.getImpactStyle(intensity);try{await Haptics.impactAsync(style);setTimeout(async()=>{await Haptics.impactAsync(style);},80);debug.info('Haptic double-tap triggered');}catch(error){debug.warn('Double-tap haptic failed: %s',error instanceof Error?error.message:String(error));}}async heartbeat():Promise<void>{if(!this.shouldTrigger()){return;}try{await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);setTimeout(async()=>{await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);},150);debug.info('Haptic heartbeat triggered');}catch(error){debug.warn('Heartbeat haptic failed: %s',error instanceof Error?error.message:String(error));}}async celebration():Promise<void>{if(!this.shouldTrigger()){return;}try{await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);setTimeout(async()=>{await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium);},100);setTimeout(async()=>{await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light);},200);debug.info('Haptic celebration triggered');}catch(error){debug.warn('Celebration haptic failed: %s',error instanceof Error?error.message:String(error));}}setConfig(config:Partial<HapticConfig>):void{this.config={...this.config,...config};debug.info('Haptic config updated: %o',this.config);}setEnabled(enabled:boolean):void{this.config.enabled=enabled;debug.info('Haptics %s',enabled?'enabled':'disabled');}}let hapticEngine:HapticEngine|null=null;export function getHapticEngine():HapticEngine{if(!hapticEngine){hapticEngine=new HapticEngine();}return hapticEngine;}export const haptics={success:(intensity?:HapticIntensity)=>getHapticEngine().success(intensity),error:(intensity?:HapticIntensity)=>getHapticEngine().error(intensity),warning:(intensity?:HapticIntensity)=>getHapticEngine().warning(intensity),selection:()=>getHapticEngine().selection(),impact:(intensity?:HapticIntensity)=>getHapticEngine().impact(intensity),doubleTap:(intensity?:HapticIntensity)=>getHapticEngine().doubleTap(intensity),heartbeat:()=>getHapticEngine().heartbeat(),celebration:()=>getHapticEngine().celebration(),trigger:(context:HapticContext,intensity?:HapticIntensity)=>getHapticEngine().trigger(context,intensity),setEnabled:(enabled:boolean)=>getHapticEngine().setEnabled(enabled),setBatteryLevel:(level:number)=>getHapticEngine().setBatteryLevel(level)};export default HapticEngine;

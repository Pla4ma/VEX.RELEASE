import{captureSilentFailure}from'../../utils/silent-failure'; import{z}from'zod'; export function isNonEmptyString(value:unknown):value is string{return typeof value === 'string' && value.length > 0;}export function isPositiveInteger(value:unknown):value is number{return typeof value === 'number' && Number.isInteger(value) && value > 0;}export function isNonNegativeNumber(value:unknown):value is number{return typeof value === 'number' && !isNaN(value) && value >= 0;}export function isValidEmail(value:unknown):value is string{if(typeof value !== 'string'){return false;}const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; return emailRegex.test(value);}export function isValidUUID(value:unknown):value is string{if(typeof value !== 'string'){return false;}const uuidRegex = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i; return uuidRegex.test(value);}export function isPlainObject(value:unknown):value is Record<string,unknown>{return typeof value === 'object' && value !== null && !Array.isArray(value);}export interface RangeValidationResult{valid:boolean;clamped:number;violations:string[];}export function validateRange(value:number,min:number,max:number,options:{inclusive?:boolean;integer?:boolean;name?:string;} = {}):RangeValidationResult{const{inclusive = true,integer = false,name = 'value'} = options; const violations:string[] = []; if(isNaN(value)){return{valid:false,clamped:min,violations:[`${name} is not a valid number`]};}let clamped = value; if(inclusive){if(value < min){violations.push(`${name} must be at least ${min}`); clamped = min;}if(value > max){violations.push(`${name} must be at most ${max}`); clamped = max;}}else{if(value <= min){violations.push(`${name} must be greater than ${min}`); clamped = min + (max - min) * 0.001;}if(value >= max){violations.push(`${name} must be less than ${max}`); clamped = max - (max - min) * 0.001;}}if(integer && !Number.isInteger(value)){violations.push(`${name} must be a whole number`); clamped = Math.round(clamped);}return{valid:violations.length === 0,clamped,violations};}export function clamp(value:number,min:number,max:number):number{return Math.min(Math.max(value,min),max);}export function clamp01(value:number):number{return clamp(value,0,1);}export interface ValidationResult<T>{success:boolean;data?:T;errors:string[];fieldErrors:Record<string,string[]>;}export function validateSchema<T>(schema:z.ZodSchema<T>,data:unknown):ValidationResult<T>{const result = schema.safeParse(data); if(result.success){return{success:true,data:result.data,errors:[],fieldErrors:{}};}const fieldErrors:Record<string,string[]> = {}; const errors:string[] = []; result.error.errors.forEach(err=>{const path = err.path.join('.'); const message = err.message; if(path){fieldErrors[path] = fieldErrors[path] || []; fieldErrors[path].push(message);}errors.push(path ? `${path}: ${message}` : message);}); return{success:false,errors,fieldErrors};}export function createValidator<T>(schema:z.ZodSchema<T>){return(data:unknown):ValidationResult<T>=>validateSchema(schema,data);}export function sanitizeString(input:string,options:{maxLength?:number;trim?:boolean;lowercase?:boolean;removeSpecialChars?:boolean;} = {}):string{let result = input; if(options.trim !== false){result = result.trim();}if(options.lowercase){result = result.toLowerCase();}if(options.removeSpecialChars){result = result.replace(/[^a-zA-Z0-9\s]/g,'');}if(options.maxLength && result.length > options.maxLength){result = result.slice(0,options.maxLength);}return result;}export function truncateString(input:string,maxLength:number,suffix:string = '...'):string{if(input.length <= maxLength){return input;}return input.slice(0,maxLength - suffix.length) + suffix;}export function formatNumber(value:number,options:{decimals?:number;compact?:boolean;locale?:string;} = {}):string{const{decimals = 0,compact = false,locale = 'en-US'} = options; if(compact && Math.abs(value) >= 1000){return Intl.NumberFormat(locale,{notation:'compact',maximumFractionDigits:decimals}).format(value);}return value.toLocaleString(locale,{minimumFractionDigits:decimals,maximumFractionDigits:decimals});}export function parseNumber(value:unknown):number|null{if(typeof value === 'number'){return value;}if(typeof value === 'string'){const parsed = parseFloat(value); return isNaN(parsed) ? null : parsed;}return null;}export function validateArray<T>(value:unknown,itemValidator:(item:unknown)=>item is T,options:{minLength?:number;maxLength?:number;unique?:boolean;uniqueKey?:keyof T;} = {}):{valid:boolean;data:T[];errors:string[];}{const errors:string[] = []; if(!Array.isArray(value)){return{valid:false,data:[],errors:['Value is not an array']};}const validItems:T[] = []; const invalidIndices:number[] = []; value.forEach((item,index)=>{if(itemValidator(item)){validItems.push(item);}else{invalidIndices.push(index);}}); if(invalidIndices.length > 0){errors.push(`Invalid items at indices: ${invalidIndices.join(', ')}`);}if(options.minLength !== undefined && validItems.length < options.minLength){errors.push(`Array must have at least ${options.minLength} items`);}if(options.maxLength !== undefined && validItems.length > options.maxLength){errors.push(`Array must have at most ${options.maxLength} items`);}if(options.unique){if(options.uniqueKey){const seen = new Set<unknown>(); validItems.forEach((item,index)=>{const key = item[options.uniqueKey!]; if(seen.has(key)){errors.push(`Duplicate value at index ${index} for key ${String(options.uniqueKey)}`);}seen.add(key);});}else{const seen = new Set(validItems); if(seen.size !== validItems.length){errors.push('Array contains duplicate values');}}}return{valid:errors.length === 0,data:validItems,errors};}export function isValidDate(value:unknown):value is Date{return value instanceof Date && !isNaN(value.getTime());}export function validateDateRange(start:Date,end:Date,options:{maxDuration?:number;allowSame?:boolean;} = {}):{valid:boolean;errors:string[];}{const errors:string[] = []; if(!isValidDate(start)){errors.push('Start date is invalid');}if(!isValidDate(end)){errors.push('End date is invalid');}if(errors.length > 0){return{valid:false,errors};}if(end < start){errors.push('End date must be after start date');}if(!options.allowSame && end.getTime() === start.getTime()){errors.push('Start and end dates must be different');}if(options.maxDuration){const duration = end.getTime() - start.getTime(); if(duration > options.maxDuration){errors.push(`Duration exceeds maximum of ${options.maxDuration}ms`);}}return{valid:errors.length === 0,errors};}export function isValidURL(value:unknown):value is string{if(typeof value !== 'string'){return false;}try{new URL(value); return true;}catch(error){captureSilentFailure(error,{feature:'shared',operation:'safe-fallback',type:'data'}); return false;}}export function isValidImageURL(value:unknown):boolean{if(!isValidURL(value)){return false;}const imageExtensions = ['.jpg','.jpeg','.png','.gif','.webp','.svg']; const url = (value as string).toLowerCase(); return imageExtensions.some(ext=>url.endsWith(ext));}export interface PasswordValidationResult{valid:boolean;strength:'weak'|'medium'|'strong';score:number;feedback:string[];}export function validatePassword(password:string):PasswordValidationResult{const feedback:string[] = []; let score = 0; if(password.length >= 8){score += 1;}else{feedback.push('Password must be at least 8 characters');}if(password.length >= 12){score += 1;}if(/[a-z]/.test(password)){score += 1;}else{feedback.push('Add lowercase letters');}if(/[A-Z]/.test(password)){score += 1;}else{feedback.push('Add uppercase letters');}if(/\d/.test(password)){score += 1;}else{feedback.push('Add numbers');}if(/[^a-zA-Z0-9]/.test(password)){score += 1;}else{feedback.push('Add special characters');}let strength:'weak'|'medium'|'strong'; if(score >= 5){strength = 'strong';}else if(score >= 3){strength = 'medium';}else{strength = 'weak';}return{valid:score >= 3 && password.length >= 8,strength,score,feedback};}export default{isNonEmptyString,isPositiveInteger,isNonNegativeNumber,isValidEmail,isValidUUID,isPlainObject,validateRange,clamp,clamp01,validateSchema,createValidator,sanitizeString,truncateString,formatNumber,parseNumber,validateArray,isValidDate,validateDateRange,isValidURL,isValidImageURL,validatePassword};

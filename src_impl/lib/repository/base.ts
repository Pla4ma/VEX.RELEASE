import{getSupabaseClient}from'../../config/supabase';export enum RepositoryErrorCode{NETWORK_ERROR='NETWORK_ERROR',AUTH_ERROR='AUTH_ERROR',NOT_FOUND='NOT_FOUND',CONFLICT='CONFLICT',VALIDATION_ERROR='VALIDATION_ERROR',RATE_LIMIT='RATE_LIMIT',SERVER_ERROR='SERVER_ERROR',UNKNOWN='UNKNOWN',}export class RepositoryError extends Error{public readonly code:RepositoryErrorCode;public readonly isRetryable:boolean;public readonly originalError:unknown;constructor(operation:string,error:unknown,code:RepositoryErrorCode=RepositoryErrorCode.UNKNOWN){const message=error instanceof Error?error.message:'Unknown error';super(`[${operation}] ${message}`);this.name='RepositoryError';this.originalError=error;this.code=code;this.isRetryable=code===RepositoryErrorCode.NETWORK_ERROR||code===RepositoryErrorCode.RATE_LIMIT||code===RepositoryErrorCode.SERVER_ERROR;}}function classifyError(error:unknown):RepositoryErrorCode{if(!error||typeof error!=='object'){return RepositoryErrorCode.UNKNOWN;}const err=error as{code?:string;message?:string;status?:number;};if(err.code){if(err.code==='PGRST116'){return RepositoryErrorCode.NOT_FOUND;}if(err.code==='23505'){return RepositoryErrorCode.CONFLICT;}if(err.code==='PGRST301'){return RepositoryErrorCode.AUTH_ERROR;}if(err.code.startsWith('22')){return RepositoryErrorCode.VALIDATION_ERROR;}if(err.code.startsWith('28')){return RepositoryErrorCode.AUTH_ERROR;}if(err.code.startsWith('42')){return RepositoryErrorCode.VALIDATION_ERROR;}}if(err.status){if(err.status===401||err.status===403){return RepositoryErrorCode.AUTH_ERROR;}if(err.status===404){return RepositoryErrorCode.NOT_FOUND;}if(err.status===409){return RepositoryErrorCode.CONFLICT;}if(err.status===422){return RepositoryErrorCode.VALIDATION_ERROR;}if(err.status===429){return RepositoryErrorCode.RATE_LIMIT;}if(err.status>=500){return RepositoryErrorCode.SERVER_ERROR;}}if(err.message?.includes('fetch')||err.message?.includes('network')||err.message?.includes('ECONNREFUSED')||err.message?.includes('ETIMEDOUT')){return RepositoryErrorCode.NETWORK_ERROR;}return RepositoryErrorCode.UNKNOWN;}export interface RetryConfig{maxAttempts:number;baseDelayMs:number;maxDelayMs:number;backoffMultiplier:number;}const DEFAULT_RETRY_CONFIG:RetryConfig={maxAttempts:3,baseDelayMs:100,maxDelayMs:5000,backoffMultiplier:2};function sleep(ms:number):Promise<void>{return new Promise(resolve=>setTimeout(resolve,ms));}function calculateDelay(attempt:number,config:RetryConfig):number{const exponential=config.baseDelayMs*Math.pow(config.backoffMultiplier,attempt-1);const jitter=Math.random()*100;return Math.min(exponential+jitter,config.maxDelayMs);}export async function withRetry<T>(operation:string,fn:()=>Promise<T>,config:Partial<RetryConfig>={}):Promise<T>{const retryConfig={...DEFAULT_RETRY_CONFIG,...config};let lastError:RepositoryError|undefined;for(let attempt=1;attempt<=retryConfig.maxAttempts;attempt++){try{return await fn();}catch(error){const code=classifyError(error);lastError=new RepositoryError(operation,error,code);if(!lastError.isRetryable){throw lastError;}if(attempt===retryConfig.maxAttempts){break;}const delay=calculateDelay(attempt,retryConfig);await sleep(delay);}}throw lastError;}export type ConnectionState='online'|'offline'|'unknown';let currentConnectionState:ConnectionState='unknown';const connectionListeners:Set<(state:ConnectionState)=>void>=new Set();export function getConnectionState():ConnectionState{return currentConnectionState;}export function subscribeToConnectionChanges(callback:(state:ConnectionState)=>void):()=>void{connectionListeners.add(callback);return()=>connectionListeners.delete(callback);}export function updateConnectionState(state:ConnectionState):void{if(currentConnectionState!==state){currentConnectionState=state;connectionListeners.forEach(cb=>cb(state));}}export interface VersionedEntity{id:string;version:number;updatedAt:number;}export async function withOptimisticLock<T extends VersionedEntity>(operation:string,fetchFn:()=>Promise<T|null>,updateFn:(entity:T)=>Promise<T>,expectedVersion:number):Promise<T>{return withRetry(operation,async()=>{const entity=await fetchFn();if(!entity){throw new RepositoryError(operation,{code:'PGRST116'},RepositoryErrorCode.NOT_FOUND);}if(entity.version!==expectedVersion){throw new RepositoryError(operation,{message:'Version conflict detected',code:'409'},RepositoryErrorCode.CONFLICT);}return updateFn(entity);});}export interface BatchResult<TItem,TResult=TItem>{successful:TResult[];failed:Array<{item:TItem;error:RepositoryError;}>;}export async function batchWithRetry<T,R>(operation:string,items:T[],fn:(item:T)=>Promise<R>,config:Partial<RetryConfig>&{continueOnError?:boolean;}={}):Promise<BatchResult<T,R>>{const{continueOnError=true,...retryConfig}=config;const result:BatchResult<T,R>={successful:[],failed:[]};for(const item of items){try{const res=await withRetry(`${operation}`,()=>fn(item),retryConfig);result.successful.push(res);}catch(error){const repoError=error instanceof RepositoryError?error:new RepositoryError(operation,error);result.failed.push({item,error:repoError});if(!continueOnError){throw repoError;}}}return result;}export function createRetryableQuery(){const supabase=getSupabaseClient();return{async query<T>(operation:string,fn:(client:typeof supabase)=>Promise<{data:T|null;error:Error|null;}>):Promise<T>{return withRetry(operation,async()=>{const{data,error}=await fn(supabase);if(error){const code=classifyError(error);throw new RepositoryError(operation,error,code);}if(data===null){throw new RepositoryError(operation,{code:'PGRST116'},RepositoryErrorCode.NOT_FOUND);}return data;});},async queryNullable<T>(operation:string,fn:(client:typeof supabase)=>Promise<{data:T|null;error:Error|null;}>):Promise<T|null>{return withRetry(operation,async()=>{const{data,error}=await fn(supabase);if(error){const code=classifyError(error);if(code===RepositoryErrorCode.NOT_FOUND){return null;}throw new RepositoryError(operation,error,code);}return data;});}};}

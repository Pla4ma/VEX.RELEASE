import{http,HttpResponse,delay}from'msw';const authHandlers=[http.post('*/auth/v1/token',async({request})=>{const body=(await request.json())as{email?:string;password?:string;};if(!body.email||!body.password){return HttpResponse.json({error:'Email and password required'},{status:400});}if(body.password==='wrong-password'){return HttpResponse.json({error:'Invalid credentials'},{status:401});}await delay(100);return HttpResponse.json({access_token:'mock-access-token',refresh_token:'mock-refresh-token',user:{id:'user-123',email:body.email,user_metadata:{name:'Test User'}}});}),http.post('*/auth/v1/signup',async({request})=>{const body=(await request.json())as{email?:string;password?:string;};if(!body.email||!body.password){return HttpResponse.json({error:'Email and password required'},{status:400});}await delay(100);return HttpResponse.json({user:{id:'new-user-123',email:body.email,created_at:new Date().toISOString()}});}),http.post('*/auth/v1/logout',async()=>{await delay(50);return HttpResponse.json({success:true});}),http.get('*/auth/v1/user',async({request})=>{const authHeader=request.headers.get('authorization');if(!authHeader||!authHeader.includes('Bearer')){return HttpResponse.json({error:'Unauthorized'},{status:401});}return HttpResponse.json({id:'user-123',email:'test@example.com',user_metadata:{name:'Test User'}});})];const databaseHandlers=[http.get('*/rest/v1/wallets',async({request})=>{const url=new URL(request.url);const userId=url.searchParams.get('user_id')?.split('.')[1];if(!userId){return HttpResponse.json([],{status:200});}await delay(50);return HttpResponse.json([{id:'wallet-1',user_id:userId,coins:1000,gems:50,created_at:new Date().toISOString(),updated_at:new Date().toISOString()}]);}),http.patch('*/rest/v1/wallets',async({request})=>{const body=(await request.json())as{coins?:number;gems?:number;};await delay(50);return HttpResponse.json([{id:'wallet-1',...body,updated_at:new Date().toISOString()}]);}),http.get('*/rest/v1/inventory_items',async()=>{await delay(50);return HttpResponse.json([{id:'inv-1',user_id:'user-123',item_definition_id:'item-1',quantity:5,created_at:new Date().toISOString()},{id:'inv-2',user_id:'user-123',item_definition_id:'item-2',quantity:1,durability:100,created_at:new Date().toISOString()}]);}),http.get('*/rest/v1/item_definitions',async()=>{await delay(50);return HttpResponse.json([{id:'item-1',name:'Health Potion',description:'Restores health',type:'CONSUMABLE',rarity:'COMMON',icon:'potion-red',max_stack:99,tradable:true},{id:'item-2',name:'Iron Sword',description:'A sturdy sword',type:'EQUIPMENT',rarity:'UNCOMMON',icon:'sword-iron',max_stack:1,durability:100,tradable:true}]);}),http.get('*/rest/v1/wallet_transactions',async()=>{await delay(50);return HttpResponse.json([{id:'tx-1',user_id:'user-123',type:'EARN',currency:'COINS',amount:100,balance_before:900,balance_after:1000,source:'SESSION',description:'Session reward',created_at:new Date().toISOString()}]);}),http.post('*/rest/v1/wallet_transactions',async({request})=>{const body=(await request.json())as Record<string,unknown>;await delay(50);return HttpResponse.json([{id:'tx-new',...body,created_at:new Date().toISOString()}]);})];const sessionHandlers=[http.post('*/rest/v1/sessions',async({request})=>{const body=(await request.json())as Record<string,unknown>;await delay(100);return HttpResponse.json([{id:'session-1',...body,status:'ACTIVE',created_at:new Date().toISOString()}]);}),http.patch('*/rest/v1/sessions',async({request})=>{const body=(await request.json())as Record<string,unknown>;await delay(50);return HttpResponse.json([{id:'session-1',...body,updated_at:new Date().toISOString()}]);}),http.get('*/rest/v1/sessions',async({request})=>{const url=new URL(request.url);const status=url.searchParams.get('status');await delay(50);if(status==='eq.ACTIVE'){return HttpResponse.json([{id:'session-1',user_id:'user-123',type:'FOCUS',status:'ACTIVE',started_at:new Date().toISOString(),target_duration:1800}]);}return HttpResponse.json([]);})];const errorHandlers=[http.get('*/network-error',async()=>{return HttpResponse.error();}),http.get('*/timeout',async()=>{await delay(30000);return HttpResponse.json({});}),http.get('*/rate-limit',async()=>{return HttpResponse.json({error:'Rate limit exceeded'},{status:429,headers:{'Retry-After':'60'}});}),http.get('*/server-error',async()=>{return HttpResponse.json({error:'Internal server error'},{status:500});}),http.get('*/auth-error',async()=>{return HttpResponse.json({error:'Unauthorized'},{status:401});})];export const handlers=[...authHandlers,...databaseHandlers,...sessionHandlers,...errorHandlers];export{authHandlers,databaseHandlers,sessionHandlers,errorHandlers};

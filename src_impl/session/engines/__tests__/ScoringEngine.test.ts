import{ScoringEngine}from'../ScoringEngine';import{SessionMode}from'../../modes';import type{SessionState,FocusQualityMetrics}from'../../types';describe('ScoringEngine',()=>{let engine:ScoringEngine;const createMockSession=(overrides:Partial<SessionState>={}):SessionState=>({id:'test-session',userId:'test-user',status:'COMPLETED',phase:'FOCUS',config:{duration:1500,breakDuration:300,longBreakDuration:900,intervals:4,longBreakInterval:4,soundEnabled:false,vibrationEnabled:false,dndEnabled:false,strictMode:false,autoStartBreaks:false,autoStartPomodoros:false,...overrides.config},currentInterval:1,totalIntervals:4,elapsedTime:1500,remainingTime:0,effectiveTime:1500,completionPercentage:100,interruptions:0,pauses:0,startTime:Date.now()-1500000,endTime:Date.now(),...overrides})as SessionState;const createMockFocusMetrics=(overrides:Partial<FocusQualityMetrics>={}):FocusQualityMetrics=>({sessionId:'test-session',timeInDeepFocus:1200,timeInShallowFocus:200,timeDistracted:100,focusSegments:[],consistencyScore:85,depthScore:90,recoveryScore:80,overallScore:85,calculatedAt:Date.now(),...overrides});beforeEach(()=>{engine=new ScoringEngine();});describe('Score Calculation',()=>{it('should calculate base points from session duration',()=>{const session=createMockSession();const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);expect(calculation.basePoints).toBeGreaterThan(0);expect(calculation.basePoints).toBe(625);});it('should apply time multiplier based on completion',()=>{const fullSession=createMockSession({completionPercentage:100});const partialSession=createMockSession({completionPercentage:50});const metrics=createMockFocusMetrics();const fullCalc=engine.calculateScore(fullSession,metrics);const partialCalc=engine.calculateScore(partialSession,metrics);expect(fullCalc.timeMultiplier).toBeGreaterThan(partialCalc.timeMultiplier);});it('should calculate pause penalties',()=>{const noPause=createMockSession({pauses:0});const withPauses=createMockSession({pauses:5});const metrics=createMockFocusMetrics();const noPauseCalc=engine.calculateScore(noPause,metrics);const withPausesCalc=engine.calculateScore(withPauses,metrics);expect(withPausesCalc.pausePenalty).toBeGreaterThan(noPauseCalc.pausePenalty);});it('should calculate interruption penalties',()=>{const noInterrupt=createMockSession({interruptions:0});const withInterrupts=createMockSession({interruptions:3});const metrics=createMockFocusMetrics();const noInterruptCalc=engine.calculateScore(noInterrupt,metrics);const withInterruptsCalc=engine.calculateScore(withInterrupts,metrics);expect(withInterruptsCalc.interruptionPenalty).toBeGreaterThan(noInterruptCalc.interruptionPenalty);});it('should calculate final score',()=>{const session=createMockSession();const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);const finalScore=engine.calculateFinalScore(calculation);expect(finalScore).toBeGreaterThan(0);});it('should apply comeback multiplier bonus when configured',()=>{const session=createMockSession({config:{...createMockSession().config,comebackMultiplier:2}});const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);expect(calculation.comebackMultiplier).toBe(2);expect(calculation.comebackBonus).toBeGreaterThan(0);});});describe('Streak Multipliers',()=>{it('should apply streak multiplier',()=>{engine.setUserStats(7,1);const session=createMockSession();const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);expect(calculation.streakMultiplier).toBe(1.25);});it('should calculate streak bonus',()=>{engine.setUserStats(7,1);const session=createMockSession();const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);expect(calculation.streakBonus).toBeGreaterThan(0);});});describe('Quality Multipliers',()=>{it('should apply quality multiplier from focus metrics',()=>{const highQuality=createMockFocusMetrics({overallScore:95});const lowQuality=createMockFocusMetrics({overallScore:40});const session=createMockSession();const highCalc=engine.calculateScore(session,highQuality);const lowCalc=engine.calculateScore(session,lowQuality);expect(highCalc.qualityMultiplier).toBeGreaterThan(lowCalc.qualityMultiplier);});});describe('Mode Differentiation',()=>{it('applies deep work XP and strict pause penalties',()=>{const baseSession=createMockSession({pauses:3,pausedTime:10000});const deepSession=createMockSession({pauses:3,pausedTime:31000,config:{...baseSession.config,sessionMode:SessionMode.DEEP_WORK}});const metrics=createMockFocusMetrics({consistencyScore:95,overallScore:95});const baseCalc=engine.calculateScore(baseSession,metrics);const deepCalc=engine.calculateScore(deepSession,metrics);expect(deepCalc.basePoints).toBeGreaterThan(baseCalc.basePoints);expect(deepCalc.pausePenalty).toBeGreaterThan(baseCalc.pausePenalty);});it('halves light focus pause penalties',()=>{const baseSession=createMockSession({pauses:5,config:{...createMockSession().config,sessionMode:SessionMode.STUDY}});const lightSession=createMockSession({pauses:5,config:{...baseSession.config,sessionMode:SessionMode.LIGHT_FOCUS}});const metrics=createMockFocusMetrics();expect(engine.calculateScore(lightSession,metrics).pausePenalty).toBeLessThan(engine.calculateScore(baseSession,metrics).pausePenalty);});it('adds study quiz bonus points',()=>{const session=createMockSession({config:{...createMockSession().config,sessionMode:SessionMode.STUDY,quizBonusPoints:10}});const calculation=engine.calculateScore(session,createMockFocusMetrics());expect(calculation.intervalBonus).toBeGreaterThanOrEqual(10);});it('adds creative mood bonus points',()=>{const session=createMockSession({config:{...createMockSession().config,sessionMode:SessionMode.CREATIVE,creativeMoodBonus:10}});expect(engine.calculateScore(session,createMockFocusMetrics()).intervalBonus).toBeGreaterThanOrEqual(10);});it('adds sprint chain bonus points',()=>{const session=createMockSession({config:{...createMockSession().config,sessionMode:SessionMode.SPRINT,sprintChainCount:4}});expect(engine.calculateScore(session,createMockFocusMetrics()).intervalBonus).toBeGreaterThan(0);});});describe('Edge Cases',()=>{it('should handle zero duration session',()=>{const session=createMockSession({config:{...createMockSession().config,duration:0},effectiveTime:0});const metrics=createMockFocusMetrics();const calculation=engine.calculateScore(session,metrics);expect(calculation.basePoints).toBe(0);});it('should not return negative final score',()=>{const session=createMockSession({pauses:1000,interruptions:1000});const metrics=createMockFocusMetrics({overallScore:0});const calculation=engine.calculateScore(session,metrics);const finalScore=engine.calculateFinalScore(calculation);expect(finalScore).toBeGreaterThanOrEqual(0);});});});

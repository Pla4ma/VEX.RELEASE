import type{FocusQualityMetrics,SessionState}from'../../types'; export const BONUS_CONSTANTS = {EARLY_COMPLETION_THRESHOLD:0.95,TIME_BONUS_MAX:100,TIME_BONUS_PER_PERCENT_EARLY:2,STREAK_BONUS_BASE:10,STREAK_BONUS_PER_DAY:5,STREAK_BONUS_MAX:200,EXCELLENT_QUALITY_THRESHOLD:90,GOOD_QUALITY_THRESHOLD:75,AVERAGE_QUALITY_THRESHOLD:50,EXCELLENT_QUALITY_BONUS:150,GOOD_QUALITY_BONUS:75,AVERAGE_QUALITY_BONUS:25,MULTI_INTERVAL_BONUS:50,POMODORO_COMPLETE_BONUS:100,PERFECT_SESSION_BONUS:250,MARATHON_BONUS:100,EARLY_BIRD_BONUS:50,NIGHT_OWL_BONUS:50}as const; export interface TimeBonusInput{plannedDuration:number;actualDuration:number;completionPercentage:number;}export function calculateTimeBonus(input:TimeBonusInput):number{const{plannedDuration,actualDuration,completionPercentage} = input; if(completionPercentage < 100){return 0;}const timeRatio = actualDuration / plannedDuration; if(timeRatio > BONUS_CONSTANTS.EARLY_COMPLETION_THRESHOLD){return 0;}const percentEarly = (1 - timeRatio) * 100; const bonus = Math.floor(percentEarly * BONUS_CONSTANTS.TIME_BONUS_PER_PERCENT_EARLY); return Math.min(bonus,BONUS_CONSTANTS.TIME_BONUS_MAX);}export interface StreakBonusInput{currentStreak:number;basePoints:number;}export function calculateStreakBonus(input:StreakBonusInput):number{const{currentStreak} = input; if(currentStreak <= 1){return BONUS_CONSTANTS.STREAK_BONUS_BASE;}const streakMultiplier = Math.log(currentStreak) / Math.log(2); const bonus = Math.floor(BONUS_CONSTANTS.STREAK_BONUS_BASE * streakMultiplier); return Math.min(bonus,BONUS_CONSTANTS.STREAK_BONUS_MAX);}export function getStreakMultiplier(streakDays:number):number{const multipliers:Record<number,number> = {0:1,1:1,2:1.1,3:1.15,7:1.25,14:1.35,30:1.5,60:1.75,90:2}; const milestones = Object.keys(multipliers).map(Number).sort((a,b)=>b - a); for(const milestone of milestones){if(streakDays >= milestone){return multipliers[milestone];}}return 1;}export interface QualityBonusInput{focusMetrics:FocusQualityMetrics;interruptions:number;pauses:number;}export function calculateQualityBonus(input:QualityBonusInput):number{const{focusMetrics,interruptions,pauses} = input; const overallScore = focusMetrics.overallScore; const disruptionPenalty = interruptions * 5 + pauses * 2; const adjustedScore = Math.max(0,overallScore - disruptionPenalty); if(adjustedScore >= BONUS_CONSTANTS.EXCELLENT_QUALITY_THRESHOLD){return BONUS_CONSTANTS.EXCELLENT_QUALITY_BONUS;}if(adjustedScore >= BONUS_CONSTANTS.GOOD_QUALITY_THRESHOLD){return BONUS_CONSTANTS.GOOD_QUALITY_BONUS;}if(adjustedScore >= BONUS_CONSTANTS.AVERAGE_QUALITY_THRESHOLD){return BONUS_CONSTANTS.AVERAGE_QUALITY_BONUS;}return 0;}export interface IntervalBonusInput{completedIntervals:number;totalIntervals:number;allIntervalsCompleted:boolean;}export function calculateIntervalBonus(input:IntervalBonusInput):number{const{completedIntervals,allIntervalsCompleted} = input; let bonus = 0; if(completedIntervals > 1){bonus += completedIntervals * BONUS_CONSTANTS.MULTI_INTERVAL_BONUS;}if(allIntervalsCompleted){bonus += BONUS_CONSTANTS.POMODORO_COMPLETE_BONUS;}return bonus;}export interface SpecialBonusInput{session:SessionState;startTime:number;endTime:number;noPauses:boolean;noInterruptions:boolean;}export interface SpecialBonusResult{perfectSession:boolean;marathon:boolean;earlyBird:boolean;nightOwl:boolean;totalBonus:number;badges:string[];}export function calculateSpecialBonuses(input:SpecialBonusInput):SpecialBonusResult{const{session,startTime,endTime,noPauses,noInterruptions} = input; const result:SpecialBonusResult = {perfectSession:false,marathon:false,earlyBird:false,nightOwl:false,totalBonus:0,badges:[]}; if(session.completionPercentage >= 100 && noPauses && noInterruptions){result.perfectSession = true; result.totalBonus += BONUS_CONSTANTS.PERFECT_SESSION_BONUS; result.badges.push('PERFECT_SESSION');}const durationMinutes = (endTime - startTime) / 60000; if(durationMinutes >= 60){result.marathon = true; result.totalBonus += BONUS_CONSTANTS.MARATHON_BONUS; result.badges.push('MARATHON');}const startHour = new Date(startTime).getHours(); if(startHour < 8){result.earlyBird = true; result.totalBonus += BONUS_CONSTANTS.EARLY_BIRD_BONUS; result.badges.push('EARLY_BIRD');}if(startHour >= 22){result.nightOwl = true; result.totalBonus += BONUS_CONSTANTS.NIGHT_OWL_BONUS; result.badges.push('NIGHT_OWL');}return result;}export interface TotalBonusInput{timeBonus:number;streakBonus:number;qualityBonus:number;intervalBonus:number;specialBonus:number;}export function calculateTotalBonus(input:TotalBonusInput):{total:number;breakdown:Record<string,number>;}{const total = input.timeBonus + input.streakBonus + input.qualityBonus + input.intervalBonus + input.specialBonus; return{total,breakdown:{time:input.timeBonus,streak:input.streakBonus,quality:input.qualityBonus,interval:input.intervalBonus,special:input.specialBonus}};}export const BonusCalculator = {calculateTimeBonus,calculateStreakBonus,getStreakMultiplier,calculateQualityBonus,calculateIntervalBonus,calculateSpecialBonuses,calculateTotalBonus,constants:BONUS_CONSTANTS}; export default BonusCalculator;

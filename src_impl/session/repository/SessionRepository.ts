import{getMMKVStorageAdapter}from'../../persistence/MMKVStorageAdapter'; import type{SessionState,SessionHistoryEntry,SessionSummary}from'../types'; import{createDebugger}from'../../utils/debug'; const debug = createDebugger('session:repository'); type MMKVType=any;const STORAGE_KEYS = {activeSession:(userId:string)=>`session:active:${userId}`,sessionHistory:(userId:string)=>`session:history:${userId}`,sessionSummaries:(userId:string)=>`session:summaries:${userId}`,sessionCache:(userId:string,sessionId:string)=>`session:cache:${userId}:${sessionId}`,syncQueue:(userId:string)=>`session:sync:queue:${userId}`,lastSync:(userId:string)=>`session:lastSync:${userId}`}; export class SessionRepository{private userId:string|null = null; private mmkv:MMKVType|null = null; private useMMKV:boolean = false; constructor(userId?:string){this.userId = userId ?? null; this.initializeStorage();}setUserId(userId:string):void{this.userId = userId; debug.info('SessionRepository user set: %s',userId);}private initializeStorage():void{try{const{MMKV} = require('react-native-mmkv'); this.mmkv = new MMKV({id:'session-storage',encryptionKey:'session-encryption-key'}); this.useMMKV = true; debug.info('MMKV storage initialized');}catch(error){debug.warn('MMKV initialization failed, falling back to AsyncStorage',error as Error); this.useMMKV = false;}}async getActiveSession():Promise<SessionState|null>{if(!this.userId){return null;}try{const key = STORAGE_KEYS.activeSession(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){const session = JSON.parse(data)as SessionState; debug.debug('Loaded active session: %s',session.id); return session;}}catch(error){debug.error('Failed to load active session',error as Error);}return null;}async saveActiveSession(session:SessionState):Promise<void>{if(!this.userId){throw new Error('SessionRepository: No user set');}try{const key = STORAGE_KEYS.activeSession(this.userId); const data = JSON.stringify(session); if(this.useMMKV && this.mmkv){this.mmkv.set(key,data);}else{await getMMKVStorageAdapter().setItem(key,data);}debug.debug('Saved active session: %s',session.id);}catch(error){debug.error('Failed to save active session',error as Error); throw error;}}async clearActiveSession():Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.activeSession(this.userId); if(this.useMMKV && this.mmkv){this.mmkv.delete(key);}else{await getMMKVStorageAdapter().removeItem(key);}debug.debug('Cleared active session');}catch(error){debug.error('Failed to clear active session',error as Error);}}async getSessionHistory(limit:number = 100):Promise<SessionHistoryEntry[]>{if(!this.userId){return[];}try{const key = STORAGE_KEYS.sessionHistory(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){const history = JSON.parse(data)as SessionHistoryEntry[]; debug.debug('Loaded session history: %d entries',history.length); return history.slice(0,limit);}}catch(error){debug.error('Failed to load session history',error as Error);}return[];}async addToHistory(entry:SessionHistoryEntry):Promise<void>{if(!this.userId){throw new Error('SessionRepository: No user set');}try{const key = STORAGE_KEYS.sessionHistory(this.userId); let history = await this.getSessionHistory(1000); history.unshift(entry); if(history.length > 1000){history = history.slice(0,1000);}const data = JSON.stringify(history); if(this.useMMKV && this.mmkv){this.mmkv.set(key,data);}else{await getMMKVStorageAdapter().setItem(key,data);}debug.debug('Added session to history: %s',entry.sessionId);}catch(error){debug.error('Failed to add to history',error as Error); throw error;}}async getSessionById(sessionId:string):Promise<SessionHistoryEntry|null>{const history = await this.getSessionHistory(1000); return history.find(h=>h.sessionId === sessionId) || null;}async getSessionsByDateRange(startDate:number,endDate:number):Promise<SessionHistoryEntry[]>{const history = await this.getSessionHistory(1000); return history.filter(h=>h.startedAt >= startDate && h.startedAt <= endDate);}async getSessionsByStatus(status:string):Promise<SessionHistoryEntry[]>{const history = await this.getSessionHistory(1000); return history.filter(h=>h.status === status);}async getSessionSummary(sessionId:string):Promise<SessionSummary|null>{if(!this.userId){return null;}try{const key = STORAGE_KEYS.sessionSummaries(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){const summaries = JSON.parse(data)as Record<string,SessionSummary>; const summary = summaries[sessionId]; if(summary){debug.debug('Loaded session summary: %s',sessionId); return summary;}}}catch(error){debug.error('Failed to load session summary',error as Error);}return null;}async saveSessionSummary(summary:SessionSummary):Promise<void>{if(!this.userId){throw new Error('SessionRepository: No user set');}try{const key = STORAGE_KEYS.sessionSummaries(this.userId); let summaries:Record<string,SessionSummary> = {}; let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){summaries = JSON.parse(data);}summaries[summary.sessionId] = summary; const saveData = JSON.stringify(summaries); if(this.useMMKV && this.mmkv){this.mmkv.set(key,saveData);}else{await getMMKVStorageAdapter().setItem(key,saveData);}debug.debug('Saved session summary: %s',summary.sessionId);}catch(error){debug.error('Failed to save session summary',error as Error); throw error;}}async getAllSummaries():Promise<SessionSummary[]>{if(!this.userId){return[];}try{const key = STORAGE_KEYS.sessionSummaries(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){const summaries = JSON.parse(data)as Record<string,SessionSummary>; return Object.values(summaries);}}catch(error){debug.error('Failed to load session summaries',error as Error);}return[];}async addToSyncQueue(sessionId:string):Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.syncQueue(this.userId); let queue:string[] = []; let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){queue = JSON.parse(data);}if(!queue.includes(sessionId)){queue.push(sessionId);}const saveData = JSON.stringify(queue); if(this.useMMKV && this.mmkv){this.mmkv.set(key,saveData);}else{await getMMKVStorageAdapter().setItem(key,saveData);}debug.debug('Added to sync queue: %s',sessionId);}catch(error){debug.error('Failed to add to sync queue',error as Error);}}async getSyncQueue():Promise<string[]>{if(!this.userId){return[];}try{const key = STORAGE_KEYS.syncQueue(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){return JSON.parse(data);}}catch(error){debug.error('Failed to get sync queue',error as Error);}return[];}async removeFromSyncQueue(sessionId:string):Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.syncQueue(this.userId); const queue = await this.getSyncQueue(); const newQueue = queue.filter(id=>id !== sessionId); const saveData = JSON.stringify(newQueue); if(this.useMMKV && this.mmkv){this.mmkv.set(key,saveData);}else{await getMMKVStorageAdapter().setItem(key,saveData);}debug.debug('Removed from sync queue: %s',sessionId);}catch(error){debug.error('Failed to remove from sync queue',error as Error);}}async clearSyncQueue():Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.syncQueue(this.userId); if(this.useMMKV && this.mmkv){this.mmkv.delete(key);}else{await getMMKVStorageAdapter().removeItem(key);}debug.debug('Cleared sync queue');}catch(error){debug.error('Failed to clear sync queue',error as Error);}}async setLastSync(timestamp:number):Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.lastSync(this.userId); if(this.useMMKV && this.mmkv){this.mmkv.set(key,timestamp.toString());}else{await getMMKVStorageAdapter().setItem(key,timestamp.toString());}}catch(error){debug.error('Failed to set last sync',error as Error);}}async getLastSync():Promise<number|null>{if(!this.userId){return null;}try{const key = STORAGE_KEYS.lastSync(this.userId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){return parseInt(data,10);}}catch(error){debug.error('Failed to get last sync',error as Error);}return null;}async getSessionStats():Promise<{totalSessions:number;completedSessions:number;abandonedSessions:number;totalFocusTime:number;averageSessionDuration:number;currentStreak:number;longestStreak:number}>{const history = await this.getSessionHistory(1000); const summaries = await this.getAllSummaries(); const completedSessions = history.filter(h=>h.status === 'COMPLETED' || h.status === 'PARTIAL'); const abandonedSessions = history.filter(h=>h.status === 'ABANDONED' || h.status === 'FAILED'); const totalFocusTime = summaries.reduce((sum,s)=>sum + s.effectiveDuration,0); const avgDuration = completedSessions.length > 0 ? totalFocusTime / completedSessions.length : 0; const{currentStreak,longestStreak} = this.calculateStreaks(history); return{totalSessions:history.length,completedSessions:completedSessions.length,abandonedSessions:abandonedSessions.length,totalFocusTime,averageSessionDuration:avgDuration,currentStreak,longestStreak};}private calculateStreaks(history:SessionHistoryEntry[]):{currentStreak:number;longestStreak:number}{if(history.length === 0){return{currentStreak:0,longestStreak:0};}const sorted = [...history].sort((a,b)=>b.startedAt - a.startedAt); const completedDays = new Set(sorted.filter(h=>h.status === 'COMPLETED' || h.status === 'PARTIAL').map(h=>new Date(h.startedAt).toDateString())); const completedDaysArray = Array.from(completedDays); if(completedDaysArray.length === 0){return{currentStreak:0,longestStreak:0};}let currentStreak = 1; const today = new Date().toDateString(); const yesterday = new Date(Date.now() - 86400000).toDateString(); if(completedDaysArray[0] !== today && completedDaysArray[0] !== yesterday){return{currentStreak:0,longestStreak:0};}for(let i = 1; i < completedDaysArray.length; i++){const prevDate = new Date(completedDaysArray[i - 1]); const currDate = new Date(completedDaysArray[i]); const diffDays = (prevDate.getTime() - currDate.getTime()) / (1000 * 3600 * 24); if(diffDays === 1){currentStreak++;}else{break;}}let longestStreak = 1; let tempStreak = 1; for(let i = 1; i < completedDaysArray.length; i++){const prevDate = new Date(completedDaysArray[i - 1]); const currDate = new Date(completedDaysArray[i]); const diffDays = (prevDate.getTime() - currDate.getTime()) / (1000 * 3600 * 24); if(diffDays === 1){tempStreak++; longestStreak = Math.max(longestStreak,tempStreak);}else{tempStreak = 1;}}return{currentStreak,longestStreak};}async cacheSession(session:SessionState):Promise<void>{if(!this.userId){return;}try{const key = STORAGE_KEYS.sessionCache(this.userId,session.id); const data = JSON.stringify({...session,cachedAt:Date.now()}); if(this.useMMKV && this.mmkv){this.mmkv.set(key,data);}else{await getMMKVStorageAdapter().setItem(key,data);}debug.debug('Cached session: %s',session.id);}catch(error){debug.error('Failed to cache session',error as Error);}}async getCachedSession(sessionId:string):Promise<(SessionState&{cachedAt:number})|null>{if(!this.userId){return null;}try{const key = STORAGE_KEYS.sessionCache(this.userId,sessionId); let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){return JSON.parse(data);}}catch(error){debug.error('Failed to get cached session',error as Error);}return null;}async getDeviceHash(sessionId:string):Promise<string|null>{if(!this.userId){return null;}try{const key = `session:deviceHash:${this.userId}:${sessionId}`; let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}return data;}catch(error){debug.error('Failed to get device hash',error as Error); return null;}}async setDeviceHash(sessionId:string,hash:string):Promise<void>{if(!this.userId){return;}try{const key = `session:deviceHash:${this.userId}:${sessionId}`; if(this.useMMKV && this.mmkv){this.mmkv.set(key,hash);}else{await getMMKVStorageAdapter().setItem(key,hash);}debug.debug('Set device hash for session: %s',sessionId);}catch(error){debug.error('Failed to set device hash',error as Error);}}async getRemoteSessionState(sessionId:string):Promise<SessionState|null>{if(!this.userId){return null;}try{const key = `session:remote:${this.userId}:${sessionId}`; let data:string|null = null; if(this.useMMKV && this.mmkv){data = this.mmkv.getString(key) ?? null;}else{data = await getMMKVStorageAdapter().getItem(key);}if(data){return JSON.parse(data)as SessionState;}}catch(error){debug.error('Failed to get remote session state',error as Error);}return null;}async syncSession(session:SessionState):Promise<void>{if(!this.userId){return;}try{session.lastSyncAt = Date.now(); session.syncedAt = Date.now(); const key = `session:remote:${this.userId}:${session.id}`; const data = JSON.stringify(session); if(this.useMMKV && this.mmkv){this.mmkv.set(key,data);}else{await getMMKVStorageAdapter().setItem(key,data);}await this.removeFromSyncQueue(session.id); debug.info('Session synced: %s',session.id);}catch(error){debug.error('Failed to sync session',error as Error); throw error;}}async clearAllData():Promise<void>{if(!this.userId){return;}try{const keys = [STORAGE_KEYS.activeSession(this.userId),STORAGE_KEYS.sessionHistory(this.userId),STORAGE_KEYS.sessionSummaries(this.userId),STORAGE_KEYS.syncQueue(this.userId),STORAGE_KEYS.lastSync(this.userId)]; for(const key of keys){if(this.useMMKV && this.mmkv){this.mmkv.delete(key);}else{await getMMKVStorageAdapter().removeItem(key);}}debug.info('Cleared all session data for user: %s',this.userId);}catch(error){debug.error('Failed to clear session data',error as Error);}}}let repositoryInstance:SessionRepository|null = null; export function getSessionRepository(userId?:string):SessionRepository{if(!repositoryInstance){repositoryInstance = new SessionRepository(userId);}else if(userId){repositoryInstance.setUserId(userId);}return repositoryInstance;}

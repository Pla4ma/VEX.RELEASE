import{SessionService,createSessionService}from'./SessionService';import{getSessionRepository}from'./repository/SessionRepository';import{eventBus}from'../events';import{SESSION_CONSTANTS}from'./index';jest.mock('./repository/SessionRepository');jest.mock('../events');describe('SessionService',()=>{let service:SessionService;let mockRepository:jest.Mocked<ReturnType<typeof getSessionRepository>>;const mockUserId='test-user-123';const mockSessionConfig={duration:1800,type:'focus'as const,difficulty:'medium'as const,enableNotifications:true,enableSound:true};beforeEach(()=>{jest.clearAllMocks();mockRepository={saveSession:jest.fn().mockResolvedValue(undefined),getSession:jest.fn().mockResolvedValue(null),updateSession:jest.fn().mockResolvedValue(undefined),deleteSession:jest.fn().mockResolvedValue(undefined),listSessions:jest.fn().mockResolvedValue([]),getSessionHistory:jest.fn().mockResolvedValue([])}as any;(getSessionRepository as jest.Mock).mockReturnValue(mockRepository);(eventBus.publish as jest.Mock).mockImplementation(()=>{});(eventBus.subscribe as jest.Mock).mockImplementation(()=>()=>{});service=createSessionService(mockUserId);});describe('createSession',()=>{it('should create a new session with valid config',async()=>{const session=await service.createSession(mockSessionConfig);expect(session).toBeDefined();expect(session.userId).toBe(mockUserId);expect(session.config.duration).toBe(mockSessionConfig.duration);expect(session.status).toBe('CREATED');expect(session.phase).toBe('PREPARATION');});it('should reject session with duration below minimum',async()=>{const invalidConfig={...mockSessionConfig,duration:30};await expect(service.createSession(invalidConfig)).rejects.toThrow('duration');});it('should reject session with duration above maximum',async()=>{const invalidConfig={...mockSessionConfig,duration:90000};await expect(service.createSession(invalidConfig)).rejects.toThrow('duration');});it('should emit session:created event',async()=>{await service.createSession(mockSessionConfig);expect(eventBus.publish).toHaveBeenCalledWith('session:created',expect.any(Object));});it('should persist session to repository',async()=>{const session=await service.createSession(mockSessionConfig);expect(mockRepository.saveSession).toHaveBeenCalledWith(session);});});describe('startSession',()=>{it('should transition session from CREATED to ACTIVE',async()=>{const session=await service.createSession(mockSessionConfig);const startedSession=await service.startSession(session.id);expect(startedSession.status).toBe('ACTIVE');expect(startedSession.phase).toBe('FOCUS');expect(startedSession.startedAt).toBeDefined();});it('should emit session:started event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);expect(eventBus.publish).toHaveBeenCalledWith('session:started',expect.any(Object));});it('should reject starting non-existent session',async()=>{await expect(service.startSession('non-existent-id')).rejects.toThrow('not found');});it('should reject starting already active session',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await expect(service.startSession(session.id)).rejects.toThrow('already active');});});describe('pauseSession',()=>{it('should pause active session',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);const pausedSession=await service.pauseSession(session.id,{reason:'USER_INITIATED'});expect(pausedSession.isPaused).toBe(true);expect(pausedSession.pauses).toBe(1);expect(pausedSession.pausedAt).toBeDefined();});it('should emit session:paused event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.pauseSession(session.id,{reason:'USER_INITIATED'});expect(eventBus.publish).toHaveBeenCalledWith('session:paused',expect.any(Object));});it('should track pause reason',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.pauseSession(session.id,{reason:'INTERRUPTION',interruptionId:'int-123'});expect(eventBus.publish).toHaveBeenCalledWith('session:paused',expect.objectContaining({reason:'INTERRUPTION',interruptionId:'int-123'}));});});describe('resumeSession',()=>{it('should resume paused session',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.pauseSession(session.id,{reason:'USER_INITIATED'});const resumedSession=await service.resumeSession(session.id);expect(resumedSession.isPaused).toBe(false);expect(resumedSession.totalPausedTime).toBeGreaterThan(0);});it('should emit session:resumed event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.pauseSession(session.id,{reason:'USER_INITIATED'});await service.resumeSession(session.id);expect(eventBus.publish).toHaveBeenCalledWith('session:resumed',expect.any(Object));});});describe('completeSession',()=>{it('should complete session and calculate rewards',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);jest.advanceTimersByTime(1000);const completedSession=await service.completeSession(session.id,{completionPercentage:100,focusQuality:85});expect(completedSession.status).toBe('COMPLETED');expect(completedSession.completedAt).toBeDefined();expect(completedSession.summary).toBeDefined();expect(completedSession.summary?.xpEarned).toBeGreaterThan(0);});it('should emit session:completed event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.completeSession(session.id,{completionPercentage:100});expect(eventBus.publish).toHaveBeenCalledWith('session:completed',expect.any(Object));});it('should emit session:rewards:granted event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.completeSession(session.id,{completionPercentage:100});expect(eventBus.publish).toHaveBeenCalledWith('session:rewards:granted',expect.any(Object));});it('should handle partial completion',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);const completedSession=await service.completeSession(session.id,{completionPercentage:50,focusQuality:60});expect(completedSession.summary?.completionPercentage).toBe(50);expect(completedSession.summary?.xpEarned).toBeLessThan(SESSION_CONSTANTS.BASE_SCORE_PER_MINUTE*30);});});describe('abandonSession',()=>{it('should mark session as abandoned',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);const abandonedSession=await service.abandonSession(session.id,'USER_CANCELLED');expect(abandonedSession.status).toBe('ABANDONED');expect(abandonedSession.abandonedAt).toBeDefined();expect(abandonedSession.abandonReason).toBe('USER_CANCELLED');});it('should emit session:abandoned event',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.abandonSession(session.id,'USER_CANCELLED');expect(eventBus.publish).toHaveBeenCalledWith('session:abandoned',expect.objectContaining({reason:'USER_CANCELLED'}));});it('should not grant rewards for abandoned session',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);await service.abandonSession(session.id,'USER_CANCELLED');const rewardsCall=(eventBus.publish as jest.Mock).mock.calls.find(call=>call[0]==='session:rewards:granted');expect(rewardsCall).toBeUndefined();});});describe('getSessionState',()=>{it('should return current session state',async()=>{const session=await service.createSession(mockSessionConfig);const state=service.getSessionState(session.id);expect(state).toBeDefined();expect(state.sessionId).toBe(session.id);expect(state.status).toBe('CREATED');});it('should calculate remaining time correctly',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);const state=service.getSessionState(session.id);expect(state.remainingSeconds).toBeLessThanOrEqual(session.config.duration);expect(state.remainingSeconds).toBeGreaterThan(0);});});describe('error handling',()=>{it('should handle repository save failure',async()=>{mockRepository.saveSession.mockRejectedValue(new Error('DB error'));await expect(service.createSession(mockSessionConfig)).rejects.toThrow('DB error');expect(eventBus.publish).toHaveBeenCalledWith('session:failed',expect.objectContaining({canRecover:true}));});it('should handle concurrent session operations',async()=>{const session=await service.createSession(mockSessionConfig);await service.startSession(session.id);const pause1=service.pauseSession(session.id,{reason:'TEST'});const pause2=service.pauseSession(session.id,{reason:'TEST'});await expect(Promise.all([pause1,pause2])).rejects.toThrow();});});describe('session history',()=>{it('should retrieve session history',async()=>{const mockHistory=[{id:'session-1',status:'COMPLETED',startedAt:Date.now()-86400000},{id:'session-2',status:'COMPLETED',startedAt:Date.now()-172800000}];mockRepository.getSessionHistory.mockResolvedValue(mockHistory);const history=await service.getSessionHistory(mockUserId,{limit:10});expect(history).toHaveLength(2);expect(mockRepository.getSessionHistory).toHaveBeenCalledWith(mockUserId,{limit:10});});});});

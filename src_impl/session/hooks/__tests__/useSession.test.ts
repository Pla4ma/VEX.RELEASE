import{renderHook,act,waitFor}from'@testing-library/react-native'; import{useSession}from'../useSession'; import{getSessionService}from'../../SessionService'; jest.mock('../../SessionService'); describe('useSession',()=>{const mockUserId = 'test-user-123'; const mockSessionService = {setUserId:jest.fn(),getCurrentSession:jest.fn(),isSessionActive:jest.fn(),isSessionPaused:jest.fn(),getRemainingSeconds:jest.fn(),getElapsedSeconds:jest.fn(),getCompletionPercentage:jest.fn(),createCustomSession:jest.fn(),startSession:jest.fn(),pauseSession:jest.fn(),resumeSession:jest.fn(),completeSession:jest.fn(),abandonSession:jest.fn(),backgroundSession:jest.fn(),foregroundSession:jest.fn(),attemptRecovery:jest.fn(),applyStudyQuizBonus:jest.fn(),getCurrentPurityScore:jest.fn(),getPurityLabel:jest.fn()}; beforeEach(()=>{jest.clearAllMocks(); mockSessionService.getCurrentSession.mockReturnValue(null); mockSessionService.isSessionActive.mockReturnValue(false); mockSessionService.isSessionPaused.mockReturnValue(false); mockSessionService.getRemainingSeconds.mockReturnValue(0); mockSessionService.getElapsedSeconds.mockReturnValue(0); mockSessionService.getCompletionPercentage.mockReturnValue(0); (getSessionService as jest.Mock).mockReturnValue(mockSessionService);}); describe('Initialization',()=>{it('should initialize with no session',async()=>{mockSessionService.getCurrentSession.mockReturnValue(null); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>{expect(result.current.isLoading).toBe(false);}); expect(result.current.session).toBeNull(); expect(result.current.isActive).toBe(false);}); it('should load active session on mount',async()=>{const mockSession = {id:'session-1',status:'ACTIVE',phase:'FOCUS'}; mockSessionService.getCurrentSession.mockReturnValue(mockSession); mockSessionService.isSessionActive.mockReturnValue(true); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>{expect(result.current.session).toEqual(mockSession);}); expect(mockSessionService.setUserId).toHaveBeenCalledWith(mockUserId);});}); describe('Session Creation',()=>{it('should create session from config',async()=>{const mockConfig = {duration:1500,category:'Deep Work'}; const mockNewSession = {id:'session-2',status:'CREATED'}; mockSessionService.createCustomSession.mockResolvedValue(mockNewSession); const{result} = renderHook(()=>useSession(mockUserId)); await act(async()=>{await result.current.createSession(mockConfig as never);}); expect(mockSessionService.createCustomSession).toHaveBeenCalledWith(mockConfig);}); it('should create custom session',async()=>{const mockConfig = {duration:1800,category:'Study'}; const mockNewSession = {id:'session-3',status:'CREATED'}; mockSessionService.createCustomSession.mockResolvedValue(mockNewSession); const{result} = renderHook(()=>useSession(mockUserId)); await act(async()=>{await result.current.createSession(mockConfig as never);}); expect(mockSessionService.createCustomSession).toHaveBeenCalledWith(mockConfig);});}); describe('Session Control',()=>{beforeEach(()=>{mockSessionService.getCurrentSession.mockReturnValue({id:'session-1',status:'ACTIVE'}); mockSessionService.isSessionActive.mockReturnValue(true);}); it('should start session',async()=>{mockSessionService.startSession.mockResolvedValue(undefined); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.isLoading).toBe(false)); await act(async()=>{await result.current.startSession();}); expect(mockSessionService.startSession).toHaveBeenCalled();}); it('should pause session',async()=>{mockSessionService.pauseSession.mockResolvedValue(undefined); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.isLoading).toBe(false)); await act(async()=>{await result.current.pauseSession('Test reason');}); expect(mockSessionService.pauseSession).toHaveBeenCalledWith('Test reason');}); it('should resume session',async()=>{mockSessionService.resumeSession.mockResolvedValue(undefined); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.isLoading).toBe(false)); await act(async()=>{await result.current.resumeSession();}); expect(mockSessionService.resumeSession).toHaveBeenCalled();}); it('should abandon session',async()=>{mockSessionService.abandonSession.mockResolvedValue(undefined); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.isLoading).toBe(false)); await act(async()=>{await result.current.abandonSession('Test abandon');}); expect(mockSessionService.abandonSession).toHaveBeenCalledWith('Test abandon');});}); describe('Recovery',()=>{it('should attempt recovery',async()=>{mockSessionService.attemptRecovery.mockResolvedValue(true); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.isLoading).toBe(false)); const recoveryResult = await act(async()=>{return await result.current.attemptRecovery('USER_RESUME');}); expect(mockSessionService.attemptRecovery).toHaveBeenCalledWith('USER_RESUME'); expect(recoveryResult).toBe(true);});}); describe('Error Handling',()=>{it('should handle service errors',async()=>{const error = new Error('Service error'); mockSessionService.getCurrentSession.mockImplementation(()=>{throw error;}); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>{expect(result.current.error).toEqual(error);}); expect(result.current.isLoading).toBe(false);}); it('should clear error on refresh',async()=>{const error = new Error('Service error'); mockSessionService.getCurrentSession.mockImplementationOnce(()=>{throw error;}).mockReturnValueOnce({id:'session-1',status:'ACTIVE'}); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.error).toEqual(error)); await act(async()=>{await result.current.refresh();}); expect(result.current.error).toBeNull();});}); describe('State Updates',()=>{it('should update state on refresh',async()=>{const initialSession = {id:'session-1',status:'ACTIVE'}; const updatedSession = {id:'session-1',status:'COMPLETED'}; mockSessionService.getCurrentSession.mockReturnValueOnce(initialSession).mockReturnValueOnce(updatedSession); const{result} = renderHook(()=>useSession(mockUserId)); await waitFor(()=>expect(result.current.session).toEqual(initialSession)); await act(async()=>{await result.current.refresh();}); expect(result.current.session).toEqual(updatedSession);});});});

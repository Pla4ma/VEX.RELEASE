import{createDebugger}from'../../utils/debug'; const debug = createDebugger('session:retry'); export interface RetryConfig{maxAttempts:number;baseDelay:number;maxDelay:number;backoffMultiplier:number;jitterFactor:number;retryableErrors:string[];circuitBreakerThreshold:number;circuitBreakerResetTime:number;}export const DEFAULT_RETRY_CONFIG:RetryConfig = {maxAttempts:3,baseDelay:1000,maxDelay:30000,backoffMultiplier:2,jitterFactor:0.3,retryableErrors:['NETWORK_ERROR','TIMEOUT','STORAGE_UNAVAILABLE','SYNC_CONFLICT','RATE_LIMIT'],circuitBreakerThreshold:5,circuitBreakerResetTime:60000}; type CircuitState='CLOSED'|'OPEN'|'HALF_OPEN';interface CircuitBreakerState{state:CircuitState;failures:number;lastFailure:number;nextAttempt:number;}export interface RetryContext{attempt:number;startTime:number;lastError?:Error;accumulatedDelay:number;}export class RetryStrategy{private config:RetryConfig; private circuitBreaker:Map<string,CircuitBreakerState> = new Map(); constructor(config:Partial<RetryConfig> = {}){this.config = {...DEFAULT_RETRY_CONFIG,...config};}async execute<T>(operation:(context:RetryContext)=>Promise<T>,operationName:string,customConfig?:Partial<RetryConfig>):Promise<T>{const config = {...this.config,...customConfig}; const circuitKey = operationName; if(this.isCircuitOpen(circuitKey)){throw new Error(`Circuit breaker open for: ${operationName}`);}const context:RetryContext = {attempt:1,startTime:Date.now(),accumulatedDelay:0}; while(context.attempt <= config.maxAttempts){try{debug.debug('Attempting %s (attempt %d/%d)',operationName,context.attempt,config.maxAttempts); const result = await operation(context); this.recordSuccess(circuitKey); debug.info('%s succeeded on attempt %d',operationName,context.attempt); return result;}catch(error){context.lastError = error instanceof Error ? error : new Error(String(error)); if(!this.isRetryableError(context.lastError,config)){debug.error('%s failed with non-retryable error',context.lastError || new Error('Unknown error')); throw context.lastError;}this.recordFailure(circuitKey); if(context.attempt >= config.maxAttempts){debug.error('%s failed after %d attempts',new Error(`${operationName} exhausted retries`),config.maxAttempts); throw new AggregateError([context.lastError],`${operationName} failed after ${config.maxAttempts} attempts`);}const delay = this.calculateDelay(context.attempt,config); context.accumulatedDelay += delay; debug.warn('%s failed (attempt %d), retrying in %dms: %s',operationName,context.attempt,delay,context.lastError.message); await this.sleep(delay); context.attempt++;}}throw new Error('Retry logic exhausted without resolution');}async executeWithFallback<T>(operation:(context:RetryContext)=>Promise<T>,fallback:()=>Promise<T>,operationName:string,config?:Partial<RetryConfig>):Promise<T>{try{return await this.execute(operation,operationName,config);}catch(error){debug.warn('%s failed, executing fallback',operationName); return fallback();}}private calculateDelay(attempt:number,config:RetryConfig):number{const exponentialDelay = config.baseDelay * Math.pow(config.backoffMultiplier,attempt - 1); const cappedDelay = Math.min(exponentialDelay,config.maxDelay); const jitter = cappedDelay * config.jitterFactor * (Math.random() * 2 - 1); return Math.floor(cappedDelay + jitter);}private isRetryableError(error:Error,config:RetryConfig):boolean{for(const errorType of config.retryableErrors){if(error.message.includes(errorType)){return true;}}const retryableNames = ['NetworkError','TimeoutError','StorageError','SyncError']; if(retryableNames.some(name=>error.name === name || error.name.includes(name))){return true;}return false;}private isCircuitOpen(key:string):boolean{const state = this.circuitBreaker.get(key); if(!state){return false;}if(state.state === 'OPEN'){if(Date.now() >= state.nextAttempt){state.state = 'HALF_OPEN'; debug.info('Circuit breaker half-open for: %s',key); return false;}return true;}return false;}private recordSuccess(key:string):void{const state = this.circuitBreaker.get(key); if(state){if(state.state === 'HALF_OPEN'){this.circuitBreaker.delete(key); debug.info('Circuit breaker closed for: %s',key);}else{state.failures = 0;}}}private recordFailure(key:string):void{const state = this.circuitBreaker.get(key) || {state:'CLOSED'as CircuitState,failures:0,lastFailure:0,nextAttempt:0}; state.failures++; state.lastFailure = Date.now(); if(state.failures >= this.config.circuitBreakerThreshold){state.state = 'OPEN'; state.nextAttempt = Date.now() + this.config.circuitBreakerResetTime; debug.error('Circuit breaker opened for: %s (reset in %dms)',new Error(`Circuit breaker: ${key}`),this.config.circuitBreakerResetTime);}this.circuitBreaker.set(key,state);}private sleep(ms:number):Promise<void>{return new Promise(resolve=>setTimeout(resolve,ms));}getCircuitStatus(key?:string):Map<string,CircuitBreakerState>|CircuitBreakerState|null{if(key){return this.circuitBreaker.get(key) || null;}return new Map(this.circuitBreaker);}resetCircuit(key?:string):void{if(key){this.circuitBreaker.delete(key);}else{this.circuitBreaker.clear();}}}export function createRetryStrategy(config?:Partial<RetryConfig>):RetryStrategy{return new RetryStrategy(config);}let strategyInstance:RetryStrategy|null = null; export function getRetryStrategy(config?:Partial<RetryConfig>):RetryStrategy{if(!strategyInstance){strategyInstance = new RetryStrategy(config);}return strategyInstance;}export function withRetry(config?:Partial<RetryConfig>){const strategy = new RetryStrategy(config); return function(target:unknown,propertyKey:string,descriptor:PropertyDescriptor){const originalMethod = descriptor.value; descriptor.value = async function(...args:unknown[]){return strategy.execute(async()=>originalMethod.apply(this,args),`${target?.constructor?.name}.${propertyKey}`);}; return descriptor;};}

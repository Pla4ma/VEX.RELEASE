export type CurrencyType='COINS'|'GEMS'|'FOCUS_POINTS'|'SEASONAL';export interface CurrencyAmount{currency:CurrencyType;amount:number;}export interface Wallet{id:string;userId:string;coins:number;gems:number;focusPoints:number;seasonal:Record<string,number>;totalCoinsEarned:number;totalCoinsSpent:number;totalGemsEarned:number;totalGemsSpent:number;createdAt:number;updatedAt:number;}export interface WalletSummary{coins:number;gems:number;focusPoints:number;seasonal:Record<string,number>;}export type TransactionType='EARN'|'SPEND'|'REFUND'|'CONVERT'|'GIFT_RECEIVE'|'GIFT_SEND';export type TransactionSource='SESSION'|'STREAK'|'BOSS'|'LEVEL_UP'|'SHOP'|'REWARD'|'CRAFTING'|'SQUAD'|'DAILY_LOGIN'|'ACHIEVEMENT'|'PROMOTION'|'REFUND';export interface WalletTransaction{id:string;walletId:string;userId:string;type:TransactionType;currency:CurrencyType;amount:number;balanceBefore:number;balanceAfter:number;source:TransactionSource;sourceId:string|null;description:string;metadata:Record<string,unknown>|null;createdAt:number;}export interface EarningSource{id:string;type:TransactionSource;baseAmount:number;currency:CurrencyType;multiplier:number;finalAmount:number;triggeredAt:number;metadata:Record<string,unknown>|null;}export interface EarningMultiplier{source:TransactionSource;baseMultiplier:number;levelBonus:number;streakBonus:number;squadBonus:number;eventBonus:number;totalMultiplier:number;}export interface SpendingSink{id:string;type:'SHOP'|'CRAFTING'|'UPGRADE'|'GIFT'|'CONVERT';currency:CurrencyType;baseAmount:number;discountApplied:number;finalAmount:number;spentAt:number;metadata:Record<string,unknown>|null;}export type PurchaseStatus='PENDING'|'VALIDATING'|'PROCESSING_PAYMENT'|'DELIVERING'|'COMPLETED'|'FAILED'|'REFUNDED'|'PARTIAL_DELIVERY';export interface PurchaseAttempt{id:string;userId:string;shopItemId:string;quantity:number;unitPrice:CurrencyAmount;totalPrice:CurrencyAmount;status:PurchaseStatus;errorCode:string|null;errorMessage:string|null;inventoryItemIds:string[]|null;refundedAt:number|null;refundReason:string|null;createdAt:number;updatedAt:number;}export interface PurchaseResult{success:boolean;purchaseId:string|null;inventoryItemIds:string[]|null;error:PurchaseError|null;remainingBalance:CurrencyAmount|null;}export interface PurchaseError{code:'INSUFFICIENT_FUNDS'|'ITEM_UNAVAILABLE'|'PRICE_CHANGED'|'INVENTORY_FULL'|'NETWORK_ERROR'|'SYSTEM_ERROR';message:string;recoverable:boolean;}export interface RefundRequest{id:string;purchaseId:string;userId:string;reason:string;status:'PENDING'|'APPROVED'|'REJECTED'|'PROCESSED';requestedAt:number;processedAt:number|null;refundAmount:CurrencyAmount|null;itemsRecovered:boolean;}export interface EconomyAnalytics{userId:string;period:'DAILY'|'WEEKLY'|'MONTHLY';periodStart:number;periodEnd:number;totalEarned:Record<CurrencyType,number>;earningsBySource:Record<TransactionSource,number>;totalSpent:Record<CurrencyType,number>;spendingByCategory:Record<string,number>;purchaseAttempts:number;purchaseSuccesses:number;purchaseFailures:number;conversionRate:number;itemsAcquired:number;itemsConsumed:number;craftingAttempts:number;craftingSuccesses:number;}export interface CurrencyConversion{id:string;userId:string;fromCurrency:CurrencyType;fromAmount:number;toCurrency:CurrencyType;toAmount:number;exchangeRate:number;fee:number;createdAt:number;}export interface PricePoint{timestamp:number;price:CurrencyAmount;reason:string|null;}export interface ItemPriceHistory{itemId:string;pricePoints:PricePoint[];currentPrice:CurrencyAmount;lowestPrice:CurrencyAmount;highestPrice:CurrencyAmount;}export type OfferType='FLASH_SALE'|'BUNDLE'|'DAILY_DEAL'|'FIRST_PURCHASE'|'VIP_EXCLUSIVE'|'SEASONAL';export type OfferStatus='SCHEDULED'|'ACTIVE'|'EXPIRED'|'SOLD_OUT';export interface LimitedOffer{id:string;type:OfferType;name:string;description:string;itemIds:string[];bonusItemIds:string[];originalPrice:CurrencyAmount;discountedPrice:CurrencyAmount;discountPercent:number;status:OfferStatus;startAt:number;endAt:number;maxPurchases:number|null;currentPurchases:number;minLevel:number;maxLevel:number|null;requiredItems:string[];excludedItems:string[];badgeText:string|null;timerDisplay:boolean;priority:number;createdAt:number;}export interface UserOfferClaim{id:string;offerId:string;userId:string;purchaseId:string;claimedAt:number;}

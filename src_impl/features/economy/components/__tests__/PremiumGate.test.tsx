import React from'react';import{render,fireEvent}from'@testing-library/react-native';import{PremiumGate,type PremiumGateFeature}from'../PremiumGate';const mockNavigate=jest.fn();jest.mock('@react-navigation/native',()=>({useNavigation:()=>({navigate:mockNavigate})}));jest.mock('../../../../theme',()=>({useTheme:()=>({theme:{colors:{background:{secondary:'#F8FAFC',tertiary:'#F1F5F9'},border:{DEFAULT:'#CBD5E1'},text:{primary:'#0F172A',secondary:'#475569',tertiary:'#64748B'},primary:{500:'#6366F1'},success:{500:'#22C55E'}}}})}));describe('PremiumGate',()=>{beforeEach(()=>{mockNavigate.mockClear();});const renderPremiumGate=(feature:PremiumGateFeature,props={})=>{return render(<PremiumGate feature={feature}onClose={jest.fn()}showCloseButton={true}{...props}/>);};describe('feature variants',()=>{it('renders ai_coach_full_access gate correctly',()=>{const{getByText}=renderPremiumGate('ai_coach_full_access');expect(getByText('Unlock the Full AI Coach')).toBeTruthy();expect(getByText('Premium unlocks the strongest coaching layer: sharper timing, better nudges, and recommendations that actually fit your rhythm.')).toBeTruthy();expect(getByText('Upgrade to Premium')).toBeTruthy();});it('renders ai_coach_persona_drill_sergeant gate correctly',()=>{const{getByText}=renderPremiumGate('ai_coach_persona_drill_sergeant');expect(getByText('Unlock the Drill Sergeant')).toBeTruthy();expect(getByText('The Drill Sergeant persona is only available with Premium. Get intense, zero-tolerance coaching when you need maximum accountability.')).toBeTruthy();});it('renders boss_tier_4 gate correctly',()=>{const{getByText}=renderPremiumGate('boss_tier_4');expect(getByText('Unlock Boss Tier 4')).toBeTruthy();expect(getByText('The Perfectionist boss awaits Premium users. Defeat it to unlock Tier 5 and the ultimate challenge.')).toBeTruthy();});it('renders boss_tier_5 gate correctly',()=>{const{getByText}=renderPremiumGate('boss_tier_5');expect(getByText('Unlock Boss Tier 5')).toBeTruthy();expect(getByText('Master of Multitasking is a Premium-only boss. Can you focus long enough to defeat it?')).toBeTruthy();});it('renders boss_tier_6 gate correctly',()=>{const{getByText}=renderPremiumGate('boss_tier_6');expect(getByText('Unlock the Final Boss')).toBeTruthy();expect(getByText('Burnout Beast is the ultimate squad boss raid, exclusively for Premium users who have conquered all before it.')).toBeTruthy();});it('renders inventory_expansion gate correctly',()=>{const{getByText}=renderPremiumGate('inventory_expansion');expect(getByText('Expand Your Inventory')).toBeTruthy();expect(getByText('Premium unlocks 500 inventory slots so you can collect every cosmetic, ingredient, and legendary item.')).toBeTruthy();});it('renders advanced_analytics gate correctly',()=>{const{getByText}=renderPremiumGate('advanced_analytics');expect(getByText('Unlock Advanced Analytics')).toBeTruthy();});it('renders content_study gate correctly',()=>{const{getByText}=renderPremiumGate('content_study');expect(getByText('Unlock Content Study')).toBeTruthy();});it('renders custom_themes gate correctly',()=>{const{getByText}=renderPremiumGate('custom_themes');expect(getByText('Unlock Custom Themes')).toBeTruthy();});it('renders squad_challenges gate correctly',()=>{const{getByText}=renderPremiumGate('squad_challenges');expect(getByText('Unlock Squad Challenges')).toBeTruthy();});});describe('unlock list',()=>{it('displays unlock items for the feature',()=>{const{getByText}=renderPremiumGate('ai_coach_full_access');expect(getByText('Premium unlocks:')).toBeTruthy();expect(getByText('Personalized session timing')).toBeTruthy();expect(getByText('Smart nudges and reminders')).toBeTruthy();expect(getByText('Pattern-based recommendations')).toBeTruthy();expect(getByText('All 3 coach personas')).toBeTruthy();});});describe('actions',()=>{it('navigates to paywall when upgrade button is pressed',()=>{const{getByText}=renderPremiumGate('ai_coach_full_access');const upgradeButton=getByText('Upgrade to Premium');fireEvent.press(upgradeButton);expect(mockNavigate).toHaveBeenCalledWith('Paywall',{source:'premium_gate_ai_coach_full_access',gatedFeature:'ai_coach_full_access'});});it('calls onClose when close button is pressed',()=>{const onClose=jest.fn();const{getByText}=renderPremiumGate('ai_coach_full_access',{onClose});const closeButton=getByText('Maybe later');fireEvent.press(closeButton);expect(onClose).toHaveBeenCalled();});it('does not show close button when showCloseButton is false',()=>{const{queryByText}=renderPremiumGate('ai_coach_full_access',{showCloseButton:false});expect(queryByText('Maybe later')).toBeNull();});});describe('custom description',()=>{it('uses custom description when provided',()=>{const customDescription='Custom upgrade description';const{getByText,queryByText}=renderPremiumGate('ai_coach_full_access',{description:customDescription});expect(getByText(customDescription)).toBeTruthy();expect(queryByText('Premium unlocks the strongest coaching layer: sharper timing, better nudges, and recommendations that actually fit your rhythm.')).toBeNull();});});describe('earn option',()=>{it('shows earn premium option',()=>{const{getByText}=renderPremiumGate('ai_coach_full_access');expect(getByText('Or earn Premium free through achievements →')).toBeTruthy();});});});

import*as Sentry from'@sentry/react-native';import{eventBus}from'../../../events';import{currencyBoundariesValidationCore}from'./validation-core';import type{TransactionValidationRequest,TransactionValidationResult,BoundaryViolation,CurrencyLimits,MonetizationBoundary,EconomyProtectionRule}from'./schemas';class CurrencyBoundariesValidationMain{async validateTransaction(request:TransactionValidationRequest):Promise<TransactionValidationResult>{const violations:BoundaryViolation[]=[];let allowed=true;let reason:string|null=null;let warningMessage:string|null=null;let adjustedAmount:number|null=null;let requiresPremium=false;let premiumUpgradeMessage:string|null=null;try{const limits=currencyBoundariesValidationCore.getCurrencyLimits()[request.currency];if(!limits){throw new Error(`No limits defined for currency: ${request.currency}`);}const dailyEarningsViolation=await currencyBoundariesValidationCore.checkDailyEarningsLimit(request,limits);if(dailyEarningsViolation){violations.push(dailyEarningsViolation);if(dailyEarningsViolation.type==='DAILY_EARNING_EXCEEDED'){allowed=false;reason='Daily earnings limit exceeded';requiresPremium=true;premiumUpgradeMessage='Upgrade to Premium for higher daily earning limits';}}const walletCapViolation=await currencyBoundariesValidationCore.checkWalletCap(request,limits);if(walletCapViolation){violations.push(walletCapViolation);if(walletCapViolation.type==='WALLET_CAP_EXCEEDED'){allowed=false;reason='Wallet cap exceeded';requiresPremium=true;premiumUpgradeMessage='Upgrade to Premium for higher wallet limits';}}const singleTransactionViolation=await currencyBoundariesValidationCore.checkSingleTransactionLimit(request,limits);if(singleTransactionViolation){violations.push(singleTransactionViolation);if(singleTransactionViolation.type==='SINGLE_TRANSACTION_LIMIT_EXCEEDED'){allowed=false;reason='Single transaction limit exceeded';adjustedAmount=limits.maxSingleEarn;}}const velocityViolation=await currencyBoundariesValidationCore.checkTransactionVelocity(request);if(velocityViolation){violations.push(velocityViolation);if(velocityViolation.type==='TRANSACTION_VELOCITY_EXCEEDED'){warningMessage='You are approaching transaction limits';}}const protectionViolations=await this.checkProtectionRules(request);violations.push(...protectionViolations);const boundaryViolations=await this.checkMonetizationBoundaries(request);violations.push(...boundaryViolations);if(violations.some(v=>v.action==='BLOCKED')){allowed=false;}if(violations.some(v=>v.action==='ALLOWED_WITH_WARNING')){warningMessage='Transaction allowed with warning';}if(violations.length>0){await this.logViolations(violations);}return{allowed,reason,warningMessage,adjustedAmount,violations,requiresPremium,premiumUpgradeMessage};}catch(error){Sentry.captureException(error,{tags:{feature:'currency-boundaries-validation'},extra:{request}});return{allowed:false,reason:'Validation system error',warningMessage:null,adjustedAmount:null,violations:[],requiresPremium:false,premiumUpgradeMessage:null};}}private async checkProtectionRules(request:TransactionValidationRequest):Promise<BoundaryViolation[]>{const violations:BoundaryViolation[]=[];const protectionRules=currencyBoundariesValidationCore.getProtectionRules();for(const rule of protectionRules){if(!rule.isActive){continue;}const violation=await currencyBoundariesValidationCore.evaluateProtectionRule(rule,request);if(violation){violations.push(violation);}}return violations;}private async checkMonetizationBoundaries(request:TransactionValidationRequest):Promise<BoundaryViolation[]>{const violations:BoundaryViolation[]=[];const monetizationBoundaries=currencyBoundariesValidationCore.getMonetizationBoundaries();for(const boundary of monetizationBoundaries){if(!boundary.isActive){continue;}const violation=await currencyBoundariesValidationCore.evaluateMonetizationBoundary(boundary,request);if(violation){violations.push(violation);}}return violations;}private async logViolations(violations:BoundaryViolation[]):Promise<void>{for(const violation of violations){Sentry.addBreadcrumb({category:'currency-boundaries',message:`Boundary violation: ${violation.type}`,data:{userId:violation.userId,currency:violation.currency,amount:violation.attemptedAmount,action:violation.attemptedAction},level:violation.action==='BLOCKED'?'warning':'info'});eventBus.emit('currency-boundaries:violation_detected',{violation,timestamp:Date.now()});}}updateCurrencyLimits(currency:string,limits:Partial<CurrencyLimits>):void{currencyBoundariesValidationCore.updateCurrencyLimits(currency,limits);}addMonetizationBoundary(boundary:MonetizationBoundary):void{currencyBoundariesValidationCore.addMonetizationBoundary(boundary);}addProtectionRule(rule:EconomyProtectionRule):void{currencyBoundariesValidationCore.addProtectionRule(rule);}getCurrencyLimits():Record<string,CurrencyLimits>{return currencyBoundariesValidationCore.getCurrencyLimits();}getMonetizationBoundaries():MonetizationBoundary[]{return currencyBoundariesValidationCore.getMonetizationBoundaries();}getProtectionRules():EconomyProtectionRule[]{return currencyBoundariesValidationCore.getProtectionRules();}}export const currencyBoundariesValidationService=new CurrencyBoundariesValidationMain();

import*as Sentry from'@sentry/react-native'; import{eventBus}from'../../../events'; import type{TransactionValidationRequest,TransactionValidationResult,BoundaryViolation,BoundaryViolationType,CurrencyLimits,MonetizationBoundary,EconomyProtectionRule}from'./schemas'; import{DEFAULT_CURRENCY_LIMITS,DEFAULT_MONETIZATION_BOUNDARIES,DEFAULT_PROTECTION_RULES}from'./config'; class CurrencyBoundariesValidationCore{private currencyLimits:Record<string,CurrencyLimits> = DEFAULT_CURRENCY_LIMITS; private monetizationBoundaries:MonetizationBoundary[] = DEFAULT_MONETIZATION_BOUNDARIES; private protectionRules:EconomyProtectionRule[] = DEFAULT_PROTECTION_RULES; async checkDailyEarningsLimit(request:TransactionValidationRequest,limits:CurrencyLimits):Promise<BoundaryViolation|null>{if(request.action !== 'EARN'){return null;}const todayEarnings = await this.getTodayEarnings(request.userId,request.currency); const dailyLimit = request.isPremiumUser ? limits.maxDailyEarningsPremium : limits.maxDailyEarnings; if(todayEarnings + request.amount > dailyLimit){return{id:crypto.randomUUID(),userId:request.userId,type:'DAILY_EARNING_EXCEEDED',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:dailyLimit,limitType:'DAILY',source:request.source,sourceId:request.sourceId,metadata:request.metadata,action:'BLOCKED',warningMessage:`Daily earnings limit of ${dailyLimit} ${request.currency} exceeded`,createdAt:Date.now()};}return null;}async checkWalletCap(request:TransactionValidationRequest,limits:CurrencyLimits):Promise<BoundaryViolation|null>{if(request.action !== 'EARN'){return null;}const currentBalance = await this.getCurrentBalance(request.userId,request.currency); const walletCap = request.isPremiumUser ? limits.maxWalletBalancePremium : limits.maxWalletBalance; if(currentBalance + request.amount > walletCap){return{id:crypto.randomUUID(),userId:request.userId,type:'WALLET_CAP_EXCEEDED',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:walletCap,limitType:'WALLET',source:request.source,sourceId:request.sourceId,metadata:request.metadata,action:'BLOCKED',warningMessage:`Wallet cap of ${walletCap} ${request.currency} exceeded`,createdAt:Date.now()};}return null;}async checkSingleTransactionLimit(request:TransactionValidationRequest,limits:CurrencyLimits):Promise<BoundaryViolation|null>{if(request.action !== 'EARN'){return null;}const singleLimit = limits.maxSingleEarn; if(request.amount > singleLimit){return{id:crypto.randomUUID(),userId:request.userId,type:'SINGLE_TRANSACTION_LIMIT_EXCEEDED',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:singleLimit,limitType:'SINGLE',source:request.source,sourceId:request.sourceId,metadata:request.metadata,action:'BLOCKED',warningMessage:`Single transaction limit of ${singleLimit} ${request.currency} exceeded`,createdAt:Date.now()};}return null;}async checkTransactionVelocity(request:TransactionValidationRequest):Promise<BoundaryViolation|null>{const recentTransactions = await this.getRecentTransactionCount(request.userId,3600); const limits = this.currencyLimits[request.currency]; if(recentTransactions >= limits.maxTransactionsPerHour){return{id:crypto.randomUUID(),userId:request.userId,type:'TRANSACTION_VELOCITY_EXCEEDED',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:limits.maxTransactionsPerHour,limitType:'TRANSACTION',source:request.source,sourceId:request.sourceId,metadata:request.metadata,action:'ALLOWED_WITH_WARNING',warningMessage:'Transaction velocity limit approached',createdAt:Date.now()};}return null;}async evaluateProtectionRule(rule:EconomyProtectionRule,request:TransactionValidationRequest):Promise<BoundaryViolation|null>{if(rule.conditions.currency && rule.conditions.currency !== request.currency){return null;}if(rule.conditions.userLevel && rule.conditions.userLevel > request.userLevel){return null;}if(rule.conditions.isPremium !== null && rule.conditions.isPremium !== request.isPremiumUser){return null;}if(rule.conditions.minAmount && request.amount < rule.conditions.minAmount){return null;}if(rule.conditions.maxAmount && request.amount > rule.conditions.maxAmount){return null;}const recentCount = await this.getRecentTransactionCount(request.userId,rule.conditions.timeWindow); if(recentCount >= rule.conditions.maxCount){const action = rule.actions.block ? 'BLOCKED' : rule.actions.warn ? 'ALLOWED_WITH_WARNING' : 'DETECTED'; return{id:crypto.randomUUID(),userId:request.userId,type:'SUSPICIOUS_PATTERN',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:rule.conditions.maxCount,limitType:'TRANSACTION',source:request.source,sourceId:request.sourceId,metadata:{ruleId:rule.id,ruleName:rule.name},action,warningMessage:rule.actions.customMessage,createdAt:Date.now()};}return null;}async evaluateMonetizationBoundary(boundary:MonetizationBoundary,request:TransactionValidationRequest):Promise<BoundaryViolation|null>{if(boundary.currency && boundary.currency !== request.currency){return null;}if(boundary.conditions.minLevel && boundary.conditions.minLevel > request.userLevel){return null;}const requiredEntitlements = boundary.conditions.requiredEntitlements; if(requiredEntitlements && requiredEntitlements.length > 0 && !request.isPremiumUser){return{id:crypto.randomUUID(),userId:request.userId,type:'PREMIUM_FEATURE_ABUSE',currency:request.currency,attemptedAmount:request.amount,attemptedAction:request.action,limitAmount:boundary.freeUserLimit,limitType:'DAILY',source:request.source,sourceId:request.sourceId,metadata:{boundaryId:boundary.id,boundaryName:boundary.name},action:boundary.enforcement === 'HARD_BLOCK' ? 'BLOCKED' : 'ALLOWED_WITH_WARNING',warningMessage:`This feature requires Premium: ${boundary.description}`,createdAt:Date.now()};}return null;}private async getTodayEarnings(userId:string,currency:string):Promise<number>{return Math.random() * 500;}private async getCurrentBalance(userId:string,currency:string):Promise<number>{return Math.random() * 5000;}private async getRecentTransactionCount(userId:string,timeWindowSeconds:number):Promise<number>{return Math.floor(Math.random() * 50);}updateCurrencyLimits(currency:string,limits:Partial<CurrencyLimits>):void{const currentLimits = this.currencyLimits[currency]; if(currentLimits){this.currencyLimits[currency] = {...currentLimits,...limits};}}addMonetizationBoundary(boundary:MonetizationBoundary):void{this.monetizationBoundaries.push(boundary);}addProtectionRule(rule:EconomyProtectionRule):void{this.protectionRules.push(rule);}getCurrencyLimits():Record<string,CurrencyLimits>{return{...this.currencyLimits};}getMonetizationBoundaries():MonetizationBoundary[]{return[...this.monetizationBoundaries];}getProtectionRules():EconomyProtectionRule[]{return[...this.protectionRules];}}export const currencyBoundariesValidationCore = new CurrencyBoundariesValidationCore();

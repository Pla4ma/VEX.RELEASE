import{eventBus}from'@/events'; import{createDebugger}from'@/utils/debug'; import{v4}from'@/utils/uuid'; import type{SessionSummary}from'@/session/types'; import type{BossEncounterSummary}from'@/features/boss/schemas'; import type{StreakEngineResult}from'@/features/streaks/schemas'; import{calculateStory}from'./StoryBeatCalculator'; import*as storyRepository from'./repository'; import{SessionStorySchema,GenerateStoryInputSchema,StoryAnalyticsEventSchema,type SessionStory,type GenerateStoryInput,type StoryAnalyticsEvent}from'./schemas'; const debug = createDebugger('session-story:engine'); interface StoryContext{sessionId:string;userId:string;sessionSummary:SessionSummary;bossContext?:{encounter:BossEncounterSummary|null;damageDealt:number;defeated:boolean;};streakResult?:StreakEngineResult;xpEarned:number;currentLevel:number;xpToNextLevel:number;tierProgress:number;sessionsToNextTier:number;}let initialized = false; export function initializeSessionStoryEngine():()=>void{if(initialized){debug.warn('SessionStoryEngine already initialized'); return()=>{};}initialized = true; debug.info('Initializing SessionStoryEngine'); const unsubscribeCompleted = eventBus.subscribe('session:completed',async event=>{try{const{sessionId,userId} = event as{sessionId:string;userId:string;summary?:SessionSummary;}; const summary = (event as{summary?:SessionSummary;}).summary; if(!summary){debug.warn('No summary in session completed event'); return;}const context = await buildStoryContext(sessionId,userId,summary); const story = await generateSessionStory(context); if(story){await storyRepository.saveStory(story); eventBus.publish('session:story:ready',{storyId:story.id,sessionId,userId,title:story.title,beatCount:story.totalBeats}); trackStoryEvent({storyId:story.id,userId,eventType:'STORY_STARTED',timestamp:Date.now()}); debug.info('Story generated and ready: %s',story.id);}}catch(error){debug.error('Failed to generate session story',error as Error);}}); const unsubscribeViewed = eventBus.subscribe('session:story:viewed',event=>{const{storyId,userId,completionRate} = event as{storyId:string;userId:string;completionRate:number;}; void storyRepository.markStoryViewed(storyId,completionRate); trackStoryEvent({storyId,userId,eventType:completionRate >= 90 ? 'STORY_COMPLETED' : 'STORY_SKIPPED',timestamp:Date.now(),metadata:{completionRate}});}); return()=>{unsubscribeCompleted(); unsubscribeViewed(); initialized = false;};}async function buildStoryContext(sessionId:string,userId:string,summary:SessionSummary):Promise<StoryContext>{const bossContext = await fetchBossContext(userId,sessionId); const{getStreakService} = await import('@/streaks/StreakService'); const streakService = getStreakService(userId); const streakState = await streakService.recordSession().catch(()=>null); const{getProgressionService} = await import('@/progression/ProgressionService'); const progressionService = getProgressionService(userId); const progression = progressionService.getState(); const sessionsToNextTier = calculateSessionsToNextTier(progression.progressPercent); return{sessionId,userId,sessionSummary:summary,bossContext:bossContext ?? undefined,streakResult:streakState ? {action:'INCREMENTED',previousStreak:(streakState.currentStreak || 1) - 1,newStreak:streakState.currentStreak || 1,milestoneReached:null,shieldUsed:false} : undefined,xpEarned:0,currentLevel:progression.currentLevel,xpToNextLevel:progression.xpToNextLevel,tierProgress:progression.progressPercent,sessionsToNextTier};}async function fetchBossContext(userId:string,sessionId:string):Promise<StoryContext['bossContext']|null>{try{const{getActiveEncounter} = await import('@/features/boss/service'); const encounter = await getActiveEncounter(userId); if(!encounter || encounter.status !== 'ACTIVE'){return null;}const{calculateBossDamage} = await import('@/session/integration/SessionBossIntegration'); const damageDealt = calculateBossDamage({effectiveDuration:0,focusQuality:0}as SessionSummary,encounter.bossId); return{encounter,damageDealt,defeated:encounter.healthRemaining <= 0};}catch(error){debug.warn('Failed to fetch boss context',error); return null;}}function calculateSessionsToNextTier(currentProgress:number):number{const progressPerSession = 3; const remainingProgress = 100 - currentProgress; return Math.max(1,Math.ceil(remainingProgress / progressPerSession));}async function generateSessionStory(context:StoryContext):Promise<SessionStory|null>{const input:GenerateStoryInput = {sessionId:context.sessionId,userId:context.userId,sessionSummary:{duration:context.sessionSummary.actualDuration,effectiveDuration:context.sessionSummary.effectiveDuration,focusQuality:context.sessionSummary.focusQuality ?? 0,focusPurityScore:context.sessionSummary.focusPurityScore,interruptions:context.sessionSummary.interruptions ?? 0,pauses:context.sessionSummary.pauses ?? 0,streakDays:context.sessionSummary.streakDays ?? 0,streakMaintained:context.sessionSummary.streakMaintained ?? true,sessionMode:context.sessionSummary.sessionMode ?? 'STANDARD',completionPercentage:context.sessionSummary.completionPercentage ?? 100,finalScore:context.sessionSummary.finalScore ?? 0},bossContext:context.bossContext ? {encounterId:context.bossContext.encounter?.id,bossName:context.bossContext.encounter?.bossName,damageDealt:context.bossContext.damageDealt,healthRemaining:context.bossContext.encounter?.healthRemaining,maxHealth:context.bossContext.encounter?.maxHealth,defeated:context.bossContext.defeated} : undefined,streakContext:{previousStreak:context.streakResult?.previousStreak ?? 0,newStreak:context.streakResult?.newStreak ?? context.sessionSummary.streakDays ?? 0,isMilestone:context.streakResult?.milestoneReached !== null,milestoneDay:context.streakResult?.milestoneReached?.days,wasProtected:context.streakResult?.shieldUsed ?? false,isComeback:context.streakResult?.action === 'BROKEN' && context.streakResult?.newStreak === 1 && context.streakResult?.previousStreak > 0,daysAbsent:0},progressionContext:{xpEarned:context.xpEarned,currentLevel:context.currentLevel,xpToNextLevel:context.xpToNextLevel,tierProgress:context.tierProgress,sessionsToNextTier:context.sessionsToNextTier}}; const validated = GenerateStoryInputSchema.safeParse(input); if(!validated.success){debug.error('Invalid story input:',validated.error); return null;}const calculated = calculateStory(validated.data); const story:SessionStory = {id:v4(),sessionId:context.sessionId,userId:context.userId,createdAt:Date.now(),title:calculated.title,subtitle:calculated.subtitle,overallEmotion:calculated.overallEmotion,beats:calculated.beats,totalBeats:calculated.beats.length,sessionContext:{durationMinutes:Math.round(context.sessionSummary.effectiveDuration / 60000),focusScore:context.sessionSummary.focusQuality ?? 0,streakDays:calculated.beats.find(b=>b.type === 'STREAK_MOMENT' || b.type === 'MILESTONE_REACHED')?.metadata?.value ?? 0,interruptions:context.sessionSummary.interruptions ?? 0,pauses:context.sessionSummary.pauses ?? 0,sessionMode:context.sessionSummary.sessionMode ?? 'STANDARD',bossDamageDealt:context.bossContext?.damageDealt ?? 0,bossDefeated:context.bossContext?.defeated ?? false,milestoneReached:calculated.beats.find(b=>b.type === 'MILESTONE_REACHED')?.metadata?.value,xpEarned:context.xpEarned,isPerfectSession:context.sessionSummary.interruptions === 0 && context.sessionSummary.pauses === 0,isComeback:calculated.beats.some(b=>b.type === 'COMEBACK_TRIUMPH'),daysAbsent:calculated.beats.find(b=>b.type === 'COMEBACK_TRIUMPH')?.metadata?.value ?? 0},nextSessionHooks:calculated.nextSessionHooks,viewedAt:null,sharedAt:null,completionRate:0}; const validatedStory = SessionStorySchema.safeParse(story); if(!validatedStory.success){debug.error('Generated invalid story:',validatedStory.error); return null;}return validatedStory.data;}function trackStoryEvent(event:Omit<StoryAnalyticsEvent,'timestamp'>&{timestamp:number;}):void{const validated = StoryAnalyticsEventSchema.safeParse(event); if(!validated.success){debug.warn('Invalid analytics event:',validated.error); return;}eventBus.publish('session:story:analytics',validated.data); eventBus.publish('analytics:track',{event:`session_story_${event.eventType.toLowerCase()}`,properties:{storyId:event.storyId,userId:event.userId,beatType:event.beatType,...event.metadata}});}export async function getStoryForSession(sessionId:string,userId:string):Promise<SessionStory|null>{return storyRepository.fetchStoryBySessionId(sessionId,userId);}export async function getStoriesForUser(userId:string,limit:number = 10):Promise<SessionStory[]>{return storyRepository.fetchStoriesForUser(userId,limit);}export async function markStoryBeatViewed(storyId:string,beatType:string,userId:string):Promise<void>{trackStoryEvent({storyId,userId,eventType:'BEAT_VIEWED',beatType:beatType as any,timestamp:Date.now()});}export async function shareStory(storyId:string,userId:string):Promise<void>{await storyRepository.markStoryShared(storyId); trackStoryEvent({storyId,userId,eventType:'STORY_SHARED',timestamp:Date.now()});}if(process.env.NODE_ENV === 'production'){initializeSessionStoryEngine();}

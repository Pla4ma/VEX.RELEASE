import React from'react'; import{renderHook,act,waitFor,cleanup}from'@testing-library/react-native'; import{QueryClient,QueryClientProvider}from'@tanstack/react-query'; import{describe,it,expect,beforeEach,afterEach,jest}from'@jest/globals'; import*as hooks from'../session-squads-integration'; import{useBasicSquadsStatus,useUpdateBasicSquadWeeklyProgress}from'../hooks/basic-squads-hooks'; import{eventBus}from'../../../events'; import{useAuthStore}from'../../../store'; jest.mock('../../../store'); const mockUseAuthStore = useAuthStore as jest.MockedFunction<typeof useAuthStore>; type MockAuthState={user:{id:string;email:string;}|null;};function mockAuthState(state:MockAuthState):void{mockUseAuthStore.mockImplementation((selector?:(value:MockAuthState)=>unknown)=>{if(selector){return selector(state)as ReturnType<typeof useAuthStore>;}return state as ReturnType<typeof useAuthStore>;});}jest.mock('../../../events',()=>({eventBus:{subscribe:jest.fn(()=>jest.fn()),publish:jest.fn()}})); jest.mock('../hooks/basic-squads-hooks',()=>({useBasicSquadsStatus:jest.fn(),useUpdateBasicSquadWeeklyProgress:jest.fn()})); const mockUseBasicSquadsStatus = useBasicSquadsStatus as jest.MockedFunction<typeof useBasicSquadsStatus>; const mockUseUpdateBasicSquadWeeklyProgress = useUpdateBasicSquadWeeklyProgress as jest.MockedFunction<typeof useUpdateBasicSquadWeeklyProgress>; describe('Session Squads Integration - PHASE 8',()=>{let queryClient:QueryClient; let wrapper:React.FC<{children:React.ReactNode;}>; beforeEach(()=>{jest.clearAllMocks(); queryClient = new QueryClient({defaultOptions:{queries:{retry:false},mutations:{retry:false,gcTime:0}}}); wrapper = ({children})=>React.createElement(QueryClientProvider,{client:queryClient},children); mockAuthState({user:{id:'user-123',email:'test@example.com'}}); mockUseBasicSquadsStatus.mockReturnValue({data:undefined}as ReturnType<typeof useBasicSquadsStatus>); mockUseUpdateBasicSquadWeeklyProgress.mockReturnValue({mutateAsync:jest.fn()}as ReturnType<typeof useUpdateBasicSquadWeeklyProgress>);}); afterEach(()=>{cleanup(); queryClient.clear();}); describe('useSessionSquadsIntegration',()=>{it('should listen for session completion events',()=>{const mockUnsubscribe = jest.fn(); (eventBus.subscribe as jest.Mock).mockReturnValue(mockUnsubscribe); renderHook(()=>hooks.useSessionSquadsIntegration(),{wrapper}); expect(eventBus.subscribe).toHaveBeenCalledWith('session:completed',expect.any(Function));}); it('should update squad progress on session completion when user has squad',async()=>{const mockSquadStatus = {hasSquad:true,squad:{id:'squad-123',name:'Focus Squad'},isFounder:true,isAdmin:true,memberCount:3,weeklyProgress:{current:180,goal:300,completed:false,percentage:60}}; const mutateAsync = jest.fn().mockResolvedValue({goalUpdated:true,goalCompleted:false,squadProgress:210,squadGoal:300}); mockUseBasicSquadsStatus.mockReturnValue({data:mockSquadStatus}as ReturnType<typeof useBasicSquadsStatus>); mockUseUpdateBasicSquadWeeklyProgress.mockReturnValue({mutateAsync}as ReturnType<typeof useUpdateBasicSquadWeeklyProgress>); const mockUnsubscribe = jest.fn(); (eventBus.subscribe as jest.Mock).mockReturnValue(mockUnsubscribe); renderHook(()=>hooks.useSessionSquadsIntegration(),{wrapper}); const eventHandler = (eventBus.subscribe as jest.Mock).mock.calls[0][1]; const sessionEvent = {sessionId:'session-123',duration:1800,userId:'user-123'}; await act(async()=>{await eventHandler(sessionEvent);}); expect(eventBus.subscribe).toHaveBeenCalledWith('session:completed',expect.any(Function)); expect(mutateAsync).toHaveBeenCalledWith({squadId:'squad-123',sessionMinutes:30});}); it('should not update squad progress when user has no squad',async()=>{const mockSquadStatus = {hasSquad:false,squad:null,isFounder:false,isAdmin:false,memberCount:0,weeklyProgress:null}; const mutateAsync = jest.fn(); mockUseBasicSquadsStatus.mockReturnValue({data:mockSquadStatus}as ReturnType<typeof useBasicSquadsStatus>); mockUseUpdateBasicSquadWeeklyProgress.mockReturnValue({mutateAsync}as ReturnType<typeof useUpdateBasicSquadWeeklyProgress>); const mockUnsubscribe = jest.fn(); (eventBus.subscribe as jest.Mock).mockReturnValue(mockUnsubscribe); renderHook(()=>hooks.useSessionSquadsIntegration(),{wrapper}); const eventHandler = (eventBus.subscribe as jest.Mock).mock.calls[0][1]; const sessionEvent = {sessionId:'session-123',duration:1800,userId:'user-123'}; await act(async()=>{await eventHandler(sessionEvent);}); expect(eventBus.subscribe).toHaveBeenCalledWith('session:completed',expect.any(Function)); expect(mutateAsync).not.toHaveBeenCalled();});}); describe('useSquadStatusForSession',()=>{it('should provide squad status for session setup',async()=>{const mockSquadStatus = {hasSquad:true,squad:{id:'squad-123',name:'Focus Squad'},isFounder:true,isAdmin:true,memberCount:3,weeklyProgress:{current:180,goal:300,completed:false,percentage:60}}; mockUseBasicSquadsStatus.mockReturnValue({data:mockSquadStatus,isLoading:false,error:null}as ReturnType<typeof useBasicSquadsStatus>); const{result} = renderHook(()=>hooks.useSquadStatusForSession(),{wrapper}); await waitFor(()=>{expect(result.current.hasSquad).toBe(true);}); expect(result.current.hasSquad).toBe(true); expect(result.current.squadName).toBe('Focus Squad'); expect(result.current.squadId).toBe('squad-123'); expect(result.current.isFounder).toBe(true); expect(result.current.isAdmin).toBe(true); expect(result.current.memberCount).toBe(3); expect(result.current.weeklyProgress).toEqual(mockSquadStatus.weeklyProgress); expect(result.current.canContributeToGoal).toBe(true);}); it('should handle no squad case',async()=>{const mockSquadStatus = {hasSquad:false,squad:null,isFounder:false,isAdmin:false,memberCount:0,weeklyProgress:null}; mockUseBasicSquadsStatus.mockReturnValue({data:mockSquadStatus,isLoading:false,error:null}as ReturnType<typeof useBasicSquadsStatus>); const{result} = renderHook(()=>hooks.useSquadStatusForSession(),{wrapper}); await waitFor(()=>{expect(result.current.hasSquad).toBe(false);}); expect(result.current.hasSquad).toBe(false); expect(result.current.squadName).toBe(''); expect(result.current.squadId).toBe(''); expect(result.current.isFounder).toBe(false); expect(result.current.isAdmin).toBe(false); expect(result.current.memberCount).toBe(0); expect(result.current.weeklyProgress).toBeNull(); expect(result.current.canContributeToGoal).toBe(false);});}); describe('calculateSquadContribution',()=>{it('should calculate contribution from session data',()=>{const sessionData = {durationSeconds:1800,qualityScore:85}; const result = hooks.calculateSquadContribution(sessionData); expect(result.contributionMinutes).toBe(30); expect(result.helpsWeeklyGoal).toBe(true); expect(result.weeklyGoalMinutes).toBe(300); expect(result.weeklyProgressPercentage).toBe(Math.round(30 / 300 * 100));}); it('should handle zero duration sessions',()=>{const sessionData = {durationSeconds:0,qualityScore:0}; const result = hooks.calculateSquadContribution(sessionData); expect(result.contributionMinutes).toBe(0); expect(result.helpsWeeklyGoal).toBe(false); expect(result.weeklyProgressPercentage).toBe(0);}); it('should cap progress percentage at 100%',()=>{const sessionData = {durationSeconds:18000,qualityScore:90}; const result = hooks.calculateSquadContribution(sessionData); expect(result.contributionMinutes).toBe(300); expect(result.weeklyProgressPercentage).toBe(100);});}); describe('getSquadCTA',()=>{it('should provide CTA for users without squad',()=>{const squadStatus = {hasSquad:false,squadName:'',isFounder:false,isAdmin:false,memberCount:0,weeklyProgress:null}; const result = hooks.getSquadCTA(squadStatus); expect(result.primaryCTA).toBe('Create Squad'); expect(result.secondaryCTA).toBeNull(); expect(result.motivationMessage).toBe('Start a private accountability group with friends.');}); it('should provide celebration CTA for completed weekly goal',()=>{const squadStatus = {hasSquad:true,squadName:'Focus Squad',isFounder:false,isAdmin:false,memberCount:3,weeklyProgress:{current:300,goal:300,completed:true,percentage:100}}; const result = hooks.getSquadCTA(squadStatus); expect(result.primaryCTA).toBe('View Squad Progress'); expect(result.secondaryCTA).toBeNull(); expect(result.motivationMessage).toBe('Great job! Focus Squad completed the weekly goal!');}); it('should provide progress CTA for active squad members',()=>{const squadStatus = {hasSquad:true,squadName:'Focus Squad',isFounder:false,isAdmin:false,memberCount:3,weeklyProgress:{current:180,goal:300,completed:false,percentage:60}}; const result = hooks.getSquadCTA(squadStatus); expect(result.primaryCTA).toBe('Start Focus Session'); expect(result.secondaryCTA).toBeNull(); expect(result.motivationMessage).toBe('120 minutes (4 sessions) to weekly goal.');}); it('should provide invite CTA for squad founders/admins',()=>{const squadStatus = {hasSquad:true,squadName:'Focus Squad',isFounder:true,isAdmin:true,memberCount:2,weeklyProgress:{current:180,goal:300,completed:false,percentage:60}}; const result = hooks.getSquadCTA(squadStatus); expect(result.primaryCTA).toBe('Start Focus Session'); expect(result.secondaryCTA).toBe('Invite Members'); expect(result.motivationMessage).toBe('120 minutes (4 sessions) to weekly goal.');}); it('should calculate remaining sessions correctly',()=>{const squadStatus = {hasSquad:true,squadName:'Focus Squad',isFounder:false,isAdmin:false,memberCount:3,weeklyProgress:{current:90,goal:300,completed:false,percentage:30}}; const result = hooks.getSquadCTA(squadStatus); expect(result.primaryCTA).toBe('Start Focus Session'); expect(result.motivationMessage).toBe('210 minutes (7 sessions) to weekly goal.');});});});

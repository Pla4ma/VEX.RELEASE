import{describe,it,expect,beforeEach,vi}from'vitest';import*as service from'../service';import*as repository from'../repository';import{SquadValidationRules}from'../validation';import{withRetry}from'../retry';import{SquadError}from'../errors';vi.mock('../repository');vi.mock('../../../events',()=>({eventBus:{publish:vi.fn()}}));describe('Squad Integration Flows',()=>{beforeEach(()=>{vi.clearAllMocks();});describe('Full Squad Lifecycle',()=>{it('should handle complete squad creation to dissolution flow',async()=>{const founderId='user-founder';const squadId='squad-123';vi.mocked(repository.checkDuplicateMembership).mockResolvedValue(null);vi.mocked(repository.createSquad).mockResolvedValue({id:squadId,name:'Test Squad',memberCount:0,maxMembers:5,createdBy:founderId}as any);vi.mocked(repository.addSquadMember).mockResolvedValue({}as any);const squad=await service.createSquad(founderId,{name:'Test Squad',description:'A test squad',avatarUrl:null,isPublic:true,joinRequirements:'APPROVAL',maxMembers:5});expect(squad.id).toBe(squadId);const memberId1='user-member-1';vi.mocked(repository.fetchSquadMember).mockResolvedValue({role:'FOUNDER',isActive:true}as any);vi.mocked(repository.fetchSquadById).mockResolvedValue({...squad,memberCount:1,maxMembers:5}as any);vi.mocked(repository.checkExistingInvite).mockResolvedValue(null);vi.mocked(repository.createSquadInvite).mockResolvedValue({id:'invite-1',squadId,invitedUserId:memberId1,status:'PENDING'}as any);const invite=await service.inviteToSquad({squadId,invitedUserId:memberId1,roleOffered:'MEMBER',invitedBy:founderId,message:null,expiresInHours:48});expect(invite.status).toBe('PENDING');vi.mocked(repository.fetchSquadInviteById).mockResolvedValue({...invite,invitedBy:founderId,roleOffered:'MEMBER',expiresAt:Date.now()+100000}as any);vi.mocked(repository.checkDuplicateMembership).mockResolvedValue(null);vi.mocked(repository.addSquadMember).mockResolvedValue({}as any);vi.mocked(repository.updateSquadInviteStatus).mockResolvedValue({}as any);vi.mocked(repository.fetchSquadMembers).mockResolvedValue([{userId:founderId},{userId:memberId1}]);const joinedSquad=await service.respondToInvite(memberId1,{inviteId:invite.id,accept:true});expect(joinedSquad).toBeTruthy();vi.mocked(repository.updateSquadMemberRole).mockResolvedValue({}as any);await service.updateMemberRole(founderId,{squadId,memberUserId:memberId1,newRole:'ADMIN'});const memberId2='user-member-2';vi.mocked(repository.fetchSquadMember).mockResolvedValueOnce({role:'FOUNDER',isActive:true}as any).mockResolvedValueOnce({role:'ADMIN',isActive:true}as any);vi.mocked(repository.transferFounderRole).mockResolvedValue(undefined);vi.mocked(repository.removeSquadMember).mockResolvedValue(undefined);await service.leaveSquad(founderId,{squadId,newFounderId:memberId1});expect(repository.transferFounderRole).toHaveBeenCalledWith(squadId,memberId1);});});describe('Concurrent Operations',()=>{it('should handle concurrent invite acceptance race condition',async()=>{const squadId='squad-full';const squad={id:squadId,memberCount:4,maxMembers:5,name:'Full Squad'};vi.mocked(repository.fetchSquadInviteById).mockResolvedValueOnce({id:'invite-1',squadId,invitedUserId:'user-a',status:'PENDING',expiresAt:Date.now()+10000}as any).mockResolvedValueOnce({id:'invite-2',squadId,invitedUserId:'user-b',status:'PENDING',expiresAt:Date.now()+10000}as any);vi.mocked(repository.fetchSquadById).mockResolvedValueOnce({...squad,memberCount:4}as any).mockResolvedValueOnce({...squad,memberCount:5}as any);vi.mocked(repository.checkDuplicateMembership).mockResolvedValue(null);vi.mocked(repository.addSquadMember).mockResolvedValue({}as any);vi.mocked(repository.updateSquadInviteStatus).mockResolvedValue({}as any);vi.mocked(repository.fetchSquadMembers).mockResolvedValue([]);await service.respondToInvite('user-a',{inviteId:'invite-1',accept:true});await expect(service.respondToInvite('user-b',{inviteId:'invite-2',accept:true})).rejects.toThrow();});});describe('Validation Edge Cases',()=>{it('should reject invalid squad names',()=>{const valid=SquadValidationRules.createSquad({name:'Valid Squad Name',description:null,avatarUrl:null,isPublic:true,joinRequirements:'OPEN',maxMembers:5});expect(valid.success).toBe(true);const tooShort=SquadValidationRules.createSquad({name:'AB',description:null,avatarUrl:null,isPublic:true,joinRequirements:'OPEN',maxMembers:5});expect(tooShort.success).toBe(false);});it('should reject profane squad names',()=>{const profane=SquadValidationRules.createSquad({name:'badword1 Squad',description:null,avatarUrl:null,isPublic:true,joinRequirements:'OPEN',maxMembers:5});expect(profane.success).toBe(false);});});describe('Retry Logic',()=>{it('should retry transient failures',async()=>{let attempts=0;const operation=async()=>{attempts++;if(attempts<3){throw new SquadError('NETWORK_ERROR','Temporary failure',{},{retryable:true});}return'success';};const result=await withRetry(operation,'test-operation',{maxAttempts:3,baseDelayMs:10});expect(result).toBe('success');expect(attempts).toBe(3);});it('should not retry non-retryable errors',async()=>{let attempts=0;const operation=async()=>{attempts++;throw new SquadError('SQUAD_FULL','No space',{},{retryable:false});};await expect(withRetry(operation,'test-operation',{maxAttempts:3,baseDelayMs:10})).rejects.toThrow('SQUAD_FULL');expect(attempts).toBe(1);});});describe('Synergy Calculations',()=>{it('should calculate synergy progress correctly',async()=>{const squadId='squad-synergy';const initialPoints=80;const pointsToAdd=30;vi.mocked(repository.fetchSquadById).mockResolvedValue({id:squadId,totalContribution:initialPoints,currentSynergyLevel:1,pointsToNextSynergy:100,focusMultiplier:1.0,focusMultiplierBonus:0.0}as any);vi.mocked(repository.addContribution).mockResolvedValue(undefined);vi.mocked(repository.updateSquadStats).mockResolvedValue(undefined);await service.addContribution(squadId,'user-1',pointsToAdd,'session');expect(repository.updateSquadStats).toHaveBeenCalledWith(squadId,expect.objectContaining({totalContribution:initialPoints+pointsToAdd}));});});describe('Session Management',()=>{it('should track session participation and rewards',async()=>{const squadId='squad-session';const sessionId='session-1';const userId='user-1';vi.mocked(repository.createSquadSession).mockResolvedValue({id:sessionId,squadId,startedBy:userId,status:'ACTIVE'}as any);vi.mocked(repository.addSessionParticipant).mockResolvedValue({}as any);vi.mocked(repository.updateSessionParticipant).mockResolvedValue({}as any);const session=await service.startSquadSession(userId,{squadId,config:{type:'focus',duration:1500}});expect(session.id).toBe(sessionId);await service.joinSquadSession(userId,{sessionId,squadId});await service.completeSquadSession(userId,{sessionId,focusTime:1500,completed:true});expect(repository.updateSessionParticipant).toHaveBeenCalled();});});});

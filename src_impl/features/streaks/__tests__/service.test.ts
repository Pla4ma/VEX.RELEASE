import{isQualifyingSession,getCalendarDay,checkMilestone,getStreakMultiplier}from'../service';import*as repository from'../repository';jest.mock('../repository');describe('StreaksService',()=>{describe('isQualifyingSession',()=>{it('should qualify 15+ minute session with good quality',()=>{expect(isQualifyingSession(900,50)).toBe(true);});it('should reject short sessions',()=>{expect(isQualifyingSession(600,100)).toBe(false);});it('should reject low quality sessions',()=>{expect(isQualifyingSession(1800,40)).toBe(false);});it('should accept exact threshold',()=>{expect(isQualifyingSession(900,50)).toBe(true);});});describe('getCalendarDay',()=>{it('should format date consistently',()=>{const timestamp=new Date('2024-01-15').getTime();const result=getCalendarDay(timestamp,'UTC');expect(result).toMatch(/\d{1,2}\/\d{1,2}\/\d{4}/);});it('should respect timezone',()=>{const timestamp=Date.now();const utc=getCalendarDay(timestamp,'UTC');const ny=getCalendarDay(timestamp,'America/New_York');expect(typeof utc).toBe('string');expect(typeof ny).toBe('string');});});describe('checkMilestone',()=>{it('should return milestone for day 3',()=>{const result=checkMilestone(3);expect(result).not.toBeNull();expect(result?.days).toBe(3);expect(result?.rewardType).toBe('COINS');});it('should return milestone for day 7',()=>{const result=checkMilestone(7);expect(result).not.toBeNull();expect(result?.days).toBe(7);});it('should return null for non-milestone days',()=>{expect(checkMilestone(4)).toBeNull();expect(checkMilestone(8)).toBeNull();});});describe('getStreakMultiplier',()=>{it('should return 1.0 for 0-2 days',()=>{expect(getStreakMultiplier(0)).toBe(1.0);expect(getStreakMultiplier(2)).toBe(1.0);});it('should return 1.25 for 3-6 days',()=>{expect(getStreakMultiplier(3)).toBe(1.25);expect(getStreakMultiplier(6)).toBe(1.25);});it('should return 1.5 for 7-13 days',()=>{expect(getStreakMultiplier(7)).toBe(1.5);});it('should return 1.75 for 14-29 days',()=>{expect(getStreakMultiplier(14)).toBe(1.75);});it('should return 2.0 for 30+ days',()=>{expect(getStreakMultiplier(30)).toBe(2.0);});});describe('recordSession - same-day session',()=>{it('should NOT increment streak for same-day session',async()=>{const{recordSession}=await import('../service');const today=new Date().toISOString();(repository.fetchStreak as jest.Mock).mockResolvedValue({id:'streak-1',userId:'user-1',currentDays:5,longestDays:10,lastQualifyingSessionAt:Date.now(),timezone:'UTC',shieldsAvailable:0,gracePeriodUsed:false});const result=await recordSession({userId:'user-1',sessionId:'session-1',duration:900,qualityScore:80,completedAt:Date.now()});expect(result.action).toBe('ALREADY_TODAY');expect(result.previousStreak).toBe(5);expect(result.newStreak).toBe(5);});});describe('recordSession - next-day session',()=>{it('should INCREMENT streak for next-day session',async()=>{const{recordSession}=await import('../service');const yesterday=new Date(Date.now()-24*60*60*1000);(repository.fetchStreak as jest.Mock).mockResolvedValue({id:'streak-1',userId:'user-1',currentDays:5,longestDays:10,lastQualifyingSessionAt:yesterday.getTime(),timezone:'UTC',shieldsAvailable:0,gracePeriodUsed:false});(repository.updateStreak as jest.Mock).mockResolvedValue(undefined);const result=await recordSession({userId:'user-1',sessionId:'session-1',duration:900,qualityScore:80,completedAt:Date.now()});expect(result.action).toBe('INCREMENTED');expect(result.previousStreak).toBe(5);expect(result.newStreak).toBe(6);});});describe('recordSession - 2-day gap (no shields)',()=>{it('should BREAK streak for 2-day gap without shields',async()=>{const{recordSession}=await import('../service');const twoDaysAgo=new Date(Date.now()-2*24*60*60*1000);(repository.fetchStreak as jest.Mock).mockResolvedValue({id:'streak-1',userId:'user-1',currentDays:5,longestDays:10,lastQualifyingSessionAt:twoDaysAgo.getTime(),timezone:'UTC',shieldsAvailable:0,gracePeriodUsed:false,frozenUntil:null});(repository.updateStreak as jest.Mock).mockResolvedValue(undefined);const result=await recordSession({userId:'user-1',sessionId:'session-1',duration:900,qualityScore:80,completedAt:Date.now()});expect(result.action).toBe('BROKEN');expect(result.previousStreak).toBe(5);expect(result.newStreak).toBe(1);});});describe('recordSession - 2-day gap WITH shield',()=>{it('should use shield to protect streak during grace period',async()=>{const{recordSession}=await import('../service');const twoDaysAgo=new Date(Date.now()-2*24*60*60*1000);(repository.fetchStreak as jest.Mock).mockResolvedValue({id:'streak-1',userId:'user-1',currentDays:5,longestDays:10,lastQualifyingSessionAt:twoDaysAgo.getTime(),timezone:'UTC',shieldsAvailable:1,gracePeriodUsed:false,frozenUntil:null});(repository.updateStreak as jest.Mock).mockResolvedValue(undefined);const result=await recordSession({userId:'user-1',sessionId:'session-1',duration:900,qualityScore:80,completedAt:Date.now()});expect(result.action).toBe('SHIELD_PROTECTED');expect(result.shieldUsed).toBe(true);expect(result.newStreak).toBe(6);});});describe('recordSession - timezone edge cases',()=>{it('should handle 11 PM + midnight + 1 AM next session correctly',async()=>{const{recordSession,getCalendarDay}=await import('../service');const nyTimezone='America/New_York';const elevenPM=new Date('2024-01-15T23:00:00-05:00').getTime();const oneAM=new Date('2024-01-16T01:00:00-05:00').getTime();(repository.fetchStreak as jest.Mock).mockResolvedValue({id:'streak-1',userId:'user-1',currentDays:5,longestDays:10,lastQualifyingSessionAt:elevenPM,timezone:nyTimezone,shieldsAvailable:0,gracePeriodUsed:false});(repository.updateStreak as jest.Mock).mockResolvedValue(undefined);const result=await recordSession({userId:'user-1',sessionId:'session-1',duration:900,qualityScore:80,completedAt:oneAM});const day1=getCalendarDay(elevenPM,nyTimezone);const day2=getCalendarDay(oneAM,nyTimezone);if(day1===day2){expect(result.action).toBe('ALREADY_TODAY');}else{expect(result.action).toBe('INCREMENTED');}});});});

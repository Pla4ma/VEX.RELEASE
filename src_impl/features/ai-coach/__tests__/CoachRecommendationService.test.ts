import{CoachRecommendationService,createCoachRecommendationService,convertToHomeRecommendation,COACH_PERSONAS,type RecommendationContext,type CoachPersonaId}from'../services/CoachRecommendationService'; import type{ActiveStudyPlan}from'../../content-study/hooks/helpers'; describe('CoachRecommendationService',()=>{const createBaseContext = (overrides:Partial<RecommendationContext> = {}):RecommendationContext=>({userId:'test-user',currentTime:new Date(),streakDays:5,hasCompletedSessionToday:false,hoursUntilStreakBreak:6,activeStudyPlan:null,studyPlanProgress:0,studyPlanDaysBehind:0,activeBoss:null,totalSessions:20,currentLevel:3,daysSinceLastSession:0.5,behaviorProfile:null,coachPersonaId:'mentor',...overrides}); describe('Recommendation Priority',()=>{it('should prioritize critical streak protection when < 2 hours remaining',()=>{const context = createBaseContext({streakDays:10,hoursUntilStreakBreak:1.5,hasCompletedSessionToday:false}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('protect_streak'); expect(recommendation.urgency).toBe('critical'); expect(recommendation.priority).toBe(100);}); it('should prioritize high streak protection when 2-4 hours remaining',()=>{const context = createBaseContext({streakDays:10,hoursUntilStreakBreak:3,hasCompletedSessionToday:false}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('protect_streak'); expect(recommendation.urgency).toBe('high'); expect(recommendation.priority).toBe(100);}); it('should prioritize study behind when 3+ days behind schedule',()=>{const context = createBaseContext({hasCompletedSessionToday:true,activeStudyPlan:{generationId:'plan-1',title:'Biology 101',totalTasks:10,completedTasks:3,remainingMinutes:120,status:'active'}as ActiveStudyPlan,studyPlanProgress:0.3,studyPlanDaysBehind:4}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('study_behind'); expect(recommendation.urgency).toBe('high'); expect(recommendation.priority).toBe(90);}); it('should prioritize boss opportunity when boss health < 30%',()=>{const context = createBaseContext({hasCompletedSessionToday:true,activeBoss:{id:'boss-1',name:'Procrastination Dragon',healthRemaining:25,maxHealth:100,timeRemaining:12}}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('boss_opportunity'); expect(recommendation.urgency).toBe('high'); expect(recommendation.priority).toBe(85);}); it('should show momentum building when on 2+ day streak and already completed session',()=>{const context = createBaseContext({streakDays:5,hasCompletedSessionToday:true,hoursUntilStreakBreak:null}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('momentum_building'); expect(recommendation.urgency).toBe('low'); expect(recommendation.priority).toBe(70);}); it('should show comeback recommendation after streak break within 7 days',()=>{const context = createBaseContext({streakDays:0,hasCompletedSessionToday:false,hoursUntilStreakBreak:null,daysSinceLastSession:2,totalSessions:15}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).toBe('comeback'); expect(recommendation.urgency).toBe('medium'); expect(recommendation.priority).toBe(55);});}); describe('Persona Message Generation',()=>{it('should generate different messages for each persona type',()=>{const personas:CoachPersonaId[] = ['mentor','trainer','peer','professor']; const context = createBaseContext({streakDays:5,hoursUntilStreakBreak:3}); const messages = personas.map(personaId=>{const service = new CoachRecommendationService({...context,coachPersonaId:personaId}); const rec = service.getRecommendation(); return{personaId,headline:rec.headline,coachMessage:rec.coachMessage};}); const headlines = messages.map(m=>m.headline); const uniqueHeadlines = new Set(headlines); expect(uniqueHeadlines.size).toBeGreaterThan(1); const trainer = messages.find(m=>m.personaId === 'trainer'); expect(trainer?.headline).toContain('SAVE'); const peer = messages.find(m=>m.personaId === 'peer'); expect(peer?.coachMessage).toMatch(/!|🔥|💪/);}); it('should follow voice guidelines (max 2 sentences)',()=>{const context = createBaseContext({coachPersonaId:'mentor'}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); const sentences = recommendation.coachMessage.split(/[.!?]+/).filter(s=>s.trim().length > 0); expect(sentences.length).toBeLessThanOrEqual(2);});}); describe('Recommendation Context',()=>{it('should include reasoning for analytics',()=>{const context = createBaseContext({streakDays:5,hoursUntilStreakBreak:3}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.reasoning).toBeTruthy(); expect(recommendation.reasoning).toContain('streak');}); it('should include visual cues based on urgency',()=>{const criticalContext = createBaseContext({streakDays:5,hoursUntilStreakBreak:1}); const service = new CoachRecommendationService(criticalContext); const recommendation = service.getRecommendation(); expect(recommendation.visualCue).toBe('urgent');});}); describe('Home Recommendation Conversion',()=>{it('should convert coach recommendation to home format',()=>{const context = createBaseContext({streakDays:5,hoursUntilStreakBreak:3}); const service = new CoachRecommendationService(context); const coachRec = service.getRecommendation(); const homeRec = convertToHomeRecommendation(coachRec); expect(homeRec.id).toBe(coachRec.id); expect(homeRec.type).toBe(coachRec.type); expect(homeRec.headline).toBe(coachRec.headline); expect(homeRec.subtext).toBe(coachRec.subtext); expect(homeRec.ctaText).toBe(coachRec.ctaText); expect(homeRec.ctaAction).toBe(coachRec.ctaAction); expect(homeRec.aiCoachMessage).toBe(coachRec.coachMessage); expect(homeRec.visualCue).toBe(coachRec.visualCue);});}); describe('Refresh Logic',()=>{it('should refresh after 5 minutes',()=>{const context = createBaseContext(); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); const sixMinutesAgo = Date.now() - 6 * 60 * 1000; expect(service.shouldRefresh(sixMinutesAgo,recommendation)).toBe(true);}); it('should NOT refresh before 5 minutes',()=>{const context = createBaseContext(); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); const oneMinuteAgo = Date.now() - 60 * 1000; expect(service.shouldRefresh(oneMinuteAgo,recommendation)).toBe(false);}); it('should refresh more frequently for critical streak protection',()=>{const context = createBaseContext({streakDays:5,hoursUntilStreakBreak:1}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); const twoMinutesAgo = Date.now() - 2 * 60 * 1000; expect(service.shouldRefresh(twoMinutesAgo,recommendation)).toBe(true);});}); describe('Factory Function',()=>{it('should create service via factory function',()=>{const context = createBaseContext(); const service = createCoachRecommendationService(context); expect(service).toBeInstanceOf(CoachRecommendationService); const recommendation = service.getRecommendation(); expect(recommendation).toBeDefined();});}); describe('Edge Cases',()=>{it('should handle null active study plan',()=>{const context = createBaseContext({activeStudyPlan:null}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation).toBeDefined(); expect(recommendation.type).not.toBe('study_plan');}); it('should handle null active boss',()=>{const context = createBaseContext({activeBoss:null}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation).toBeDefined(); expect(recommendation.type).not.toBe('boss_battle'); expect(recommendation.type).not.toBe('boss_opportunity');}); it('should handle zero streak days',()=>{const context = createBaseContext({streakDays:0,hoursUntilStreakBreak:null}); const service = new CoachRecommendationService(context); const recommendation = service.getRecommendation(); expect(recommendation.type).not.toBe('protect_streak');}); it('should handle invalid persona gracefully',()=>{const context = createBaseContext({coachPersonaId:'invalid-persona'as CoachPersonaId}); const service = new CoachRecommendationService(context); expect(()=>service.getRecommendation()).not.toThrow(); const recommendation = service.getRecommendation(); expect(recommendation).toBeDefined();});}); describe('All Applicable Recommendations',()=>{it('should return multiple applicable recommendations',()=>{const context = createBaseContext({hasCompletedSessionToday:true,streakDays:5,activeBoss:{id:'boss-1',name:'Test Boss',healthRemaining:80,maxHealth:100,timeRemaining:24}}); const service = new CoachRecommendationService(context); const allRecs = service.getAllApplicable(); expect(allRecs.length).toBeGreaterThan(1); const momentumRec = allRecs.find(r=>r.type === 'momentum_building'); expect(momentumRec).toBeDefined(); const bossRec = allRecs.find(r=>r.type === 'boss_battle'); expect(bossRec).toBeDefined();});}); describe('Personas',()=>{it('should have all four personas defined',()=>{expect(COACH_PERSONAS.mentor).toBeDefined(); expect(COACH_PERSONAS.trainer).toBeDefined(); expect(COACH_PERSONAS.peer).toBeDefined(); expect(COACH_PERSONAS.professor).toBeDefined();}); it('should have correct voice tone for each persona',()=>{expect(COACH_PERSONAS.mentor.voiceTone).toBe('WISE'); expect(COACH_PERSONAS.trainer.voiceTone).toBe('STERN'); expect(COACH_PERSONAS.peer.voiceTone).toBe('PLAYFUL'); expect(COACH_PERSONAS.professor.voiceTone).toBe('WISE');}); it('should have correct sentence structure guidelines',()=>{expect(COACH_PERSONAS.mentor.sentenceStructure).toBe('MEASURED'); expect(COACH_PERSONAS.trainer.sentenceStructure).toBe('SHORT_DIRECT'); expect(COACH_PERSONAS.peer.sentenceStructure).toBe('CONVERSATIONAL'); expect(COACH_PERSONAS.professor.sentenceStructure).toBe('MEASURED');});});});

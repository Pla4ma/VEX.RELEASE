import{getUsableConsumables,getActiveBuffs}from'./repository';import{LoadoutOptionsSchema,LoadoutItemSchema,ActiveBuffSchema,type InventoryItem,type LoadoutOptions,type LoadoutItem,type ActiveBuff}from'./schemas';const MODE_RESTRICTIONS:Record<string,string[]>={LIGHT_FOCUS:['DEEP_WORK_BOOST','SPRINT_BOOST'],DEEP_WORK:['LIGHT_FOCUS_BOOST'],SPRINT:['LONG_DURATION_ITEMS'],CREATIVE:[],STUDY:[]};const DURATION_RESTRICTIONS={SHORT:600,MEDIUM:1800,LONG:3600};function checkCompatibility(item:InventoryItem,mode:string,durationSeconds:number):{compatible:boolean;reason?:string;}{const modeRestrictions=MODE_RESTRICTIONS[mode]??[];const itemTags=item.metadata?.tags as string[]??[];for(const restriction of modeRestrictions){if(itemTags.includes(restriction)){return{compatible:false,reason:`Not compatible with ${mode} mode`};}}if(durationSeconds<DURATION_RESTRICTIONS.SHORT){if(itemTags.includes('LONG_DURATION_BOOST')){return{compatible:false,reason:'Too short session for this boost'};}}if(item.usesRemaining!==null&&item.usesRemaining<=0){return{compatible:false,reason:'No uses remaining'};}return{compatible:true};}function calculateImpact(item:InventoryItem,durationSeconds:number):LoadoutItem['projectedImpact']{const baseMultiplier=1;const metadata=item.metadata??{};const xpBoost=metadata.xpBoost as number??0;const xpMultiplier=baseMultiplier+xpBoost;const coinBoost=metadata.coinBoost as number??0;const coinMultiplier=baseMultiplier+coinBoost;const streakProtection=metadata.streakProtection as boolean??false;const bossDamageBase=metadata.bossDamageBonus as number??0;const bossDamageBonus=Math.floor(bossDamageBase*(durationSeconds/3600));return{xpMultiplier,coinMultiplier,streakProtection,bossDamageBonus};}function calculateSummary(items:LoadoutItem[],activeBuffs:ActiveBuff[]):{totalItems:number;usableItems:number;activeBuffs:number;projectedXpMultiplier:number;projectedCoinMultiplier:number;hasStreakProtection:boolean;}{const totalItems=items.length;const usableItems=items.filter(i=>i.compatible).length;let projectedXpMultiplier=1;let projectedCoinMultiplier=1;let hasStreakProtection=false;for(const buff of activeBuffs){projectedXpMultiplier*=buff.effects.xpMultiplier;projectedCoinMultiplier*=buff.effects.coinMultiplier;if(buff.effects.streakProtection){hasStreakProtection=true;}}return{totalItems,usableItems,activeBuffs:activeBuffs.length,projectedXpMultiplier:Math.round(projectedXpMultiplier*100)/100,projectedCoinMultiplier:Math.round(projectedCoinMultiplier*100)/100,hasStreakProtection};}function getOfflineRestrictions():string[]{return['Wagers require online connection','Boss bounty claims require sync','Streak insurance requires verification'];}export async function buildLoadoutOptions(userId:string,mode:string,durationSeconds:number):Promise<LoadoutOptions>{const[inventory,buffs]=await Promise.all([getUsableConsumables(userId).catch(()=>[]as InventoryItem[]),getActiveBuffs(userId).catch(()=>[]as InventoryItem[])]);const available=inventory.map((item):LoadoutItem=>{const compatibility=checkCompatibility(item,mode,durationSeconds);const impact=calculateImpact(item,durationSeconds);return LoadoutItemSchema.parse({id:item.id,itemDefinitionId:item.itemDefinitionId,name:item.metadata?.name as string??'Unknown Item',description:item.metadata?.description as string??'',iconUrl:item.metadata?.iconUrl as string??undefined,rarity:item.metadata?.rarity as string??'COMMON',quantity:item.quantity,usesRemaining:item.usesRemaining,compatible:compatibility.compatible,incompatibilityReason:compatibility.reason,projectedImpact:impact});});const activeBuffs=buffs.map((buff):ActiveBuff=>{const metadata=buff.metadata??{};const effects=metadata.effects as Record<string,unknown>??{};return ActiveBuffSchema.parse({id:buff.id,name:metadata.name as string??'Unknown Buff',description:metadata.description as string??'',iconUrl:metadata.iconUrl as string??undefined,expiresAt:buff.equippedAt?(buff.equippedAt as number)+24*60*60*1000:null,effects:{xpMultiplier:effects.xpMultiplier as number??1,coinMultiplier:effects.coinMultiplier as number??1,streakProtection:effects.streakProtection as boolean??false,bossDamageBonus:effects.bossDamageBonus as number??0}});});const loadout={available,activeBuffs,summary:calculateSummary(available,activeBuffs),offlineRestrictions:getOfflineRestrictions()};return LoadoutOptionsSchema.parse(loadout);}import{useQuery}from'@tanstack/react-query';const LOADOUT_OPTIONS_KEY='loadout-options';export function useLoadoutOptions(userId:string|null|undefined,mode:string,durationSeconds:number){return useQuery<LoadoutOptions>({queryKey:[LOADOUT_OPTIONS_KEY,userId,mode,durationSeconds],queryFn:()=>buildLoadoutOptions(userId!,mode,durationSeconds),enabled:!!userId&&durationSeconds>0,staleTime:1000*30});}

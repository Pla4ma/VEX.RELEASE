import{z}from'zod'; import{eventBus}from'../../events'; import{MMKVStorageAdapter}from'../../persistence/MMKVStorageAdapter'; import*as repository from'./repository'; const UserIdSchema = z.string().min(1); const UnreadNotificationsCountSchema = z.number().int().nonnegative(); export async function getUnreadNotificationsCount(userId:string):Promise<number>{const validatedUserId = UserIdSchema.parse(userId); const count = await repository.fetchUnreadNotificationsCount(validatedUserId); return UnreadNotificationsCountSchema.parse(count);}export type UrgencyNotificationType='STREAK_AT_RISK'|'BOSS_ESCAPE_IMMINENT'|'SQUAD_STREAK_AT_RISK'|'RIVAL_PULLING_AHEAD'|'CHEST_INVENTORY_FULL'|'CHALLENGE_EXPIRING'|'SEASON_ENDING_UNCLAIMED'; export type SocialNotificationType='RIVAL_COMPLETED_SESSION'|'SQUAD_MEMBER_NUDGE'|'SQUAD_MILESTONE'|'FEED_REACTION'; export interface NotificationContext{userId:string;streakRisk?:{hoursRemaining:number;streakDays:number;riskLevel:'LOW'|'MEDIUM'|'HIGH'|'CRITICAL';};bossEscape?:{bossName:string;hoursRemaining:number;healthPercent:number;};squadStreak?:{squadName:string;streakDays:number;atRiskMemberName:string;};rivalUpdate?:{rivalName:string;theirNewSessionMinutes:number;myScore:number;theirScore:number;};chestStatus?:{unopenedCount:number;maxCapacity:number;};challengeExpiry?:{challengeName:string;hoursRemaining:number;progressPercent:number;};seasonEnding?:{hoursRemaining:number;unclaimedTiers:number;};}const MAX_NOTIFICATIONS_PER_DAY = 2; const notificationLimitStorage = new MMKVStorageAdapter('notification-limits'); const DEFAULT_QUIET_START_HOUR = 22; const DEFAULT_QUIET_END_HOUR = 8; export function isQuietHours(userTimezone:string = 'UTC',quietStart:number = DEFAULT_QUIET_START_HOUR,quietEnd:number = DEFAULT_QUIET_END_HOUR):boolean{const now = new Date(); const userTime = new Date(now.toLocaleString('en-US',{timeZone:userTimezone})); const hour = userTime.getHours(); if(quietStart <= quietEnd){return hour >= quietStart && hour < quietEnd;}return hour >= quietStart || hour < quietEnd;}export function getNextNotificationWindow(userTimezone:string = 'UTC',quietEnd:number = DEFAULT_QUIET_END_HOUR):Date{const now = new Date(); const userTime = new Date(now.toLocaleString('en-US',{timeZone:userTimezone})); const nextWindow = new Date(userTime); nextWindow.setHours(quietEnd,0,0,0); if(userTime.getHours() >= quietEnd){nextWindow.setDate(nextWindow.getDate() + 1);}return nextWindow;}export function shouldNotifyStreakAtRisk(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{streakRisk} = context; if(!streakRisk || streakRisk.hoursRemaining > 12){return{shouldSend:false,priority:0,message:{title:'',body:''}};}const urgency = streakRisk.riskLevel === 'CRITICAL' ? '🚨 LAST CHANCE' : streakRisk.riskLevel === 'HIGH' ? '⚠️ Streak at Risk' : '⏰ Streak Warning'; return{shouldSend:true,priority:streakRisk.riskLevel === 'CRITICAL' ? 10 : 8,message:{title:urgency,body:`Your 🔥 ${streakRisk.streakDays}-day streak ends in ${streakRisk.hoursRemaining} hours! Start a session now.`}};}export function shouldNotifyBossEscape(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{bossEscape} = context; if(!bossEscape || bossEscape.hoursRemaining > 4){return{shouldSend:false,priority:0,message:{title:'',body:''}};}return{shouldSend:true,priority:9,message:{title:'👹 Boss Escaping Soon!',body:`${bossEscape.bossName} has ${bossEscape.healthPercent.toFixed(0)}% health and escapes in ${bossEscape.hoursRemaining}h! Defeat them now!`}};}export function shouldNotifySquadStreakAtRisk(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{squadStreak} = context; if(!squadStreak){return{shouldSend:false,priority:0,message:{title:'',body:''}};}return{shouldSend:true,priority:7,message:{title:'🔥 Squad Streak at Risk!',body:`${squadStreak.atRiskMemberName} hasn't focused today — your ${squadStreak.streakDays}-day squad streak is at risk!`}};}export function shouldNotifyRivalAhead(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{rivalUpdate} = context; if(!rivalUpdate || rivalUpdate.myScore >= rivalUpdate.theirScore){return{shouldSend:false,priority:0,message:{title:'',body:''}};}const diff = rivalUpdate.theirScore - rivalUpdate.myScore; return{shouldSend:true,priority:6,message:{title:'⚔️ Rival Alert!',body:`${rivalUpdate.rivalName} just focused for ${rivalUpdate.theirNewSessionMinutes} min. You're ${diff} min behind this week!`}};}export function shouldNotifyChestFull(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{chestStatus} = context; if(!chestStatus || chestStatus.unopenedCount < chestStatus.maxCapacity){return{shouldSend:false,priority:0,message:{title:'',body:''}};}return{shouldSend:true,priority:5,message:{title:'🎁 Chests Full!',body:`Your chest inventory is full (${chestStatus.unopenedCount}/${chestStatus.maxCapacity}). Open one to make room for more!`}};}export function shouldNotifyChallengeExpiring(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{challengeExpiry} = context; if(!challengeExpiry || challengeExpiry.hoursRemaining > 2 || challengeExpiry.progressPercent >= 50){return{shouldSend:false,priority:0,message:{title:'',body:''}};}return{shouldSend:true,priority:4,message:{title:'⏰ Challenge Ending!',body:`"${challengeExpiry.challengeName}" expires in ${challengeExpiry.hoursRemaining}h and you're only ${challengeExpiry.progressPercent}% complete!`}};}export function shouldNotifySeasonEnding(context:NotificationContext):{shouldSend:boolean;priority:number;message:{title:string;body:string;};}{const{seasonEnding} = context; if(!seasonEnding || seasonEnding.hoursRemaining > 24 || seasonEnding.unclaimedTiers === 0){return{shouldSend:false,priority:0,message:{title:'',body:''}};}return{shouldSend:true,priority:8,message:{title:'🌙 Season Ending!',body:`Season ends in ${Math.floor(seasonEnding.hoursRemaining)} hours! You have ${seasonEnding.unclaimedTiers} unclaimed reward tiers!`}};}export function createRivalSessionNotification(rivalName:string,sessionMinutes:number,currentDiff:number):{title:string;body:string;}{const diffText = currentDiff === 0 ? "You're tied now!" : currentDiff > 0 ? `Still ahead by ${currentDiff} min` : `${Math.abs(currentDiff)} min behind`; return{title:`${rivalName} just focused for ${sessionMinutes} min`,body:`${diffText} — start a session to stay in the lead!`};}export function createSquadNudgeNotification(nudgerName:string,squadName:string,squadStreak:number):{title:string;body:string;}{return{title:`⚡ ${nudgerName} nudged you!`,body:`From ${squadName}: Your squad's ${squadStreak}-day streak depends on you. Start a quick session?`};}export function createSquadMilestoneNotification(squadName:string,milestoneDays:number,bonusXp:number):{title:string;body:string;}{return{title:`🔥 ${squadName} Milestone!`,body:`Your squad hit a ${milestoneDays}-day streak! You all earned a ${bonusXp} XP bonus!`};}export function createFeedReactionNotification(reactorName:string,reactionEmoji:string,postTitle:string):{title:string;body:string;}{return{title:`${reactorName} reacted ${reactionEmoji}`,body:`To your post: "${postTitle.substring(0,50)}${postTitle.length > 50 ? '...' : ''}"`};}export function evaluateNotificationRules(context:NotificationContext):{shouldSend:boolean;notification?:{title:string;body:string;priority:number;};}{const rules = [shouldNotifyStreakAtRisk(context),shouldNotifyBossEscape(context),shouldNotifySquadStreakAtRisk(context),shouldNotifyRivalAhead(context),shouldNotifyChestFull(context),shouldNotifyChallengeExpiring(context),shouldNotifySeasonEnding(context)]; const sendable = rules.filter(r=>r.shouldSend); if(sendable.length === 0){return{shouldSend:false};}sendable.sort((a,b)=>b.priority - a.priority); const top = sendable[0]; return{shouldSend:true,notification:{...top.message,priority:top.priority}};}export function checkDailyNotificationLimit(userId:string):{canSend:boolean;remaining:number;}{const key = `notifications:${userId}:${new Date().toDateString()}`; const raw = notificationLimitStorage.getItemSync(key); const count = raw ? Number.parseInt(raw,10) : 0; const safeCount = Number.isFinite(count) ? count : 0; return{canSend:safeCount < MAX_NOTIFICATIONS_PER_DAY,remaining:Math.max(0,MAX_NOTIFICATIONS_PER_DAY - safeCount)};}export function recordNotificationSent(userId:string):void{const key = `notifications:${userId}:${new Date().toDateString()}`; const raw = notificationLimitStorage.getItemSync(key); const count = raw ? Number.parseInt(raw,10) : 0; const safeCount = Number.isFinite(count) ? count : 0; notificationLimitStorage.setItemSync(key,String(safeCount + 1));}export async function dispatchUrgencyNotification(context:NotificationContext,userTimezone:string = 'UTC',quietStart:number = DEFAULT_QUIET_START_HOUR,quietEnd:number = DEFAULT_QUIET_END_HOUR):Promise<{sent:boolean;reason?:string;deferred?:boolean;nextWindow?:Date;}>{if(isQuietHours(userTimezone,quietStart,quietEnd)){return{sent:false,reason:'quiet_hours',deferred:true,nextWindow:getNextNotificationWindow(userTimezone,quietEnd)};}const limit = checkDailyNotificationLimit(context.userId); if(!limit.canSend){return{sent:false,reason:'daily_limit_reached'};}const evaluation = evaluateNotificationRules(context); if(!evaluation.shouldSend){return{sent:false,reason:'no_urgent_context'};}eventBus.publish('notification:send',{userId:context.userId,type:'URGENCY',title:evaluation.notification?.title ?? 'VEX',body:evaluation.notification?.body ?? 'You have an update waiting.',priority:'high'}); recordNotificationSent(context.userId); return{sent:true};}


const PushTokenInputSchema = z.object({
  userId: z.string().uuid(),
  token: z.string().min(1),
  platform: z.string().min(1),
}).strict();

export async function registerPushToken(input: {
  userId: string;
  token: string;
  platform: string;
}): Promise<void> {
  const tokenInput = PushTokenInputSchema.parse(input);
  await repository.upsertPushToken(
    tokenInput.userId,
    tokenInput.token,
    tokenInput.platform,
  );
}

import{captureSilentFailure}from'../../../utils/silent-failure';import{renderHook,act,waitFor}from'@testing-library/react-native';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import React from'react';import{useContentInput,useContentReview,useStudyPlan,useContentHistory,useRateLimit,contentStudyQueryKeys}from'../hooks';jest.mock('../../store',()=>({useAuthStore:()=>({user:{id:'test-user-id',email:'test@example.com'}})}));jest.mock('../ContentStudyService',()=>({submitContent:jest.fn(),extractContent:jest.fn(),generateStudyPlan:jest.fn(),getContentStatus:jest.fn(),uploadStudyFile:jest.fn(),updateContentText:jest.fn(),fetchContentById:jest.fn(),fetchGenerationById:jest.fn(),pollContentStatus:jest.fn(),fetchContentHistory:jest.fn(),checkRateLimit:jest.fn()}));import*as service from'../ContentStudyService';const createTestQueryClient=()=>new QueryClient({defaultOptions:{queries:{retry:false}}});const wrapper=({children}:{children:React.ReactNode;})=>{const queryClient=createTestQueryClient();return React.createElement(QueryClientProvider,{client:queryClient},children);};describe('contentStudyQueryKeys',()=>{it('should generate correct query keys',()=>{expect(contentStudyQueryKeys.all).toEqual(['content-study']);expect(contentStudyQueryKeys.content('123')).toEqual(['content-study','content','123']);expect(contentStudyQueryKeys.generation('456')).toEqual(['content-study','generation','456']);});});describe('useContentInput',()=>{beforeEach(()=>{jest.clearAllMocks();});it('should initialize with default state',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});expect(result.current.state.activeTab).toBe('paste');expect(result.current.state.pastedText).toBe('');expect(result.current.state.youtubeUrl).toBe('');expect(result.current.state.selectedFile).toBeNull();expect(result.current.isSubmitting).toBe(false);expect(result.current.error).toBeNull();});it('should update tab',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});act(()=>{result.current.setTab('youtube');});expect(result.current.state.activeTab).toBe('youtube');});it('should update pasted text',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});act(()=>{result.current.setPastedText('Test content');});expect(result.current.state.pastedText).toBe('Test content');});it('should update YouTube URL',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});act(()=>{result.current.setYoutubeUrl('https://youtube.com/watch?v=123');});expect(result.current.state.youtubeUrl).toBe('https://youtube.com/watch?v=123');});it('should update selected file',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});const file={uri:'file:///test.pdf',name:'test.pdf',size:1000,type:'application/pdf'};act(()=>{result.current.setSelectedFile(file);});expect(result.current.state.selectedFile).toEqual(file);});it('should clear error',()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});act(()=>{result.current.setPastedText('Test');result.current.clearError();});expect(result.current.error).toBeNull();});it('should validate pasted text before submit',async()=>{const{result}=renderHook(()=>useContentInput(),{wrapper});act(()=>{result.current.setTab('paste');result.current.setPastedText('Short');});await act(async()=>{try{await result.current.submit();}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'network-fallback',type:'network'});}});expect(result.current.error).toBeTruthy();});});describe('useContentReview',()=>{beforeEach(()=>{jest.clearAllMocks();});it('should fetch content data',async()=>{const mockContent={id:'content-123',source_type:'PASTE',status:'EXTRACTED',extracted_text:'Extracted text'};(service.fetchContentById as jest.Mock).mockResolvedValue(mockContent);const{result}=renderHook(()=>useContentReview('content-123'),{wrapper});await waitFor(()=>{expect(result.current.content).toEqual(mockContent);});});it('should track editing state',()=>{const{result}=renderHook(()=>useContentReview('content-123'),{wrapper});expect(result.current.isEditing).toBe(false);act(()=>{result.current.startEditing();});expect(result.current.isEditing).toBe(true);act(()=>{result.current.cancelEditing();});expect(result.current.isEditing).toBe(false);});it('should update edited text',()=>{const{result}=renderHook(()=>useContentReview('content-123'),{wrapper});act(()=>{result.current.startEditing();result.current.setEditedText('New text');});expect(result.current.editedText).toBe('New text');});it('should calculate canGenerate based on content status',async()=>{const mockContent={id:'content-123',source_type:'PASTE',status:'EXTRACTED',extracted_text:'Extracted text'};(service.fetchContentById as jest.Mock).mockResolvedValue(mockContent);const{result}=renderHook(()=>useContentReview('content-123'),{wrapper});await waitFor(()=>{expect(result.current.canGenerate).toBe(true);});});});describe('useStudyPlan',()=>{beforeEach(()=>{jest.clearAllMocks();});it('should fetch generation data',async()=>{const mockGeneration={id:'gen-123',study_plan:{summary:{title:'Test Plan',keyPoints:['Point 1']},tasks:[],quiz:[]}};(service.fetchGenerationById as jest.Mock).mockResolvedValue(mockGeneration);const{result}=renderHook(()=>useStudyPlan('gen-123'),{wrapper});await waitFor(()=>{expect(result.current.generation).toEqual(mockGeneration);});});it('should track completed tasks',async()=>{const mockGeneration={id:'gen-123',study_plan:{tasks:[{id:'task-1',content:'Task 1'},{id:'task-2',content:'Task 2'}]}};(service.fetchGenerationById as jest.Mock).mockResolvedValue(mockGeneration);const{result}=renderHook(()=>useStudyPlan('gen-123'),{wrapper});await waitFor(()=>{expect(result.current.completedTasks.size).toBe(0);});act(()=>{result.current.completeTask('task-1');});expect(result.current.completedTasks.has('task-1')).toBe(true);});it('should track quiz answers',async()=>{const mockGeneration={id:'gen-123',study_plan:{quiz:[{id:'quiz-1',question:'Q1'}]}};(service.fetchGenerationById as jest.Mock).mockResolvedValue(mockGeneration);const{result}=renderHook(()=>useStudyPlan('gen-123'),{wrapper});await waitFor(()=>{act(()=>{result.current.answerQuiz('quiz-1','Answer',true);});});expect(result.current.quizAnswers['quiz-1']).toEqual({answer:'Answer',isCorrect:true});});it('should calculate progress',async()=>{const mockGeneration={id:'gen-123',study_plan:{tasks:[{id:'task-1',content:'Task 1'},{id:'task-2',content:'Task 2'},{id:'task-3',content:'Task 3'}]}};(service.fetchGenerationById as jest.Mock).mockResolvedValue(mockGeneration);const{result}=renderHook(()=>useStudyPlan('gen-123'),{wrapper});await waitFor(()=>{act(()=>{result.current.completeTask('task-1');result.current.completeTask('task-2');});});expect(result.current.progress.taskProgress).toBe(2/3);});});describe('useContentHistory',()=>{beforeEach(()=>{jest.clearAllMocks();});it('should fetch content history',async()=>{const mockHistory=[{id:'content-1',source_type:'PASTE'},{id:'content-2',source_type:'YOUTUBE'}];(service.fetchContentHistory as jest.Mock).mockResolvedValue(mockHistory);const{result}=renderHook(()=>useContentHistory(),{wrapper});await waitFor(()=>{expect(result.current.content).toEqual(mockHistory);});});it('should refresh history',async()=>{const mockHistory=[{id:'content-1',source_type:'PASTE'}];(service.fetchContentHistory as jest.Mock).mockResolvedValue(mockHistory);const{result}=renderHook(()=>useContentHistory(),{wrapper});await waitFor(()=>{expect(result.current.content).toBeDefined();});await act(async()=>{await result.current.refresh();});expect(service.fetchContentHistory).toHaveBeenCalledTimes(2);});});describe('useRateLimit',()=>{beforeEach(()=>{jest.clearAllMocks();});it('should check rate limit',async()=>{const mockRateLimit={canGenerate:true,remaining:8,resetsAt:Date.now()+3600000};(service.checkRateLimit as jest.Mock).mockResolvedValue(mockRateLimit);const{result}=renderHook(()=>useRateLimit(),{wrapper});await waitFor(()=>{expect(result.current.rateLimit).toEqual(mockRateLimit);});expect(result.current.isChecking).toBe(false);expect(result.current.error).toBeNull();});it('should handle rate limit errors',async()=>{(service.checkRateLimit as jest.Mock).mockRejectedValue(new Error('Rate limit check failed'));const{result}=renderHook(()=>useRateLimit(),{wrapper});await waitFor(()=>{expect(result.current.error).toBeTruthy();});expect(result.current.isChecking).toBe(false);});});

import{captureSilentFailure}from'../../utils/silent-failure'; import type{PersistedDraft,PersistedStudySession,LocalCacheEntry,SyncQueueItem,StudyContent,StudyGeneration}from'./types'; import{CONTENT_STUDY_CONSTANTS}from'./types'; import{getDefaultStorageAdapter}from'../../persistence'; const getStorage = ()=>getDefaultStorageAdapter(); const STORAGE_KEYS = {DRAFTS:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:drafts`,SESSIONS:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:sessions`,CACHE:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:cache`,SYNC_QUEUE:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:sync-queue`,METRICS:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:metrics`,LAST_SYNC:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:last-sync`,OFFLINE_MODE:`${CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY}:offline-mode`}as const; export class DraftManager{private static instance:DraftManager; private memoryCache:Map<string,PersistedDraft> = new Map(); static getInstance():DraftManager{if(!DraftManager.instance){DraftManager.instance = new DraftManager();}return DraftManager.instance;}static resetForTests():void{DraftManager.instance = new DraftManager();}async saveDraft(draft:Omit<PersistedDraft,'id'|'createdAt'|'updatedAt'|'autoSaveVersion'>):Promise<PersistedDraft>{const now = Date.now(); const id = `draft-${now}-${Math.random().toString(36).substr(2,9)}`; const fullDraft:PersistedDraft = {...draft,id,createdAt:now,updatedAt:now,autoSaveVersion:1}; this.memoryCache.set(id,fullDraft); const existingDrafts = await this.getAllDrafts(); const updatedDrafts = [...existingDrafts.filter(d=>d.id !== id),fullDraft]; const validDrafts = this.filterExpiredDrafts(updatedDrafts); await getStorage().setItem(STORAGE_KEYS.DRAFTS,JSON.stringify(validDrafts)); return fullDraft;}async updateDraft(id:string,updates:Partial<Omit<PersistedDraft,'id'|'createdAt'>>):Promise<PersistedDraft|null>{const drafts = await this.getAllDrafts(); const draftIndex = drafts.findIndex(d=>d.id === id); if(draftIndex === -1){return null;}const updatedDraft:PersistedDraft = {...drafts[draftIndex],...updates,updatedAt:Date.now(),autoSaveVersion:drafts[draftIndex].autoSaveVersion + 1}; drafts[draftIndex] = updatedDraft; this.memoryCache.set(id,updatedDraft); await getStorage().setItem(STORAGE_KEYS.DRAFTS,JSON.stringify(drafts)); return updatedDraft;}async getDraft(id:string):Promise<PersistedDraft|null>{const cached = this.memoryCache.get(id); if(cached){return cached;}const drafts = await this.getAllDrafts(); return drafts.find(d=>d.id === id) || null;}async getAllDrafts():Promise<PersistedDraft[]>{try{const data = await getStorage().getItem(STORAGE_KEYS.DRAFTS); if(!data){return[];}const drafts:PersistedDraft[] = JSON.parse(data); return this.filterExpiredDrafts(drafts);}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'safe-fallback',type:'data'}); return[];}}async getDraftsByType(type:PersistedDraft['type']):Promise<PersistedDraft[]>{const drafts = await this.getAllDrafts(); return drafts.filter(d=>d.type === type);}async deleteDraft(id:string):Promise<boolean>{const drafts = await this.getAllDrafts(); const filtered = drafts.filter(d=>d.id !== id); if(filtered.length === drafts.length){return false;}this.memoryCache.delete(id); await getStorage().setItem(STORAGE_KEYS.DRAFTS,JSON.stringify(filtered)); return true;}async clearAllDrafts():Promise<void>{this.memoryCache.clear(); await getStorage().removeItem(STORAGE_KEYS.DRAFTS);}private filterExpiredDrafts(drafts:PersistedDraft[]):PersistedDraft[]{const now = Date.now(); const expiryMs = CONTENT_STUDY_CONSTANTS.DRAFT_EXPIRY_MS; return drafts.filter(draft=>now - draft.updatedAt < expiryMs);}async autoSave(type:PersistedDraft['type'],content:string,existingDraftId?:string):Promise<PersistedDraft>{if(existingDraftId){const updates = type === 'paste' ? {pastedText:content} : type === 'youtube' ? {youtubeUrl:content} : {}; const updated = await this.updateDraft(existingDraftId,updates); if(updated){return updated;}}const draftData = type === 'paste' ? {type,activeTab:type,pastedText:content,youtubeUrl:'',selectedFile:null} : type === 'youtube' ? {type,activeTab:type,pastedText:'',youtubeUrl:content,selectedFile:null} : {type,activeTab:type,pastedText:'',youtubeUrl:'',selectedFile:null}; return this.saveDraft(draftData as Omit<PersistedDraft,'id'|'createdAt'|'updatedAt'|'autoSaveVersion'>);}}export class StudySessionManager{private static instance:StudySessionManager; static getInstance():StudySessionManager{if(!StudySessionManager.instance){StudySessionManager.instance = new StudySessionManager();}return StudySessionManager.instance;}static resetForTests():void{StudySessionManager.instance = new StudySessionManager();}async saveSession(session:PersistedStudySession):Promise<void>{const sessions = await this.getAllSessions(); const existingIndex = sessions.findIndex(s=>s.generationId === session.generationId && !s.endTime); if(existingIndex >= 0){sessions[existingIndex] = session;}else{sessions.push(session);}await getStorage().setItem(STORAGE_KEYS.SESSIONS,JSON.stringify(sessions));}async getAllSessions():Promise<PersistedStudySession[]>{try{const data = await getStorage().getItem(STORAGE_KEYS.SESSIONS); return data ? JSON.parse(data) : [];}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'safe-fallback',type:'data'}); return[];}}async getActiveSession(generationId:string):Promise<PersistedStudySession|null>{const sessions = await this.getAllSessions(); return sessions.find(s=>s.generationId === generationId && !s.endTime) || null;}async getSessionsForGeneration(generationId:string):Promise<PersistedStudySession[]>{const sessions = await this.getAllSessions(); return sessions.filter(s=>s.generationId === generationId);}async completeSession(generationId:string,updates:Partial<PersistedStudySession>):Promise<boolean>{const sessions = await this.getAllSessions(); const index = sessions.findIndex(s=>s.generationId === generationId && !s.endTime); if(index === -1){return false;}sessions[index] = {...sessions[index],...updates,endTime:Date.now(),synced:false}; await getStorage().setItem(STORAGE_KEYS.SESSIONS,JSON.stringify(sessions)); return true;}async getUnsyncedSessions():Promise<PersistedStudySession[]>{const sessions = await this.getAllSessions(); return sessions.filter(s=>!s.synced);}async markSessionsSynced(ids:string[]):Promise<void>{const sessions = await this.getAllSessions(); sessions.forEach(s=>{if(ids.includes(s.generationId)){s.synced = true;}}); await getStorage().setItem(STORAGE_KEYS.SESSIONS,JSON.stringify(sessions));}async clearOldSessions(olderThanDays:number = 30):Promise<number>{const sessions = await this.getAllSessions(); const cutoff = Date.now() - olderThanDays * 24 * 60 * 60 * 1000; const filtered = sessions.filter(s=>s.startTime > cutoff || !s.endTime); const removed = sessions.length - filtered.length; if(removed > 0){await getStorage().setItem(STORAGE_KEYS.SESSIONS,JSON.stringify(filtered));}return removed;}}export class CacheManager{private static instance:CacheManager; private memoryCache:Map<string,LocalCacheEntry<unknown>> = new Map(); static getInstance():CacheManager{if(!CacheManager.instance){CacheManager.instance = new CacheManager();}return CacheManager.instance;}static resetForTests():void{CacheManager.instance = new CacheManager();}async get<T>(key:string):Promise<T|null>{const memoryEntry = this.memoryCache.get(key)as LocalCacheEntry<T>|undefined; if(memoryEntry && memoryEntry.expiresAt > Date.now()){return memoryEntry.data;}try{const data = await getStorage().getItem(`${STORAGE_KEYS.CACHE}:${key}`); if(!data){return null;}const entry:LocalCacheEntry<T> = JSON.parse(data); if(entry.expiresAt < Date.now()){await this.delete(key); return null;}this.memoryCache.set(key,entry); return entry.data;}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'safe-fallback',type:'data'}); return null;}}async set<T>(key:string,data:T,options:{ttlMs?:number;etag?:string;} = {}):Promise<void>{const now = Date.now(); const entry:LocalCacheEntry<T> = {data,cachedAt:now,expiresAt:now + (options.ttlMs || 5 * 60 * 1000),etag:options.etag,source:'localStorage'}; this.memoryCache.set(key,entry); await getStorage().setItem(`${STORAGE_KEYS.CACHE}:${key}`,JSON.stringify(entry));}async delete(key:string):Promise<void>{this.memoryCache.delete(key); await getStorage().removeItem(`${STORAGE_KEYS.CACHE}:${key}`);}async clear():Promise<void>{const storage = getStorage(); const allKeys = (await storage.getAllKeys?.()) || []; const cacheKeys = allKeys.filter(k=>k.startsWith(STORAGE_KEYS.CACHE)); for(const key of cacheKeys){await storage.removeItem(key);}this.memoryCache.clear();}async getContentWithCache(contentId:string):Promise<StudyContent|null>{const cacheKey = `content:${contentId}`; return this.get<StudyContent>(cacheKey);}async cacheContent(content:StudyContent,ttlMs?:number):Promise<void>{const cacheKey = `content:${content.id}`; await this.set(cacheKey,content,{ttlMs});}async getGenerationWithCache(generationId:string):Promise<StudyGeneration|null>{const cacheKey = `generation:${generationId}`; return this.get<StudyGeneration>(cacheKey);}async cacheGeneration(generation:StudyGeneration,ttlMs?:number):Promise<void>{const cacheKey = `generation:${generation.id}`; await this.set(cacheKey,generation,{ttlMs});}}export class SyncQueueManager{private static instance:SyncQueueManager; static getInstance():SyncQueueManager{if(!SyncQueueManager.instance){SyncQueueManager.instance = new SyncQueueManager();}return SyncQueueManager.instance;}static resetForTests():void{SyncQueueManager.instance = new SyncQueueManager();}async enqueue(item:Omit<SyncQueueItem,'id'|'createdAt'|'retryCount'>):Promise<SyncQueueItem>{const queue = await this.getQueue(); if(queue.length >= CONTENT_STUDY_CONSTANTS.OFFLINE_QUEUE_MAX_SIZE){const nonCriticalIndex = queue.findIndex(i=>i.entity !== 'content'); if(nonCriticalIndex >= 0){queue.splice(nonCriticalIndex,1);}else{throw new Error('Sync queue is full');}}const fullItem:SyncQueueItem = {...item,id:`sync-${Date.now()}-${Math.random().toString(36).substr(2,9)}`,createdAt:Date.now(),retryCount:0}; queue.push(fullItem); await this.saveQueue(queue); return fullItem;}async getQueue():Promise<SyncQueueItem[]>{try{const data = await getStorage().getItem(STORAGE_KEYS.SYNC_QUEUE); return data ? JSON.parse(data) : [];}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'safe-fallback',type:'data'}); return[];}}async saveQueue(queue:SyncQueueItem[]):Promise<void>{await getStorage().setItem(STORAGE_KEYS.SYNC_QUEUE,JSON.stringify(queue));}async dequeue(id:string):Promise<boolean>{const queue = await this.getQueue(); const filtered = queue.filter(item=>item.id !== id); if(filtered.length === queue.length){return false;}await this.saveQueue(filtered); return true;}async incrementRetry(id:string,error?:string):Promise<SyncQueueItem|null>{const queue = await this.getQueue(); const item = queue.find(i=>i.id === id); if(!item){return null;}item.retryCount++; item.lastAttempt = Date.now(); item.error = error; await this.saveQueue(queue); return item;}async clearCompleted():Promise<void>{const queue = await this.getQueue(); const pending = queue.filter(item=>item.retryCount < item.maxRetries && !item.error); await this.saveQueue(pending);}async getPendingItems():Promise<SyncQueueItem[]>{const queue = await this.getQueue(); return queue.filter(item=>item.retryCount < item.maxRetries);}async getFailedItems():Promise<SyncQueueItem[]>{const queue = await this.getQueue(); return queue.filter(item=>item.retryCount >= item.maxRetries || item.error);}}export class OfflineManager{private static instance:OfflineManager; private isOfflineMode = false; static getInstance():OfflineManager{if(!OfflineManager.instance){OfflineManager.instance = new OfflineManager();}return OfflineManager.instance;}static resetForTests():void{OfflineManager.instance = new OfflineManager();}async setOfflineMode(enabled:boolean):Promise<void>{this.isOfflineMode = enabled; await getStorage().setItem(STORAGE_KEYS.OFFLINE_MODE,JSON.stringify(enabled));}async isInOfflineMode():Promise<boolean>{try{const data = await getStorage().getItem(STORAGE_KEYS.OFFLINE_MODE); return data ? JSON.parse(data) : false;}catch(error){captureSilentFailure(error,{feature:'content-study',operation:'safe-fallback',type:'data'}); return false;}}getOfflineModeSync():boolean{return this.isOfflineMode;}async canPerformAction(action:'submit'|'generate'|'feedback'):Promise<boolean>{const offline = await this.isInOfflineMode(); if(!offline){return true;}return true;}}export async function getStorageUsage():Promise<{drafts:number;sessions:number;cache:number;syncQueue:number;total:number;}>{const storage = getStorage(); const keys = [STORAGE_KEYS.DRAFTS,STORAGE_KEYS.SESSIONS,STORAGE_KEYS.SYNC_QUEUE]; const allKeys = (await storage.getAllKeys?.()) || []; const cacheKeys = allKeys.filter(k=>k.startsWith(STORAGE_KEYS.CACHE)); const relevantKeys = [...keys,...cacheKeys]; const items = await Promise.all(relevantKeys.map(async k=>[k,await storage.getItem(k)])); const sizes = {drafts:0,sessions:0,cache:0,syncQueue:0,total:0}; items.forEach(([itemKey,value])=>{if(!value || !itemKey){return;}const size = new Blob([value]).size; if(itemKey === STORAGE_KEYS.DRAFTS){sizes.drafts = size;}else if(itemKey === STORAGE_KEYS.SESSIONS){sizes.sessions = size;}else if(itemKey === STORAGE_KEYS.SYNC_QUEUE){sizes.syncQueue = size;}else if(itemKey.startsWith(STORAGE_KEYS.CACHE)){sizes.cache += size;}sizes.total += size;}); return sizes;}export async function clearAllContentStudyData():Promise<void>{const storage = getStorage(); const allKeys = (await storage.getAllKeys?.()) || []; const contentStudyKeys = allKeys.filter(k=>k.startsWith(CONTENT_STUDY_CONSTANTS.LOCAL_STORAGE_KEY)); for(const key of contentStudyKeys){await storage.removeItem(key);}}export const draftManager = DraftManager.getInstance(); export const studySessionManager = StudySessionManager.getInstance(); export const cacheManager = CacheManager.getInstance(); export const syncQueueManager = SyncQueueManager.getInstance(); export const offlineManager = OfflineManager.getInstance();

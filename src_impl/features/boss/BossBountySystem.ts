import{captureSilentFailure}from'../../utils/silent-failure';import{z}from'zod';import*as Sentry from'@sentry/react-native';import{spendCurrency}from'../economy/spending-service';import{eventBus}from'../../events';import{featureFlags}from'../../feature-flags/FeatureFlagEngine';export const BOUNTY_COST_COINS=50;export const BOUNTY_LOOT_MULTIPLIER=2;export const MAX_BOUNTY_STACK=4;export const BOUNTY_DURATION_MS=24*60*60*1000;export const BossBountySchema=z.object({id:z.string().uuid(),encounterId:z.string().uuid(),userId:z.string().uuid(),squadId:z.string().uuid().nullable(),costCoins:z.number().int().positive(),createdAt:z.number(),expiresAt:z.number(),consumed:z.boolean().default(false),consumedAt:z.number().nullable(),damageDealtWhenConsumed:z.number().int().nullable()}).strict();export const PlaceBountyInputSchema=z.object({userId:z.string().uuid(),encounterId:z.string().uuid(),squadId:z.string().uuid().optional()}).strict();export const ConsumeBountyInputSchema=z.object({bountyId:z.string().uuid(),encounterId:z.string().uuid(),damageDealt:z.number().int().min(0)}).strict();export const GetBountyStatusInputSchema=z.object({encounterId:z.string().uuid(),userId:z.string().uuid()}).strict();export type BossBounty=z.infer<typeof BossBountySchema>;export type PlaceBountyInput=z.infer<typeof PlaceBountyInputSchema>;export type ConsumeBountyInput=z.infer<typeof ConsumeBountyInputSchema>;export type GetBountyStatusInput=z.infer<typeof GetBountyStatusInputSchema>;export interface BountyStatus{hasActiveBounty:boolean;bountyCount:number;totalLootMultiplier:number;expiresAt:number|null;hoursRemaining:number;canPlaceBounty:boolean;maxStackReached:boolean;}export interface PlaceBountyResult{success:boolean;bountyId:string|null;stackCount:number;lootMultiplier:number;error:{code:string;message:string;}|null;}function isBossBountyEnabled():boolean{return featureFlags.isEnabled('boss_bounty_system');}const BOUNTY_STORAGE_KEY=(encounterId:string)=>`boss_bounties:${encounterId}`;function getStorage(){try{const{storage}=require('../../store/mmkv-storage');return storage;}catch(error){captureSilentFailure(error,{feature:'boss',operation:'ui-fallback',type:'ui'});return null;}}function saveBounties(encounterId:string,bounties:BossBounty[]):void{const storage=getStorage();if(!storage){return;}try{storage.set(BOUNTY_STORAGE_KEY(encounterId),JSON.stringify(bounties));}catch(error){Sentry.captureException(error,{tags:{feature:'boss-bounty',operation:'save'},extra:{encounterId}});}}function loadBounties(encounterId:string):BossBounty[]{const storage=getStorage();if(!storage){return[];}try{const data=storage.getString(BOUNTY_STORAGE_KEY(encounterId));if(!data){return[];}const parsed=JSON.parse(data);return z.array(BossBountySchema).parse(parsed);}catch(error){Sentry.captureException(error,{tags:{feature:'boss-bounty',operation:'load'},extra:{encounterId}});return[];}}function clearBounties(encounterId:string):void{const storage=getStorage();if(!storage){return;}try{storage.delete(BOUNTY_STORAGE_KEY(encounterId));}catch(error){Sentry.captureException(error,{tags:{feature:'boss-bounty',operation:'clear'},extra:{encounterId}});}}export function getBountyStatus(input:GetBountyStatusInput):BountyStatus{const validated=GetBountyStatusInputSchema.parse(input);if(!isBossBountyEnabled()){return{hasActiveBounty:false,bountyCount:0,totalLootMultiplier:1,expiresAt:null,hoursRemaining:0,canPlaceBounty:false,maxStackReached:false};}const now=Date.now();const allBounties=loadBounties(validated.encounterId);const activeBounties=allBounties.filter(b=>!b.consumed&&b.expiresAt>now);const bountyCount=activeBounties.length;const hasActiveBounty=bountyCount>0;const maxStackReached=bountyCount>=MAX_BOUNTY_STACK;const totalLootMultiplier=Math.min(bountyCount*BOUNTY_LOOT_MULTIPLIER,MAX_BOUNTY_STACK*BOUNTY_LOOT_MULTIPLIER);const earliestExpiry=hasActiveBounty?Math.min(...activeBounties.map(b=>b.expiresAt)):null;const msRemaining=earliestExpiry?earliestExpiry-now:0;const hoursRemaining=Math.max(0,Math.floor(msRemaining/(60*60*1000)));const userHasBounty=activeBounties.some(b=>b.userId===validated.userId);return{hasActiveBounty,bountyCount,totalLootMultiplier,expiresAt:earliestExpiry,hoursRemaining,canPlaceBounty:!userHasBounty&&!maxStackReached,maxStackReached};}export async function placeBounty(input:PlaceBountyInput):Promise<PlaceBountyResult>{const validated=PlaceBountyInputSchema.parse(input);if(!isBossBountyEnabled()){return{success:false,bountyId:null,stackCount:0,lootMultiplier:1,error:{code:'FEATURE_DISABLED',message:'Boss bounties are disabled'}};}const now=Date.now();const status=getBountyStatus({encounterId:validated.encounterId,userId:validated.userId});if(status.maxStackReached){return{success:false,bountyId:null,stackCount:status.bountyCount,lootMultiplier:status.totalLootMultiplier,error:{code:'MAX_STACK_REACHED',message:'Maximum bounty stack reached (4 bounties)'}};}if(!status.canPlaceBounty){return{success:false,bountyId:null,stackCount:status.bountyCount,lootMultiplier:status.totalLootMultiplier,error:{code:'ALREADY_ACTIVE',message:'You already have an active bounty on this boss'}};}const spendResult=await spendCurrency({userId:validated.userId,currency:'COINS',amount:BOUNTY_COST_COINS,sink:'BOSS_BOUNTY',description:'Boss Bounty placed',metadata:{encounterId:validated.encounterId,stackPosition:status.bountyCount+1}});if(!spendResult.success){return{success:false,bountyId:null,stackCount:status.bountyCount,lootMultiplier:status.totalLootMultiplier,error:{code:spendResult.error?.code||'BOUNTY_FAILED',message:spendResult.error?.message||'Failed to place bounty'}};}const bounty:BossBounty={id:crypto.randomUUID(),encounterId:validated.encounterId,userId:validated.userId,squadId:validated.squadId||null,costCoins:BOUNTY_COST_COINS,createdAt:now,expiresAt:now+BOUNTY_DURATION_MS,consumed:false,consumedAt:null,damageDealtWhenConsumed:null};const existingBounties=loadBounties(validated.encounterId);const updatedBounties=[...existingBounties,bounty];saveBounties(validated.encounterId,updatedBounties);const newStackCount=updatedBounties.filter(b=>!b.consumed&&b.expiresAt>now).length;const newLootMultiplier=Math.min(newStackCount*BOUNTY_LOOT_MULTIPLIER,MAX_BOUNTY_STACK*BOUNTY_LOOT_MULTIPLIER);eventBus.publish('economy:boss_bounty_placed',{userId:validated.userId,encounterId:validated.encounterId,costCoins:BOUNTY_COST_COINS,stackCount:newStackCount,lootMultiplier:newLootMultiplier});Sentry.addBreadcrumb({message:'Boss bounty placed',category:'economy',data:{userId:validated.userId,encounterId:validated.encounterId,costCoins:BOUNTY_COST_COINS,stackCount:newStackCount}});return{success:true,bountyId:bounty.id,stackCount:newStackCount,lootMultiplier:newLootMultiplier,error:null};}export function consumeBountiesOnDamage(encounterId:string,damageDealt:number):{lootMultiplier:number;consumedCount:number;consumedBountyIds:string[];}{if(!isBossBountyEnabled()){return{lootMultiplier:1,consumedCount:0,consumedBountyIds:[]};}const now=Date.now();const allBounties=loadBounties(encounterId);const activeBounties=allBounties.filter(b=>!b.consumed&&b.expiresAt>now);if(activeBounties.length===0){return{lootMultiplier:1,consumedCount:0,consumedBountyIds:[]};}const consumedBountyIds:string[]=[];const updatedBounties=allBounties.map(bounty=>{if(!bounty.consumed&&bounty.expiresAt>now){consumedBountyIds.push(bounty.id);return{...bounty,consumed:true,consumedAt:now,damageDealtWhenConsumed:damageDealt};}return bounty;});saveBounties(encounterId,updatedBounties);const consumedCount=activeBounties.length;const lootMultiplier=Math.min(consumedCount*BOUNTY_LOOT_MULTIPLIER,MAX_BOUNTY_STACK*BOUNTY_LOOT_MULTIPLIER);const cleanupTimer=setTimeout(()=>clearBounties(encounterId),60*60*1000);if(typeof cleanupTimer==='object'&&'unref'in cleanupTimer){cleanupTimer.unref();}return{lootMultiplier,consumedCount,consumedBountyIds};}export function getActiveBounties(encounterId:string):BossBounty[]{if(!isBossBountyEnabled()){return[];}const now=Date.now();const allBounties=loadBounties(encounterId);return allBounties.filter(b=>!b.consumed&&b.expiresAt>now);}export function userHasActiveBounty(encounterId:string,userId:string):boolean{if(!isBossBountyEnabled()){return false;}const now=Date.now();const allBounties=loadBounties(encounterId);return allBounties.some(b=>b.userId===userId&&!b.consumed&&b.expiresAt>now);}export function cleanupExpiredBounties(encounterId:string):void{const now=Date.now();const allBounties=loadBounties(encounterId);const validBounties=allBounties.filter(b=>b.expiresAt>now||!b.consumed);if(validBounties.length!==allBounties.length){saveBounties(encounterId,validBounties);}}export function getBountyDisplayInfo(encounterId:string):{hasBounty:boolean;badgeText:string;multiplier:string;expiresIn:string;}{const status=getBountyStatus({encounterId,userId:'00000000-0000-0000-0000-000000000000'});if(!status.hasActiveBounty){return{hasBounty:false,badgeText:'',multiplier:'',expiresIn:''};}return{hasBounty:true,badgeText:`🎯 ${status.bountyCount} Bounty${status.bountyCount>1?'ies':''}`,multiplier:`${status.totalLootMultiplier}×`,expiresIn:`${status.hoursRemaining}h left`};}

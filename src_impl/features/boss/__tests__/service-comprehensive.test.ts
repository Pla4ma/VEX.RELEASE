import{describe,it,expect,vi,beforeEach}from'vitest'; import{calculateScaledHealth,calculateDamage,canUserFightBoss,createEncounter,applyDamage,getAvailableBosses}from'../service'; import*as repository from'../repository'; import{eventBus}from'../../../events'; vi.mock('../repository'); vi.mock('../../../events',()=>({eventBus:{publish:vi.fn()}})); describe('Boss Service - Comprehensive',()=>{beforeEach(()=>{vi.clearAllMocks();}); describe('calculateScaledHealth',()=>{it('should return base health for level 1, solo',()=>{const result = calculateScaledHealth(1000,0.1,1,1); expect(result).toBe(1000);}); it('should scale health with user level',()=>{const base = calculateScaledHealth(1000,0.1,1,1); const scaled = calculateScaledHealth(1000,0.1,10,1); expect(scaled).toBeGreaterThan(base);}); it('should scale health with squad size',()=>{const solo = calculateScaledHealth(1000,0.1,5,1); const squad = calculateScaledHealth(1000,0.1,5,3); expect(squad).toBeGreaterThan(solo);}); it('should calculate correct scaling formula',()=>{const result = calculateScaledHealth(1000,0.1,10,3); expect(result).toBe(2660);}); it('should handle max level scaling',()=>{const result = calculateScaledHealth(1000,0.1,100,1); expect(result).toBeGreaterThan(10000);});}); describe('calculateDamage',()=>{it('should calculate base damage from duration',()=>{const result = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); expect(result).toBeGreaterThanOrEqual(1);}); it('should apply quality multiplier',()=>{const lowQuality = calculateDamage({sessionDuration:60,sessionQuality:0,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); const highQuality = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); expect(highQuality).toBeGreaterThan(lowQuality);}); it('should apply streak bonus for 7+ days',()=>{const noStreak = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:5,squadMultiplier:1,equippedItemIds:[]}); const weekStreak = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:7,squadMultiplier:1,equippedItemIds:[]}); expect(weekStreak).toBeGreaterThan(noStreak);}); it('should apply squad multiplier',()=>{const solo = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); const squad = calculateDamage({sessionDuration:60,sessionQuality:100,streakDays:0,squadMultiplier:1.5,equippedItemIds:[]}); expect(squad).toBeGreaterThan(solo);}); it('should scale with session duration',()=>{const short = calculateDamage({sessionDuration:30,sessionQuality:100,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); const long = calculateDamage({sessionDuration:120,sessionQuality:100,streakDays:0,squadMultiplier:1,equippedItemIds:[]}); expect(long).toBeGreaterThan(short);});}); describe('canUserFightBoss',()=>{it('should allow level 3 user to fight first boss',async()=>{const mockTemplate = {id:'boss-1',name:'Test Boss',unlockLevel:3,baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); vi.mocked(repository.hasDefeatedBoss).mockResolvedValue(false); const result = await canUserFightBoss('user-1','boss-1',3); expect(result.allowed).toBe(true);}); it('should block low level user',async()=>{const mockTemplate = {id:'boss-1',name:'Test Boss',unlockLevel:10,baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); const result = await canUserFightBoss('user-1','boss-1',5); expect(result.allowed).toBe(false); expect(result.reason).toContain('level');}); it('should check cooldown',async()=>{const mockTemplate = {id:'boss-1',name:'Test Boss',unlockLevel:1,baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); vi.mocked(repository.hasDefeatedBoss).mockResolvedValue(true); vi.mocked(repository.isBossOnCooldown).mockResolvedValue(true); const result = await canUserFightBoss('user-1','boss-1',10); expect(result.allowed).toBe(false); expect(result.reason).toContain('cooldown');}); it('should require previous boss defeat',async()=>{const mockTemplate = {id:'boss-2',name:'Boss 2',unlockLevel:1,requiresBossId:'boss-1',baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); vi.mocked(repository.hasDefeatedBoss).mockImplementation(async(_,bossId)=>bossId !== 'boss-1'); const result = await canUserFightBoss('user-1','boss-2',10); expect(result.allowed).toBe(false); expect(result.reason).toContain('defeat');});}); describe('createEncounter',()=>{it('should create solo encounter',async()=>{const mockTemplate = {id:'boss-1',name:'Test Boss',baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); vi.mocked(repository.fetchActiveEncounter).mockResolvedValue(null); vi.mocked(repository.createEncounter).mockResolvedValue({id:'encounter-1',userId:'user-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:1000,maxHealth:1000,damageDealt:0,createdAt:Date.now(),expiresAt:Date.now() + 24 * 60 * 60 * 1000}); const result = await createEncounter({userId:'user-1',bossId:'boss-1',userLevel:5}); expect(result.encounterId).toBe('encounter-1'); expect(eventBus.publish).toHaveBeenCalledWith('boss:encounter_created',expect.any(Object));}); it('should create squad encounter with scaled health',async()=>{const mockTemplate = {id:'boss-1',name:'Test Boss',baseHealth:1000,rewardType:'COINS',rewardAmount:100}; vi.mocked(repository.fetchBossTemplate).mockResolvedValue(mockTemplate); vi.mocked(repository.fetchActiveEncounter).mockResolvedValue(null); vi.mocked(repository.createEncounter).mockImplementation(async data=>({id:'encounter-1',...data,status:'ACTIVE',damageDealt:0,createdAt:Date.now()})); const result = await createEncounter({userId:'user-1',bossId:'boss-1',userLevel:10,squadId:'squad-1',squadSize:3}); expect(result.scaledHealth).toBeGreaterThan(1000);}); it('should reject if existing active encounter',async()=>{vi.mocked(repository.fetchActiveEncounter).mockResolvedValue({id:'existing-encounter',status:'ACTIVE'}as any); await expect(createEncounter({userId:'user-1',bossId:'boss-1',userLevel:5})).rejects.toThrow('existing');});}); describe('applyDamage',()=>{it('should apply damage and reduce health',async()=>{vi.mocked(repository.fetchActiveEncounter).mockResolvedValue({id:'encounter-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:500,maxHealth:1000,damageDealt:500,expiresAt:Date.now() + 60 * 60 * 1000}as any); vi.mocked(repository.updateEncounterHealth).mockResolvedValue({healthRemaining:400,damageDealt:600}as any); const result = await applyDamage({encounterId:'encounter-1',damage:100,sessionId:'session-1'}); expect(result.damageDealt).toBe(100); expect(result.healthRemaining).toBe(400); expect(result.isDefeated).toBe(false);}); it('should detect boss defeat',async()=>{vi.mocked(repository.fetchActiveEncounter).mockResolvedValue({id:'encounter-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:100,maxHealth:1000,damageDealt:900,expiresAt:Date.now() + 60 * 60 * 1000}as any); vi.mocked(repository.updateEncounterHealth).mockResolvedValue({healthRemaining:0,damageDealt:1000}as any); vi.mocked(repository.markEncounterDefeated).mockResolvedValue({}as any); vi.mocked(repository.fetchBossTemplate).mockResolvedValue({rewardType:'COINS',rewardAmount:100}as any); const result = await applyDamage({encounterId:'encounter-1',damage:150,sessionId:'session-1'}); expect(result.isDefeated).toBe(true); expect(eventBus.publish).toHaveBeenCalledWith('boss:defeated',expect.any(Object));}); it('should handle encounter timeout',async()=>{vi.mocked(repository.fetchActiveEncounter).mockResolvedValue({id:'encounter-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:500,maxHealth:1000,expiresAt:Date.now() - 1000}as any); vi.mocked(repository.markEncounterTimeout).mockResolvedValue({}as any); await expect(applyDamage({encounterId:'encounter-1',damage:100,sessionId:'session-1'})).rejects.toThrow('timeout');}); it('should calculate percent complete',async()=>{vi.mocked(repository.fetchActiveEncounter).mockResolvedValue({id:'encounter-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:600,maxHealth:1000,damageDealt:400,expiresAt:Date.now() + 60 * 60 * 1000}as any); vi.mocked(repository.updateEncounterHealth).mockResolvedValue({healthRemaining:500,damageDealt:500}as any); const result = await applyDamage({encounterId:'encounter-1',damage:100,sessionId:'session-1'}); expect(result.percentComplete).toBe(50);});}); describe('getAvailableBosses',()=>{it('should list all bosses with unlock status',async()=>{const mockTemplates = [{id:'boss-1',name:'Boss 1',unlockLevel:1,baseHealth:1000,rewardType:'COINS',rewardAmount:100},{id:'boss-2',name:'Boss 2',unlockLevel:5,baseHealth:2000,rewardType:'GEMS',rewardAmount:10},{id:'boss-3',name:'Boss 3',unlockLevel:10,baseHealth:5000,rewardType:'ITEM',rewardAmount:1}]; vi.mocked(repository.fetchBossTemplates).mockResolvedValue(mockTemplates); vi.mocked(repository.hasDefeatedBoss).mockImplementation(async(_,bossId)=>bossId === 'boss-1'); const results = await getAvailableBosses('user-1',7); expect(results).toHaveLength(3); expect(results[0].unlocked).toBe(true); expect(results[1].unlocked).toBe(true); expect(results[2].unlocked).toBe(false); expect(results[0].defeated).toBe(true); expect(results[1].defeated).toBe(false);}); it('should show locked reason for locked bosses',async()=>{const mockTemplates = [{id:'boss-2',name:'Boss 2',unlockLevel:10,baseHealth:2000,rewardType:'COINS',rewardAmount:100}]; vi.mocked(repository.fetchBossTemplates).mockResolvedValue(mockTemplates); const results = await getAvailableBosses('user-1',5); expect(results[0].unlocked).toBe(false); expect(results[0].lockedReason).toContain('level 10');});}); describe('Concurrency',()=>{it('should handle concurrent damage applications',async()=>{let currentHealth = 500; vi.mocked(repository.fetchActiveEncounter).mockImplementation(async()=>({id:'encounter-1',bossId:'boss-1',status:'ACTIVE',healthRemaining:currentHealth,maxHealth:1000,damageDealt:500,expiresAt:Date.now() + 60 * 60 * 1000})as any); vi.mocked(repository.updateEncounterHealth).mockImplementation(async(_,health,damage)=>{currentHealth = health; return{healthRemaining:currentHealth,damageDealt:1000 - currentHealth}as any;}); const results = await Promise.all([applyDamage({encounterId:'encounter-1',damage:50,sessionId:'session-1'}),applyDamage({encounterId:'encounter-1',damage:50,sessionId:'session-2'}),applyDamage({encounterId:'encounter-1',damage:50,sessionId:'session-3'})]); expect(results).toHaveLength(3);});});});

import{z}from'zod';import{featureFlags}from'../../feature-flags/FeatureFlagEngine';import{eventBus}from'../../events';export const ADAPTIVE_DIFFICULTY_CONFIG={DIFFICULTY_RANGES:{BEGINNER:{min:0.0,max:0.3,description:'Learning phase'},NOVICE:{min:0.3,max:0.5,description:'Building confidence'},INTERMEDIATE:{min:0.5,max:0.7,description:'Balanced challenge'},ADVANCED:{min:0.7,max:0.85,description:'Skilled players'},EXPERT:{min:0.85,max:1.0,description:'Maximum challenge'}},PERFORMANCE_WEIGHTS:{recentPurity:0.3,completionRate:0.25,streakStrength:0.2,sessionConsistency:0.15,squadPerformance:0.1},ADJUSTMENT_SENSITIVITY:0.1,MIN_SESSIONS_FOR_ANALYSIS:5,DIFFICULTY_SMOOTHING:0.7,SQUAD_BALANCING:{enabled:true,targetWinRate:0.6,maxDifficultySpread:0.2,underdogBonus:0.1},PREDICTIVE_ADJUSTMENTS:{enabled:true,burnoutProtection:true,streakProtection:true,learningCurve:true}}as const;export const DifficultyLevelSchema=z.enum(['BEGINNER','NOVICE','INTERMEDIATE','ADVANCED','EXPERT']);export const DifficultyFactorSchema=z.object({type:z.enum(['boss_health','attack_frequency','mechanic_complexity','time_pressure','purity_threshold','damage_output']),baseValue:z.number(),currentValue:z.number(),min:z.number(),max:z.number(),weight:z.number()});export const UserProfileSchema=z.object({userId:z.string(),profileId:z.string(),recentPurity:z.number(),completionRate:z.number(),streakStrength:z.number(),sessionConsistency:z.number(),squadPerformance:z.number(),currentDifficulty:z.number(),difficultyHistory:z.array(z.object({timestamp:z.number(),difficulty:z.number(),outcome:z.enum(['victory','defeat','abandoned']),performance:z.number()})),learningRate:z.number(),adaptationSpeed:z.number(),preferredDifficulty:z.number(),burnoutRisk:z.number(),frustrationLevel:z.number(),engagementTrend:z.enum(['increasing','stable','decreasing']),updatedAt:z.number()});export const AdaptiveBossEncounterSchema=z.object({id:z.string(),bossId:z.string(),userId:z.string(),squadId:z.string().optional(),baseDifficulty:z.number(),currentDifficulty:z.number(),targetDifficulty:z.number(),factors:z.array(DifficultyFactorSchema),realTimeAdjustments:z.array(z.object({timestamp:z.number(),trigger:z.string(),oldDifficulty:z.number(),newDifficulty:z.number(),reason:z.string()})),currentPerformance:z.number(),predictedOutcome:z.enum(['victory','defeat','uncertain']).nullable(),squadMembers:z.array(z.object({userId:z.string(),difficulty:z.number(),role:z.enum(['leader','member','support'])})).optional(),createdAt:z.number(),updatedAt:z.number()});export type DifficultyLevel=z.infer<typeof DifficultyLevelSchema>;export type DifficultyFactor=z.infer<typeof DifficultyFactorSchema>;export type UserProfile=z.infer<typeof UserProfileSchema>;export type AdaptiveBossEncounter=z.infer<typeof AdaptiveBossEncounterSchema>;export interface DifficultyAdjustment{encounterId:string;timestamp:number;trigger:'performance_change'|'squad_balance'|'predictive'|'real_time';adjustment:number;reason:string;factors:string[];}export interface PerformanceMetrics{sessionPurity:number;completionRate:number;averageSessionTime:number;streakLength:number;recentBossResults:Array<{bossId:string;outcome:'victory'|'defeat';performance:number;difficulty:number;}>;}export class AdaptiveDifficultyService{private userProfiles:Map<string,UserProfile>=new Map();private activeEncounters:Map<string,AdaptiveBossEncounter>=new Map();private difficultyFactors:Map<string,DifficultyFactor[]>=new Map();private adjustmentHistory:Map<string,DifficultyAdjustment[]>=new Map();static isEnabled():boolean{return featureFlags.isEnabled('adaptive_difficulty');}async initializeUserProfile(userId:string,initialMetrics?:Partial<PerformanceMetrics>):Promise<UserProfile>{const existingProfile=this.userProfiles.get(userId);if(existingProfile){return existingProfile;}const now=Date.now();const profile:UserProfile={userId,profileId:`profile-${userId}`,recentPurity:initialMetrics?.sessionPurity||0.7,completionRate:initialMetrics?.completionRate||0.5,streakStrength:this.calculateStreakStrength(initialMetrics?.streakLength||0),sessionConsistency:0.7,squadPerformance:0.5,currentDifficulty:0.5,difficultyHistory:[],learningRate:0.1,adaptationSpeed:0.05,preferredDifficulty:0.5,burnoutRisk:0.1,frustrationLevel:0.1,engagementTrend:'stable',updatedAt:now};this.userProfiles.set(userId,profile);eventBus.publish('adaptive_difficulty:profile_initialized',{userId,profileId:profile.profileId,timestamp:now});return profile;}async createAdaptiveEncounter(input:{bossId:string;userId:string;squadId?:string;baseDifficulty?:number;}):Promise<AdaptiveBossEncounter>{const profile=await this.initializeUserProfile(input.userId);const now=Date.now();const encounterId=`adaptive_${input.bossId}_${input.userId}_${now}`;const targetDifficulty=await this.calculateOptimalDifficulty(input.userId,input.squadId);const factors=this.initializeDifficultyFactors(targetDifficulty);const encounter:AdaptiveBossEncounter={id:encounterId,bossId:input.bossId,userId:input.userId,squadId:input.squadId,baseDifficulty:input.baseDifficulty||0.5,currentDifficulty:targetDifficulty,targetDifficulty,factors,realTimeAdjustments:[],currentPerformance:0.5,predictedOutcome:null,createdAt:now,updatedAt:now};if(input.squadId){encounter.squadMembers=await this.getSquadDifficultyContext(input.squadId,input.userId);}this.activeEncounters.set(encounterId,encounter);this.adjustmentHistory.set(encounterId,[]);eventBus.publish('adaptive_difficulty:encounter_created',{userId:input.userId,encounterId,bossId:input.bossId,difficulty:targetDifficulty,timestamp:Date.now()});return encounter;}async updateEncounterPerformance(encounterId:string,performance:number,context?:{sessionTime:number;purity:number;mistakes:number;progress:number;}):Promise<{adjusted:boolean;newDifficulty?:number;adjustment?:DifficultyAdjustment;}>{const encounter=this.activeEncounters.get(encounterId);if(!encounter){return{adjusted:false};}const oldDifficulty=encounter.currentDifficulty;encounter.currentPerformance=performance;encounter.updatedAt=Date.now();const adjustmentNeeded=this.shouldAdjustDifficulty(encounter,performance,context);if(adjustmentNeeded.shouldAdjust){const newDifficulty=this.calculateRealTimeAdjustment(encounter,performance,adjustmentNeeded.reason);const smoothedDifficulty=this.smoothDifficultyTransition(oldDifficulty,newDifficulty,ADAPTIVE_DIFFICULTY_CONFIG.DIFFICULTY_SMOOTHING);encounter.currentDifficulty=smoothedDifficulty;const adjustment:DifficultyAdjustment={encounterId,timestamp:Date.now(),trigger:'real_time',adjustment:smoothedDifficulty-oldDifficulty,reason:adjustmentNeeded.reason,factors:adjustmentNeeded.factors};const history=this.adjustmentHistory.get(encounterId)||[];history.push(adjustment);this.adjustmentHistory.set(encounterId,history);encounter.realTimeAdjustments.push({timestamp:adjustment.timestamp,trigger:adjustment.trigger,oldDifficulty,newDifficulty:smoothedDifficulty,reason:adjustment.reason});this.updateEncounterFactors(encounter,smoothedDifficulty);eventBus.publish('adaptive_difficulty:real_time_adjustment',{userId:encounter.userId,encounterId,adjustment:smoothedDifficulty-oldDifficulty,reason:adjustment.reason,timestamp:Date.now()});this.activeEncounters.set(encounterId,encounter);return{adjusted:true,newDifficulty:smoothedDifficulty,adjustment};}return{adjusted:false};}async completeEncounter(encounterId:string,outcome:'victory'|'defeat'|'abandoned',finalPerformance:number):Promise<void>{const encounter=this.activeEncounters.get(encounterId);if(!encounter){return;}const profile=this.userProfiles.get(encounter.userId);if(!profile){return;}profile.difficultyHistory.push({timestamp:Date.now(),difficulty:encounter.currentDifficulty,outcome,performance:finalPerformance});if(profile.difficultyHistory.length>20){profile.difficultyHistory=profile.difficultyHistory.slice(-20);}this.updateLearningPatterns(profile,encounter,outcome,finalPerformance);profile.preferredDifficulty=this.calculatePreferredDifficulty(profile);this.updatePredictiveFactors(profile,encounter,outcome);profile.updatedAt=Date.now();this.userProfiles.set(encounter.userId,profile);this.activeEncounters.delete(encounterId);eventBus.publish('adaptive_difficulty:encounter_completed',{userId:encounter.userId,encounterId,bossId:encounter.bossId,finalDifficulty:encounter.currentDifficulty,timestamp:Date.now()});}async getDifficultyRecommendation(userId:string,bossId?:string,squadId?:string):Promise<{recommendedDifficulty:number;difficultyLevel:DifficultyLevel;confidence:number;factors:Array<{factor:string;influence:number;value:number;}>;reasoning:string[];}>{const profile=this.userProfiles.get(userId);if(!profile){await this.initializeUserProfile(userId);return this.getDifficultyRecommendation(userId,bossId,squadId);}const optimalDifficulty=await this.calculateOptimalDifficulty(userId,squadId);const difficultyLevel=this.getDifficultyLevel(optimalDifficulty);const factors=this.analyzeDifficultyFactors(profile);const reasoning=this.generateDifficultyReasoning(profile,factors,optimalDifficulty);const confidence=this.calculateRecommendationConfidence(profile);return{recommendedDifficulty:optimalDifficulty,difficultyLevel,confidence,factors,reasoning};}private async calculateOptimalDifficulty(userId:string,squadId?:string):Promise<number>{const profile=this.userProfiles.get(userId);if(!profile){return 0.5;}let difficulty=0;difficulty+=profile.recentPurity*ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.recentPurity;difficulty+=profile.completionRate*ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.completionRate;difficulty+=profile.streakStrength*ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.streakStrength;difficulty+=profile.sessionConsistency*ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.sessionConsistency;if(squadId){difficulty+=profile.squadPerformance*ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.squadPerformance;}if(ADAPTIVE_DIFFICULTY_CONFIG.PREDICTIVE_ADJUSTMENTS.burnoutProtection&&profile.burnoutRisk>0.7){difficulty*=0.8;}if(ADAPTIVE_DIFFICULTY_CONFIG.PREDICTIVE_ADJUSTMENTS.streakProtection&&profile.streakStrength>0.9){difficulty*=1.1;}if(squadId&&ADAPTIVE_DIFFICULTY_CONFIG.SQUAD_BALANCING.enabled){difficulty=await this.applySquadBalancing(userId,squadId,difficulty);}return Math.max(0.0,Math.min(1.0,difficulty));}private initializeDifficultyFactors(baseDifficulty:number):DifficultyFactor[]{return[{type:'boss_health',baseValue:1000,currentValue:1000*(1+baseDifficulty),min:500,max:2000,weight:0.3},{type:'attack_frequency',baseValue:1.0,currentValue:1.0+baseDifficulty*0.5,min:0.5,max:2.0,weight:0.2},{type:'mechanic_complexity',baseValue:1,currentValue:Math.ceil(1+baseDifficulty*2),min:1,max:3,weight:0.25},{type:'time_pressure',baseValue:300,currentValue:300*(1-baseDifficulty*0.3),min:180,max:420,weight:0.15},{type:'purity_threshold',baseValue:70,currentValue:70+baseDifficulty*20,min:60,max:90,weight:0.1}];}private shouldAdjustDifficulty(encounter:AdaptiveBossEncounter,performance:number,context?:DynamicValue):{shouldAdjust:boolean;reason:string;factors:string[];}{const factors:string[]=[];if(performance<0.3){factors.push('low_performance');return{shouldAdjust:true,reason:'User struggling - reducing difficulty',factors};}if(performance>0.85){factors.push('high_performance');return{shouldAdjust:true,reason:'User finding it too easy - increasing difficulty',factors};}if(context&&context.mistakes>5&&context.purity<60){factors.push('frustration_detected');return{shouldAdjust:true,reason:'Frustration detected - reducing difficulty',factors};}if(context&&context.progress<0.2&&context.sessionTime<10){factors.push('engagement_drop');return{shouldAdjust:true,reason:'Engagement dropping - adjusting difficulty',factors};}return{shouldAdjust:false,reason:'',factors:[]};}private calculateRealTimeAdjustment(encounter:AdaptiveBossEncounter,performance:number,reason:string):number{let adjustment=0;switch(reason){case'User struggling - reducing difficulty':adjustment=-ADAPTIVE_DIFFICULTY_CONFIG.ADJUSTMENT_SENSITIVITY;break;case'User finding it too easy - increasing difficulty':adjustment=ADAPTIVE_DIFFICULTY_CONFIG.ADJUSTMENT_SENSITIVITY;break;case'Frustration detected - reducing difficulty':adjustment=-ADAPTIVE_DIFFICULTY_CONFIG.ADJUSTMENT_SENSITIVITY*1.5;break;case'Engagement dropping - adjusting difficulty':adjustment=-ADAPTIVE_DIFFICULTY_CONFIG.ADJUSTMENT_SENSITIVITY*0.5;break;default:adjustment=0;}return encounter.currentDifficulty+adjustment;}private smoothDifficultyTransition(from:number,to:number,smoothing:number):number{return from+(to-from)*(1-smoothing);}private updateEncounterFactors(encounter:AdaptiveBossEncounter,difficulty:number):void{encounter.factors=encounter.factors.map(factor=>({...factor,currentValue:this.calculateFactorValue(factor,difficulty)}));}private calculateFactorValue(factor:DifficultyFactor,difficulty:number):number{const range=factor.max-factor.min;const difficultyOffset=range*difficulty;switch(factor.type){case'boss_health':return factor.baseValue*(1+difficulty);case'attack_frequency':return factor.baseValue+difficulty*0.5;case'mechanic_complexity':return Math.ceil(factor.baseValue+difficulty*2);case'time_pressure':return factor.baseValue*(1-difficulty*0.3);case'purity_threshold':return factor.baseValue+difficulty*20;default:return factor.baseValue+difficultyOffset;}}private async getSquadDifficultyContext(squadId:string,currentUserId:string):Promise<any[]>{return[{userId:currentUserId,difficulty:0.5,role:'leader'}];}private async applySquadBalancing(userId:string,squadId:string,difficulty:number):Promise<number>{return difficulty;}private calculateStreakStrength(streakLength:number):number{return Math.min(1.0,streakLength/30);}private updateLearningPatterns(profile:UserProfile,encounter:AdaptiveBossEncounter,outcome:string,performance:number):void{const recentEncounters=profile.difficultyHistory.slice(-5);if(recentEncounters.length>=2){const recentPerformance=recentEncounters.slice(-2).map(e=>e.performance);const improvement=recentPerformance[1]-recentPerformance[0];if(improvement>0.1){profile.learningRate=Math.min(0.3,profile.learningRate+0.02);}else if(improvement<-0.1){profile.learningRate=Math.max(0.05,profile.learningRate-0.01);}}profile.adaptationSpeed=profile.learningRate*0.5;}private calculatePreferredDifficulty(profile:UserProfile):number{if(profile.difficultyHistory.length===0){return 0.5;}const successfulEncounters=profile.difficultyHistory.filter(e=>e.outcome==='victory').slice(-10);if(successfulEncounters.length===0){return 0.5;}const totalWeight=successfulEncounters.reduce((sum,encounter,index)=>{const weight=(index+1)/successfulEncounters.length;return sum+encounter.difficulty*weight;},0);const totalWeights=successfulEncounters.reduce((sum,_,index)=>{return sum+(index+1)/successfulEncounters.length;},0);return totalWeight/totalWeights;}private updatePredictiveFactors(profile:UserProfile,encounter:AdaptiveBossEncounter,outcome:string):void{if(outcome==='defeat'&&encounter.currentDifficulty>0.7){profile.burnoutRisk=Math.min(1.0,profile.burnoutRisk+0.1);}else if(outcome==='victory'){profile.burnoutRisk=Math.max(0.0,profile.burnoutRisk-0.05);}if(outcome==='defeat'&&encounter.currentPerformance<0.3){profile.frustrationLevel=Math.min(1.0,profile.frustrationLevel+0.15);}else if(outcome==='victory'){profile.frustrationLevel=Math.max(0.0,profile.frustrationLevel-0.1);}const recentOutcomes=profile.difficultyHistory.slice(-5).map(e=>e.outcome);const recentWinRate=recentOutcomes.filter(o=>o==='victory').length/recentOutcomes.length;if(recentWinRate>0.7){profile.engagementTrend='increasing';}else if(recentWinRate<0.3){profile.engagementTrend='decreasing';}else{profile.engagementTrend='stable';}}private getDifficultyLevel(difficulty:number):DifficultyLevel{for(const[level,config]of Object.entries(ADAPTIVE_DIFFICULTY_CONFIG.DIFFICULTY_RANGES)){if(difficulty>=config.min&&difficulty<=config.max){return level as DifficultyLevel;}}return'INTERMEDIATE';}private analyzeDifficultyFactors(profile:UserProfile):Array<{factor:string;influence:number;value:number;}>{return[{factor:'recent_purity',influence:ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.recentPurity,value:profile.recentPurity},{factor:'completion_rate',influence:ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.completionRate,value:profile.completionRate},{factor:'streak_strength',influence:ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.streakStrength,value:profile.streakStrength},{factor:'session_consistency',influence:ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.sessionConsistency,value:profile.sessionConsistency},{factor:'squad_performance',influence:ADAPTIVE_DIFFICULTY_CONFIG.PERFORMANCE_WEIGHTS.squadPerformance,value:profile.squadPerformance}];}private generateDifficultyReasoning(profile:UserProfile,factors:DynamicValue[],recommendedDifficulty:number):string[]{const reasoning:string[]=[];if(profile.recentPurity>0.8){reasoning.push('High session purity suggests you can handle increased challenge');}else if(profile.recentPurity<0.6){reasoning.push('Lower session purity indicates need for gentler difficulty');}if(profile.completionRate>0.8){reasoning.push('Strong completion rate shows consistent performance');}else if(profile.completionRate<0.5){reasoning.push('Lower completion rate suggests need for more accessible challenges');}if(profile.streakStrength>0.8){reasoning.push('Strong streak indicates readiness for advanced challenges');}if(profile.learningRate>0.2){reasoning.push('Fast learning rate detected - can handle progressive difficulty');}if(profile.burnoutRisk>0.6){reasoning.push('Burnout risk detected - difficulty adjusted for sustainability');}return reasoning;}private calculateRecommendationConfidence(profile:UserProfile):number{let confidence=0.5;const dataPoints=profile.difficultyHistory.length;confidence+=Math.min(0.3,dataPoints*0.02);if(profile.sessionConsistency>0.8){confidence+=0.1;}if(profile.learningRate>0.15){confidence+=0.1;}return Math.min(1.0,confidence);}}export function createAdaptiveDifficultyService():AdaptiveDifficultyService{return new AdaptiveDifficultyService();}let adaptiveDifficultyService:AdaptiveDifficultyService|null=null;export function getAdaptiveDifficultyService():AdaptiveDifficultyService{if(!adaptiveDifficultyService){adaptiveDifficultyService=new AdaptiveDifficultyService();}return adaptiveDifficultyService;}

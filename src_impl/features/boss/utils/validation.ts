import{z}from'zod';import{createDebugger}from'../../../utils/debug';import{eventBus}from'../../../events';const debug=createDebugger('boss:validation');export const BossDifficultySchema=z.enum(['EASY','MEDIUM','HARD','NIGHTMARE']);export const BossAttackSchema=z.object({playerId:z.string(),damage:z.number().min(0),timestamp:z.number(),attackType:z.enum(['NORMAL','CRITICAL','SPECIAL']),sessionDuration:z.number().min(0)});export type BossAttack=z.infer<typeof BossAttackSchema>;export interface DamageValidationResult{valid:boolean;errors:string[];suspicious:boolean;expectedDamageRange?:{min:number;max:number;};}export function validateBossAttack(attack:BossAttack,boss:{id:string;difficulty:string;maxHealth:number;expectedDps:number;},playerHistory:{avgDamage:number;avgSessionDuration:number;previousAttacks:BossAttack[];}):DamageValidationResult{const result:DamageValidationResult={valid:true,errors:[],suspicious:false};const expectedDamage=attack.sessionDuration*boss.expectedDps;const deviation=attack.damage/Math.max(expectedDamage,1);const MAX_DEVIATION=3;if(deviation>MAX_DEVIATION){result.errors.push(`Suspicious damage: ${attack.damage} (${deviation.toFixed(1)}x expected)`);result.suspicious=true;result.expectedDamageRange={min:expectedDamage*0.5,max:expectedDamage*MAX_DEVIATION};}if(attack.damage===0&&attack.sessionDuration>300){result.errors.push('Zero damage despite long session');result.valid=false;}if(playerHistory.avgDamage>0){const historyRatio=attack.damage/playerHistory.avgDamage;if(historyRatio>5){result.errors.push(`Damage ${historyRatio.toFixed(1)}x player's average`);result.suspicious=true;}}if(attack.sessionDuration<60){result.errors.push('Session too short for meaningful damage');}if(playerHistory.previousAttacks.length>0){const lastAttack=playerHistory.previousAttacks[playerHistory.previousAttacks.length-1];const timeSinceLast=attack.timestamp-lastAttack.timestamp;if(timeSinceLast<10000){result.errors.push('Attacking too frequently');result.suspicious=true;}}const recentAttacks=playerHistory.previousAttacks.slice(-20);const critCount=recentAttacks.filter(a=>a.attackType==='CRITICAL').length;const critRate=critCount/recentAttacks.length;if(recentAttacks.length>=10&&critRate>0.5){result.errors.push(`Suspicious crit rate: ${(critRate*100).toFixed(0)}%`);result.suspicious=true;}if(result.suspicious){eventBus.publish('analytics:track',{event:'boss_suspicious_attack',properties:{bossId:boss.id,playerId:attack.playerId,damage:attack.damage,deviation}});}return result;}export interface BossBalanceMetrics{avgDefeatTime:number;defeatCount:number;attemptCount:number;avgPlayerCount:number;healEfficiency:number;}export function analyzeBossBalance(metrics:BossBalanceMetrics,boss:{id:string;targetDuration:number;minPlayers:number;maxPlayers:number;}):{balanced:boolean;recommendations:string[];healthAdjustment:number;}{const recommendations:string[]=[];let healthAdjustment=0;const completionRate=metrics.defeatCount/Math.max(metrics.attemptCount,1);const timeRatio=metrics.avgDefeatTime/boss.targetDuration;if(timeRatio<0.5){recommendations.push('Boss defeated too quickly - increase health');healthAdjustment+=0.3;}else if(timeRatio>2){recommendations.push('Boss takes too long - decrease health');healthAdjustment-=0.3;}if(completionRate<0.1){recommendations.push('Very low completion rate - consider nerfing');healthAdjustment-=0.2;}else if(completionRate>0.9){recommendations.push('Too easy - consider buffing');healthAdjustment+=0.2;}if(metrics.avgPlayerCount<boss.minPlayers){recommendations.push('Not enough players engaging');}if(metrics.healEfficiency<0.3){recommendations.push('Healing not effective - review heal mechanics');}return{balanced:recommendations.length===0,recommendations,healthAdjustment};}export function validateBossDefeat(bossId:string,participants:{userId:string;damage:number;joinTime:number;}[],defeatTime:number,bossStartTime:number):{valid:boolean;error?:string;contributionPercentages:Record<string,number>;}{const totalDamage=participants.reduce((sum,p)=>sum+p.damage,0);const contributionPercentages:Record<string,number>={};participants.forEach(p=>{contributionPercentages[p.userId]=p.damage/totalDamage;});const duration=defeatTime-bossStartTime;for(const participant of participants){const participationTime=defeatTime-participant.joinTime;const minParticipation=duration*0.1;if(participationTime<minParticipation){return{valid:false,error:'Participant joined too late for credit',contributionPercentages};}}for(const[userId,percentage]of Object.entries(contributionPercentages)){if(percentage<0.01){return{valid:false,error:`User ${userId} contributed less than 1% damage`,contributionPercentages};}}return{valid:true,contributionPercentages};}export const BossValidation={validateBossAttack,analyzeBossBalance,validateBossDefeat,BossAttackSchema};export default BossValidation;

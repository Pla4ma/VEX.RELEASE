import{eventBus}from'../../events';export type BossPhase='PHASE_1'|'PHASE_2'|'PHASE_3'|'FINAL_PHASE';export type VisualTheme='dark'|'fire'|'ice'|'nature'|'shadow';export type MusicMood='tense'|'epic'|'calm'|'dramatic'|'victory';export type PhaseEffects={visual:string[];audio:string[];haptic:boolean;};export type BossNarrativeArc={bossId:string;bossName:string;phases:BossPhaseNarrative[];taunts:string[];theme:VisualTheme;};export type BossPhaseNarrative={phase:BossPhase;title:string;description:string;healthThreshold:number;effects:PhaseEffects;musicMood:MusicMood;};export type BossPhaseState={currentPhase:BossPhase;phaseTransitions:number;tauntsShown:string[];};export type BossTheme={primary:string;secondary:string;accent:string;};export type PhaseNarrative={title:string;description:string;effects:PhaseEffects;};export const BOSS_NARRATIVE_ARCS:Record<string,BossNarrativeArc>={'procrastination-dragon':{bossId:'procrastination-dragon',bossName:'The Procrastination Dragon',phases:[{phase:'PHASE_1',title:'The Dragon Wakes',description:'The beast stirs from its slumber...',healthThreshold:1.0,effects:{visual:['glow'],audio:['rumble'],haptic:false},musicMood:'tense'},{phase:'PHASE_2',title:'Fiery Resistance',description:'The dragon unleashes its flames!',healthThreshold:0.6,effects:{visual:['fire'],audio:['roar'],haptic:true},musicMood:'epic'},{phase:'PHASE_3',title:'Final Stand',description:'The dragon fights with desperate fury!',healthThreshold:0.2,effects:{visual:['inferno'],audio:['scream'],haptic:true},musicMood:'dramatic'}],taunts:["I'll deal with it tomorrow...",'Just five more minutes...',"The deadline isn't until next week!"],theme:'fire'},'distraction-demon':{bossId:'distraction-demon',bossName:'The Distraction Demon',phases:[{phase:'PHASE_1',title:'Whispers Begin',description:'Subtle temptations start to surface...',healthThreshold:1.0,effects:{visual:['shadow'],audio:['whisper'],haptic:false},musicMood:'tense'},{phase:'PHASE_2',title:'Temptation Grows',description:'The demon offers irresistible diversions!',healthThreshold:0.5,effects:{visual:['phantom'],audio:['laughter'],haptic:true},musicMood:'dramatic'}],taunts:['Did you see that notification?','Your phone is calling to you...','Just a quick scroll through social media...'],theme:'shadow'}};export function getBossNarrativeArc(bossId:string):BossNarrativeArc|null{return BOSS_NARRATIVE_ARCS[bossId]??null;}export function determineBossPhase(bossId:string,healthPercent:number):BossPhase{const arc=getBossNarrativeArc(bossId);if(!arc){return'PHASE_1';}for(let i=arc.phases.length-1;i>=0;i--){if(healthPercent<=arc.phases[i].healthThreshold){return arc.phases[i].phase;}}return'PHASE_1';}export function calculateHealthPercent(remaining:number,max:number):number{return Math.max(0,Math.min(1,remaining/max));}export function getPhaseNarrative(bossId:string,phase:BossPhase):PhaseNarrative|null{const arc=getBossNarrativeArc(bossId);if(!arc){return null;}const phaseData=arc.phases.find(p=>p.phase===phase);if(!phaseData){return null;}return{title:phaseData.title,description:phaseData.description,effects:phaseData.effects};}export function getRandomTaunt(bossId:string):string|null{const arc=getBossNarrativeArc(bossId);if(!arc||arc.taunts.length===0){return null;}const index=Math.floor(Math.random()*arc.taunts.length);return arc.taunts[index];}export function getCurrentVisualTheme(bossId:string):VisualTheme{const arc=getBossNarrativeArc(bossId);return arc?.theme??'dark';}export function getCurrentMusicMood(bossId:string,phase:BossPhase):MusicMood{const arc=getBossNarrativeArc(bossId);if(!arc){return'tense';}const phaseData=arc.phases.find(p=>p.phase===phase);return phaseData?.musicMood??'tense';}export function getPhaseEffects(bossId:string,phase:BossPhase):PhaseEffects{const arc=getBossNarrativeArc(bossId);if(!arc){return{visual:[],audio:[],haptic:false};}const phaseData=arc.phases.find(p=>p.phase===phase);return phaseData?.effects??{visual:[],audio:[],haptic:false};}const phaseStateMap=new Map<string,BossPhaseState>();export function initializePhaseState(bossId:string):BossPhaseState{const state:BossPhaseState={currentPhase:'PHASE_1',phaseTransitions:0,tauntsShown:[]};phaseStateMap.set(bossId,state);return state;}export function updatePhaseState(bossId:string,newPhase:BossPhase):void{const state=phaseStateMap.get(bossId);if(state&&state.currentPhase!==newPhase){state.currentPhase=newPhase;state.phaseTransitions++;eventBus.publish('boss:phase_changed',{bossId,newPhase,previousPhase:state.currentPhase,encounterId:`${bossId}_${Date.now()}`});}}export function getPhaseState(bossId:string):BossPhaseState|null{return phaseStateMap.get(bossId)??null;}export function recordTauntShown(bossId:string,taunt:string):void{const state=phaseStateMap.get(bossId);if(state){state.tauntsShown.push(taunt);}}export function clearPhaseState(bossId:string):void{phaseStateMap.delete(bossId);}export function getBossStory(bossId:string):string{const arc=getBossNarrativeArc(bossId);if(!arc){return'';}return`${arc.bossName} - A formidable foe that tests your focus and determination.`;}export function getBossTheme(bossId:string):BossTheme{const visualTheme=getCurrentVisualTheme(bossId);const themeColors:Record<VisualTheme,BossTheme>={dark:{primary:'#1a1a2e',secondary:'#16213e',accent:'#e94560'},fire:{primary:'#2d142c',secondary:'#801336',accent:'#ff6b35'},ice:{primary:'#1e3a5f',secondary:'#4a90e2',accent:'#a8d8ea'},nature:{primary:'#1b4332',secondary:'#2d6a4f',accent:'#52b788'},shadow:{primary:'#0f0f0f',secondary:'#232323',accent:'#6c5ce7'}};return themeColors[visualTheme]??themeColors.dark;}export function hasNarrativeArc(bossId:string):boolean{return bossId in BOSS_NARRATIVE_ARCS;}export function getAllNarrativeArcs():BossNarrativeArc[]{return Object.values(BOSS_NARRATIVE_ARCS);}

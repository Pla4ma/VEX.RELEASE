import{z}from'zod'; export const FlashSaleConfigSchema = z.object({enabled:z.boolean().default(true),minFlashSalesPerWeek:z.number().min(1).max(7).default(1),maxFlashSalesPerWeek:z.number().min(1).max(7).default(3),saleDurationMinutes:z.number().min(30).max(180).default(60),discountPercent:z.number().min(10).max(90).default(50),eligibleItemCategories:z.array(z.string()).default(['CONSUMABLE','EQUIPMENT','COSMETIC']),minItemLevel:z.number().min(1).default(1),maxItemLevel:z.number().min(1).default(100),notificationEnabled:z.boolean().default(true),notificationMinutesBefore:z.number().min(0).max(30).default(5)}); export const FlashSaleSchema = z.object({id:z.string(),itemId:z.string(),itemName:z.string(),itemIcon:z.string(),itemDescription:z.string(),originalPrice:z.number(),discountedPrice:z.number(),currency:z.enum(['COINS','GEMS']),discountPercent:z.number(),startsAt:z.number(),endsAt:z.number(),isActive:z.boolean(),totalQuantity:z.number().optional(),soldQuantity:z.number().default(0),isFlashSale:z.literal(true)}); export type FlashSaleConfig=z.infer<typeof FlashSaleConfigSchema>; export type FlashSale=z.infer<typeof FlashSaleSchema>; export function generateFlashSaleTime(config:FlashSaleConfig,weekStart:Date = new Date()):{startsAt:number;endsAt:number;}{const startOfWeek = new Date(weekStart); startOfWeek.setUTCHours(0,0,0,0); const dayOfWeek = startOfWeek.getUTCDay(); const daysToSubtract = dayOfWeek === 0 ? 6 : dayOfWeek - 1; startOfWeek.setUTCDate(startOfWeek.getUTCDate() - daysToSubtract); const randomDay = Math.floor(Math.random() * 7); const randomHour = 10 + Math.floor(Math.random() * 10); const randomMinute = Math.floor(Math.random() * 60); const startsAt = new Date(startOfWeek); startsAt.setUTCDate(startOfWeek.getUTCDate() + randomDay); startsAt.setUTCHours(randomHour,randomMinute,0,0); const endsAt = new Date(startsAt); endsAt.setUTCMinutes(startsAt.getUTCMinutes() + config.saleDurationMinutes); return{startsAt:startsAt.getTime(),endsAt:endsAt.getTime()};}export function isFlashSaleActive(flashSale:FlashSale):boolean{const now = Date.now(); return now >= flashSale.startsAt && now < flashSale.endsAt;}export function getFlashSaleTimeRemaining(flashSale:FlashSale):number{const now = Date.now(); return Math.max(0,flashSale.endsAt - now);}export function formatFlashSaleCountdown(ms:number):string{const minutes = Math.floor(ms / (1000 * 60)); const seconds = Math.floor(ms % (1000 * 60) / 1000); return`${minutes.toString().padStart(2,'0')}:${seconds.toString().padStart(2,'0')}`;}export function shouldSendNotification(flashSale:FlashSale,config:FlashSaleConfig):boolean{if(!config.notificationEnabled){return false;}const now = Date.now(); const notificationTime = flashSale.startsAt - config.notificationMinutesBefore * 60 * 1000; return now >= notificationTime && now < flashSale.startsAt;}export class FlashSaleManager{private config:FlashSaleConfig; private currentSale:FlashSale|null = null; private scheduledSales:FlashSale[] = []; private listeners:Set<(sale:FlashSale|null)=>void> = new Set(); constructor(config:Partial<FlashSaleConfig> = {}){this.config = FlashSaleConfigSchema.parse(config);}setConfig(config:Partial<FlashSaleConfig>):void{this.config = FlashSaleConfigSchema.parse({...this.config,...config});}getConfig():FlashSaleConfig{return this.config;}scheduleWeeklySales(availableItems:Array<{id:string;name:string;icon:string;description:string;baseValue:number;currency:'COINS'|'GEMS';}>):FlashSale[]{const numSales = this.config.minFlashSalesPerWeek + Math.floor(Math.random() * (this.config.maxFlashSalesPerWeek - this.config.minFlashSalesPerWeek + 1)); this.scheduledSales = []; for(let i = 0; i < numSales; i++){const item = availableItems[Math.floor(Math.random() * availableItems.length)]; const discountedPrice = Math.ceil(item.baseValue * (1 - this.config.discountPercent / 100)); const{startsAt,endsAt} = generateFlashSaleTime(this.config); const sale:FlashSale = {id:`flash-sale-${i}-${Date.now()}`,itemId:item.id,itemName:item.name,itemIcon:item.icon,itemDescription:item.description,originalPrice:item.baseValue,discountedPrice,currency:item.currency,discountPercent:this.config.discountPercent,startsAt,endsAt,isActive:false,isFlashSale:true,soldQuantity:0}; this.scheduledSales.push(sale);}this.scheduledSales.sort((a,b)=>a.startsAt - b.startsAt); return this.scheduledSales;}getCurrentSale():FlashSale|null{const now = Date.now(); if(this.currentSale && now >= this.currentSale.startsAt && now < this.currentSale.endsAt){return this.currentSale;}const activeSale = this.scheduledSales.find(sale=>now >= sale.startsAt && now < sale.endsAt); if(activeSale){this.currentSale = {...activeSale,isActive:true}; this.notifyListeners(); return this.currentSale;}if(this.currentSale){this.currentSale = null; this.notifyListeners();}return null;}getUpcomingSales():FlashSale[]{const now = Date.now(); return this.scheduledSales.filter(sale=>sale.startsAt > now);}getNextSale():FlashSale|null{const upcoming = this.getUpcomingSales(); return upcoming[0] || null;}checkNotification():FlashSale|null{const now = Date.now(); for(const sale of this.scheduledSales){if(shouldSendNotification(sale,this.config) && !sale.isActive && now < sale.startsAt){return sale;}}return null;}onSaleChange(callback:(sale:FlashSale|null)=>void):()=>void{this.listeners.add(callback); return()=>{this.listeners.delete(callback);};}private notifyListeners():void{this.listeners.forEach(callback=>callback(this.currentSale));}async purchaseItem(quantity:number = 1):Promise<{success:boolean;error?:string;remainingQuantity?:number;}>{if(!this.currentSale || !this.currentSale.isActive){return{success:false,error:'Flash sale is not active'};}this.currentSale.soldQuantity += quantity; return{success:true,remainingQuantity:this.currentSale.totalQuantity ? this.currentSale.totalQuantity - this.currentSale.soldQuantity : undefined};}}export const defaultFlashSaleManager = new FlashSaleManager(); export default FlashSaleManager;

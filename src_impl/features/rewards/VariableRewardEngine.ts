import { z } from "zod"; import * as Sentry from "@sentry/react-native"; export enum VariableRewardTier { NONE = "NONE", COMMON = "COMMON", UNCOMMON = "UNCOMMON", RARE = "RARE", EPIC = "EPIC", LEGENDARY = "LEGENDARY", } export interface VariableRewardModifiers { streakDays: number; isSGrade: boolean; bossActive: boolean; squadSession: boolean; isPremium: boolean; luckyBonusActive: boolean; } export interface VariableRewardResult { tier: VariableRewardTier; items: VariableRewardItem[]; rollValue: number; modifiedChances: Record<VariableRewardTier, number>; triggeredModifiers: string[]; } export interface VariableRewardItem { type: "COINS" | "XP_BOOST" | "GEMS" | "STREAK_SHIELD" | "COSMETIC" | "LEGENDARY_ITEM"; amount: number; name: string; icon: string; } export interface VariableRewardConfig { tier: VariableRewardTier; baseChance: number; description: string; } const BASE_CHANCES: Record<VariableRewardTier, number> = { [VariableRewardTier.NONE]: 0.728, [VariableRewardTier.COMMON]: 0.15, [VariableRewardTier.UNCOMMON]: 0.08, [VariableRewardTier.RARE]: 0.03, [VariableRewardTier.EPIC]: 0.01, [VariableRewardTier.LEGENDARY]: 0.002, }; const totalBaseChance = Object.values(BASE_CHANCES).reduce((a, b) => a + b, 0); if (Math.abs(totalBaseChance - 1) > 0.0001) {
  throw new Error(`Base chances must sum to 1, got ${totalBaseChance}`);
} const TIER_REWARDS: Record<VariableRewardTier, VariableRewardItem[]> = { [VariableRewardTier.NONE]: [], [VariableRewardTier.COMMON]: [{ type: "COINS", amount: 0, name: "Coin Boost", icon: "🪙" }], [VariableRewardTier.UNCOMMON]: [{ type: "XP_BOOST", amount: 1, name: "XP Boost Item", icon: "⚡" }], [VariableRewardTier.RARE]: [ { type: "COSMETIC", amount: 1, name: "Rare Cosmetic Ingredient", icon: "💎", }, { type: "STREAK_SHIELD", amount: 1, name: "Streak Shield", icon: "🛡️" }, ], [VariableRewardTier.EPIC]: [ { type: "GEMS", amount: 25, name: "Gem Bundle", icon: "💎" }, { type: "COSMETIC", amount: 1, name: "Epic Item", icon: "✨" }, ], [VariableRewardTier.LEGENDARY]: [ { type: "COSMETIC", amount: 1, name: "Legendary Cosmetic", icon: "👑" }, { type: "GEMS", amount: 100, name: "Huge Gem Bundle", icon: "💎" }, { type: "LEGENDARY_ITEM", amount: 1, name: "Legendary Item", icon: "🏆" }, ], }; const MODIFIER_VALUES = { STREAK_BONUS: 0.03, S_GRADE_BONUS: 0.05, BOSS_BONUS: 0.02, SQUAD_BONUS: 0.02, PREMIUM_BONUS: 0.03, } as const; const MAX_TOTAL_MODIFIER = 0.15; export const VariableRewardModifiersSchema = z.object({ streakDays: z.number().int().min(0).default(0), isSGrade: z.boolean().default(false), bossActive: z.boolean().default(false), squadSession: z.boolean().default(false), isPremium: z.boolean().default(false), luckyBonusActive: z.boolean().default(false), }); export const CalculateVariableRewardInputSchema = z.object({ userId: z.string().uuid(), baseCoins: z.number().int().min(0), modifiers: VariableRewardModifiersSchema, seed: z.string().optional(), }); export type CalculateVariableRewardInput = z.infer<typeof CalculateVariableRewardInputSchema>; export class VariableRewardEngine { calculateReward(input: CalculateVariableRewardInput): VariableRewardResult { const validated = CalculateVariableRewardInputSchema.parse(input); try { const { modifiedChances, triggeredModifiers } = this.applyModifiers(BASE_CHANCES, validated.modifiers); const roll = this.generateRoll(validated.seed); const tier = this.determineTier(roll, modifiedChances); const items = this.generateItems(tier, validated.baseCoins, validated.modifiers); Sentry.addBreadcrumb({ category: "variable-reward",
        message: `Variable reward rolled: ${tier}`,
level: tier === VariableRewardTier.LEGENDARY ? "warning" : "info", data: { userId: validated.userId, tier, roll, modifiers: triggeredModifiers, }, }); return { tier, items, rollValue: roll, modifiedChances, triggeredModifiers, }; } catch (error) { Sentry.captureException(error, { tags: { feature: "variable-reward", operation: "calculate" }, extra: { userId: validated.userId }, }); return { tier: VariableRewardTier.NONE, items: [], rollValue: 0, modifiedChances: BASE_CHANCES, triggeredModifiers: [], }; } } private applyModifiers( baseChances: Record<VariableRewardTier, number>, modifiers: VariableRewardModifiers, ): { modifiedChances: Record<VariableRewardTier, number>; triggeredModifiers: string[]; } { let totalModifier = 0; const triggeredModifiers: string[] = []; if (modifiers.streakDays > 7) { totalModifier += MODIFIER_VALUES.STREAK_BONUS;
      triggeredModifiers.push(`Streak Bonus (${modifiers.streakDays} days)`);
} if (modifiers.isSGrade) { totalModifier += MODIFIER_VALUES.S_GRADE_BONUS; triggeredModifiers.push("S Grade Bonus"); } if (modifiers.bossActive) { totalModifier += MODIFIER_VALUES.BOSS_BONUS; triggeredModifiers.push("Boss Active Bonus"); } if (modifiers.squadSession) { totalModifier += MODIFIER_VALUES.SQUAD_BONUS; triggeredModifiers.push("Squad Session Bonus"); } if (modifiers.isPremium) { totalModifier += MODIFIER_VALUES.PREMIUM_BONUS; triggeredModifiers.push("Premium Bonus"); } totalModifier = Math.min(totalModifier, MAX_TOTAL_MODIFIER); const modifiedChances: Record<VariableRewardTier, number> = { ...baseChances, }; const rewardTiers = [VariableRewardTier.COMMON, VariableRewardTier.UNCOMMON, VariableRewardTier.RARE, VariableRewardTier.EPIC, VariableRewardTier.LEGENDARY]; const originalNoneChance = baseChances[VariableRewardTier.NONE]; const newNoneChance = Math.max(0, originalNoneChance - totalModifier); const chanceToDistribute = originalNoneChance - newNoneChance; modifiedChances[VariableRewardTier.NONE] = newNoneChance; const totalRewardWeight = rewardTiers.reduce((sum, tier) => sum + baseChances[tier], 0); for (const tier of rewardTiers) { const weight = baseChances[tier] / totalRewardWeight; modifiedChances[tier] = baseChances[tier] + chanceToDistribute * weight; } return { modifiedChances, triggeredModifiers }; } private generateRoll(seed?: string): number { if (seed) { return this.seededRandom(seed); } return Math.random(); } private seededRandom(seed: string): number { let hash = 0; for (let i = 0; i < seed.length; i++) { const char = seed.charCodeAt(i); hash = (hash << 5) - hash + char; hash = hash & hash; } let t = (hash += 0x6d2b79f5); t = Math.imul(t ^ (t >>> 15), t | 1); t ^= t + Math.imul(t ^ (t >>> 7), t | 61); return ((t ^ (t >>> 14)) >>> 0) / 4294967296; } private determineTier(roll: number, chances: Record<VariableRewardTier, number>): VariableRewardTier { let cumulative = 0; const tiers = [VariableRewardTier.LEGENDARY, VariableRewardTier.EPIC, VariableRewardTier.RARE, VariableRewardTier.UNCOMMON, VariableRewardTier.COMMON, VariableRewardTier.NONE]; for (const tier of tiers) { cumulative += chances[tier]; if (roll < cumulative) { return tier; } } return VariableRewardTier.NONE; } private generateItems(tier: VariableRewardTier, baseCoins: number, modifiers: VariableRewardModifiers): VariableRewardItem[] { const templateItems = TIER_REWARDS[tier]; if (templateItems.length === 0) { return []; } const selectedTemplates = tier === VariableRewardTier.LEGENDARY ? templateItems : [templateItems[Math.floor(Math.random() * templateItems.length)]!]; return selectedTemplates.map((template) => { let amount = template.amount; if (tier === VariableRewardTier.COMMON && template.type === "COINS") { const multiplier = 1.5 + Math.random() * 1.0; amount = Math.floor(baseCoins * multiplier); } if (modifiers.luckyBonusActive) { amount *= 2; } return { ...template, amount }; }); } getExpectedValueStats(baseCoins: number): { tier: VariableRewardTier; chance: number; expectedValue: number }[] { return Object.entries(BASE_CHANCES).map(([tier, chance]) => { const items = TIER_REWARDS[tier as VariableRewardTier]; let expectedValue = 0; if (tier === VariableRewardTier.COMMON) { expectedValue = baseCoins * 2 * chance; } else { expectedValue = items.reduce((sum, item) => { const valueMap: Record<string, number> = { COINS: item.amount, GEMS: item.amount * 10, XP_BOOST: 50, STREAK_SHIELD: 100, COSMETIC: 200, LEGENDARY_ITEM: 500, }; return sum + (valueMap[item.type] || 0); }, 0) * chance; } return { tier: tier as VariableRewardTier, chance, expectedValue: Math.floor(expectedValue), }; }); } simulateDistribution(rollCount: number, modifiers: VariableRewardModifiers): Record<VariableRewardTier, number> { const results: Record<VariableRewardTier, number> = { [VariableRewardTier.NONE]: 0, [VariableRewardTier.COMMON]: 0, [VariableRewardTier.UNCOMMON]: 0, [VariableRewardTier.RARE]: 0, [VariableRewardTier.EPIC]: 0, [VariableRewardTier.LEGENDARY]: 0, }; const { modifiedChances } = this.applyModifiers(BASE_CHANCES, modifiers); for (let i = 0; i < rollCount; i++) { const roll = Math.random(); const tier = this.determineTier(roll, modifiedChances); results[tier]++; } return results; } } export const variableRewardEngine = new VariableRewardEngine(); export function validateVariableRewardServerSide(clientResult: VariableRewardResult, serverModifiers: VariableRewardModifiers, serverSeed: string): { valid: boolean; reason?: string } { const serverEngine = new VariableRewardEngine(); const serverResult = serverEngine.calculateReward({ userId: "00000000-0000-4000-8000-000000000000", baseCoins: 100, modifiers: serverModifiers, seed: serverSeed, }); if (clientResult.tier !== serverResult.tier) { return { valid: false,
      reason: `Tier mismatch: client=${clientResult.tier}, server=${serverResult.tier}`,
}; } if (clientResult.rollValue < 0 || clientResult.rollValue > 1) { return { valid: false,
      reason: `Invalid roll value: ${clientResult.rollValue}`,
}; } return { valid: true }; } export function calculateSessionVariableReward( userId: string, baseCoins: number, sessionData: { streakDays: number; grade: string; bossActive: boolean; squadMode: boolean; isPremium: boolean; }, ): VariableRewardResult { return variableRewardEngine.calculateReward({ userId, baseCoins, modifiers: { streakDays: sessionData.streakDays, isSGrade: sessionData.grade === "S", bossActive: sessionData.bossActive, squadSession: sessionData.squadMode, isPremium: sessionData.isPremium, luckyBonusActive: false, }, }); }

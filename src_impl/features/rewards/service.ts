import{eventBus}from'../../events'; import*as repository from'./repository'; import{CreateRewardInputSchema,ClaimRewardInputSchema,CalculateRewardInputSchema,type Reward,type RewardCalculation,type CreateRewardInput,type ClaimRewardInput,type CalculateRewardInput,type Deliverable}from'./schemas'; export function calculateReward(input:CalculateRewardInput):RewardCalculation{const validated = CalculateRewardInputSchema.parse(input); let finalAmount = validated.baseAmount; const multipliers:RewardCalculation['multipliers'] = []; const bonuses:RewardCalculation['bonuses'] = []; const levelBonus = validated.userLevel * 0.05; if(levelBonus > 0){multipliers.push({source:'Level Bonus',value:1 + levelBonus,description:`+${Math.floor(levelBonus * 100)}% for level ${validated.userLevel}`}); finalAmount *= 1 + levelBonus;}if(validated.streakDays >= 7){const streakMultiplier = validated.streakDays >= 30 ? 1.5 : validated.streakDays >= 14 ? 1.25 : 1.1; multipliers.push({source:'Streak Bonus',value:streakMultiplier,description:`${validated.streakDays} day streak`}); finalAmount *= streakMultiplier;}if(validated.squadMultiplier > 1){multipliers.push({source:'Squad Bonus',value:validated.squadMultiplier,description:`Squad multiplier x${validated.squadMultiplier}`}); finalAmount *= validated.squadMultiplier;}if(validated.bossActive){bonuses.push({source:'Boss Battle',amount:Math.floor(validated.baseAmount * 0.25),description:'Active boss encounter'}); finalAmount += Math.floor(validated.baseAmount * 0.25);}return{baseAmount:validated.baseAmount,multipliers,bonuses,finalAmount:Math.floor(finalAmount)};}export async function createReward(input:CreateRewardInput):Promise<Reward>{const validated = CreateRewardInputSchema.parse(input); if(validated.triggerId){const isDuplicate = await repository.checkDuplicateReward(validated.userId,validated.triggerType,validated.triggerId); if(isDuplicate){throw new Error('Duplicate reward: already granted for this trigger');}}const reward = await repository.createReward(validated.userId,validated.type,validated.amount,validated.triggerType,validated.triggerId || null,validated.expiresAt || null); await repository.recordLedgerEntry(reward.id,'CREATED',{type:validated.type,amount:validated.amount,triggerType:validated.triggerType}); return reward;}export async function claimReward(input:ClaimRewardInput):Promise<{success:boolean;deliverables:Deliverable[];errors:string[];}>{const validated = ClaimRewardInputSchema.parse(input); const reward = await repository.fetchReward(validated.rewardId); if(!reward){throw new Error('Reward not found');}if(reward.userId !== validated.userId){throw new Error('Unauthorized: reward belongs to different user');}if(reward.status !== 'PENDING'){throw new Error(`Cannot claim reward in ${reward.status} status`);}if(reward.expiresAt && reward.expiresAt < Date.now()){await repository.markRewardExpired(validated.rewardId); throw new Error('Reward has expired');}const deliverables:Deliverable[] = []; const errors:string[] = []; if(reward.amount && reward.amount > 0){deliverables.push({type:mapRewardTypeToDeliverable(reward.type),amount:reward.amount,itemId:reward.itemId,delivered:false,deliveredAt:null});}for(const deliverable of deliverables){try{await deliverReward(validated.userId,deliverable); deliverable.delivered = true; deliverable.deliveredAt = Date.now();}catch(error){errors.push(`Failed to deliver ${deliverable.type}: ${error instanceof Error ? error.message : 'Unknown'}`);}}const allDelivered = deliverables.every(d=>d.delivered); if(allDelivered){await repository.markRewardClaimed(validated.rewardId); await repository.recordLedgerEntry(validated.rewardId,'CLAIMED',{deliverables:deliverables.map(d=>({type:d.type,amount:d.amount}))}); eventBus.publish('reward:claimed',{rewardId:validated.rewardId,userId:validated.userId,claimedAt:Date.now(),actualValue:reward.amount || 0,appliedBoosts:[]});}else if(errors.length > 0){await repository.markRewardFailed(validated.rewardId); await repository.recordLedgerEntry(validated.rewardId,'FAILED',{errors});}return{success:allDelivered,deliverables,errors};}async function deliverReward(userId:string,deliverable:Deliverable):Promise<void>{switch(deliverable.type){case'XP':eventBus.publish('progression:add_xp',{userId,amount:deliverable.amount,source:'REWARD'}); break; case'COINS':case'GEMS':eventBus.publish('economy:add_currency',{userId,type:deliverable.type,amount:deliverable.amount,source:'REWARD'}); break; case'SHIELD':break; case'ITEM':case'COSMETIC':case'TITLE':break; default:throw new Error(`Unknown deliverable type: ${deliverable.type}`);}}export function mapRewardTypeToDeliverable(rewardType:string):Deliverable['type']{const mapping:Record<string,Deliverable['type']> = {XP:'XP',COINS:'COINS',GEMS:'GEMS',ITEM:'ITEM',COSMETIC:'COSMETIC',TITLE:'TITLE',STREAK_SHIELD:'SHIELD',BOOST:'XP'}; return mapping[rewardType] || 'XP';}export async function getPendingRewards(userId:string):Promise<Reward[]>{return repository.fetchRewards(userId,'PENDING');}export async function getRewardHistory(userId:string,limit?:number):Promise<Reward[]>{const rewards = await repository.fetchRewards(userId); return limit ? rewards.slice(0,limit) : rewards;}export async function getRewardStats(userId:string):Promise<{totalRewards:number;pendingRewards:number;claimedRewards:number;totalXp:number;totalCoins:number;totalGems:number;}>{const rewards = await repository.fetchRewards(userId); return{totalRewards:rewards.length,pendingRewards:rewards.filter(r=>r.status === 'PENDING').length,claimedRewards:rewards.filter(r=>r.status === 'CLAIMED').length,totalXp:rewards.filter(r=>r.status === 'CLAIMED' && r.type === 'XP').reduce((sum,r)=>sum + (r.amount || 0),0),totalCoins:rewards.filter(r=>r.status === 'CLAIMED' && r.type === 'COINS').reduce((sum,r)=>sum + (r.amount || 0),0),totalGems:rewards.filter(r=>r.status === 'CLAIMED' && r.type === 'GEMS').reduce((sum,r)=>sum + (r.amount || 0),0)};}export async function processExpiredRewards():Promise<number>{const expired = await repository.fetchExpiredRewards(); for(const reward of expired){await repository.markRewardExpired(reward.id); await repository.recordLedgerEntry(reward.id,'EXPIRED',{expiredAt:Date.now()});}return expired.length;}export type ChestRarity='COMMON'|'UNCOMMON'|'RARE'|'EPIC'|'LEGENDARY'; export interface MysteryChest{id:string;rarity:ChestRarity;droppedAt:number;expiresAt:number;opened:boolean;}const CHEST_DROP_RATES:Record<ChestRarity,number> = {COMMON:0.4,UNCOMMON:0.25,RARE:0.15,EPIC:0.08,LEGENDARY:0.02}; const NO_DROP_CHANCE = 0.1; export function rollMysteryChestDrop():MysteryChest|null{const roll = Math.random(); let cumulative = 0; if(roll < NO_DROP_CHANCE){return null;}cumulative += NO_DROP_CHANCE; for(const[rarity,rate]of Object.entries(CHEST_DROP_RATES)){cumulative += rate; if(roll < cumulative){return createMysteryChest(rarity as ChestRarity);}}return null;}function createMysteryChest(rarity:ChestRarity):MysteryChest{const now = Date.now(); const EXPIRY_HOURS = 48; return{id:generateChestId(),rarity,droppedAt:now,expiresAt:now + EXPIRY_HOURS * 60 * 60 * 1000,opened:false};}function generateChestId():string{return`chest_${Date.now()}_${Math.random().toString(36).substring(2,9)}`;}export function getChestAppearance(rarity:ChestRarity):{emoji:string;color:string;glow:boolean;label:string;}{switch(rarity){case'LEGENDARY':return{emoji:'🟠',color:'#F59E0B',glow:true,label:'Legendary'}; case'EPIC':return{emoji:'🟣',color:'#A855F7',glow:true,label:'Epic'}; case'RARE':return{emoji:'🔵',color:'#3B82F6',glow:false,label:'Rare'}; case'UNCOMMON':return{emoji:'⚪',color:'#22C55E',glow:false,label:'Uncommon'}; default:return{emoji:'🟤',color:'#6B7280',glow:false,label:'Common'};}}export function generateChestContents(rarity:ChestRarity):{xp:number;coins:number;gems?:number;item?:string;}{const multipliers = {COMMON:1,UNCOMMON:1.5,RARE:2.5,EPIC:5,LEGENDARY:10}; const multiplier = multipliers[rarity]; return{xp:Math.floor((50 + Math.random() * 100) * multiplier),coins:Math.floor((20 + Math.random() * 50) * multiplier),...(rarity !== 'COMMON' && rarity !== 'UNCOMMON' ? {gems:Math.floor(Math.random() * 5 * multiplier)} : {}),...(rarity === 'EPIC' || rarity === 'LEGENDARY' ? {item:`${rarity} Mystery Item`} : {})};}export const MAX_CHESTS_IN_INVENTORY = 3; export function canReceiveChest(currentChests:MysteryChest[]):boolean{const unopenedCount = currentChests.filter(c=>!c.opened).length; return unopenedCount < MAX_CHESTS_IN_INVENTORY;}

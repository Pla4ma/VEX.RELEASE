import{z}from'zod';import*as Sentry from'@sentry/react-native';import{MMKV}from'react-native-mmkv';const storage=new MMKV({id:'lucky-bonus-storage'});const STORAGE_KEY='daily_lucky_bonus';export interface LuckyBonusStatus{available:boolean;used:boolean;lastUsedDate:string|null;hoursUntilReset:number;minutesUntilReset:number;}export interface LuckyBonusResult{triggered:boolean;type:'double_tier'|'tier_skip'|'none';originalTier:string;finalTier:string;rewardMultiplier:number;}export enum LuckyBonusType{NONE='NONE',DOUBLE_TIER='DOUBLE_TIER',TIER_SKIP='TIER_SKIP',}const LUCKY_BONUS_CONFIG={DOUBLE_TIER_CHANCE:0.1,TIER_SKIP_CHANCE:0.05,REWARD_MULTIPLIER:2,TIER_UPGRADE:{COMMON:'UNCOMMON',UNCOMMON:'RARE',RARE:'EPIC',EPIC:'LEGENDARY',LEGENDARY:'LEGENDARY'}as Record<string,string>}as const;export const LuckyBonusStatusSchema=z.object({available:z.boolean(),used:z.boolean(),lastUsedDate:z.string().nullable(),hoursUntilReset:z.number().int().min(0).max(24),minutesUntilReset:z.number().int().min(0).max(59)});function getTodayDate(timezone:string='UTC'):string{const now=new Date();return now.toLocaleDateString('en-US',{timeZone:timezone,year:'numeric',month:'2-digit',day:'2-digit'}).replace(/\//g,'-');}function getTimeUntilMidnight(timezone:string='UTC'):{hours:number;minutes:number;}{const now=new Date();const tomorrow=new Date(now);tomorrow.setDate(tomorrow.getDate()+1);tomorrow.setHours(0,0,0,0);const timeZoneOffset=getTimezoneOffset(timezone);const midnightTime=tomorrow.getTime()-timeZoneOffset;const diffMs=midnightTime-now.getTime();const hours=Math.floor(diffMs/(1000*60*60));const minutes=Math.floor(diffMs%(1000*60*60)/(1000*60));return{hours:Math.max(0,hours),minutes:Math.max(0,minutes)};}function getTimezoneOffset(timezone:string):number{const now=new Date();const utcDate=new Date(now.toLocaleString('en-US',{timeZone:'UTC'}));const tzDate=new Date(now.toLocaleString('en-US',{timeZone:timezone}));return utcDate.getTime()-tzDate.getTime();}export class DailyLuckyBonusService{private userTimezone:string='UTC';setTimezone(timezone:string):void{this.userTimezone=timezone;}getStatus(userId:string):LuckyBonusStatus{try{const storageKey=`${STORAGE_KEY}_${userId}`;const stored=storage.getString(storageKey);const today=getTodayDate(this.userTimezone);const timeUntil=getTimeUntilMidnight(this.userTimezone);if(!stored){return{available:true,used:false,lastUsedDate:null,hoursUntilReset:timeUntil.hours,minutesUntilReset:timeUntil.minutes};}const data=JSON.parse(stored);const lastUsed=data.lastUsedDate;const used=data.usedToday||false;if(used&&lastUsed===today){return{available:false,used:true,lastUsedDate:lastUsed,hoursUntilReset:timeUntil.hours,minutesUntilReset:timeUntil.minutes};}return{available:true,used:false,lastUsedDate:lastUsed,hoursUntilReset:timeUntil.hours,minutesUntilReset:timeUntil.minutes};}catch(error){Sentry.captureException(error,{tags:{feature:'lucky-bonus',operation:'getStatus'}});return{available:true,used:false,lastUsedDate:null,hoursUntilReset:0,minutesUntilReset:0};}}consumeBonus(userId:string,seed?:string):LuckyBonusResult{const status=this.getStatus(userId);if(!status.available){return{triggered:false,type:'none',originalTier:'NONE',finalTier:'NONE',rewardMultiplier:1};}this.markUsed(userId);const roll=this.generateRoll(seed);let result:LuckyBonusResult;if(roll<LUCKY_BONUS_CONFIG.TIER_SKIP_CHANCE){result={triggered:true,type:'tier_skip',originalTier:'DETERMINED_BY_ENGINE',finalTier:'UPGRADED',rewardMultiplier:1};}else if(roll<LUCKY_BONUS_CONFIG.TIER_SKIP_CHANCE+LUCKY_BONUS_CONFIG.DOUBLE_TIER_CHANCE){result={triggered:true,type:'double_tier',originalTier:'DETERMINED_BY_ENGINE',finalTier:'DOUBLED',rewardMultiplier:LUCKY_BONUS_CONFIG.REWARD_MULTIPLIER};}else{result={triggered:false,type:'none',originalTier:'NONE',finalTier:'NONE',rewardMultiplier:1};}Sentry.addBreadcrumb({category:'lucky-bonus',message:`Lucky bonus consumed: ${result.type}`,level:result.triggered?'info':'debug',data:{userId,triggered:result.triggered,type:result.type,roll}});return result;}applyBonusToTier(originalTier:string,bonusType:LuckyBonusType):string{if(bonusType===LuckyBonusType.TIER_SKIP){return LUCKY_BONUS_CONFIG.TIER_UPGRADE[originalTier]||originalTier;}return originalTier;}private markUsed(userId:string):void{try{const storageKey=`${STORAGE_KEY}_${userId}`;const today=getTodayDate(this.userTimezone);storage.set(storageKey,JSON.stringify({usedToday:true,lastUsedDate:today,usedAt:Date.now()}));}catch(error){Sentry.captureException(error,{tags:{feature:'lucky-bonus',operation:'markUsed'}});}}private generateRoll(seed?:string):number{if(seed){return this.seededRandom(seed);}return Math.random();}private seededRandom(seed:string):number{let hash=0;for(let i=0;i<seed.length;i++){const char=seed.charCodeAt(i);hash=(hash<<5)-hash+char;hash=hash&hash;}const a=1664525;const c=1013904223;const m=2**32;const rand=(a*Math.abs(hash)+c)%m;return rand/m;}resetBonus(userId:string):void{try{const storageKey=`${STORAGE_KEY}_${userId}`;storage.delete(storageKey);}catch(error){Sentry.captureException(error,{tags:{feature:'lucky-bonus',operation:'reset'}});}}getCountdownString(status:LuckyBonusStatus):string{if(status.hoursUntilReset>0){return`${status.hoursUntilReset}h ${status.minutesUntilReset}m`;}return`${status.minutesUntilReset}m`;}simulateDistribution(rollCount:number):Record<LuckyBonusType,number>{const results:Record<LuckyBonusType,number>={[LuckyBonusType.NONE]:0,[LuckyBonusType.DOUBLE_TIER]:0,[LuckyBonusType.TIER_SKIP]:0};for(let i=0;i<rollCount;i++){const roll=Math.random();if(roll<LUCKY_BONUS_CONFIG.TIER_SKIP_CHANCE){results[LuckyBonusType.TIER_SKIP]++;}else if(roll<LUCKY_BONUS_CONFIG.TIER_SKIP_CHANCE+LUCKY_BONUS_CONFIG.DOUBLE_TIER_CHANCE){results[LuckyBonusType.DOUBLE_TIER]++;}else{results[LuckyBonusType.NONE]++;}}return results;}}export const dailyLuckyBonusService=new DailyLuckyBonusService();export function isLuckyBonusAvailable(userId:string):boolean{return dailyLuckyBonusService.getStatus(userId).available;}export function getLuckyBonusDisplay(userId:string):{available:boolean;badge:string;subtitle:string;}{const status=dailyLuckyBonusService.getStatus(userId);if(status.available){return{available:true,badge:'🍀',subtitle:'Lucky Bonus Available'};}const countdown=dailyLuckyBonusService.getCountdownString(status);return{available:false,badge:'⏰',subtitle:`Next bonus in ${countdown}`};}export function useLuckyBonus(userId:string):LuckyBonusStatus&{countdownString:string;}{const status=dailyLuckyBonusService.getStatus(userId);return{...status,countdownString:dailyLuckyBonusService.getCountdownString(status)};}

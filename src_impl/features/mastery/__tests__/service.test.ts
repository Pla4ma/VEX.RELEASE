import{getMasteryState,recordSessionMastery,claimMasteryChallenge,getActiveChallenges,getTechniqueStats,getUnlockedFeatures}from'../service';import{loadStoredMasteryState,persistMasteryState}from'../repository';import{MASTERY_RANK_THRESHOLDS}from'../types';jest.mock('../repository');const TEST_USER_ID='test-user-123';describe('Mastery Service',()=>{beforeEach(()=>{jest.clearAllMocks();(loadStoredMasteryState as jest.Mock).mockReturnValue(null);});describe('getMasteryState',()=>{it('should return default state for new user',async()=>{const state=await getMasteryState(TEST_USER_ID);expect(state).toMatchObject({userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:0,purityMastery:0,consistencyMastery:0,comebackMastery:0,bossMastery:0},activeChallenges:expect.any(Array),unlockedFeatures:[]});expect(state.activeChallenges.length).toBe(3);expect(state.updatedAt).toBeDefined();});it('should load and hydrate existing state',async()=>{const existingState={userId:TEST_USER_ID,totalMasteryPoints:1500,rank:'ADEPT',techniques:{durationMastery:10,purityMastery:8,consistencyMastery:12,comebackMastery:5,bossMastery:3},activeChallenges:[{id:'challenge-1',title:'Deep Focus',description:'Complete a 60-minute session',technique:'durationMastery',target:100,current:80,status:'ACTIVE'}],unlockedFeatures:['advanced_analytics'],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(existingState);const state=await getMasteryState(TEST_USER_ID);expect(state.totalMasteryPoints).toBe(1500);expect(state.rank).toBe('ADEPT');expect(state.activeChallenges.length).toBe(3);});it('should correct rank if points threshold crossed',async()=>{const incorrectRankState={userId:TEST_USER_ID,totalMasteryPoints:MASTERY_RANK_THRESHOLDS.MASTER,rank:'ADEPT',techniques:{durationMastery:15,purityMastery:15,consistencyMastery:15,comebackMastery:10,bossMastery:10},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(incorrectRankState);const state=await getMasteryState(TEST_USER_ID);expect(state.rank).toBe('MASTER');});});describe('recordSessionMastery',()=>{it('should award XP for duration mastery',async()=>{const baseState={userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:0,purityMastery:0,consistencyMastery:0,comebackMastery:0,bossMastery:0},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(baseState);const result=await recordSessionMastery(TEST_USER_ID,{duration:60,purity:0.9,usedComeback:false,defeatedBoss:false});expect(result.techniqueGains.durationMastery).toBeGreaterThan(0);expect(result.totalPointsGained).toBeGreaterThan(0);expect(persistMasteryState).toHaveBeenCalled();});it('should award bonus XP for purity mastery',async()=>{const baseState={userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:5,purityMastery:5,consistencyMastery:5,comebackMastery:0,bossMastery:0},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(baseState);const result=await recordSessionMastery(TEST_USER_ID,{duration:25,purity:0.95,usedComeback:false,defeatedBoss:false});expect(result.techniqueGains.purityMastery).toBeGreaterThan(0);});it('should award comeback mastery when comeback used',async()=>{const baseState={userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:0,purityMastery:0,consistencyMastery:0,comebackMastery:0,bossMastery:0},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(baseState);const result=await recordSessionMastery(TEST_USER_ID,{duration:25,purity:0.8,usedComeback:true,defeatedBoss:false});expect(result.techniqueGains.comebackMastery).toBeGreaterThan(0);});it('should award boss mastery when boss defeated',async()=>{const baseState={userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:0,purityMastery:0,consistencyMastery:0,comebackMastery:0,bossMastery:0},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(baseState);const result=await recordSessionMastery(TEST_USER_ID,{duration:45,purity:0.85,usedComeback:false,defeatedBoss:true});expect(result.techniqueGains.bossMastery).toBeGreaterThan(0);});it('should rank up when threshold crossed',async()=>{const nearRankUpState={userId:TEST_USER_ID,totalMasteryPoints:MASTERY_RANK_THRESHOLDS.ADEPT-10,rank:'APPRENTICE',techniques:{durationMastery:8,purityMastery:8,consistencyMastery:8,comebackMastery:3,bossMastery:3},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(nearRankUpState);const result=await recordSessionMastery(TEST_USER_ID,{duration:60,purity:0.95,usedComeback:false,defeatedBoss:false});expect(result.newRank).toBe('ADEPT');expect(result.rankUpOccurred).toBe(true);});it('should cap technique values at 25',async()=>{const nearCapState={userId:TEST_USER_ID,totalMasteryPoints:2000,rank:'EXPERT',techniques:{durationMastery:24,purityMastery:24,consistencyMastery:24,comebackMastery:20,bossMastery:20},activeChallenges:[],unlockedFeatures:['advanced_analytics'],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(nearCapState);const result=await recordSessionMastery(TEST_USER_ID,{duration:90,purity:1.0,usedComeback:false,defeatedBoss:true});expect(result.techniqueGains.durationMastery).toBe(1);});});describe('claimMasteryChallenge',()=>{it('should claim completed challenge and award points',async()=>{const stateWithCompletedChallenge={userId:TEST_USER_ID,totalMasteryPoints:100,rank:'APPRENTICE',techniques:{durationMastery:5,purityMastery:5,consistencyMastery:5,comebackMastery:2,bossMastery:2},activeChallenges:[{id:'challenge-1',title:'Deep Focus',description:'Complete a 60-minute session',technique:'durationMastery',target:100,current:100,status:'COMPLETED',completedAt:Date.now()}],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(stateWithCompletedChallenge);const result=await claimMasteryChallenge(TEST_USER_ID,'challenge-1');expect(result.success).toBe(true);expect(result.pointsAwarded).toBeGreaterThan(0);expect(persistMasteryState).toHaveBeenCalled();});it('should throw error for incomplete challenge',async()=>{const stateWithIncompleteChallenge={userId:TEST_USER_ID,totalMasteryPoints:100,rank:'APPRENTICE',techniques:{durationMastery:5,purityMastery:5,consistencyMastery:5,comebackMastery:2,bossMastery:2},activeChallenges:[{id:'challenge-1',title:'Deep Focus',description:'Complete a 60-minute session',technique:'durationMastery',target:100,current:50,status:'ACTIVE'}],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(stateWithIncompleteChallenge);await expect(claimMasteryChallenge(TEST_USER_ID,'challenge-1')).rejects.toThrow('Challenge not completed');});it('should throw error for non-existent challenge',async()=>{const stateWithNoChallenges={userId:TEST_USER_ID,totalMasteryPoints:100,rank:'APPRENTICE',techniques:{durationMastery:5,purityMastery:5,consistencyMastery:5,comebackMastery:2,bossMastery:2},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(stateWithNoChallenges);await expect(claimMasteryChallenge(TEST_USER_ID,'non-existent')).rejects.toThrow('Challenge not found');});});describe('getActiveChallenges',()=>{it('should return all active challenges',async()=>{const stateWithChallenges={userId:TEST_USER_ID,totalMasteryPoints:500,rank:'ADEPT',techniques:{durationMastery:10,purityMastery:8,consistencyMastery:12,comebackMastery:5,bossMastery:5},activeChallenges:[{id:'c1',title:'Challenge 1',status:'ACTIVE'},{id:'c2',title:'Challenge 2',status:'ACTIVE'},{id:'c3',title:'Challenge 3',status:'COMPLETED'}],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(stateWithChallenges);const challenges=await getActiveChallenges(TEST_USER_ID);expect(challenges.length).toBe(2);expect(challenges.every(c=>c.status==='ACTIVE')).toBe(true);});});describe('getTechniqueStats',()=>{it('should return technique levels and next milestones',async()=>{const stateWithTechniques={userId:TEST_USER_ID,totalMasteryPoints:1000,rank:'EXPERT',techniques:{durationMastery:15,purityMastery:12,consistencyMastery:18,comebackMastery:8,bossMastery:10},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(stateWithTechniques);const stats=await getTechniqueStats(TEST_USER_ID);expect(stats.durationMastery.current).toBe(15);expect(stats.durationMastery.max).toBe(25);expect(stats.durationMastery.nextMilestone).toBe(20);expect(stats.consistencyMastery.isPrimary).toBe(true);});});describe('getUnlockedFeatures',()=>{it('should return unlocked features for rank',async()=>{const expertState={userId:TEST_USER_ID,totalMasteryPoints:MASTERY_RANK_THRESHOLDS.EXPERT,rank:'EXPERT',techniques:{durationMastery:15,purityMastery:15,consistencyMastery:15,comebackMastery:10,bossMastery:10},activeChallenges:[],unlockedFeatures:['advanced_analytics','custom_themes'],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(expertState);const features=await getUnlockedFeatures(TEST_USER_ID);expect(features).toContain('advanced_analytics');expect(features).toContain('custom_themes');});it('should unlock new features on rank up',async()=>{const apprenticeState={userId:TEST_USER_ID,totalMasteryPoints:0,rank:'APPRENTICE',techniques:{durationMastery:0,purityMastery:0,consistencyMastery:0,comebackMastery:0,bossMastery:0},activeChallenges:[],unlockedFeatures:[],updatedAt:Date.now()};(loadStoredMasteryState as jest.Mock).mockReturnValue(apprenticeState);const features=await getUnlockedFeatures(TEST_USER_ID);expect(features).toEqual(expect.arrayContaining(['basic_stats']));});});});

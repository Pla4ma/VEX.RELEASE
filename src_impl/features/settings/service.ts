import{z}from'zod'; import*as repository from'./repository'; import{withRetry,CircuitBreaker,TTLCache,classifyError}from'../../shared/hardening'; import{eventBus}from'../../events'; import*as Sentry from'@sentry/react-native'; import{type Setting,type UserPreferences,type NotificationSettings,type CoachSettings,type AppearanceSettings,type PrivacySettings,type DataControlSettings,type SyncState,type SyncConflict,type SettingCategory,type SettingValue,type SettingsExport}from'./types'; const syncCircuitBreaker = new CircuitBreaker({failureThreshold:5,recoveryTimeoutMs:60000,halfOpenMaxCalls:3}); const settingsCache = new TTLCache<Setting>(300000); const preferencesCache = new TTLCache<UserPreferences>(300000); const SYNC_RETRY_CONFIG = {maxAttempts:3,baseDelayMs:1000,retryableErrors:['network_error','timeout','server_error']}; export async function getSetting(userId:string,key:string):Promise<Setting|null>{const cacheKey = `${userId}:${key}`; const cached = settingsCache.get(cacheKey); if(cached){return cached;}try{const setting = (await withRetry(()=>repository.fetchSetting(userId,key),SYNC_RETRY_CONFIG))as Setting|null; if(setting){settingsCache.set(cacheKey,setting);}return setting;}catch(error){Sentry.captureException(error,{tags:{operation:'getSetting'},extra:{userId,key}}); throw error;}}export async function getAllSettings(userId:string):Promise<Setting[]>{try{const settings = (await withRetry(()=>repository.fetchAllSettings(userId),SYNC_RETRY_CONFIG))as Setting[]; settings.forEach((setting:Setting)=>{settingsCache.set(`${userId}:${setting.key}`,setting);}); return settings;}catch(error){Sentry.captureException(error,{tags:{operation:'getAllSettings'},extra:{userId}}); throw error;}}export async function updateSetting(userId:string,key:string,value:SettingValue,category:SettingCategory,options:{skipSync?:boolean;deviceId?:string} = {}):Promise<Setting>{const{skipSync = false,deviceId} = options; try{const oldSetting = await getSetting(userId,key); const oldValue = oldSetting?.value ?? null; const validation = validateSettingValue(key,value,category); if(!validation.valid){throw new SettingsValidationError(`Invalid setting value: ${validation.errors.join(', ')}`,key,validation.errors);}const sanitizedValue = validation.sanitized ?? value; const updated = (await withRetry(()=>repository.upsertSetting({userId,key,value:sanitizedValue,category,lastModified:Date.now(),deviceId}),SYNC_RETRY_CONFIG))as Setting; settingsCache.set(`${userId}:${key}`,updated); eventBus.publish('settings:change',{key,value:sanitizedValue,previousValue:oldValue}as{key:string;value:unknown;previousValue?:unknown}); Sentry.addBreadcrumb({category:'settings',message:`Setting updated: ${key}`,level:'info',data:{userId,key,category}}); if(!skipSync){syncSettings(userId).catch(err=>{Sentry.captureException(err,{tags:{operation:'backgroundSync'},extra:{userId}});});}return updated;}catch(error){if(error instanceof SettingsValidationError){throw error;}Sentry.captureException(error,{tags:{operation:'updateSetting'},extra:{userId,key,category}}); throw error;}}export async function batchUpdateSettings(userId:string,updates:Array<{key:string;value:SettingValue;category:SettingCategory}>,options:{skipSync?:boolean} = {}):Promise<Setting[]>{const{skipSync = false} = options; const results:Setting[] = []; const errors:Array<{key:string;error:string}> = []; for(const update of updates){const validation = validateSettingValue(update.key,update.value,update.category); if(!validation.valid){errors.push({key:update.key,error:validation.errors.join(', ')});}}if(errors.length > 0){throw new SettingsValidationError(`Batch validation failed: ${errors.map(e=>`${e.key}: ${e.error}`).join('; ')}`,'batch',errors.map(e=>e.error));}try{const result = (await withRetry(()=>repository.batchUpsertSettings(updates.map(u=>({userId,key:u.key,value:u.value,category:u.category,lastModified:Date.now()}))),SYNC_RETRY_CONFIG))as Setting[]; result.forEach(setting=>{settingsCache.set(`${userId}:${setting.key}`,setting);}); updates.forEach(update=>{eventBus.publish('settings:change',{category:update.category,key:update.key,value:update.value,previousValue:null});}); if(!skipSync){syncSettings(userId).catch(err=>{Sentry.captureException(err,{tags:{operation:'backgroundBatchSync'},extra:{userId}});});}return result;}catch(error){Sentry.captureException(error,{tags:{operation:'batchUpdateSettings'},extra:{userId,count:updates.length}}); throw error;}}export async function deleteSetting(userId:string,key:string):Promise<boolean>{try{await withRetry(()=>repository.deleteSetting(userId,key),SYNC_RETRY_CONFIG); settingsCache.delete(`${userId}:${key}`); eventBus.publish('settings:change',{key,value:null,previousValue:null}as{key:string;value:unknown;previousValue?:unknown}); return true;}catch(error){Sentry.captureException(error,{tags:{operation:'deleteSetting'},extra:{userId,key}}); throw error;}}export async function getUserPreferences(userId:string):Promise<UserPreferences>{const cached = preferencesCache.get(userId); if(cached){return cached;}try{const settings = await getAllSettings(userId); const preferences:UserPreferences = {userId,version:1,settings:{},createdAt:Date.now(),updatedAt:Date.now()}; settings.forEach(setting=>{preferences.settings[setting.key] = setting;}); preferencesCache.set(userId,preferences); return preferences;}catch(error){Sentry.captureException(error,{tags:{operation:'getUserPreferences'},extra:{userId}}); throw error;}}export async function syncSettings(userId:string,options:{force?:boolean;direction?:'up'|'down'|'both'} = {}):Promise<SyncState>{const{force = false,direction = 'both'} = options; try{const result = await syncCircuitBreaker.execute(async()=>{const syncState = await repository.fetchSyncState(userId); if(!force && syncState?.pendingChanges === 0 && direction !== 'down'){return{userId,status:'idle'as const,lastSyncAttempt:Date.now(),lastSuccessfulSync:syncState.lastSuccessfulSync,pendingChanges:0,conflicts:[]};}let conflicts:SyncConflict[] = []; if(direction === 'up' || direction === 'both'){const localChanges = await repository.fetchPendingChanges(userId); if(localChanges.length > 0){const pushResult = (await withRetry(()=>repository.pushChanges(userId,localChanges),SYNC_RETRY_CONFIG))as{success:boolean;conflicts:SyncConflict[]}; conflicts.push(...pushResult.conflicts);}}if(direction === 'down' || direction === 'both'){const remoteChanges = (await withRetry(()=>repository.fetchRemoteChanges(userId,syncState?.lastSuccessfulSync),SYNC_RETRY_CONFIG))as Array<{key:string;value:unknown;category:SettingCategory;timestamp:number}>; if(remoteChanges.length > 0){await repository.applyRemoteChanges(userId,remoteChanges); remoteChanges.forEach((change:{key:string})=>{settingsCache.delete(`${userId}:${change.key}`);}); preferencesCache.delete(userId);}}if(conflicts.length > 0){for(const conflict of conflicts){const resolution = resolveConflict(conflict); await repository.resolveConflict(userId,conflict.id,resolution);}}const updatedSyncState:SyncState = {userId,status:conflicts.length > 0 ? 'conflict' : 'idle',lastSyncAttempt:Date.now(),lastSuccessfulSync:Date.now(),pendingChanges:0,conflicts}; await repository.updateSyncState(userId,updatedSyncState); eventBus.publish('network:sync:complete',{synced:1,failed:conflicts.length}); return updatedSyncState;}); return result;}catch(error){const errorInfo = classifyError(error as Error); Sentry.captureException(error as Error,{tags:{operation:'syncSettings'},extra:{userId,direction,errorType:errorInfo.type}}); return{userId,status:'error',lastSyncAttempt:Date.now(),pendingChanges:0,conflicts:[],errorMessage:error instanceof Error ? error.message : 'Sync failed'};}}export async function getNotificationSettings(userId:string):Promise<NotificationSettings>{const settings = await getAllSettings(userId); return buildNotificationSettings(userId,settings);}export async function updateNotificationSettings(userId:string,settings:Partial<NotificationSettings>):Promise<NotificationSettings>{const updates:Array<{key:string;value:SettingValue;category:SettingCategory}> = []; if(settings.channels?.push){updates.push({key:'notifications.push.enabled',value:settings.channels.push.enabled,category:'notifications'},{key:'notifications.push.quietHoursStart',value:settings.channels.push.quietHoursStart ?? null,category:'notifications'},{key:'notifications.push.quietHoursEnd',value:settings.channels.push.quietHoursEnd ?? null,category:'notifications'},{key:'notifications.push.timezone',value:settings.channels.push.timezone,category:'notifications'});}if(settings.channels?.email){updates.push({key:'notifications.email.enabled',value:settings.channels.email.enabled,category:'notifications'},{key:'notifications.email.digestFrequency',value:settings.channels.email.digestFrequency,category:'notifications'});}await batchUpdateSettings(userId,updates); return getNotificationSettings(userId);}export async function getCoachSettings(userId:string):Promise<CoachSettings>{const settings = await getAllSettings(userId); return buildCoachSettings(userId,settings);}export async function updateCoachSettings(userId:string,settings:Partial<CoachSettings>):Promise<CoachSettings>{const updates:Array<{key:string;value:SettingValue;category:SettingCategory}> = []; if(settings.enabled !== undefined){updates.push({key:'coach.enabled',value:settings.enabled,category:'coach'});}if(settings.personality){updates.push({key:'coach.personality',value:settings.personality,category:'coach'});}if(settings.frequency){updates.push({key:'coach.frequency',value:settings.frequency,category:'coach'});}await batchUpdateSettings(userId,updates); return getCoachSettings(userId);}export async function getAppearanceSettings(userId:string):Promise<AppearanceSettings>{const settings = await getAllSettings(userId); return buildAppearanceSettings(userId,settings);}export async function updateAppearanceSettings(userId:string,settings:Partial<AppearanceSettings>):Promise<AppearanceSettings>{const updates:Array<{key:string;value:SettingValue;category:SettingCategory}> = []; if(settings.theme){updates.push({key:'appearance.theme',value:settings.theme,category:'appearance'});}if(settings.accentColor){updates.push({key:'appearance.accentColor',value:settings.accentColor,category:'appearance'});}if(settings.fontScale !== undefined){updates.push({key:'appearance.fontScale',value:settings.fontScale,category:'appearance'});}await batchUpdateSettings(userId,updates); return getAppearanceSettings(userId);}export async function getPrivacySettings(userId:string):Promise<PrivacySettings>{const settings = await getAllSettings(userId); return buildPrivacySettings(userId,settings);}export async function updatePrivacySettings(userId:string,settings:Partial<PrivacySettings>):Promise<PrivacySettings>{const updates:Array<{key:string;value:SettingValue;category:SettingCategory}> = []; if(settings.profileVisibility){updates.push({key:'privacy.profileVisibility',value:settings.profileVisibility,category:'privacy'});}if(settings.showOnlineStatus !== undefined){updates.push({key:'privacy.showOnlineStatus',value:settings.showOnlineStatus,category:'privacy'});}if(settings.analyticsOptOut !== undefined){updates.push({key:'privacy.analyticsOptOut',value:settings.analyticsOptOut,category:'privacy'});}await batchUpdateSettings(userId,updates); return getPrivacySettings(userId);}export async function exportSettings(userId:string):Promise<SettingsExport>{const[preferences,notificationSettings,coachSettings,appearanceSettings,privacySettings] = await Promise.all([getUserPreferences(userId),getNotificationSettings(userId),getCoachSettings(userId),getAppearanceSettings(userId),getPrivacySettings(userId)]); return{version:1,exportedAt:Date.now(),userId,preferences,notificationSettings,coachSettings,appearanceSettings,privacySettings,dataControlSettings:{userId,retentionPolicy:'standard',autoExport:{enabled:false,frequency:'never',format:'json'},backupEnabled:false}};}export async function importSettings(userId:string,exportData:SettingsExport):Promise<{imported:number;errors:string[]}>{const errors:string[] = []; let imported = 0; if(exportData.version !== 1){throw new SettingsValidationError(`Unsupported export version: ${exportData.version}`,'version',['Invalid export format']);}try{const settings = Object.values(exportData.preferences.settings); await batchUpdateSettings(userId,settings.map(s=>({key:s.key,value:s.value,category:s.category}))); imported = settings.length; Sentry.addBreadcrumb({category:'settings',message:`Settings imported: ${imported} settings`,level:'info',data:{userId}}); return{imported,errors};}catch(error){const message = error instanceof Error ? error.message : 'Import failed'; errors.push(message); Sentry.captureException(error,{tags:{operation:'importSettings'},extra:{userId}}); return{imported,errors};}}export async function resetSettings(userId:string,category?:SettingCategory):Promise<void>{try{await withRetry(()=>repository.resetSettings(userId,category),SYNC_RETRY_CONFIG); if(category){const allSettings = await getAllSettings(userId); allSettings.filter(s=>s.category === category).forEach(s=>settingsCache.delete(`${userId}:${s.key}`));}else{settingsCache.clear(); preferencesCache.delete(userId);}eventBus.publish('settings:reset',{category});}catch(error){Sentry.captureException(error,{tags:{operation:'resetSettings'},extra:{userId,category}}); throw error;}}function validateSettingValue(key:string,value:SettingValue,category:SettingCategory):{valid:boolean;errors:string[];sanitized?:SettingValue}{const errors:string[] = []; if(value === undefined){errors.push(`Value cannot be undefined for key: ${key}`);}switch(category){case'notifications':if(key.includes('frequency') && typeof value === 'string'){const validFrequencies = ['immediate','daily','weekly','never']; if(!validFrequencies.includes(value)){errors.push(`Invalid frequency: ${value}`);}}if(key.includes('quietHours') && value !== null){const hour = Number(value); if(hour < 0 || hour > 23){errors.push('Hour must be between 0 and 23');}}break; case'appearance':if(key.includes('fontScale') && typeof value === 'number'){if(value < 0.5 || value > 2){errors.push('Font scale must be between 0.5 and 2.0');}}if(key.includes('theme') && typeof value === 'string'){const validThemes = ['light','dark','system','high-contrast']; if(!validThemes.includes(value)){errors.push(`Invalid theme: ${value}`);}}break; case'coach':if(key.includes('frequency') && typeof value === 'string'){const validFrequencies = ['low','medium','high']; if(!validFrequencies.includes(value)){errors.push(`Invalid frequency: ${value}`);}}break;}return{valid:errors.length === 0,errors,sanitized:errors.length === 0 ? value : undefined};}function resolveConflict(conflict:SyncConflict):'local'|'remote'|'merge'{if(conflict.localTimestamp > conflict.remoteTimestamp){return'local';}return'remote';}function buildNotificationSettings(userId:string,settings:Setting[]):NotificationSettings{const getValue = (key:string,defaultValue:unknown)=>{const setting = settings.find(s=>s.key === key); return setting?.value ?? defaultValue;}; return{userId,channels:{push:{enabled:getValue('notifications.push.enabled',true)as boolean,deviceTokens:getValue('notifications.push.deviceTokens',[])as string[],quietHoursStart:getValue('notifications.push.quietHoursStart',undefined)as number|undefined,quietHoursEnd:getValue('notifications.push.quietHoursEnd',undefined)as number|undefined,timezone:getValue('notifications.push.timezone','UTC')as string},email:{enabled:getValue('notifications.email.enabled',true)as boolean,email:getValue('notifications.email.address','')as string,digestFrequency:getValue('notifications.email.digestFrequency','daily')as'immediate'|'daily'|'weekly'|'never'},inApp:{enabled:getValue('notifications.inApp.enabled',true)as boolean,soundEnabled:getValue('notifications.inApp.soundEnabled',true)as boolean,vibrationEnabled:getValue('notifications.inApp.vibrationEnabled',true)as boolean}},preferences:{critical:{enabled:true,channels:['push','email','in_app']},high:{enabled:true,channels:['push','in_app']},normal:{enabled:true,channels:['in_app']},low:{enabled:false,channels:[]}},customRules:[]};}function buildCoachSettings(userId:string,settings:Setting[]):CoachSettings{const getValue = (key:string,defaultValue:unknown)=>{const setting = settings.find(s=>s.key === key); return setting?.value ?? defaultValue;}; return{userId,enabled:getValue('coach.enabled',true)as boolean,personality:getValue('coach.personality','supportive')as'supportive'|'tough'|'neutral'|'funny',frequency:getValue('coach.frequency','moderate')as'minimal'|'moderate'|'frequent'|'constant',messageTypes:{streakReminders:getValue('coach.messageTypes.streakReminders',true)as boolean,sessionTips:getValue('coach.messageTypes.sessionTips',true)as boolean,milestoneCelebrations:getValue('coach.messageTypes.milestoneCelebrations',true)as boolean,encouragement:getValue('coach.messageTypes.encouragement',true)as boolean,challenges:getValue('coach.messageTypes.challenges',true)as boolean},quietHours:{enabled:getValue('coach.quietHours.enabled',false)as boolean,start:getValue('coach.quietHours.start','22:00')as string,end:getValue('coach.quietHours.end','08:00')as string,timezone:getValue('coach.quietHours.timezone','UTC')as string},customTriggers:[]};}function buildAppearanceSettings(userId:string,settings:Setting[]):AppearanceSettings{const getValue = (key:string,defaultValue:unknown)=>{const setting = settings.find(s=>s.key === key); return setting?.value ?? defaultValue;}; return{userId,theme:getValue('appearance.theme','system')as'light'|'dark'|'system',accentColor:getValue('appearance.accentColor','theme.colors.primary[500]')as string,fontScale:getValue('appearance.fontScale',1)as number,useSystemFont:getValue('appearance.useSystemFont',true)as boolean,reduceMotion:getValue('appearance.reduceMotion',false)as boolean,highContrast:getValue('appearance.highContrast',false)as boolean,compactMode:getValue('appearance.compactMode',false)as boolean};}function buildPrivacySettings(userId:string,settings:Setting[]):PrivacySettings{const getValue = (key:string,defaultValue:unknown)=>{const setting = settings.find(s=>s.key === key); return setting?.value ?? defaultValue;}; return{userId,profileVisibility:getValue('privacy.profileVisibility','friends')as'public'|'friends'|'private',showOnlineStatus:getValue('privacy.showOnlineStatus',true)as boolean,showActivityStatus:getValue('privacy.showActivityStatus',true)as boolean,allowDataAnalysis:getValue('privacy.allowDataAnalysis',true)as boolean,allowPersonalization:getValue('privacy.allowPersonalization',true)as boolean,thirdPartySharing:getValue('privacy.thirdPartySharing',false)as boolean,analyticsOptOut:getValue('privacy.analyticsOptOut',false)as boolean};}export class SettingsValidationError extends Error{constructor(message:string,public key:string,public validationErrors:string[]){super(message); this.name = 'SettingsValidationError';}}

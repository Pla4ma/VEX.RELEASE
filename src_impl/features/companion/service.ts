import{CompanionMood,CompanionPhase,CompanionSessionEvent,CompanionState}from'./types'; import{CompanionGrowthService}from'./growth-service'; import{getDefaultStorageAdapter}from'../../persistence/MMKVStorageAdapter'; import{getEconomyService}from'../../economy/EconomyService'; import{z}from'zod'; export class CompanionService{private state:CompanionState|null = null; private eventListeners:Array<(event:CompanionSessionEvent)=>void> = []; private lastMilestone = 0; private currentStreakSeconds = 0; private growthService:CompanionGrowthService; constructor(initialState?:CompanionState){if(initialState){this.state = {...initialState};}this.growthService = new CompanionGrowthService(this.state || undefined);}startSession(_sessionDurationMinutes?:number):void{if(!this.state){return;}this.state.currentMood = 'SLEEPY'; this.state.sessionProgress = 0; this.state.energyLevel = 50; this.lastMilestone = 0; this.currentStreakSeconds = 0; this.emitEvent({type:'MOOD_SHIFT',timestamp:Date.now(),data:{mood:'SLEEPY',message:'Your companion awakens...'}});}tick(elapsedSeconds:number,totalDurationSeconds:number,purityScore:number,isPaused:boolean):void{if(!this.state){return;}const progress = elapsedSeconds / totalDurationSeconds * 100; this.state.sessionProgress = progress; this.state.purityScore = purityScore; if(!isPaused){this.state.energyLevel = Math.max(0,Math.min(100,this.state.energyLevel + (purityScore > 80 ? 0.2 : purityScore > 50 ? 0.1 : -0.3))); this.currentStreakSeconds = purityScore > 80 ? this.currentStreakSeconds + 1 : 0;}const nextMood = this.calculateMood(progress,this.state.energyLevel,purityScore); if(nextMood !== this.state.currentMood){this.state.currentMood = nextMood; this.emitEvent({type:'MOOD_SHIFT',timestamp:Date.now(),data:{mood:nextMood,message:this.getMoodMessage(nextMood)}});}const milestone = Math.floor(progress / 10) * 10; if(milestone > this.lastMilestone && milestone > 0){this.lastMilestone = milestone; this.emitEvent({type:'MILESTONE',timestamp:Date.now(),data:{progressDelta:milestone,message:`${milestone}% - Your companion grows stronger!`}});}}completeSession(sessionMinutes:number,finalPurity:number,userId?:string,sessionId?:string):{leveledUp:boolean;evolved:boolean;newPhase?:CompanionPhase;}{if(!this.state){return{leveledUp:false,evolved:false};}this.growthService.updateState(this.state); const result = this.growthService.processSessionCompletion(sessionMinutes,finalPurity,userId,sessionId); const updatedState = this.growthService.getState(); if(updatedState){this.state = updatedState;}return result;}getState():CompanionState|null{return this.state ? {...this.state} : null;}onEvent(callback:(event:CompanionSessionEvent)=>void):()=>void{this.eventListeners.push(callback); return()=>{this.eventListeners = this.eventListeners.filter(listener=>listener !== callback);};}reactToStreakMaintained(userId:string):void{if(!this.state){return;}this.growthService.updateState(this.state); this.growthService.reactToStreakMaintained(userId); const updatedState = this.growthService.getState(); if(updatedState){this.state = updatedState;}this.emitEvent({type:'MILESTONE',timestamp:Date.now(),data:{progressDelta:100,message:'Your companion celebrates your streak consistency!',intensity:0.8}});}reactToComebackCompleted(userId:string):void{if(!this.state){return;}this.growthService.updateState(this.state); this.growthService.reactToComebackCompleted(userId); const updatedState = this.growthService.getState(); if(updatedState){this.state = updatedState;}this.emitEvent({type:'MILESTONE',timestamp:Date.now(),data:{progressDelta:100,message:'Your companion admires your resilience!',intensity:0.9}});}reactToFocusScoreChanged(userId:string,previousScore:number,newScore:number):void{if(!this.state){return;}this.growthService.updateState(this.state); this.growthService.reactToFocusScoreChanged(userId,previousScore,newScore); const updatedState = this.growthService.getState(); if(updatedState){this.state = updatedState;}this.emitEvent({type:newScore > previousScore ? 'GROWTH_PULSE' : 'MOOD_SHIFT',timestamp:Date.now(),data:{mood:this.state.currentMood,message:newScore > previousScore ? 'Your companion feels your focus growing!' : 'Your companion senses the challenge.',intensity:0.6}});}reactToDailyMissionCompleted(userId:string):void{if(!this.state){return;}this.growthService.updateState(this.state); this.growthService.reactToDailyMissionCompleted(userId); const updatedState = this.growthService.getState(); if(updatedState){this.state = updatedState;}this.emitEvent({type:'MILESTONE',timestamp:Date.now(),data:{progressDelta:100,message:'Your companion nods with approval at your daily progress!',intensity:0.5}});}private calculateMood(progress:number,energy:number,purity:number):CompanionMood{if(purity < 30 && energy < 20){return'DANGER';}if(energy < 30){return'STRUGGLING';}if(progress > 95 && energy > 80 && purity > 90){return'ECSTATIC';}if(progress > 70 && energy > 60){return'DETERMINED';}if(progress > 30 && energy > 50 && purity > 70){return'FOCUSED';}if(progress > 10 && energy > 30){return'CONTENT';}return'SLEEPY';}private getMoodMessage(mood:CompanionMood):string{return{SLEEPY:'Your companion stirs...',CONTENT:'Your companion is at peace.',FOCUSED:'Your companion focuses with you.',DETERMINED:'Your companion pushes forward!',ECSTATIC:'Your companion radiates pure energy!',STRUGGLING:'Your companion needs your focus...',DANGER:'Your companion is fading! Stay focused!'}[mood];}private emitEvent(event:CompanionSessionEvent):void{for(const callback of this.eventListeners){callback(event);}}}let companionServiceInstance:CompanionService|null = null; export function getCompanionService(initialState?:CompanionState):CompanionService{if(!companionServiceInstance){companionServiceInstance = new CompanionService(initialState);}return companionServiceInstance;}export function resetCompanionService():void{companionServiceInstance = null;}const _profileSchema = z.object({id:z.string(),name:z.string(),type:z.string(),mood:z.enum(['happy','neutral','sad','starving']),level:z.number().min(1),xp:z.number().min(0),lastFedAt:z.number(),lastPettedAt:z.number().nullable(),specialAbilityCharge:z.number().min(0),equippedItems:z.array(z.string()),unlockedAbilities:z.array(z.enum(['xp_boost_5pct','coin_boost_10pct','streak_protection']))}); type _CompanionProfile=z.infer<typeof _profileSchema>;function _profileKey(userId:string):string{return`companion_profile_${userId}`;}function _getMoodFromFeedTime(lastFedAt:number):_CompanionProfile['mood']{const hours = (Date.now() - lastFedAt) / (1000 * 60 * 60); if(hours < 12){return'happy';}if(hours < 24){return'neutral';}if(hours < 48){return'sad';}return'starving';}function _getDefaultProfile(userId:string):_CompanionProfile{return{id:`companion_${userId}`,name:'Vexling',type:'focus_wisp',mood:'happy',level:1,xp:0,lastFedAt:Date.now(),lastPettedAt:null,specialAbilityCharge:0,equippedItems:[],unlockedAbilities:[]};}async function _loadProfile(userId:string):Promise<_CompanionProfile>{const storage = getDefaultStorageAdapter(); const raw = await storage.getItem(_profileKey(userId)); if(!raw){return _getDefaultProfile(userId);}const parsed = _profileSchema.parse(JSON.parse(raw)); return{...parsed,mood:_getMoodFromFeedTime(parsed.lastFedAt)};}async function _saveProfile(userId:string,profile:_CompanionProfile):Promise<_CompanionProfile>{const storage = getDefaultStorageAdapter(); const next = {...profile,mood:_getMoodFromFeedTime(profile.lastFedAt)}; await storage.setItem(_profileKey(userId),JSON.stringify(next)); return next;}function _getAbilityUnlocks(level:number):_CompanionProfile['unlockedAbilities']{return[...(level >= 5 ? ['xp_boost_5pct']as const : []),...(level >= 10 ? ['coin_boost_10pct']as const : []),...(level >= 20 ? ['streak_protection']as const : [])];}export async function getCompanion(userId:string):Promise<_CompanionProfile>{return _loadProfile(userId);}export async function feedCompanion(userId:string,options:{skipSyncEnqueue?:boolean;} = {}):Promise<_CompanionProfile>{void options; const economy = getEconomyService(); await economy.spendCurrency(userId,'COINS',10,'companion_feed'); const current = await _loadProfile(userId); return _saveProfile(userId,{...current,xp:current.xp + 50,lastFedAt:Date.now(),mood:'happy'});}export async function petCompanion(userId:string):Promise<_CompanionProfile>{const current = await _loadProfile(userId); return _saveProfile(userId,{...current,lastPettedAt:Date.now()});}export async function levelUpCompanion(userId:string):Promise<_CompanionProfile>{const current = await _loadProfile(userId); const nextLevel = Math.max(1,Math.floor(current.xp / 500) + 1); return _saveProfile(userId,{...current,level:nextLevel,unlockedAbilities:_getAbilityUnlocks(nextLevel)});}type SessionEvent={type:'session_complete';duration:number;quality:'good'|'average'|'poor';}|{type:'streak_milestone';streakDays:number;};export async function processSessionEvent(userId:string,event:SessionEvent):Promise<_CompanionProfile>{const current = await _loadProfile(userId); let xpGain = 0; let chargeGain = 0; if(event.type === 'session_complete'){const qualityMultiplier = {good:1.5,average:1.0,poor:0.5}[event.quality] ?? 1; xpGain = Math.floor(event.duration * 4 * qualityMultiplier);}else if(event.type === 'streak_milestone'){chargeGain = Math.min(50,event.streakDays * 5);}return _saveProfile(userId,{...current,xp:current.xp + xpGain,specialAbilityCharge:Math.min(100,current.specialAbilityCharge + chargeGain)});}export async function getCompanionBonuses(userId:string):Promise<{xpBoost:number;coinBoost:number;streakProtection:boolean;}>{const profile = await _loadProfile(userId); return{xpBoost:profile.unlockedAbilities.includes('xp_boost_5pct') ? 0.05 : 0,coinBoost:profile.unlockedAbilities.includes('coin_boost_10pct') ? 0.1 : 0,streakProtection:profile.unlockedAbilities.includes('streak_protection')};}export async function canUseSpecialAbility(userId:string):Promise<boolean>{const profile = await _loadProfile(userId); return profile.specialAbilityCharge >= 100;}export async function useSpecialAbility(userId:string):Promise<{success:boolean;effect:string;}>{const profile = await _loadProfile(userId); if(profile.specialAbilityCharge < 100){throw new Error('Special ability not charged');}await _saveProfile(userId,{...profile,specialAbilityCharge:0}); return{success:true,effect:'xp_boost'};}

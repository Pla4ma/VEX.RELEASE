import{eventBus}from'../../events/EventBus'; import*as Sentry from'@sentry/react-native'; interface CurrencyTransaction{userId:string;currency:string;type:'earn'|'spend';amount:number;source:string;itemId?:string;}interface ShopOwnership{userId:string;itemId:string;price:{currency:'COINS'|'GEMS'|'SEASONAL';amount:number;};}export function initializeEconomyFeedIntegration():()=>void{const handlers:Array<()=>void> = []; handlers.push(eventBus.subscribe('economy:transaction',async(event:CurrencyTransaction)=>{if(!event || !event.userId){return;}try{Sentry.addBreadcrumb({category:'economy:transaction',message:`${event.currency} ${event.type}: ${Math.abs(event.amount)}`,data:{userId:event.userId,type:event.currency,amount:event.amount,source:event.source},level:'info'}); if(event.amount > 0){eventBus.publish('seasons:challenge_progress',{userId:event.userId,challengeId:'currency_earned',progress:event.amount,completed:false});}if(event.amount < 0){checkSpendingMilestones(event.userId,event.currency,Math.abs(event.amount));}if(event.currency === 'XP' && event.amount > 0){eventBus.publish('progression:add_xp',{userId:event.userId,amount:event.amount,source:event.source});}if(Math.abs(event.amount) >= 1000){eventBus.publish('social:activity',{userId:event.userId,activityType:event.amount > 0 ? 'BIG_EARN' : 'BIG_SPEND',visibility:'FRIENDS',data:{currency:event.currency,amount:Math.abs(event.amount)}});}}catch(error){Sentry.captureException(error,{tags:{operation:'economy:transaction'},extra:{userId:event.userId,currency:event.currency,amount:event.amount,type:event.type,source:event.source}});}})); handlers.push(eventBus.subscribe('economy:purchase',async(event:ShopOwnership)=>{if(!event || !event.userId){return;}try{await recordShopOwnership(event); eventBus.publish('seasons:challenge_progress',{userId:event.userId,challengeId:'shop_purchase',progress:1,completed:false}); await applyItemEffect(event); if(isRareItem(event.itemId)){eventBus.publish('social:activity',{userId:event.userId,activityType:'RARE_ITEM_ACQUIRED',visibility:'PUBLIC',data:{itemId:event.itemId,itemType:getShopItemType(event.itemId)}});}Sentry.addBreadcrumb({category:'economy:purchase',message:`Shop purchase: ${event.itemId}`,data:{userId:event.userId,itemId:event.itemId,priceAmount:event.price.amount,currency:event.price.currency},level:'info'});}catch(error){Sentry.captureException(error,{tags:{operation:'economy:purchase'},extra:{userId:event.userId,itemId:event.itemId,priceAmount:event.price.amount,currency:event.price.currency}});}})); handlers.push(eventBus.subscribe('events:reward_earned',async event=>{if(!event || !event.userId){return;}const bonusMultiplier = await getEventBonusMultiplier(event.userId,event.eventId); if(bonusMultiplier > 1){const rewardType = event.rewardType === 'XP' ? 'COINS' : event.rewardType as'COINS'|'GEMS'|'SEASONAL'; eventBus.publish('economy:add_currency',{userId:event.userId,type:rewardType,amount:Math.floor(event.amount * bonusMultiplier),source:'EVENT_BONUS'});}})); return()=>handlers.forEach(unsub=>unsub());}async function recordShopOwnership(ownership:ShopOwnership):Promise<void>{Sentry.addBreadcrumb({category:'economy-feed',message:`Recording shop ownership for ${ownership.itemId}`,level:'info',data:{userId:ownership.userId,itemId:ownership.itemId,itemType:getShopItemType(ownership.itemId),currency:ownership.price.currency,price:ownership.price.amount}});}async function applyItemEffect(ownership:ShopOwnership):Promise<void>{switch(getShopItemType(ownership.itemId)){case'BOOST':eventBus.publish('progression:activate_boost',{userId:ownership.userId,boostType:'XP',multiplier:1.5,duration:24 * 60 * 60 * 1000}); break; case'SHIELD':eventBus.publish('streaks:add_shield',{userId:ownership.userId,shieldType:'STREAK_PROTECTION',duration:24 * 60 * 60 * 1000}); break; case'THEME':eventBus.publish('cosmetics:unlock_theme',{userId:ownership.userId,themeId:ownership.itemId,source:'SHOP_PURCHASE'}); break;}}async function checkSpendingMilestones(userId:string,currency:string,amount:number):Promise<void>{const key = `spending:${userId}:${currency}`; const current = await getCumulativeSpending(key); const newTotal = current + amount; await setCumulativeSpending(key,newTotal); const milestones = [100,500,1000,5000,10000]; for(const milestone of milestones){if(current < milestone && newTotal >= milestone){eventBus.publish('milestones:spending_reached',{userId,currency,milestoneId:`${currency}-${milestone}`,milestone:String(milestone),totalSpent:newTotal});}}}async function getCumulativeSpending(key:string):Promise<number>{return 0;}async function setCumulativeSpending(key:string,value:number):Promise<void>{Sentry.addBreadcrumb({category:'economy-feed',message:'Persisting cumulative spending',level:'info',data:{key,value}});}async function getEventBonusMultiplier(userId:string,eventId:string):Promise<number>{return 1.0;}function isRareItem(itemId:string):boolean{const rarePrefixes = ['legendary_','mythic_','epic_']; return rarePrefixes.some(prefix=>itemId.startsWith(prefix));}function getShopItemType(itemId:string):'BOOST'|'COSMETIC'|'SHIELD'|'THEME'{if(itemId.includes('boost')){return'BOOST';}if(itemId.includes('shield')){return'SHIELD';}if(itemId.includes('theme')){return'THEME';}return'COSMETIC';}

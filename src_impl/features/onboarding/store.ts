import{captureSilentFailure}from'../../utils/silent-failure';import{create}from'zustand';import{persist,createJSONStorage}from'zustand/middleware';import{getMMKVStorageAdapter}from'../../persistence/MMKVStorageAdapter';import type{OnboardingState,FocusGoal,FocusDuration}from'./schemas';import{OnboardingStateSchema}from'./schemas';const initialState:OnboardingState={isOnboarded:false,currentStep:0,goal:null,focusDuration:null,displayName:null,startedAt:null,completedAt:null};interface OnboardingActions{startOnboarding:()=>void;setGoal:(goal:FocusGoal)=>void;setFocusDuration:(duration:FocusDuration)=>void;setDisplayName:(name:string)=>void;nextStep:()=>void;previousStep:()=>void;skipOnboarding:()=>void;completeOnboarding:()=>void;resetOnboarding:()=>void;canSkipCurrentStep:()=>boolean;getDraft:(userId:string)=>{goal?:FocusGoal;focusDuration?:FocusDuration;displayName?:string;starterPresetId?:string;element?:string;personaId?:string;squadId?:string|null;}|undefined;saveDraft:(userId:string,draft:{goal?:FocusGoal;focusDuration?:FocusDuration;displayName?:string;starterPresetId?:string;element?:string;personaId?:string;squadId?:string|null;})=>void;}export type OnboardingStore=OnboardingState&OnboardingActions;const mmkvStorage=getMMKVStorageAdapter();export const useOnboardingStore=create<OnboardingStore>()(persist((set,get)=>({...initialState,startOnboarding:()=>{set({...initialState,startedAt:Date.now()});},setGoal:goal=>{set({goal});setTimeout(()=>{get().nextStep();},300);},setFocusDuration:duration=>{set({focusDuration:duration});setTimeout(()=>{get().nextStep();},300);},setDisplayName:name=>{const trimmed=name.trim();if(trimmed.length>=2){set({displayName:trimmed});}},nextStep:()=>{const{currentStep}=get();if(currentStep<4){set({currentStep:currentStep+1});}},previousStep:()=>{const{currentStep}=get();if(currentStep>0){set({currentStep:currentStep-1});}},skipOnboarding:()=>{set({isOnboarded:true,completedAt:Date.now()});},completeOnboarding:()=>{set({isOnboarded:true,completedAt:Date.now()});},resetOnboarding:()=>{set(initialState);},canSkipCurrentStep:()=>{const{currentStep}=get();return currentStep>=1;},getDraft:(_userId:string)=>{const state=get();return{goal:state.goal??undefined,focusDuration:state.focusDuration??undefined,displayName:state.displayName??undefined,starterPresetId:undefined,element:undefined,personaId:undefined,squadId:null};},saveDraft:(_userId:string,draft:{goal?:FocusGoal;focusDuration?:FocusDuration;displayName?:string;starterPresetId?:string;element?:string;personaId?:string;squadId?:string|null;})=>{if(draft.goal){get().setGoal(draft.goal);}if(draft.focusDuration){get().setFocusDuration(draft.focusDuration);}if(draft.displayName){get().setDisplayName(draft.displayName);}}}),{name:'onboarding-storage',storage:createJSONStorage(()=>({getItem:async(name:string):Promise<string|null>=>{return await mmkvStorage.getItem(name);},setItem:async(name:string,value:string):Promise<void>=>{await mmkvStorage.setItem(name,value);},removeItem:async(name:string):Promise<void>=>{await mmkvStorage.removeItem(name);}})),partialize:state=>({isOnboarded:state.isOnboarded,currentStep:state.currentStep,goal:state.goal,focusDuration:state.focusDuration,displayName:state.displayName,startedAt:state.startedAt,completedAt:state.completedAt}),onRehydrateStorage:()=>state=>{if(state){try{OnboardingStateSchema.parse(state);}catch(error){captureSilentFailure(error,{feature:'onboarding',operation:'safe-fallback',type:'data'});}}}}));export function useOnboardingProgress(){const store=useOnboardingStore();const totalSteps=5;const percentComplete=(store.currentStep+1)/totalSteps*100;return{stepNumber:store.currentStep+1,totalSteps,percentComplete,canSkip:store.currentStep>=1,isComplete:store.isOnboarded};}export function useNeedsOnboarding():boolean{const{isOnboarded}=useOnboardingStore();return!isOnboarded;}

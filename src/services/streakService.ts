import { createDebugger } from "../utils/debug"; import { capture } from "../shared/analytics/analytics-service"; import { StreakEvents } from "../shared/analytics/analytics-events"; const debug = createDebugger("streak-service"); export interface StreakData { currentStreak: number; longestStreak: number; lastSessionDate: string | null; streakHistory: { date: string; sessionsCompleted: number; maintained: boolean; }[]; isAtRisk: boolean; hoursRemaining: number; } export interface StreakUpdate { newStreak: number; streakMaintained: boolean; streakBroken: boolean; newLongestStreak: boolean; } class StreakService { private userId: string = ""; private streakData: StreakData = { currentStreak: 0, longestStreak: 0, lastSessionDate: null, streakHistory: [], isAtRisk: false, hoursRemaining: 24, }; setUserId(userId: string): void { this.userId = userId; debug.info("Streak service initialized for user:", userId); } getStreakData(): StreakData { return { ...this.streakData }; } async updateStreak(sessionDate?: string): Promise<StreakUpdate> { if (!this.userId) { debug.error("No user ID set for streak service"); return { newStreak: 0, streakMaintained: false, streakBroken: false, newLongestStreak: false, }; } const today = new Date(); const sessionDateTime = sessionDate ? new Date(sessionDate) : today; const previousStreak = this.streakData.currentStreak; try { const update = this.calculateStreakUpdate(sessionDateTime); this.streakData.currentStreak = update.newStreak; this.streakData.lastSessionDate = sessionDateTime.toISOString(); if (update.newLongestStreak) { this.streakData.longestStreak = update.newStreak; } this.addToStreakHistory(sessionDateTime, update.streakMaintained); this.updateRiskStatus(); capture(StreakEvents.STREAK_UPDATED, { user_id: this.userId, previous_streak: previousStreak, new_streak: update.newStreak, streak_maintained: update.streakMaintained, streak_broken: update.streakBroken, new_longest_streak: update.newLongestStreak, }); debug.info("Streak updated successfully", { previousStreak, newStreak: update.newStreak, maintained: update.streakMaintained, broken: update.streakBroken, }); return update; } catch (error) { debug.error("Failed to update streak:", error); return { newStreak: previousStreak, streakMaintained: false, streakBroken: false, newLongestStreak: false, }; } } private calculateStreakUpdate(sessionDate: Date): StreakUpdate { const now = new Date(); const today = new Date(now.getFullYear(), now.getMonth(), now.getDate()); const sessionDay = new Date(sessionDate.getFullYear(), sessionDate.getMonth(), sessionDate.getDate()); const lastSessionDay = this.streakData.lastSessionDate ? new Date(new Date(this.streakData.lastSessionDate).getFullYear(), new Date(this.streakData.lastSessionDate).getMonth(), new Date(this.streakData.lastSessionDate).getDate()) : null; let newStreak = this.streakData.currentStreak; let streakMaintained = false; let streakBroken = false; let newLongestStreak = false; if (!lastSessionDay) { newStreak = 1; streakMaintained = true; } else if (sessionDay.getTime() === today.getTime()) { newStreak = this.streakData.currentStreak; streakMaintained = true; } else if (sessionDay.getTime() === today.getTime() - 24 * 60 * 60 * 1000) { newStreak = this.streakData.currentStreak + 1; streakMaintained = true; } else if (sessionDay.getTime() < today.getTime() - 24 * 60 * 60 * 1000) { const daysSinceLastSession = Math.floor((today.getTime() - lastSessionDay.getTime()) / (24 * 60 * 60 * 1000)); if (daysSinceLastSession > 1) { newStreak = 1; streakBroken = true; streakMaintained = false; } else { newStreak = this.streakData.currentStreak + 1; streakMaintained = true; } } else if (sessionDay.getTime() > today.getTime()) { debug.warn("Session date is in the future, ignoring"); return { newStreak: this.streakData.currentStreak, streakMaintained: false, streakBroken: false, newLongestStreak: false, }; } newLongestStreak = newStreak > this.streakData.longestStreak; return { newStreak, streakMaintained, streakBroken, newLongestStreak }; } private addToStreakHistory(sessionDate: Date, maintained: boolean): void { const dateStr = sessionDate.toISOString().split("T")[0]!; this.streakData.streakHistory = this.streakData.streakHistory.filter((entry) => entry.date !== dateStr); this.streakData.streakHistory.unshift({ date: dateStr, sessionsCompleted: 1, maintained, }); if (this.streakData.streakHistory.length > 30) { this.streakData.streakHistory = this.streakData.streakHistory.slice(0, 30); } } private updateRiskStatus(): void { if (!this.streakData.lastSessionDate) { this.streakData.isAtRisk = false; this.streakData.hoursRemaining = 24; return; } const now = new Date(); const lastSession = new Date(this.streakData.lastSessionDate); const hoursSinceLastSession = (now.getTime() - lastSession.getTime()) / (1000 * 60 * 60); this.streakData.isAtRisk = hoursSinceLastSession > 20 && this.streakData.currentStreak > 0; this.streakData.hoursRemaining = Math.max(0, 24 - hoursSinceLastSession); }
getStreakSummary() { return { currentStreak: this.streakData.currentStreak, longestStreak: this.streakData.longestStreak, isAtRisk: this.streakData.isAtRisk, hoursRemaining: Math.ceil(this.streakData.hoursRemaining), lastSessionDate: this.streakData.lastSessionDate, streakHistory: this.streakData.streakHistory.slice(0, 7), }; } canClaimStreakFreeze(): boolean { return this.streakData.isAtRisk && this.streakData.currentStreak >= 3; } async applyStreakFreeze(): Promise<boolean> { if (!this.canClaimStreakFreeze()) { debug.warn("Cannot apply streak freeze - conditions not met"); return false; } try { if (this.streakData.lastSessionDate) { const extendedDate = new Date(this.streakData.lastSessionDate); extendedDate.setHours(extendedDate.getHours() + 24); this.streakData.lastSessionDate = extendedDate.toISOString(); } this.updateRiskStatus(); capture(StreakEvents.STREAK_FREEZE_USED, { user_id: this.userId, streak_length: this.streakData.currentStreak, }); debug.info("Streak freeze applied successfully"); return true; } catch (error) { debug.error("Failed to apply streak freeze:", error); return false; } } reset(): void { this.userId = ""; this.streakData = { currentStreak: 0, longestStreak: 0, lastSessionDate: null, streakHistory: [], isAtRisk: false, hoursRemaining: 24, }; debug.info("Streak service reset"); } } export const streakService = new StreakService();

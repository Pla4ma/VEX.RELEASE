import{z}from'zod'; import{getMMKVStorageAdapter}from'../persistence/MMKVStorageAdapter'; import{eventBus}from'./EventBus'; import{getAnalyticsService}from'../analytics/AnalyticsService'; import{createDebugger}from'../utils/debug'; const debug = createDebugger('events'); export const EventTypeSchema = z.enum(['DAILY','WEEKLY','WEEKEND','SEASONAL','SPECIAL','COMPETITION','COMMUNITY']); export const EventStatusSchema = z.enum(['UPCOMING','ACTIVE','ENDING_SOON','ENDED']); export const ChallengeTypeSchema = z.enum(['SESSION_COUNT','SESSION_DURATION','STREAK_DAYS','XP_EARNED','CURRENCY_EARNED','SOCIAL_SHARES','LEVEL_REACHED','ACHIEVEMENT_UNLOCKED']); export const ChallengeStatusSchema = z.enum(['LOCKED','ACTIVE','COMPLETED','CLAIMED','EXPIRED']); export type EventType=z.infer<typeof EventTypeSchema>; export type EventStatus=z.infer<typeof EventStatusSchema>; export type ChallengeType=z.infer<typeof ChallengeTypeSchema>; export type ChallengeStatus=z.infer<typeof ChallengeStatusSchema>; export const EventSchema = z.object({id:z.string(),title:z.string(),description:z.string(),type:EventTypeSchema,status:EventStatusSchema,startAt:z.number(),endAt:z.number(),theme:z.string().optional(),icon:z.string().optional(),bannerImage:z.string().optional(),rewards:z.array(z.object({type:z.string(),amount:z.number(),rarity:z.string()})).optional(),challenges:z.array(z.string()),leaderboardsEnabled:z.boolean().default(false),teamsEnabled:z.boolean().default(false),maxParticipants:z.number().optional(),currentParticipants:z.number().default(0),rules:z.record(z.unknown()).optional(),tags:z.array(z.string()).default([]),seasonId:z.string().optional()}); export const ChallengeSchema = z.object({id:z.string(),eventId:z.string().optional(),title:z.string(),description:z.string(),type:ChallengeTypeSchema,status:ChallengeStatusSchema,requirement:z.object({target:z.number(),metric:z.string(),timeframe:z.string().optional()}),progress:z.number().default(0),progressHistory:z.array(z.object({timestamp:z.number(),value:z.number()})).default([]),rewards:z.array(z.object({type:z.string(),amount:z.number()})),startAt:z.number(),endAt:z.number(),difficulty:z.enum(['EASY','MEDIUM','HARD','EXPERT']),prerequisites:z.array(z.string()).optional(),participants:z.array(z.string()).default([]),completedBy:z.array(z.string()).default([]),claimedBy:z.array(z.string()).default([])}); export type Event=z.infer<typeof EventSchema>; export type Challenge=z.infer<typeof ChallengeSchema>; export class EventService{private userId:string|null = null; private activeEvents:Map<string,Event> = new Map(); private activeChallenges:Map<string,Challenge> = new Map(); private userProgress:Map<string,{challengeId:string;progress:number}> = new Map(); constructor(userId?:string){this.userId = userId ?? null; this.setupEventListeners(); this.loadState();}setUserId(userId:string):void{this.userId = userId; this.loadState(); debug.info('EventService user set: %s',userId);}private setupEventListeners():void{eventBus.subscribe('session:completed',(payload:{userId:string;duration:number})=>{if(payload.userId === this.userId){this.updateChallengeProgress('SESSION_COUNT',1); this.updateChallengeProgress('SESSION_DURATION',payload.duration);}}); eventBus.subscribe('progression:xp_added',(payload:{userId:string;amount:number})=>{if(payload.userId === this.userId){this.updateChallengeProgress('XP_EARNED',payload.amount);}}); eventBus.subscribe('streak:updated',(payload:{userId:string;state:{currentStreak:number}})=>{if(payload.userId === this.userId){this.updateChallengeProgress('STREAK_DAYS',payload.state.currentStreak);}}); eventBus.subscribe('economy:currency_added',(payload:{userId:string;amount:number})=>{if(payload.userId === this.userId){this.updateChallengeProgress('CURRENCY_EARNED',payload.amount);}}); eventBus.subscribe('progression:level_up',(payload:{userId:string;newLevel:number})=>{if(payload.userId === this.userId){this.updateChallengeProgress('LEVEL_REACHED',payload.newLevel);}}); eventBus.subscribe('achievement:unlock',(payload:{userId:string})=>{if(payload.userId === this.userId){this.updateChallengeProgress('ACHIEVEMENT_UNLOCKED',1);}}); eventBus.subscribe('social:share',(payload:{userId:string})=>{if(payload.userId === this.userId){this.updateChallengeProgress('SOCIAL_SHARES',1);}});}registerEvent(event:Omit<Event,'status'|'currentParticipants'>):Event{const fullEvent = EventSchema.parse({...event,status:this.calculateEventStatus(event.startAt,event.endAt),currentParticipants:0}); this.activeEvents.set(fullEvent.id,fullEvent); debug.info('Event registered: %s (%s)',fullEvent.title,fullEvent.id); eventBus.publish('event:created',{eventId:fullEvent.id,title:fullEvent.title,type:fullEvent.type}); return fullEvent;}joinEvent(eventId:string):void{if(!this.userId){throw new Error('EventService: No user set');}const event = this.activeEvents.get(eventId); if(!event){throw new Error(`Event not found: ${eventId}`);}if(event.status === 'ENDED'){throw new Error('Event has ended');}if(event.maxParticipants && event.currentParticipants >= event.maxParticipants){throw new Error('Event is full');}event.currentParticipants++; for(const challengeId of event.challenges){this.joinChallenge(challengeId);}eventBus.publish('event:joined',{userId:this.userId,eventId,joinedAt:Date.now()}); const analytics = getAnalyticsService(); analytics.track('event_joined',{user_id:this.userId,event_id:eventId,event_type:event.type}); debug.info('User joined event: %s',eventId);}leaveEvent(eventId:string):void{if(!this.userId){return;}const event = this.activeEvents.get(eventId); if(!event){return;}event.currentParticipants = Math.max(0,event.currentParticipants - 1); eventBus.publish('event:left',{userId:this.userId,eventId});}registerChallenge(challenge:Challenge):Challenge{const fullChallenge = ChallengeSchema.parse(challenge); this.activeChallenges.set(fullChallenge.id,fullChallenge); debug.info('Challenge registered: %s',fullChallenge.title); return fullChallenge;}joinChallenge(challengeId:string):void{if(!this.userId){return;}const challenge = this.activeChallenges.get(challengeId); if(!challenge){return;}if(challenge.status === 'EXPIRED'){return;}if(challenge.status === 'LOCKED' && challenge.prerequisites){for(const prereqId of challenge.prerequisites){const prereq = this.activeChallenges.get(prereqId); if(!prereq || !prereq.completedBy.includes(this.userId)){debug.debug('Challenge prerequisite not met: %s',prereqId); return;}}}if(!challenge.participants.includes(this.userId)){challenge.participants.push(this.userId);}if(challenge.status === 'LOCKED'){challenge.status = 'ACTIVE';}debug.info('User joined challenge: %s',challengeId);}private updateChallengeProgress(type:ChallengeType,amount:number):void{if(!this.userId){return;}for(const challenge of this.activeChallenges.values()){if(challenge.type !== type){continue;}if(!challenge.participants.includes(this.userId)){continue;}if(challenge.status !== 'ACTIVE'){continue;}const oldProgress = challenge.progress; challenge.progress = Math.min(challenge.requirement.target,challenge.progress + amount); challenge.progressHistory.push({timestamp:Date.now(),value:challenge.progress}); this.userProgress.set(`${this.userId}:${challenge.id}`,{challengeId:challenge.id,progress:challenge.progress}); if(challenge.progress >= challenge.requirement.target && !challenge.completedBy.includes(this.userId)){this.completeChallenge(challenge.id);}if(oldProgress !== challenge.progress){eventBus.publish('challenge:progress',{userId:this.userId,challengeId:challenge.id,progress:challenge.progress,target:challenge.requirement.target,percent:Math.floor(challenge.progress / challenge.requirement.target * 100)});}}this.saveState();}private completeChallenge(challengeId:string):void{if(!this.userId){return;}const challenge = this.activeChallenges.get(challengeId); if(!challenge){return;}challenge.completedBy.push(this.userId); challenge.status = 'COMPLETED'; eventBus.publish('challenge:completed',{userId:this.userId,challengeId,completedAt:Date.now()}); this.grantChallengeRewards(challenge); const analytics = getAnalyticsService(); analytics.track('challenge_completed',{user_id:this.userId,challenge_id:challengeId,challenge_type:challenge.type,difficulty:challenge.difficulty}); if(challenge.eventId){this.checkEventCompletion(challenge.eventId);}debug.info('Challenge completed: %s',challengeId);}claimChallengeReward(challengeId:string):void{if(!this.userId){return;}const challenge = this.activeChallenges.get(challengeId); if(!challenge){throw new Error(`Challenge not found: ${challengeId}`);}if(!challenge.completedBy.includes(this.userId)){throw new Error('Challenge not completed');}if(challenge.claimedBy.includes(this.userId)){throw new Error('Reward already claimed');}challenge.claimedBy.push(this.userId); challenge.status = 'CLAIMED'; eventBus.publish('challenge:reward_claimed',{userId:this.userId,challengeId,claimedAt:Date.now()}); this.saveState();}private grantChallengeRewards(challenge:Challenge):void{if(!this.userId){return;}for(const reward of challenge.rewards){if(reward.type === 'XP'){eventBus.publish('progression:add_xp',{userId:this.userId,amount:reward.amount,source:'CHALLENGE_BONUS'});}}}private checkEventCompletion(eventId:string):void{const event = this.activeEvents.get(eventId); if(!event){return;}const allChallengesComplete = event.challenges.every(challengeId=>{const challenge = this.activeChallenges.get(challengeId); return challenge && challenge.completedBy.includes(this.userId!);}); if(allChallengesComplete && this.userId){eventBus.publish('event:all_challenges_complete',{userId:this.userId,eventId,completedAt:Date.now()});}}private calculateEventStatus(startAt:number,endAt:number):EventStatus{const now = Date.now(); if(now < startAt){return'UPCOMING';}if(now > endAt){return'ENDED';}if(endAt - now < 24 * 60 * 60 * 1000){return'ENDING_SOON';}return'ACTIVE';}getActiveEvents():Event[]{return Array.from(this.activeEvents.values()).filter(e=>e.status === 'ACTIVE' || e.status === 'ENDING_SOON');}getEventById(id:string):Event|undefined{return this.activeEvents.get(id);}getActiveChallenges():Challenge[]{return Array.from(this.activeChallenges.values()).filter(c=>c.status === 'ACTIVE' && c.participants.includes(this.userId!));}getChallengeById(id:string):Challenge|undefined{return this.activeChallenges.get(id);}getUserChallengeProgress(challengeId:string):number{if(!this.userId){return 0;}const progress = this.userProgress.get(`${this.userId}:${challengeId}`); return progress?.progress || 0;}getCompletedChallenges():Challenge[]{if(!this.userId){return[];}return Array.from(this.activeChallenges.values()).filter(c=>c.completedBy.includes(this.userId!));}private async loadState():Promise<void>{if(!this.userId){return;}try{const key = `events:progress:${this.userId}`; const storage = getMMKVStorageAdapter(); const data = await storage.getItem(key); if(data){const parsed = JSON.parse(data); this.userProgress = new Map(Object.entries(parsed.userProgress || {})); debug.info('Loaded event progress for %d challenges',this.userProgress.size);}}catch(error){debug.error('Failed to load event state',error as Error);}}private async saveState():Promise<void>{if(!this.userId){return;}try{const key = `events:progress:${this.userId}`; const data = {userProgress:Object.fromEntries(this.userProgress)}; const storage = getMMKVStorageAdapter(); await storage.setItem(key,JSON.stringify(data));}catch(error){debug.error('Failed to save event state',error as Error);}}cleanupEndedEvents():void{const now = Date.now(); for(const[id,event]of this.activeEvents){if(event.endAt < now - 7 * 24 * 60 * 60 * 1000){this.activeEvents.delete(id); debug.debug('Cleaned up ended event: %s',id);}}}}let eventServiceInstance:EventService|null = null; export function getEventService(userId?:string):EventService{if(!eventServiceInstance){eventServiceInstance = new EventService(userId);}else if(userId){eventServiceInstance.setUserId(userId);}return eventServiceInstance;}

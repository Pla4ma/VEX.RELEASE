import { create } from "zustand"; import { persist, createJSONStorage } from "zustand/middleware"; import { immer } from "zustand/middleware/immer"; import { getMMKVStorageAdapter } from "../persistence/MMKVStorageAdapter"; import type { User } from "../types/models"; import type { Nullable } from "../types/global"; let integrationsInitializedForUserId: string | null = null; let cleanupIntegrations: (() => void) | null = null; import { getSecureStorage, SecureStorageKeys } from "../persistence/SecureStorage"; import { signInWithEmail, signUpWithEmail, signOut, getCurrentUser } from "../services/supabaseAuth"; import { setSentryUser, clearSentryUser, captureException } from "../config/sentry"; import { revenueCatService } from "../shared/monetization/revenuecat-service"; import { progressionService } from "../services/progressionService"; import { economyService } from "../services/economyService"; import { rewardService } from "../services/rewardService"; import { streakService } from "../services/streakService"; import { createDebugger } from "../utils/debug"; const debug = createDebugger("store"); function toError(error: unknown): Error { return error instanceof Error ? error : new Error(String(error)); } function resetServiceSingletonsForLogout(): void { try { progressionService.reset(); } catch (error) { debug.error("Failed to reset progression service on logout", error as Error); } try { economyService.reset(); } catch (error) { debug.error("Failed to reset economy service on logout", error as Error); } try { rewardService.reset(); } catch (error) { debug.error("Failed to reset reward service on logout", error as Error); } try { streakService.reset(); } catch (error) { debug.error("Failed to reset streak service on logout", error as Error); } } interface AuthState { user: Nullable<User>; isAuthenticated: boolean; isLoading: boolean; error: Nullable<string>; setUser: (user: User) => void; clearUser: () => void; setLoading: (loading: boolean) => void; setError: (error: string | null) => void; clearError: () => void; login: (user: User) => void; loginWithCredentials: (email: string, password: string) => Promise<boolean>; register: (data: { email: string; password: string; firstName: string; lastName: string; confirmPassword: string; agreeToTerms: boolean; phone?: string }) => Promise<boolean>; logout: () => Promise<void>; checkAuth: () => Promise<void>; } export const useAuthStore = create<AuthState>()( immer( persist( (set) => ({ user: null, isAuthenticated: false, isLoading: false, error: null, setUser: (user) => set((state) => { state.user = user; state.isAuthenticated = true; }), clearUser: () => set((state) => { state.user = null; state.isAuthenticated = false; state.error = null; }), setLoading: (loading) => set((state) => { state.isLoading = loading; }), setError: (error) => set((state) => { state.error = error; }), login: (user: User) => { set((state) => { state.user = user; state.isAuthenticated = true; state.isLoading = false; state.error = null; }); }, loginWithCredentials: async (email: string, password: string) => { try { set((state) => { state.isLoading = true; state.error = null; }); const { user, error } = await signInWithEmail(email, password); if (error || !user) { set((state) => { state.isLoading = false; state.error = error?.message ?? "Sign in failed"; }); return false; } set((state) => { state.user = user; state.isAuthenticated = true; state.isLoading = false; state.error = null; }); setSentryUser(user.id, user.email, user.username); try { revenueCatService.setUserId(user.id); } catch (revenueCatError) { debug.error("[AuthStore] Failed to set RevenueCat user ID:", revenueCatError); } if (integrationsInitializedForUserId !== user.id) { progressionService.setUserId(user.id); economyService.setUserId(user.id); rewardService.setUserId(user.id); streakService.setUserId(user.id); integrationsInitializedForUserId = user.id; } return true; } catch (err) { set((state) => { state.isLoading = false; state.error = toError(err).message; }); captureException(toError(err), { tags: { feature: "auth-login" } }); return false; } }, register: async (data) => { try { set((state) => { state.isLoading = true; state.error = null; }); const { user, error } = await signUpWithEmail(data.email, data.password, { firstName: data.firstName, lastName: data.lastName }); if (error || !user) { set((state) => { state.isLoading = false; state.error = error?.message ?? "Registration failed"; }); return false; } set((state) => { state.user = user; state.isAuthenticated = true; state.isLoading = false; state.error = null; }); setSentryUser(user.id, user.email, user.username); try { revenueCatService.setUserId(user.id); } catch (revenueCatError) { debug.error("[AuthStore] Failed to set RevenueCat user ID:", revenueCatError); } if (integrationsInitializedForUserId !== user.id) { progressionService.setUserId(user.id); economyService.setUserId(user.id); rewardService.setUserId(user.id); streakService.setUserId(user.id); integrationsInitializedForUserId = user.id; } return true; } catch (err) {
set((state) => { state.isLoading = false; state.error = toError(err).message; }); captureException(toError(err), { tags: { feature: "auth-register" }, }); return false; } }, clearError: () => set((state) => { state.error = null; }) ,logout: async () => { const { error: signOutError } = await signOut(); if (signOutError) { debug.error("[AuthStore] Sign out failed:", signOutError); captureException(signOutError, { tags: { feature: "auth-logout" } }); return; } const secureStorage = getSecureStorage(); await secureStorage.removeItem(SecureStorageKeys.AUTH_TOKEN); await secureStorage.removeItem(SecureStorageKeys.REFRESH_TOKEN); set((state) => { state.user = null; state.isAuthenticated = false; state.isLoading = false; state.error = null; }); resetServiceSingletonsForLogout(); clearSentryUser(); try { revenueCatService.clearUserId(); } catch (error) { debug.error("[AuthStore] Failed to clear RevenueCat user ID:", error); } if (cleanupIntegrations) { cleanupIntegrations(); cleanupIntegrations = null; } integrationsInitializedForUserId = null; }, checkAuth: async () => { set((state) => { state.isLoading = true; state.error = null; }); try { const { user, error: authError } = await getCurrentUser(); if (authError) { throw authError; } if (user) { set((state) => { state.user = user; state.isAuthenticated = true; state.isLoading = false; }); setSentryUser(user.id, user.email, user.username); try { revenueCatService.setUserId(user.id); } catch (revenueCatError) { debug.error("[AuthStore] Failed to set RevenueCat user ID during checkAuth:", revenueCatError); } if (integrationsInitializedForUserId !== user.id) { progressionService.setUserId(user.id); economyService.setUserId(user.id); rewardService.setUserId(user.id); streakService.setUserId(user.id); integrationsInitializedForUserId = user.id; } } else { set((state) => { state.user = null; state.isAuthenticated = false; state.isLoading = false; }); clearSentryUser(); try { revenueCatService.clearUserId(); } catch (revenueCatError) { debug.error("[AuthStore] Failed to clear RevenueCat user ID during checkAuth:", revenueCatError); } } } catch (err) { const message = err instanceof Error ? err.message : "Auth check failed"; const isNetworkError = /network|timeout|fetch|unreachable|abort/i.test(message); set((state) => { state.isLoading = false; state.error = message; if (!isNetworkError) { state.isAuthenticated = false; state.user = null; state.error = null; } }); if (!isNetworkError) { clearSentryUser(); try { revenueCatService.clearUserId(); } catch { /* non-critical */ } } } }, }), { name: "auth-storage", storage: createJSONStorage(() => getMMKVStorageAdapter()), partialize: (state) => (state.user?.role === "admin" ? { user: null, isAuthenticated: false } : { user: state.user, isAuthenticated: state.isAuthenticated, }), }, ), ), ); interface AppState { isInitialized: boolean; isOnline: boolean; lastSyncTime: Nullable<number>; setInitialized: (initialized: boolean) => void; setOnline: (online: boolean) => void; setLastSyncTime: (time: number) => void; } export const useAppStore = create<AppState>()( immer((set) => ({ isInitialized: false, isOnline: true, lastSyncTime: null, setInitialized: (initialized) => set((state) => { state.isInitialized = initialized; }), setOnline: (online) => set((state) => { state.isOnline = online; }), setLastSyncTime: (time) => set((state) => { state.lastSyncTime = time; }), })), ); interface UIState { toast: Nullable<{ id: string; message: string; type: "success" | "error" | "warning" | "info"; duration?: number; }>; activeModal: Nullable<string>; modalProps: Record<string, unknown>; showToast: (toast: Omit<NonNullable<UIState["toast"]>, "id">) => void; hideToast: () => void; showModal: (name: string, props?: Record<string, unknown>) => void; hideModal: () => void; } export const useUIStore = create<UIState>()( immer((set) => ({ toast: null, activeModal: null, modalProps: {}, showToast: (toast) => set((state) => {
        state.toast = { ...toast, id: `${Date.now()}-${Math.random()}` };
}), hideToast: () => set((state) => { state.toast = null; }), showModal: (name, props = {}) => set((state) => { state.activeModal = name; state.modalProps = props; }), hideModal: () => set((state) => { state.activeModal = null; state.modalProps = {}; }), })), ); export function useStore() { return { auth: useAuthStore(), app: useAppStore(), ui: useUIStore() }; }

import{getSetting,getAllSettings,updateSetting,batchUpdateSettings,deleteSetting,getUserPreferences,syncSettings,getNotificationSettings,updateNotificationSettings,getCoachSettings,updateCoachSettings,getAppearanceSettings,updateAppearanceSettings,getPrivacySettings,updatePrivacySettings,exportSettings,importSettings,resetSettings,SettingsValidationError}from'../service'; import*as repository from'../repository'; import{eventBus}from'../../../events'; import*as Sentry from'@sentry/react-native'; jest.mock('../repository'); jest.mock('../../../events',()=>({eventBus:{publish:jest.fn()}})); jest.mock('@sentry/react-native',()=>({addBreadcrumb:jest.fn(),captureException:jest.fn()})); describe('SettingsService',()=>{const mockUserId = 'user-123'; beforeEach(()=>{jest.clearAllMocks();}); describe('getSetting',()=>{it('should fetch a single setting',async()=>{const mockSetting = {id:'setting-1',userId:mockUserId,key:'general.language',value:'en',category:'general'as const,isDefault:true,lastModified:Date.now()}; (repository.fetchSetting as jest.Mock).mockResolvedValue(mockSetting); const result = await getSetting(mockUserId,'general.language'); expect(result).toEqual(mockSetting); expect(repository.fetchSetting).toHaveBeenCalledWith(mockUserId,'general.language');}); it('should return null for non-existent setting',async()=>{(repository.fetchSetting as jest.Mock).mockResolvedValue(null); const result = await getSetting(mockUserId,'nonexistent'); expect(result).toBeNull();}); it('should handle errors gracefully',async()=>{(repository.fetchSetting as jest.Mock).mockRejectedValue(new Error('DB error')); await expect(getSetting(mockUserId,'test')).rejects.toThrow('DB error'); expect(Sentry.captureException).toHaveBeenCalled();});}); describe('getAllSettings',()=>{it('should fetch all settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'a',value:'1',category:'general'as const,isDefault:true,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'b',value:'2',category:'appearance'as const,isDefault:false,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getAllSettings(mockUserId); expect(result).toHaveLength(2); expect(repository.fetchAllSettings).toHaveBeenCalledWith(mockUserId);});}); describe('updateSetting',()=>{it('should update a setting and emit event',async()=>{const mockSetting = {id:'setting-1',userId:mockUserId,key:'general.language',value:'es',category:'general'as const,isDefault:false,lastModified:Date.now()}; (repository.fetchSetting as jest.Mock).mockResolvedValue(null); (repository.upsertSetting as jest.Mock).mockResolvedValue(mockSetting); const result = await updateSetting(mockUserId,'general.language','es','general'); expect(result).toEqual(mockSetting); expect(eventBus.publish).toHaveBeenCalledWith('settings:change',expect.any(Object));}); it('should throw validation error for invalid value',async()=>{await expect(updateSetting(mockUserId,'appearance.fontScale',3,'appearance')).rejects.toThrow(SettingsValidationError);});}); describe('batchUpdateSettings',()=>{it('should update multiple settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'a',value:'1',category:'general'as const,isDefault:false,lastModified:Date.now()}]; (repository.batchUpsertSettings as jest.Mock).mockResolvedValue(mockSettings); const updates = [{key:'notifications.push.enabled',value:true,category:'notifications'as const},{key:'appearance.theme',value:'dark',category:'appearance'as const}]; const result = await batchUpdateSettings(mockUserId,updates); expect(result).toHaveLength(1); expect(repository.batchUpsertSettings).toHaveBeenCalled();}); it('should throw validation error for invalid batch',async()=>{const updates = [{key:'appearance.fontScale',value:3,category:'appearance'as const}]; await expect(batchUpdateSettings(mockUserId,updates)).rejects.toThrow(SettingsValidationError);});}); describe('deleteSetting',()=>{it('should delete a setting',async()=>{(repository.deleteSetting as jest.Mock).mockResolvedValue(undefined); const result = await deleteSetting(mockUserId,'test.setting'); expect(result).toBe(true); expect(repository.deleteSetting).toHaveBeenCalledWith(mockUserId,'test.setting'); expect(eventBus.publish).toHaveBeenCalledWith('settings:change',expect.any(Object));});}); describe('getUserPreferences',()=>{it('should aggregate settings into preferences',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'general.language',value:'en',category:'general'as const,isDefault:true,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'appearance.theme',value:'dark',category:'appearance'as const,isDefault:false,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getUserPreferences(mockUserId); expect(result.userId).toBe(mockUserId); expect(result.settings['general.language']).toBeDefined(); expect(result.settings['appearance.theme']).toBeDefined();});}); describe('syncSettings',()=>{it('should sync settings successfully',async()=>{(repository.fetchSyncState as jest.Mock).mockResolvedValue({userId:mockUserId,status:'idle'as const,lastSyncAttempt:Date.now(),pendingChanges:1}); (repository.fetchPendingChanges as jest.Mock).mockResolvedValue([]); (repository.fetchRemoteChanges as jest.Mock).mockResolvedValue([]); (repository.pushChanges as jest.Mock).mockResolvedValue({success:true,conflicts:[]}); const result = await syncSettings(mockUserId); expect(result.userId).toBe(mockUserId); expect(['idle','synced','conflict','error']).toContain(result.status);}); it('should handle sync errors',async()=>{(repository.fetchSyncState as jest.Mock).mockRejectedValue(new Error('Network error')); const result = await syncSettings(mockUserId); expect(result.status).toBe('error'); expect(result.errorMessage).toBeDefined();});}); describe('notification settings',()=>{it('should get notification settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'notifications.push.enabled',value:true,category:'notifications'as const,isDefault:true,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'notifications.email.enabled',value:false,category:'notifications'as const,isDefault:true,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getNotificationSettings(mockUserId); expect(result.userId).toBe(mockUserId); expect(result.channels.push.enabled).toBe(true); expect(result.channels.email.enabled).toBe(false);}); it('should update notification settings',async()=>{(repository.fetchAllSettings as jest.Mock).mockResolvedValue([]); (repository.batchUpsertSettings as jest.Mock).mockResolvedValue([]); await updateNotificationSettings(mockUserId,{channels:{push:{enabled:false,deviceTokens:[],timezone:'UTC'},email:{enabled:true,email:'test@example.com',digestFrequency:'daily'},inApp:{enabled:true,soundEnabled:true,vibrationEnabled:false}},preferences:{critical:{enabled:true,channels:['push','email','in_app']},high:{enabled:true,channels:['push','in_app']},normal:{enabled:true,channels:['in_app']},low:{enabled:false,channels:[]}},customRules:[]}); expect(repository.batchUpsertSettings).toHaveBeenCalled();});}); describe('coach settings',()=>{it('should get coach settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'coach.enabled',value:true,category:'coach'as const,isDefault:true,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'coach.personality',value:'supportive',category:'coach'as const,isDefault:true,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getCoachSettings(mockUserId); expect(result.enabled).toBe(true); expect(result.personality).toBe('supportive');}); it('should update coach settings',async()=>{(repository.fetchAllSettings as jest.Mock).mockResolvedValue([]); (repository.batchUpsertSettings as jest.Mock).mockResolvedValue([]); await updateCoachSettings(mockUserId,{enabled:false,personality:'tough',frequency:'frequent'}); expect(repository.batchUpsertSettings).toHaveBeenCalled();});}); describe('appearance settings',()=>{it('should get appearance settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'appearance.theme',value:'dark',category:'appearance'as const,isDefault:false,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'appearance.fontScale',value:1.2,category:'appearance'as const,isDefault:false,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getAppearanceSettings(mockUserId); expect(result.theme).toBe('dark'); expect(result.fontScale).toBe(1.2);}); it('should update appearance settings',async()=>{(repository.fetchAllSettings as jest.Mock).mockResolvedValue([]); (repository.batchUpsertSettings as jest.Mock).mockResolvedValue([]); await updateAppearanceSettings(mockUserId,{theme:'light',accentColor:'#ff0000'}); expect(repository.batchUpsertSettings).toHaveBeenCalled();});}); describe('privacy settings',()=>{it('should get privacy settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'privacy.profileVisibility',value:'private',category:'privacy'as const,isDefault:false,lastModified:Date.now()},{id:'2',userId:mockUserId,key:'privacy.analyticsOptOut',value:true,category:'privacy'as const,isDefault:false,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await getPrivacySettings(mockUserId); expect(result.profileVisibility).toBe('private'); expect(result.analyticsOptOut).toBe(true);}); it('should update privacy settings',async()=>{(repository.fetchAllSettings as jest.Mock).mockResolvedValue([]); (repository.batchUpsertSettings as jest.Mock).mockResolvedValue([]); await updatePrivacySettings(mockUserId,{profileVisibility:'friends',showOnlineStatus:false}); expect(repository.batchUpsertSettings).toHaveBeenCalled();});}); describe('export/import',()=>{it('should export settings',async()=>{const mockSettings = [{id:'1',userId:mockUserId,key:'general.language',value:'en',category:'general'as const,isDefault:true,lastModified:Date.now()}]; (repository.fetchAllSettings as jest.Mock).mockResolvedValue(mockSettings); const result = await exportSettings(mockUserId); expect(result.userId).toBe(mockUserId); expect(result.version).toBe(1); expect(result.preferences).toBeDefined();}); it('should import settings',async()=>{(repository.batchUpsertSettings as jest.Mock).mockResolvedValue([]); const exportData = {version:1,exportedAt:Date.now(),userId:mockUserId,preferences:{userId:mockUserId,version:1,settings:{'general.language':{id:'1',userId:mockUserId,key:'general.language',value:'es',category:'general'as const,isDefault:false,lastModified:Date.now()}},createdAt:Date.now(),updatedAt:Date.now()},notificationSettings:{userId:mockUserId,channels:{push:{enabled:true,deviceTokens:[],timezone:'UTC'},email:{enabled:false,email:'',digestFrequency:'daily'as const},inApp:{enabled:true,soundEnabled:true,vibrationEnabled:true}},preferences:{critical:{enabled:true,channels:['push','email','in_app']},high:{enabled:true,channels:['push','in_app']},normal:{enabled:true,channels:['in_app']},low:{enabled:false,channels:[]}},customRules:[]},coachSettings:{userId:mockUserId,enabled:true,personality:'supportive'as const,frequency:'moderate'as const,messageTypes:{streakReminders:true,sessionTips:true,milestoneCelebrations:true,encouragement:true,challenges:false},quietHours:{enabled:true,start:'22:00',end:'08:00',timezone:'UTC'},customTriggers:[]},appearanceSettings:{userId:mockUserId,theme:'system'as const,accentColor:'#6366f1',fontScale:1,useSystemFont:true,reduceMotion:false,highContrast:false,compactMode:false},privacySettings:{userId:mockUserId,profileVisibility:'friends'as const,showOnlineStatus:true,showActivityStatus:true,allowDataAnalysis:true,allowPersonalization:true,thirdPartySharing:false,analyticsOptOut:false},dataControlSettings:{userId:mockUserId,retentionPolicy:'standard'as const,autoExport:{enabled:false,frequency:'never'as const,format:'json'as const},backupEnabled:false}}; const result = await importSettings(mockUserId,exportData); expect(result.imported).toBeGreaterThan(0); expect(result.errors).toHaveLength(0);}); it('should reject invalid export version',async()=>{const exportData = {version:999,exportedAt:Date.now(),userId:mockUserId,preferences:{}as any,notificationSettings:{}as any,coachSettings:{}as any,appearanceSettings:{}as any,privacySettings:{}as any,dataControlSettings:{}as any}; await expect(importSettings(mockUserId,exportData)).rejects.toThrow(SettingsValidationError);});}); describe('resetSettings',()=>{it('should reset settings by category',async()=>{(repository.resetSettings as jest.Mock).mockResolvedValue(undefined); await resetSettings(mockUserId,'appearance'); expect(repository.resetSettings).toHaveBeenCalledWith(mockUserId,'appearance'); expect(eventBus.publish).toHaveBeenCalledWith('settings:reset',expect.any(Object));}); it('should reset all settings',async()=>{(repository.resetSettings as jest.Mock).mockResolvedValue(undefined); await resetSettings(mockUserId); expect(repository.resetSettings).toHaveBeenCalledWith(mockUserId,undefined);});});});

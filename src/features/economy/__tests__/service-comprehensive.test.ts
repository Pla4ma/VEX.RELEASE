import{describe,it,expect,beforeEach,type MockedFunction}from'@jest/globals'; import{getOrCreateWallet,hasEnoughBalance,addCurrency,spendCurrency}from'../service'; import*as repository from'../repository'; import{eventBus}from'../../../events'; jest.mock('../repository'); jest.mock('../../../events',()=>({eventBus:{publish:jest.fn()}})); describe('EconomyService - Comprehensive',()=>{beforeEach(()=>{jest.clearAllMocks();}); describe('spend below balance',()=>{it('should succeed when spending COINS below balance',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:1000,gems:50,seasonal:{},totalCoinsEarned:5000,totalCoinsSpent:4000,totalGemsEarned:100,totalGemsSpent:50,createdAt:Date.now(),updatedAt:Date.now()}); jest.mocked(repository.updateWalletBalance).mockResolvedValue(undefined); jest.mocked(repository.createTransaction).mockResolvedValue({id:'tx-1',walletId:'wallet-1',type:'SPEND',currency:'COINS',amount:100}as any); const result = await spendCurrency({userId:'user-1',currency:'COINS',amount:100,sink:'SHOP',description:'Buy item'}); expect(result.success).toBe(true); expect(result.newBalance).toBe(900); expect(result.error).toBeNull();}); it('should succeed when spending GEMS below balance',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:100,gems:100,seasonal:{},totalCoinsEarned:0,totalCoinsSpent:0,totalGemsEarned:100,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); jest.mocked(repository.updateWalletBalance).mockResolvedValue(undefined); jest.mocked(repository.createTransaction).mockResolvedValue({id:'tx-1',walletId:'wallet-1',type:'SPEND',currency:'GEMS',amount:10}as any); const result = await spendCurrency({userId:'user-1',currency:'GEMS',amount:10,sink:'SHOP',description:'Buy premium item'}); expect(result.success).toBe(true); expect(result.newBalance).toBe(90);});}); describe('spend above balance',()=>{it('should fail with INSUFFICIENT_FUNDS for COINS',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:50,gems:10,seasonal:{},totalCoinsEarned:50,totalCoinsSpent:0,totalGemsEarned:10,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); const result = await spendCurrency({userId:'user-1',currency:'COINS',amount:100,sink:'SHOP',description:'Buy expensive item'}); expect(result.success).toBe(false); expect(result.error?.code).toBe('INSUFFICIENT_FUNDS'); expect(result.error?.recoverable).toBe(false); expect(repository.updateWalletBalance).not.toHaveBeenCalled();}); it('should fail with INSUFFICIENT_FUNDS for GEMS',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:1000,gems:5,seasonal:{},totalCoinsEarned:1000,totalCoinsSpent:0,totalGemsEarned:5,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); const result = await spendCurrency({userId:'user-1',currency:'GEMS',amount:10,sink:'SHOP',description:'Buy premium item'}); expect(result.success).toBe(false); expect(result.error?.code).toBe('INSUFFICIENT_FUNDS'); expect(result.error?.message).toContain('insufficient');});}); describe('concurrent spends',()=>{it('should handle concurrent spend attempts - only one succeeds',async()=>{let currentCoins = 100; jest.mocked(repository.fetchWallet).mockImplementation(async()=>({id:'wallet-1',userId:'user-1',coins:currentCoins,gems:0,seasonal:{},totalCoinsEarned:100,totalCoinsSpent:0,totalGemsEarned:0,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()})); jest.mocked(repository.updateWalletBalance).mockImplementation(async(_userId:string,data:{coins?:number;gems?:number;seasonal?:Record<string,number>;})=>{if(data.coins !== undefined){currentCoins = data.coins;}}); const[result1,result2] = await Promise.all([spendCurrency({userId:'user-1',currency:'COINS',amount:60,sink:'SHOP',description:'Purchase 1'}),spendCurrency({userId:'user-1',currency:'COINS',amount:60,sink:'SHOP',description:'Purchase 2'})]); const successCount = [result1,result2].filter(r=>r.success).length; const failCount = [result1,result2].filter(r=>!r.success).length; expect(successCount + failCount).toBe(2); if(successCount === 2){expect(currentCoins).toBeLessThanOrEqual(100);}}); it('should maintain atomicity - no partial transactions',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:100,gems:50,seasonal:{},totalCoinsEarned:100,totalCoinsSpent:0,totalGemsEarned:50,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); jest.mocked(repository.updateWalletBalance).mockRejectedValue(new Error('DB error')); await expect(spendCurrency({userId:'user-1',currency:'COINS',amount:50,sink:'SHOP',description:'Test purchase'})).rejects.toThrow(); expect(eventBus.publish).not.toHaveBeenCalledWith('economy:currency_spent',expect.any(Object));});}); describe('daily rate limit enforcement',()=>{it('should enforce daily spending limit for COINS',async()=>{const today = new Date().toISOString().split('T')[0]; jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:10000,gems:1000,seasonal:{},totalCoinsEarned:10000,totalCoinsSpent:9900,totalGemsEarned:1000,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now(),dailySpendLimit:10000,dailySpendToday:9900,dailySpendDate:today}as any); const result = await spendCurrency({userId:'user-1',currency:'COINS',amount:200,sink:'SHOP',description:'Large purchase'}); expect(result).toBeDefined();}); it('should reset daily limit at midnight',async()=>{const yesterday = new Date(Date.now() - 24 * 60 * 60 * 1000).toISOString().split('T')[0]; const today = new Date().toISOString().split('T')[0]; jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:10000,gems:1000,seasonal:{},dailySpendLimit:1000,dailySpendToday:999,dailySpendDate:yesterday,createdAt:Date.now(),updatedAt:Date.now()}as any); jest.mocked(repository.updateWalletBalance).mockImplementation(async(_userId:string,_data:{coins?:number;gems?:number;seasonal?:Record<string,number>;})=>{}); await spendCurrency({userId:'user-1',currency:'COINS',amount:100,sink:'SHOP',description:'Purchase after reset'});});}); describe('balance verification',()=>{it('should correctly report hasEnoughBalance',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:100,gems:50,seasonal:{season1:200},totalCoinsEarned:100,totalCoinsSpent:0,totalGemsEarned:50,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); const wallet = await getOrCreateWallet('user-1'); expect(hasEnoughBalance(wallet,'COINS',50)).toBe(true); expect(hasEnoughBalance(wallet,'COINS',100)).toBe(true); expect(hasEnoughBalance(wallet,'COINS',101)).toBe(false); expect(hasEnoughBalance(wallet,'GEMS',50)).toBe(true); expect(hasEnoughBalance(wallet,'GEMS',51)).toBe(false);}); it('should update balance after successful spend',async()=>{let walletCoins = 500; jest.mocked(repository.fetchWallet).mockImplementation(async()=>({id:'wallet-1',userId:'user-1',coins:walletCoins,gems:100,seasonal:{},totalCoinsEarned:500,totalCoinsSpent:0,totalGemsEarned:100,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()})); jest.mocked(repository.updateWalletBalance).mockImplementation(async(_userId:string,data:{coins?:number;gems?:number;seasonal?:Record<string,number>;})=>{if(data.coins !== undefined){walletCoins = data.coins;}}); const result = await spendCurrency({userId:'user-1',currency:'COINS',amount:100,sink:'SHOP',description:'Test spend'}); expect(result.success).toBe(true); expect(walletCoins).toBe(400);});}); describe('add currency',()=>{it('should add COINS successfully',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:100,gems:50,seasonal:{},totalCoinsEarned:100,totalCoinsSpent:0,totalGemsEarned:50,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); jest.mocked(repository.updateWalletBalance).mockResolvedValue(undefined); const result = await addCurrency({userId:'user-1',currency:'COINS',amount:50,source:'REWARD',description:'Test reward',skipEvents:false}); expect(result.newBalance).toBe(150); expect(repository.updateWalletBalance).toHaveBeenCalledWith('user-1',expect.objectContaining({coins:150,totalCoinsEarned:150}));}); it('should add GEMS successfully',async()=>{jest.mocked(repository.fetchWallet).mockResolvedValue({id:'wallet-1',userId:'user-1',coins:100,gems:50,seasonal:{},totalCoinsEarned:100,totalCoinsSpent:0,totalGemsEarned:50,totalGemsSpent:0,createdAt:Date.now(),updatedAt:Date.now()}); jest.mocked(repository.updateWalletBalance).mockResolvedValue(undefined); const result = await addCurrency({userId:'user-1',currency:'GEMS',amount:25,source:'SHOP',description:'Gem purchase',skipEvents:false}); expect(result.newBalance).toBe(75);});});});

import * as Sentry from "@sentry/react-native"; import { eventBus } from "../../../events"; import type { DeduplicationRequest, DeduplicationResult, DeduplicationKey, DeduplicationRule, DeduplicationAttempt, ExploitPattern, ExploitDetection } from "./schemas"; import { DEFAULT_DEDUPLICATION_RULES, DEFAULT_EXPLOIT_PATTERNS } from "./config"; class AntiDuplicationCore { private deduplicationRules: DeduplicationRule[] = DEFAULT_DEDUPLICATION_RULES; private exploitPatterns: ExploitPattern[] = DEFAULT_EXPLOIT_PATTERNS; private keyCache: Map<string, DeduplicationKey> = new Map(); private attemptCache: Map<string, number> = new Map(); findRule(actionType: string): DeduplicationRule | null { return this.deduplicationRules.find((rule) => rule.actionType === actionType && rule.isActive) || null; } checkRuleConditions(rule: DeduplicationRule, request: DeduplicationRequest): boolean { const { conditions } = rule; if (conditions.minLevel && request.userLevel < conditions.minLevel) { return false; } if (conditions.maxLevel && request.userLevel > conditions.maxLevel) { return false; } if (conditions.requiredEntitlements && conditions.requiredEntitlements.length > 0 && !request.isPremiumUser) { return false; } return true; } generateContextKey(rule: DeduplicationRule, request: DeduplicationRequest): string { let key = rule.keyTemplate; key = key.replace("{userId}", request.userId); key = key.replace("{actionType}", request.actionType); rule.keyVariables.forEach((variable) => { const value = request.contextData[variable]; if (value !== undefined) {
        key = key.replace(`{${variable}}`, String(value));
} }); if (key.includes("{date}")) { const today = new Date().toISOString().split("T")[0]!; key = key.replace("{date}", today); } return key; } async getExistingKey(userId: string, contextKey: string): Promise<DeduplicationKey | null> {
    const cacheKey = `${userId}:${contextKey}`;
const cached = this.keyCache.get(cacheKey); if (cached) { return cached; } return null; } isKeyExpired(key: DeduplicationKey): boolean { if (key.timeWindow === 0) { return false; } if (!key.expiresAt) { return false; } return Date.now() > key.expiresAt; } async markKeyExpired(key: DeduplicationKey): Promise<void> {
    const cacheKey = `${key.userId}:${key.contextKey}`;
this.keyCache.delete(cacheKey); } async createOrUpdateKey(request: DeduplicationRequest, contextKey: string, rule: DeduplicationRule): Promise<string> { const now = Date.now(); const expiresAt = rule.timeWindow > 0 ? now + rule.timeWindow * 1000 : undefined; const key: DeduplicationKey = { id: crypto.randomUUID(), userId: request.userId, actionType: request.actionType, contextKey, contextData: request.contextData, timeWindow: rule.timeWindow, expiresAt, source: request.source, sourceId: request.sourceId, metadata: request.metadata, isUsed: true, usedAt: now, attempts: 1, createdAt: now, updatedAt: now, };
    const cacheKey = `${request.userId}:${contextKey}`;
this.keyCache.set(cacheKey, key); return key.id; } async checkExploitPatterns(request: DeduplicationRequest): Promise<ExploitDetection | null> { for (const pattern of this.exploitPatterns) { if (!pattern.isActive) { continue; } const detection = await this.evaluateExploitPattern(pattern, request); if (detection) { return detection; } } return null; } async evaluateExploitPattern(pattern: ExploitPattern, request: DeduplicationRequest): Promise<ExploitDetection | null> { if (!pattern.criteria.actionTypes.includes(request.actionType)) { return null; } if (pattern.criteria.minLevel && request.userLevel < pattern.criteria.minLevel) { return null; } const recentCount = await this.getRecentActionCount(request.userId, request.actionType, pattern.criteria.timeWindow); if (recentCount >= pattern.criteria.maxAttempts) { const confidence = Math.min(recentCount / pattern.criteria.maxAttempts, 1.0); if (confidence >= pattern.criteria.suspiciousThreshold) { const detection: ExploitDetection = { id: crypto.randomUUID(), userId: request.userId, patternId: pattern.id, patternName: pattern.name, triggerAction: request.actionType, triggerContext: JSON.stringify(request.contextData), triggerData: request.metadata || {}, confidence, severity: confidence >= 0.9 ? "HIGH" : confidence >= 0.7 ? "MEDIUM" : "LOW", actionsTaken: [], status: "DETECTED", resolvedAt: null, resolutionNotes: null, createdAt: Date.now(), updatedAt: Date.now(), }; if (pattern.actions.block) { detection.actionsTaken.push("blocked"); } if (pattern.actions.flagForReview) { detection.actionsTaken.push("flagged_for_review"); } if (pattern.actions.temporaryRestriction) { detection.actionsTaken.push("temporary_restriction"); } if (pattern.actions.notifyAdmins) { detection.actionsTaken.push("notified_admins"); } return detection; } } return null; } async getRecentActionCount(userId: string, actionType: string, timeWindowSeconds: number): Promise<number> {
    const cacheKey = `${userId}:${actionType}:${timeWindowSeconds}`;
const cached = this.attemptCache.get(cacheKey); if (cached !== undefined) { return cached; } const count = Math.floor(Math.random() * 10); this.attemptCache.set(cacheKey, count); setTimeout(() => { this.attemptCache.delete(cacheKey); }, 60000); return count; } async logAttempt(request: DeduplicationRequest, result: DeduplicationAttempt["result"], reason: string, rule: DeduplicationRule): Promise<void> { const attempt: DeduplicationAttempt = { id: crypto.randomUUID(), userId: request.userId, actionType: request.actionType, contextKey: this.generateContextKey(rule, request), result, reason, source: request.source, sourceId: request.sourceId, metadata: request.metadata, createdAt: Date.now(), }; Sentry.addBreadcrumb({ category: "anti-duplication",
      message: `Deduplication attempt: ${result}`,
data: { userId: request.userId, actionType: request.actionType, result, reason, }, level: result === "BLOCKED_DUPLICATE" ? "warning" : "info", }); eventBus.publish("anti-duplication:attempt_logged", { attempt, timestamp: Date.now(), }); } async logExploitDetection(detection: ExploitDetection): Promise<void> { Sentry.addBreadcrumb({ category: "anti-duplication",
      message: `Exploit detected: ${detection.patternName}`,
data: { userId: detection.userId, pattern: detection.patternName, confidence: detection.confidence, severity: detection.severity, }, level: detection.severity === "HIGH" ? "error" : "warning", }); eventBus.publish("anti-duplication:exploit_detected", { detection, timestamp: Date.now(), }); } createErrorResult(reason: string): DeduplicationResult { return { allowed: false, deduplicationKey: null, result: "ERROR", reason, warningMessage: null, existingKey: null, ruleApplied: null, requiresPremium: false, premiumUpgradeMessage: null, }; } createBlockedResult(result: "BLOCKED_DUPLICATE" | "BLOCKED_RULE", reason: string, rule: DeduplicationRule): DeduplicationResult { return { allowed: false, deduplicationKey: null, result, reason, warningMessage: rule.actions.customMessage, existingKey: null, ruleApplied: rule, requiresPremium: false, premiumUpgradeMessage: null, }; } createDuplicateResult(existingKey: DeduplicationKey, rule: DeduplicationRule): DeduplicationResult { return { allowed: false, deduplicationKey: null, result: "BLOCKED_DUPLICATE", reason: "Duplicate action detected", warningMessage: rule.actions.customMessage, existingKey, ruleApplied: rule, requiresPremium: false, premiumUpgradeMessage: null, }; } addDeduplicationRule(rule: DeduplicationRule): void { this.deduplicationRules.push(rule); } updateDeduplicationRule(ruleId: string, updates: Partial<DeduplicationRule>): void { const index = this.deduplicationRules.findIndex((r) => r.id === ruleId); if (index !== -1) { this.deduplicationRules[index] = { ...this.deduplicationRules[index], ...updates, } as DeduplicationRule; } } addExploitPattern(pattern: ExploitPattern): void { this.exploitPatterns.push(pattern); } updateExploitPattern(patternId: string, updates: Partial<ExploitPattern>): void { const index = this.exploitPatterns.findIndex((p) => p.id === patternId); if (index !== -1) { this.exploitPatterns[index] = { ...this.exploitPatterns[index], ...updates, } as ExploitPattern; } } getDeduplicationRules(): DeduplicationRule[] { return [...this.deduplicationRules]; } getExploitPatterns(): ExploitPattern[] { return [...this.exploitPatterns]; } clearCache(): void { this.keyCache.clear(); this.attemptCache.clear(); } getCacheStats(): { keys: number; attempts: number } { return { keys: this.keyCache.size, attempts: this.attemptCache.size }; } } export const antiDuplicationCore = new AntiDuplicationCore();

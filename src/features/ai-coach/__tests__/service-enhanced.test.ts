import { captureSilentFailure } from '../../../utils/silent-failure';
import{describe,it,expect,jest,beforeEach,afterEach}from'@jest/globals'; import{determineOptimalState,transitionState,checkAutoTransitions,StateTransitionError}from'../service/coach-state-machine'; import{evaluateInterventions,InterventionError,DailyLimitExceededError}from'../service/intervention-engine'; import{processBehaviorSignal,rebuildBehaviorProfile,detectPatterns}from'../service/behavior-analytics'; import{generateMessage}from'../service/message-generator'; import{CircuitBreaker,CircuitBreakerOpenError}from'../utils/retry'; import*as repository from'../repository'; import{type BehaviorProfile,type CoachState,type InterventionRule}from'../schemas'; jest.mock('../repository'); describe('Coach State Machine',()=>{const mockUserId = 'user-123'; beforeEach(()=>{jest.clearAllMocks();}); describe('determineOptimalState',()=>{it('returns COLD_START for new users with no data',async()=>{(repository.fetchCoachState as jest.Mock).mockResolvedValue(null); (repository.fetchActiveComebackPlan as jest.Mock).mockResolvedValue(null); const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'LOW',coldStart:true,dataPoints:0}; const state = await determineOptimalState(mockUserId,profile); expect(state).toBe('COLD_START');}); it('returns COMEBACK_MODE when comeback is active',async()=>{(repository.fetchActiveComebackPlan as jest.Mock).mockResolvedValue({id:'comeback-1',userId:mockUserId,status:'ACTIVE'}); const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:50}; const state = await determineOptimalState(mockUserId,profile); expect(state).toBe('COMEBACK_MODE');}); it('returns STREAK_AT_RISK when streak is at risk',async()=>{(repository.fetchActiveComebackPlan as jest.Mock).mockResolvedValue(null); (repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,currentState:'STREAK_AT_RISK'}); const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:50}; const state = await determineOptimalState(mockUserId,profile); expect(state).toBe('STREAK_AT_RISK');}); it('returns LOW_CONFIDENCE for users with 5-19 data points',async()=>{(repository.fetchActiveComebackPlan as jest.Mock).mockResolvedValue(null); const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'LOW',coldStart:false,dataPoints:10}; const state = await determineOptimalState(mockUserId,profile); expect(state).toBe('LOW_CONFIDENCE');}); it('returns HIGH_CONFIDENCE for users with 20+ data points',async()=>{(repository.fetchActiveComebackPlan as jest.Mock).mockResolvedValue(null); const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:25}; const state = await determineOptimalState(mockUserId,profile); expect(state).toBe('HIGH_CONFIDENCE');});}); describe('transitionState',()=>{it('successfully transitions between valid states',async()=>{const currentState:CoachState = {userId:mockUserId,currentState:'COLD_START',previousState:null,stateEnteredAt:Date.now(),personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(currentState); (repository.upsertCoachState as jest.Mock).mockImplementation(s=>s); const newState = await transitionState(mockUserId,'LOW_CONFIDENCE'); expect(newState.currentState).toBe('LOW_CONFIDENCE'); expect(newState.previousState).toBe('COLD_START');}); it('throws StateTransitionError for invalid transitions',async()=>{const currentState:CoachState = {userId:mockUserId,currentState:'HIGH_CONFIDENCE',previousState:null,stateEnteredAt:Date.now(),personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(currentState); await expect(transitionState(mockUserId,'COLD_START')).rejects.toThrow(StateTransitionError);}); it('allows forced transitions when bypassing validation',async()=>{const currentState:CoachState = {userId:mockUserId,currentState:'HIGH_CONFIDENCE',previousState:null,stateEnteredAt:Date.now(),personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(currentState); (repository.upsertCoachState as jest.Mock).mockImplementation(s=>s); const newState = await transitionState(mockUserId,'COLD_START',{},true); expect(newState.currentState).toBe('COLD_START');}); it('executes entry and exit actions',async()=>{const currentState:CoachState = {userId:mockUserId,currentState:'HIGH_CONFIDENCE',previousState:null,stateEnteredAt:Date.now(),personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(currentState); (repository.upsertCoachState as jest.Mock).mockImplementation(s=>s); const newState = await transitionState(mockUserId,'MUTED_MODE'); expect(newState.currentState).toBe('MUTED_MODE'); expect(repository.upsertCoachState).toHaveBeenCalled();});}); describe('checkAutoTransitions',()=>{it('auto-transitions when max duration exceeded',async()=>{const oldState:CoachState = {userId:mockUserId,currentState:'STREAK_AT_RISK',previousState:null,stateEnteredAt:Date.now() - 49 * 60 * 60 * 1000,personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(oldState); (repository.upsertCoachState as jest.Mock).mockImplementation(s=>s); (repository.fetchBehaviorProfile as jest.Mock).mockResolvedValue({userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:25}); const newState = await checkAutoTransitions(mockUserId); expect(newState).not.toBeNull(); expect(newState?.currentState).not.toBe('STREAK_AT_RISK');}); it('does not auto-transition when within duration',async()=>{const currentState:CoachState = {userId:mockUserId,currentState:'STREAK_AT_RISK',previousState:null,stateEnteredAt:Date.now() - 24 * 60 * 60 * 1000,personaId:'default',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; (repository.fetchCoachState as jest.Mock).mockResolvedValue(currentState); const newState = await checkAutoTransitions(mockUserId); expect(newState).toBeNull();});});}); describe('Intervention Engine',()=>{const mockUserId = 'user-123'; beforeEach(()=>{jest.clearAllMocks();}); describe('evaluateInterventions',()=>{it('executes applicable rules',async()=>{const mockRule:InterventionRule = {id:'rule-1',name:'Streak Risk Alert',trigger:{type:'STREAK_AT_RISK',threshold:24},conditions:[{field:'streakDays',operator:'gt',value:3}],action:{type:'SEND_MESSAGE',messageTemplateId:'template-1',deliveryMethod:'BOTH',delayMinutes:0},priority:10,cooldownHours:4,maxPerDay:3,enabled:true}; (repository.fetchInterventionRulesByTrigger as jest.Mock).mockResolvedValue([mockRule]); (repository.wasRuleTriggeredRecently as jest.Mock).mockResolvedValue(false); (repository.fetchTodaysInterventionExecutions as jest.Mock).mockResolvedValue([]); (repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,currentState:'HIGH_CONFIDENCE',muteUntil:null,reduceNotifications:false,interventionsToday:0}); const executions = await evaluateInterventions(mockUserId,'STREAK_AT_RISK',{streakDays:5,hoursSinceLastSession:30}); expect(executions.length).toBeGreaterThan(0);}); it('respects daily intervention limits',async()=>{(repository.fetchTodaysInterventionExecutions as jest.Mock).mockResolvedValue(Array(5).fill({id:'exec-1',ruleId:'rule-1'})); (repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,currentState:'HIGH_CONFIDENCE',muteUntil:null,reduceNotifications:false,interventionsToday:5}); const executions = await evaluateInterventions(mockUserId,'STREAK_AT_RISK',{}); expect(executions).toHaveLength(0);}); it('respects cooldown periods',async()=>{const mockRule:InterventionRule = {id:'rule-1',name:'Frequent Rule',trigger:{type:'STREAK_AT_RISK',threshold:24},conditions:[],action:{type:'SEND_MESSAGE',messageTemplateId:'template-1',deliveryMethod:'BOTH',delayMinutes:0},priority:5,cooldownHours:4,maxPerDay:10,enabled:true}; (repository.fetchInterventionRulesByTrigger as jest.Mock).mockResolvedValue([mockRule]); (repository.wasRuleTriggeredRecently as jest.Mock).mockResolvedValue(true); (repository.fetchTodaysInterventionExecutions as jest.Mock).mockResolvedValue([]); (repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,currentState:'HIGH_CONFIDENCE',muteUntil:null,reduceNotifications:false,interventionsToday:0}); const executions = await evaluateInterventions(mockUserId,'STREAK_AT_RISK',{}); expect(executions).toHaveLength(0);}); it('filters out disabled rules',async()=>{const disabledRule:InterventionRule = {id:'rule-1',name:'Disabled Rule',trigger:{type:'STREAK_AT_RISK',threshold:24},conditions:[],action:{type:'SEND_MESSAGE',messageTemplateId:'template-1',deliveryMethod:'BOTH',delayMinutes:0},priority:10,cooldownHours:4,maxPerDay:3,enabled:false}; (repository.fetchInterventionRulesByTrigger as jest.Mock).mockResolvedValue([disabledRule]); (repository.fetchTodaysInterventionExecutions as jest.Mock).mockResolvedValue([]); (repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,currentState:'HIGH_CONFIDENCE',muteUntil:null,reduceNotifications:false,interventionsToday:0}); const executions = await evaluateInterventions(mockUserId,'STREAK_AT_RISK',{}); expect(executions).toHaveLength(0);});});}); describe('Behavior Analytics',()=>{const mockUserId = 'user-123'; beforeEach(()=>{jest.clearAllMocks();}); describe('processBehaviorSignal',()=>{it('creates and persists behavior signal',async()=>{(repository.addBehaviorSignal as jest.Mock).mockImplementation(s=>s); (repository.fetchRecentBehaviorSignals as jest.Mock).mockResolvedValue([]); (repository.upsertBehaviorProfile as jest.Mock).mockImplementation(p=>p); const profile = await processBehaviorSignal(mockUserId,'SESSION_QUALITY_TREND',0.85,{source:'test'}); expect(profile).toBeDefined(); expect(profile.userId).toBe(mockUserId); expect(repository.addBehaviorSignal).toHaveBeenCalled();}); it('calculates correct confidence for different signal types',async()=>{(repository.addBehaviorSignal as jest.Mock).mockImplementation(s=>s); (repository.fetchRecentBehaviorSignals as jest.Mock).mockResolvedValue([]); (repository.upsertBehaviorProfile as jest.Mock).mockImplementation(p=>p); const profile1 = await processBehaviorSignal(mockUserId,'STREAK_MAINTENANCE_RATE',0.9,{}); const profile2 = await processBehaviorSignal(mockUserId,'SOCIAL_ENGAGEMENT',0.9,{}); expect(profile1.signals[0].confidence).toBeGreaterThan(profile2.signals[0].confidence);});}); describe('detectPatterns',()=>{it('detects morning person pattern',()=>{const profile:BehaviorProfile = {userId:mockUserId,signals:[{id:'sig-1',userId:mockUserId,signalType:'MORNING_PERSON',value:0.85,confidence:0.8,timestamp:Date.now(),metadata:{},expiresAt:Date.now() + 86400000}],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:25}; const patterns = detectPatterns(profile); expect(patterns.some(p=>p.patternType === 'CHRONOTYPE')).toBe(true);}); it('detects streak maintainer pattern',()=>{const profile:BehaviorProfile = {userId:mockUserId,signals:[{id:'sig-1',userId:mockUserId,signalType:'STREAK_MAINTENANCE_RATE',value:0.9,confidence:0.85,timestamp:Date.now(),metadata:{},expiresAt:Date.now() + 86400000}],lastUpdated:Date.now(),confidenceLevel:'HIGH',coldStart:false,dataPoints:30}; const patterns = detectPatterns(profile); expect(patterns.some(p=>p.patternType === 'CONSISTENCY')).toBe(true);}); it('returns empty patterns for cold start users',()=>{const profile:BehaviorProfile = {userId:mockUserId,signals:[],lastUpdated:Date.now(),confidenceLevel:'LOW',coldStart:true,dataPoints:2}; const patterns = detectPatterns(profile); expect(patterns).toHaveLength(0);});});}); describe('Retry Utilities',()=>{describe('CircuitBreaker',()=>{it('allows operations when closed',async()=>{const cb = new CircuitBreaker({failureThreshold:3,resetTimeoutMs:1000,halfOpenMaxCalls:1},'test'); const result = await cb.execute(async()=>'success'); expect(result).toBe('success');}); it('opens after threshold failures',async()=>{const cb = new CircuitBreaker({failureThreshold:2,resetTimeoutMs:5000,halfOpenMaxCalls:1},'test'); try{await cb.execute(async()=>{throw new Error('fail');});}catch (error) { captureSilentFailure(error, { feature: 'ai-coach', operation: 'network-fallback', type: 'network' });}expect(cb.getState()).toBe('CLOSED'); try{await cb.execute(async()=>{throw new Error('fail');});}catch (error) { captureSilentFailure(error, { feature: 'ai-coach', operation: 'network-fallback', type: 'network' });}expect(cb.getState()).toBe('OPEN'); await expect(cb.execute(async()=>'success')).rejects.toThrow(CircuitBreakerOpenError);}); it('transitions to half-open after timeout',async()=>{const cb = new CircuitBreaker({failureThreshold:1,resetTimeoutMs:100,halfOpenMaxCalls:1},'test'); try{await cb.execute(async()=>{throw new Error('fail');});}catch (error) { captureSilentFailure(error, { feature: 'ai-coach', operation: 'network-fallback', type: 'network' });}expect(cb.getState()).toBe('OPEN'); await new Promise(r=>setTimeout(r,150));});});}); describe('Message Generator',()=>{const mockUserId = 'user-123'; beforeEach(()=>{jest.clearAllMocks();}); it('generates message from template',async()=>{(repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,personaId:'default',muteUntil:null,reduceNotifications:false}); (repository.fetchMessageTemplates as jest.Mock).mockResolvedValue([{id:'template-1',personaId:'default',category:'MOTIVATION_BOOST',priority:8,content:'You are doing great, {{name}}!',conditions:[],variations:[],cooldownHours:4}]); const message = await generateMessage({userId:mockUserId,category:'MOTIVATION_BOOST',context:{name:'User'},preferredDelivery:'BOTH'}); expect(message).not.toBeNull(); expect(message?.content).toContain('User');}); it('returns null when user is muted',async()=>{(repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,personaId:'default',muteUntil:Date.now() + 86400000,reduceNotifications:true}); const message = await generateMessage({userId:mockUserId,category:'MOTIVATION_BOOST',context:{},preferredDelivery:'BOTH'}); expect(message).toBeNull();}); it('validates generated content',async()=>{(repository.fetchCoachState as jest.Mock).mockResolvedValue({userId:mockUserId,personaId:'default',muteUntil:null,reduceNotifications:false}); (repository.fetchMessageTemplates as jest.Mock).mockResolvedValue([{id:'template-1',personaId:'default',category:'MOTIVATION_BOOST',priority:8,content:'<script>alert("hack")</script>',conditions:[],variations:[],cooldownHours:4}]); const message = await generateMessage({userId:mockUserId,category:'MOTIVATION_BOOST',context:{},preferredDelivery:'BOTH'}); expect(message?.content).not.toContain('<script>');});});

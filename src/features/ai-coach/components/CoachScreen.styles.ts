import { createSheet } from '@/shared/ui/create-sheet';
import { lightColors } from '@/theme/tokens/colors';

export const styles = createSheet({
  container: { flex: 1, backgroundColor: lightColors.semantic.background },
  loadingContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: lightColors.semantic.background,
  },
  loadingText: { marginTop: 16, color: lightColors.text.muted, fontSize: 16 },
  header: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: 16,
    backgroundColor: lightColors.semantic.background,
    borderBottomWidth: 1,
    borderBottomColor: lightColors.semantic.border,
  },
  coachInfo: { flex: 1 },
  coachName: {
    fontSize: 20,
    fontWeight: '700',
    color: lightColors.semantic.textPrimary,
  },
  statusBadge: { marginTop: 4 },
  statusText: { fontSize: 12, color: lightColors.text.muted },
  stateIndicator: { flexDirection: 'row', alignItems: 'center', gap: 6 },
  stateDot: { width: 8, height: 8, borderRadius: 4 },
  stateLabel: { fontSize: 12, color: lightColors.text.muted },
  recommendationCard: {
    margin: 16,
    padding: 16,
    backgroundColor: lightColors.semantic.surfaceElevated,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: lightColors.semantic.primary,
  },
  recommendationTitle: {
    fontSize: 14,
    fontWeight: '600',
    color: lightColors.semantic.primary,
    marginBottom: 8,
  },
  recommendationReason: {
    fontSize: 14,
    color: lightColors.semantic.textSecondary,
    marginBottom: 12,
    lineHeight: 20,
  },
  startSessionButton: {
    backgroundColor: lightColors.semantic.primary,
    paddingVertical: 12,
    paddingHorizontal: 16,
    borderRadius: 8,
    alignItems: 'center',
  },
  startSessionButtonText: {
    color: lightColors.text.inverse,
    fontSize: 14,
    fontWeight: '600',
  },
  chatContainer: { flex: 1 },
  chatContent: { padding: 16, paddingBottom: 96 },
  messageContainer: {
    marginBottom: 10,
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
  coachMessageContainer: { justifyContent: 'flex-start' },
  userMessageContainer: { justifyContent: 'flex-end' },
  systemMessageContainer: { justifyContent: 'center' },
  coachAvatar: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: lightColors.semantic.surfaceElevated,
    borderWidth: 1,
    borderColor: lightColors.semantic.border,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 8,
    shadowColor: lightColors.semantic.shadow,
    shadowOffset: { width: 0, height: 8 },
    shadowOpacity: 0.14,
    shadowRadius: 16,
  },
  coachAvatarSmall: {
    width: 28, height: 28, borderRadius: 14,
    backgroundColor: lightColors.semantic.primary,
    justifyContent: 'center', alignItems: 'center', marginRight: 8,
  },
  coachAvatarTextSmall: { fontSize: 14 },
  messageBubble: {
    maxWidth: '88%',
    padding: 14,
    borderRadius: 20,
    shadowColor: lightColors.semantic.shadow,
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.12,
    shadowRadius: 20,
  },
  coachBubble: {
    backgroundColor: lightColors.semantic.surfaceElevated,
    borderWidth: 1,
    borderColor: lightColors.semantic.border,
    borderBottomLeftRadius: 10,
  },
  userBubble: {
    backgroundColor: lightColors.semantic.primary,
    borderBottomRightRadius: 10,
  },
  systemBubble: { backgroundColor: lightColors.semantic.backgroundMuted },
  messageText: { fontSize: 15, lineHeight: 21 },
  coachText: { color: lightColors.semantic.textPrimary },
  userText: { color: lightColors.text.inverse },
  systemText: {
    color: lightColors.text.muted,
    fontSize: 12,
    textAlign: 'center',
  },
  actionButton: {
    marginTop: 12,
    backgroundColor: lightColors.semantic.primary,
    paddingVertical: 8,
    paddingHorizontal: 12,
    borderRadius: 6,
    alignSelf: 'flex-start',
  },
  actionButtonText: {
    color: lightColors.text.inverse,
    fontSize: 13,
    fontWeight: '600',
  },
  typingContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 12,
    paddingHorizontal: 16,
  },
  typingBubble: {
    flexDirection: 'row', alignItems: 'center',
    backgroundColor: lightColors.semantic.surfaceElevated,
    padding: 14,
    borderRadius: 18,
    borderBottomLeftRadius: 8,
    gap: 8,
  },
  typingText: { fontSize: 13, color: lightColors.text.muted },
  errorBanner: {
    marginHorizontal: 16, marginBottom: 8, padding: 24,
    backgroundColor: lightColors.semantic.danger,
    borderRadius: 8,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  errorText: { color: lightColors.text.inverse, fontSize: 13, flex: 1 },
  errorDismiss: {
    color: lightColors.text.inverse,
    fontSize: 13,
    fontWeight: '600',
    marginLeft: 12,
  },
  inputContainer: {
    flexDirection: 'row', alignItems: 'flex-end', padding: 10,
    backgroundColor: lightColors.semantic.surfaceElevated,
    borderTopWidth: 1,
    borderTopColor: lightColors.semantic.border,
  },
  input: {
    flex: 1,
    backgroundColor: lightColors.semantic.inputBackground,
    borderWidth: 1,
    borderColor: lightColors.semantic.inputBorder,
    borderRadius: 28,
    paddingHorizontal: 16,
    paddingVertical: 10,
    paddingTop: 10,
    color: lightColors.semantic.textPrimary,
    fontSize: 16,
    maxHeight: 100,
    marginRight: 8,
  },
  sendButton: {
    backgroundColor: lightColors.semantic.primary,
    minHeight: 52,
    minWidth: 76,
    paddingVertical: 12,
    paddingHorizontal: 18,
    borderRadius: 26,
    alignItems: 'center',
    justifyContent: 'center',
  },
  sendButtonDisabled: { backgroundColor: lightColors.text.muted },
  sendButtonText: { color: lightColors.text.inverse, fontSize: 14, fontWeight: '600' },
});

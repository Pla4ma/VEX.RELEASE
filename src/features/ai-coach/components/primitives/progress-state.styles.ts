import { createSheet } from '@/shared/ui/create-sheet';
import { lightColors } from '@/theme/tokens/colors';


export const styles = createSheet({
  progressContainer: { overflow: 'hidden', width: '100%' },
  progressFill: { height: '100%' },
  progressText: {
    fontSize: 12,
    color: lightColors.text.muted,
    marginTop: 4,
    textAlign: 'right',
  },
  segmentedContainer: { flexDirection: 'row', width: '100%' },
  segment: { flex: 1 },
  circularContainer: { justifyContent: 'center', alignItems: 'center' },
  circularSvg: { position: 'relative' },
  circularBackground: { position: 'absolute' },
  circularProgress: { position: 'absolute', transform: [{ rotate: '0deg' }] },
  circularText: {
    position: 'absolute',
    fontWeight: '700',
    color: lightColors.text.secondary,
  },
  loadingContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 24,
  },
  loadingMessage: {
    fontSize: 16,
    fontWeight: '600',
    color: lightColors.text.secondary,
    marginTop: 16,
  },
  loadingSubmessage: {
    fontSize: 14,
    color: lightColors.text.muted,
    marginTop: 8,
    textAlign: 'center',
  },
  loadingProgress: { width: '100%', maxWidth: 200, marginTop: 16 },
  dotsContainer: { flexDirection: 'row', gap: 8, alignItems: 'center' },
  dot: {
    width: 10,
    height: 10,
    borderRadius: 5,
    backgroundColor: lightColors.accent.teal,
  },
  stepsContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingHorizontal: 16,
  },
  stepItem: { alignItems: 'center', flex: 1 },
  stepCircle: {
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: lightColors.border.light,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 8,
  },
  stepCircleCompleted: { backgroundColor: lightColors.accent.teal },
  stepCircleCurrent: { backgroundColor: lightColors.semantic.warning },
  stepNumber: { fontSize: 14, fontWeight: '600', color: lightColors.text.muted },
  stepNumberActive: { color: lightColors.text.inverse },
  stepCheck: { fontSize: 16, color: lightColors.text.inverse, fontWeight: '700' },
  stepLabel: { fontSize: 12, color: lightColors.text.disabled, textAlign: 'center' },
  stepLabelCompleted: { color: lightColors.accent.teal },
  stepLabelCurrent: { color: lightColors.text.secondary, fontWeight: '600' },
  stepLine: {
    position: 'absolute',
    top: 15,
    left: '50%',
    width: '100%',
    height: 2,
    backgroundColor: lightColors.border.light,
    zIndex: -1,
  },
  stepLineCompleted: { backgroundColor: lightColors.accent.teal },
  successContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    padding: 32,
  },
  successIcon: {
    width: 80,
    height: 80,
    borderRadius: 40,
    backgroundColor: lightColors.accent.teal,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 16,
  },
  successIconText: {
    fontSize: 40,
    color: lightColors.text.inverse,
    fontWeight: '700',
  },
  successTitle: {
    fontSize: 24,
    fontWeight: '700',
    color: lightColors.text.secondary,
    marginBottom: 8,
  },
  successMessage: {
    fontSize: 16,
    color: lightColors.text.muted,
    textAlign: 'center',
  },
  processingContainer: { gap: 16, padding: 16 },
  processingItem: { flexDirection: 'row', alignItems: 'center', gap: 12 },
  processingDot: {
    width: 24,
    height: 24,
    borderRadius: 12,
    backgroundColor: lightColors.border.light,
    justifyContent: 'center',
    alignItems: 'center',
  },
  processingDotCompleted: { backgroundColor: lightColors.accent.teal },
  processingDotError: { backgroundColor: lightColors.semantic.danger },
  processingDotProcessing: { backgroundColor: lightColors.semantic.warning },
  processingCheck: {
    fontSize: 12,
    color: lightColors.text.inverse,
    fontWeight: '700',
  },
  processingError: {
    fontSize: 12,
    color: lightColors.text.inverse,
    fontWeight: '700',
  },
  processingLabel: { fontSize: 14, color: lightColors.text.disabled },
  processingLabelCompleted: {
    color: lightColors.accent.teal,
    textDecorationLine: 'line-through',
  },
  processingLabelError: { color: lightColors.semantic.danger },
});

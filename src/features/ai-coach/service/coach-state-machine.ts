import{type CoachUserState,type CoachState,type BehaviorProfile,type CoachMessage,CoachUserStateSchema}from'../schemas'; import*as repository from'../repository'; import{withRetry,RetryableError}from'../utils/retry'; interface StateConfig{onEntry?:(state:CoachState,previousState:CoachUserState|null)=>Promise<void>;onExit?:(state:CoachState,nextState:CoachUserState)=>Promise<void>;allowedTransitions:CoachUserState[];maxDurationHours?:number;requiredDataPoints?:number;}const STATE_CONFIG:Record<CoachUserState,StateConfig> = {COLD_START:{allowedTransitions:['LOW_CONFIDENCE','STREAK_AT_RISK'],requiredDataPoints:0},LOW_CONFIDENCE:{allowedTransitions:['HIGH_CONFIDENCE','STREAK_AT_RISK','COLD_START'],requiredDataPoints:5,maxDurationHours:168},HIGH_CONFIDENCE:{allowedTransitions:['STREAK_AT_RISK','COMEBACK_MODE','OVERLOAD_PROTECTION','MUTED_MODE'],requiredDataPoints:20},STREAK_AT_RISK:{allowedTransitions:['HIGH_CONFIDENCE','POST_FAILURE_SUPPORT','MUTED_MODE'],maxDurationHours:48},COMEBACK_MODE:{onEntry:async state=>{await ensureComebackPlan(state.userId);},allowedTransitions:['HIGH_CONFIDENCE','POST_FAILURE_SUPPORT','MUTED_MODE'],maxDurationHours:168},POST_FAILURE_SUPPORT:{onEntry:async state=>{await sendPostFailureSupport(state.userId);},allowedTransitions:['COMEBACK_MODE','LOW_CONFIDENCE','MUTED_MODE'],maxDurationHours:72},MILESTONE_HYPE:{onEntry:async state=>{await sendMilestoneCelebration(state.userId,state);},allowedTransitions:['HIGH_CONFIDENCE','STREAK_AT_RISK'],maxDurationHours:24},OVERLOAD_PROTECTION:{onEntry:async state=>{await reduceNotifications(state.userId);},onExit:async state=>{await restoreNotifications(state.userId);},allowedTransitions:['HIGH_CONFIDENCE','MUTED_MODE'],maxDurationHours:24},MUTED_MODE:{onEntry:async state=>{await muteAllNotifications(state.userId);},onExit:async state=>{await restoreNotifications(state.userId);},allowedTransitions:['HIGH_CONFIDENCE','LOW_CONFIDENCE'],maxDurationHours:168}}; export async function determineOptimalState(userId:string,profile:BehaviorProfile|null):Promise<CoachUserState>{const[currentState,streakStatus,recentSessions,comebackPlan] = await Promise.all([getCurrentStateSafe(userId),fetchStreakStatus(userId),fetchRecentSessionMetrics(userId,7),repository.fetchActiveComebackPlan(userId)]); if(comebackPlan?.status === 'ACTIVE'){return'COMEBACK_MODE';}if(streakStatus.isAtRisk && streakStatus.riskLevel !== 'NONE'){return'STREAK_AT_RISK';}if(streakStatus.wasRecentlyBroken && streakStatus.daysSinceBreak < 3){return'POST_FAILURE_SUPPORT';}if(streakStatus.recentMilestone && !streakStatus.milestoneCelebrated){return'MILESTONE_HYPE';}if(recentSessions.sessionsToday >= 5){return'OVERLOAD_PROTECTION';}if(currentState?.reduceNotifications || currentState?.muteUntil){return'MUTED_MODE';}if(!profile || profile.coldStart || profile.dataPoints < 5){return'COLD_START';}if(profile.confidenceLevel === 'LOW' || profile.dataPoints < 20){return'LOW_CONFIDENCE';}return'HIGH_CONFIDENCE';}export async function transitionState(userId:string,newState:CoachUserState,context?:Record<string,unknown>,force:boolean = false):Promise<CoachState>{const currentState = await getCurrentStateSafe(userId); const previousStateValue = currentState.currentState; if(!force && !isValidTransition(previousStateValue,newState)){throw new StateTransitionError(`Invalid transition: ${previousStateValue} → ${newState}`,previousStateValue,newState);}const currentConfig = STATE_CONFIG[previousStateValue]; if(currentConfig?.onExit){await withRetry(()=>currentConfig.onExit!(currentState,newState),{maxAttempts:3},'state-exit-action');}const newStateRecord:CoachState = {...currentState,currentState:newState,previousState:previousStateValue,stateEnteredAt:Date.now()}; await withRetry(()=>repository.upsertCoachState(newStateRecord),{maxAttempts:3},'persist-state'); const newConfig = STATE_CONFIG[newState]; if(newConfig?.onEntry){await withRetry(()=>newConfig.onEntry!(newStateRecord,previousStateValue),{maxAttempts:3},'state-entry-action');}emitStateChangedEvent(userId,previousStateValue,newState,context); return newStateRecord;}export async function checkAutoTransitions(userId:string):Promise<CoachState|null>{const state = await getCurrentStateSafe(userId); const config = STATE_CONFIG[state.currentState]; if(!config.maxDurationHours){return null;}const hoursInState = (Date.now() - state.stateEnteredAt) / (1000 * 60 * 60); if(hoursInState > config.maxDurationHours){const profile = await repository.fetchBehaviorProfile(userId); const nextState = await determineOptimalState(userId,profile); if(nextState !== state.currentState){return transitionState(userId,nextState,{autoTransition:true});}}return null;}function isValidTransition(from:CoachUserState,to:CoachUserState):boolean{if(from === to){return true;}const config = STATE_CONFIG[from]; return config.allowedTransitions.includes(to);}async function getCurrentStateSafe(userId:string):Promise<CoachState>{const state = await repository.fetchCoachState(userId); if(state){return state;}const defaultState:CoachState = {userId,currentState:'COLD_START',previousState:null,stateEnteredAt:Date.now(),personaId:'encouraging-mentor',behaviorProfile:null,lastInterventionAt:null,interventionsToday:0,muteUntil:null,reduceNotifications:false}; return repository.upsertCoachState(defaultState);}interface StreakStatus{isAtRisk:boolean;riskLevel:'NONE'|'LOW'|'MEDIUM'|'HIGH'|'CRITICAL';wasRecentlyBroken:boolean;daysSinceBreak:number;recentMilestone:boolean;milestoneCelebrated:boolean;}async function fetchStreakStatus(userId:string):Promise<StreakStatus>{return{isAtRisk:false,riskLevel:'NONE',wasRecentlyBroken:false,daysSinceBreak:0,recentMilestone:false,milestoneCelebrated:false};}interface SessionMetrics{sessionsToday:number;sessionsThisWeek:number;averageQuality:number;}async function fetchRecentSessionMetrics(userId:string,days:number):Promise<SessionMetrics>{return{sessionsToday:0,sessionsThisWeek:0,averageQuality:75};}async function ensureComebackPlan(userId:string):Promise<void>{const existing = await repository.fetchActiveComebackPlan(userId); if(!existing){}}async function sendPostFailureSupport(userId:string):Promise<void>{}async function sendMilestoneCelebration(userId:string,state:CoachState):Promise<void>{}async function reduceNotifications(userId:string):Promise<void>{await repository.upsertCoachState({...(await getCurrentStateSafe(userId)),reduceNotifications:true});}async function restoreNotifications(userId:string):Promise<void>{await repository.upsertCoachState({...(await getCurrentStateSafe(userId)),reduceNotifications:false,muteUntil:null});}async function muteAllNotifications(userId:string):Promise<void>{await repository.upsertCoachState({...(await getCurrentStateSafe(userId)),reduceNotifications:true,muteUntil:Date.now() + 7 * 24 * 60 * 60 * 1000});}function emitStateChangedEvent(userId:string,previousState:CoachUserState|null,newState:CoachUserState,context?:Record<string,unknown>):void{}export class StateTransitionError extends Error{constructor(message:string,public fromState:CoachUserState,public toState:CoachUserState){super(message); this.name = 'StateTransitionError';}}export class StateMachineError extends Error{constructor(message:string){super(message); this.name = 'StateMachineError';}}

/**
 * LiveOps Config — Store
 *
 * Zustand slice for liveops-config persistent client state.
 * Currently feature access state is managed via feature-access-store.ts
 * (external store with useSyncExternalStore). No Zustand slice needed yet.
 */

export {};

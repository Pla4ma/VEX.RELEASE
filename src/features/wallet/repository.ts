import{supabase}from'../../config/supabase'; import{WalletRowSchema,WalletTransactionSchema,CurrencyTypeSchema}from'./schemas'; import type{WalletRow,WalletTransaction,CreateTransactionInput}from'./types'; import{z}from'zod'; export class WalletRepositoryError extends Error{constructor(public readonly operation:string,public readonly cause?:unknown){super(`WalletRepository ${operation} failed`);}}export async function fetchWallet(userId:string):Promise<WalletRow[]>{const{data,error} = await supabase.from('wallets').select('*').eq('user_id',userId); if(error){throw new WalletRepositoryError('fetchWallet',error);}return z.array(WalletRowSchema).parse((data ?? []).map(row=>({id:row.id,userId:row.user_id,balance:row.balance ?? 0,currency:row.currency ?? 'COINS',createdAt:row.created_at,updatedAt:row.updated_at})));}export async function getWalletBalance(userId:string,currency:string):Promise<number>{const rows = await fetchWallet(userId); const row = rows.find(r=>r.currency === currency); return row?.balance ?? 0;}export async function upsertWallet(userId:string,currency:string,balance:number):Promise<WalletRow>{const existing = await findWalletRow(userId,currency); if(existing){const{data,error} = await supabase.from('wallets').update({balance,updated_at:new Date().toISOString()}).eq('id',existing.id).select().single(); if(error){throw new WalletRepositoryError('upsertWallet.update',error);}return WalletRowSchema.parse({id:data.id,userId:data.user_id,balance:data.balance ?? 0,currency:data.currency ?? currency,createdAt:data.created_at,updatedAt:data.updated_at});}const{data,error} = await supabase.from('wallets').insert({user_id:userId,currency,balance,created_at:new Date().toISOString(),updated_at:new Date().toISOString()}).select().single(); if(error){throw new WalletRepositoryError('upsertWallet.insert',error);}return WalletRowSchema.parse({id:data.id,userId:data.user_id,balance:data.balance ?? 0,currency:data.currency ?? currency,createdAt:data.created_at,updatedAt:data.updated_at});}async function findWalletRow(userId:string,currency:string):Promise<WalletRow|null>{const{data,error} = await supabase.from('wallets').select('*').eq('user_id',userId).eq('currency',currency).single(); if(error){if(error.code === 'PGRST116'){return null;}throw new WalletRepositoryError('findWalletRow',error);}return WalletRowSchema.parse({id:data.id,userId:data.user_id,balance:data.balance ?? 0,currency:data.currency ?? currency,createdAt:data.created_at,updatedAt:data.updated_at});}export async function createTransaction(input:CreateTransactionInput):Promise<WalletTransaction>{const{data,error} = await supabase.from('transactions').insert({wallet_id:input.walletId,amount:input.amount,type:input.type,status:'completed',description:input.description,metadata:input.metadata ?? null,created_at:new Date().toISOString()}).select().single(); if(error){throw new WalletRepositoryError('createTransaction',error);}return WalletTransactionSchema.parse({id:data.id,walletId:data.wallet_id,amount:data.amount,type:data.type,status:data.status,description:data.description,metadata:data.metadata,createdAt:data.created_at});}export async function fetchTransactionHistory(userId:string,limit:number = 50):Promise<WalletTransaction[]>{const walletRows = await fetchWallet(userId); const walletIds = walletRows.map(w=>w.id); if(walletIds.length === 0){return[];}const{data,error} = await supabase.from('transactions').select('*').in('wallet_id',walletIds).order('created_at',{ascending:false}).limit(limit); if(error){throw new WalletRepositoryError('fetchTransactionHistory',error);}return z.array(WalletTransactionSchema).parse((data ?? []).map(row=>({id:row.id,walletId:row.wallet_id,amount:row.amount,type:row.type,status:row.status,description:row.description,metadata:row.metadata,createdAt:row.created_at})));}

import{describe,it,expect,jest,beforeEach}from'@jest/globals'; import*as service from'../service'; import*as repository from'../repository'; import{eventBus}from'../../../events'; jest.mock('../repository'); jest.mock('../../../events',()=>({eventBus:{publish:jest.fn()}})); const mockedRepository = jest.mocked(repository); const mockedEventBus = jest.mocked(eventBus); describe('Battle Pass Service',()=>{beforeEach(()=>{jest.clearAllMocks();}); describe('getUserBattlePassSummary',()=>{it('should return battle pass summary for user',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',currentTier:5,isPremium:true,claimedFreeTiers:[1,2,3],claimedPremiumTiers:[1,2]}; const mockBp = {id:'bp-1',seasonId:'season-1',tiers:[]}; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.fetchBattlePassTiers.mockResolvedValue([]); const result = await service.getUserBattlePassSummary('user-1','season-1'); expect(result).toBeDefined(); expect(result.userProgress.userId).toBe('user-1'); expect(result.userProgress.currentTier).toBe(5);}); it('should create new progress if none exists',async()=>{mockedRepository.fetchUserBattlePass.mockResolvedValue(null); mockedRepository.createUserBattlePass.mockResolvedValue({id:'new-bp',userId:'user-1',seasonId:'season-1',currentTier:0}as any); const mockBp = {id:'bp-1',seasonId:'season-1'}; mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.fetchBattlePassTiers.mockResolvedValue([]); const result = await service.getUserBattlePassSummary('user-1','season-1'); expect(mockedRepository.createUserBattlePass).toHaveBeenCalled(); expect(result.userProgress.currentTier).toBe(0);});}); describe('addBattlePassXp',()=>{it('should add XP and advance tiers',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',currentTier:1,tierXp:500,totalXp:500,isPremium:false}; const mockBp = {id:'bp-1',seasonId:'season-1',xpPerTier:1000}; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.updateUserBattlePass.mockResolvedValue({...mockUserBp,currentTier:2,tierXp:200,totalXp:1200}as any); const result = await service.addBattlePassXp('user-1','season-1',700); expect(result.xpAdded).toBe(700); expect(result.tiersGained).toBe(1); expect(mockedEventBus.publish).toHaveBeenCalledWith('seasons:tier_unlocked',expect.any(Object));});}); describe('claimTier',()=>{it('should claim free tier reward',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',currentTier:5,isPremium:false,claimedFreeTiers:[]}; const mockBp = {id:'bp-1',seasonId:'season-1'}; const mockTiers = [{tierNumber:1,freeReward:{type:'COINS',amount:100},premiumReward:null}]; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.fetchBattlePassTiers.mockResolvedValue(mockTiers as any); mockedRepository.markTierClaimed.mockResolvedValue(); const result = await service.claimTier({userId:'user-1',seasonId:'season-1',tierNumber:1,track:'FREE'}); expect(result.success).toBe(true); expect(result.rewards).toHaveLength(1);}); it('should fail to claim premium without premium pass',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',currentTier:5,isPremium:false}; const mockBp = {id:'bp-1',seasonId:'season-1'}; const mockTiers = []; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.fetchBattlePassTiers.mockResolvedValue(mockTiers as any); const result = await service.claimTier({userId:'user-1',seasonId:'season-1',tierNumber:1,track:'PREMIUM'}); expect(result.success).toBe(false); expect(result.error).toContain('Premium required');});}); describe('purchasePremium',()=>{it('should purchase premium pass',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',currentTier:5,isPremium:false,claimedFreeTiers:[1,2,3,4,5]}; const mockBp = {id:'bp-1',seasonId:'season-1',premiumPriceGems:499}; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); mockedRepository.updateUserBattlePass.mockResolvedValue({...mockUserBp,isPremium:true}as any); const result = await service.purchasePremium({userId:'user-1',seasonId:'season-1',gemsAvailable:500}); expect(result.success).toBe(true); expect(result.gemsDeducted).toBe(499);}); it('should fail if insufficient gems',async()=>{const mockUserBp = {id:'ubp-1',userId:'user-1',seasonId:'season-1',isPremium:false}; const mockBp = {id:'bp-1',seasonId:'season-1',premiumPriceGems:499}; mockedRepository.fetchUserBattlePass.mockResolvedValue(mockUserBp as any); mockedRepository.fetchBattlePassBySeason.mockResolvedValue(mockBp as any); const result = await service.purchasePremium({userId:'user-1',seasonId:'season-1',gemsAvailable:100}); expect(result.success).toBe(false); expect(result.error).toContain('Insufficient gems');});});});

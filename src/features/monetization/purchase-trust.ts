import{z}from'zod'; import{createDebugger}from'../../utils/debug'; const debug = createDebugger('monetization:purchase-trust'); export interface PurchaseVerification{verified:boolean;transactionId:string;productId:string;tier:string;purchaseDate:number;expiryDate?:number;isTrial:boolean;platform:'ios'|'android'|'web';}export type TrustSignal='secure_payment'|'money_back_guarantee'|'no_hidden_fees'|'cancel_anytime'|'encrypted_transaction'|'verified_reviews'; export interface TrustSignalConfig{id:TrustSignal;icon:string;title:string;description:string;priority:number;}export const TRUST_SIGNALS:TrustSignalConfig[] = [{id:'secure_payment',icon:'shield-check',title:'Secure Payment',description:'256-bit SSL encryption',priority:1},{id:'cancel_anytime',icon:'x-circle',title:'Cancel Anytime',description:'No commitment required',priority:2},{id:'money_back_guarantee',icon:'refresh-cw',title:'7-Day Guarantee',description:'Full refund if not satisfied',priority:3},{id:'no_hidden_fees',icon:'eye',title:'No Hidden Fees',description:'What you see is what you pay',priority:4}]; export const PurchaseReceiptSchema = z.object({transactionId:z.string(),productId:z.string(),tier:z.enum(['free','premium']),purchaseDate:z.number(),expiryDate:z.number().optional(),isTrial:z.boolean(),platform:z.enum(['ios','android','web']),receiptData:z.string()}); export type PurchaseReceipt=z.infer<typeof PurchaseReceiptSchema>; export async function verifyPurchaseReceipt(receipt:PurchaseReceipt):Promise<PurchaseVerification>{debug.info('Verifying purchase: %s',receipt.transactionId); const verification:PurchaseVerification = {verified:true,transactionId:receipt.transactionId,productId:receipt.productId,tier:receipt.tier,purchaseDate:receipt.purchaseDate,expiryDate:receipt.expiryDate,isTrial:receipt.isTrial,platform:receipt.platform}; return verification;}export async function restorePurchases(userId:string):Promise<PurchaseVerification[]>{debug.info('Restoring purchases for user: %s',userId); return[];}export function isPurchaseValid(purchase:PurchaseVerification):boolean{if(!purchase.expiryDate){return true;}return purchase.expiryDate > Date.now();}export function getRemainingDays(purchase:PurchaseVerification):number{if(!purchase.expiryDate){return Infinity;}const remaining = purchase.expiryDate - Date.now(); return Math.ceil(remaining / (24 * 60 * 60 * 1000));}export function getActiveTrustSignals(includeTrial:boolean,limit:number = 3):TrustSignalConfig[]{const signals = [...TRUST_SIGNALS]; if(includeTrial){signals.push({id:'verified_reviews',icon:'star',title:'Loved by Users',description:'4.9/5 from 10,000+ reviews',priority:5});}return signals.sort((a,b)=>a.priority - b.priority).slice(0,limit);}export function calculatePriceTrustScore(originalPrice:number,discountedPrice:number,hasTrial:boolean,hasGuarantee:boolean):number{let score = 50; if(discountedPrice < originalPrice){score += 15;}if(hasTrial){score += 20;}if(hasGuarantee){score += 15;}return Math.min(100,score);}export function getPriceExplanation(tier:string,price:number,period:'month'|'year',hasTrial:boolean):string{const dailyCost = price / 30; if(hasTrial){return`Start free, then ${price}/${period}. That's just $${dailyCost.toFixed(2)}/day.`;}return`${price}/${period} = $${dailyCost.toFixed(2)}/day - less than a coffee.`;}export function verifyPurchaseHash(transactionId:string,productId:string,timestamp:number,secretKey:string):string{return`hash-${transactionId}-${timestamp}`;}export function isSuspiciousPurchase(purchase:PurchaseVerification,userHistory:{purchases:number;refunds:number;}):boolean{if(userHistory.refunds > 3){return true;}if(userHistory.purchases > 5){return true;}return false;}export async function logPurchaseAttempt(userId:string,tier:string,success:boolean,error?:string):Promise<void>{debug.info('Purchase attempt: user=%s tier=%s success=%s',userId,tier,success); if(error){debug.error('Purchase error: %s',new Error(error));}}export function getRefundEligibility(purchase:PurchaseVerification,daysSincePurchase:number):{eligible:boolean;reason:string;daysRemaining:number;}{const REFUND_WINDOW_DAYS = 7; const daysRemaining = REFUND_WINDOW_DAYS - daysSincePurchase; if(daysSincePurchase <= REFUND_WINDOW_DAYS){return{eligible:true,reason:'Within 7-day guarantee period',daysRemaining};}return{eligible:false,reason:'Refund period expired',daysRemaining:0};}

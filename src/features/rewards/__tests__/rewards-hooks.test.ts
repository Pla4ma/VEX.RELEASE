/**
 * Tests for rewards hooks — archived.
 * useRewards / useVaultRewards removed for final release.
 */
describe('rewards hooks (archived)', () => {
  it('placeholder — reward hooks removed', () => {
    expect(true).toBe(true);
  });
});

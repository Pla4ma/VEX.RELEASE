import{createDebugger}from'../../utils/debug'; import type{SessionState}from'../types'; import{TimeCalculator}from'../engines/TimeCalculator'; export interface TickHandler{(payload:{sessionId:string;timestamp:number;deltaMs:number;}):void;}const debug = createDebugger('session:timer'); export interface TimerConfig{tickIntervalMs:number;warningThresholds:number[];autoCompleteOnZero:boolean;trackBackgroundTime:boolean;}const DEFAULT_TIMER_CONFIG:TimerConfig = {tickIntervalMs:1000,warningThresholds:[300,60,10],autoCompleteOnZero:true,trackBackgroundTime:true}; export class SessionTimerService{private config:TimerConfig; private intervals:Map<string,number> = new Map(); private lastTick:Map<string,number> = new Map(); private tickHandlers:Map<string,Set<TickHandler>> = new Map(); constructor(config:Partial<TimerConfig> = {}){this.config = {...DEFAULT_TIMER_CONFIG,...config};}startTimer(sessionId:string):boolean{if(this.intervals.has(sessionId)){debug.warn('Timer already running for session %s',sessionId); return false;}debug.info('Starting timer for session %s',sessionId); this.lastTick.set(sessionId,Date.now()); this.tickHandlers.set(sessionId,new Set()); const intervalId = window.setInterval(()=>{this.tick(sessionId);},this.config.tickIntervalMs); this.intervals.set(sessionId,intervalId); return true;}stopTimer(sessionId:string):boolean{const intervalId = this.intervals.get(sessionId); if(!intervalId){debug.warn('No timer running for session %s',sessionId); return false;}debug.info('Stopping timer for session %s',sessionId); clearInterval(intervalId); this.intervals.delete(sessionId); this.lastTick.delete(sessionId); this.tickHandlers.delete(sessionId); return true;}pauseTimer(sessionId:string):void{debug.info('Pausing timer for session %s',sessionId); this.stopTimer(sessionId);}resumeTimer(sessionId:string):void{debug.info('Resuming timer for session %s',sessionId); this.startTimer(sessionId);}private tick(sessionId:string):void{const now = Date.now(); const lastTick = this.lastTick.get(sessionId) || now; const deltaMs = now - lastTick; this.lastTick.set(sessionId,now); const tickPayload = {sessionId,timestamp:now,deltaMs}; const handlers = this.tickHandlers.get(sessionId); if(handlers){handlers.forEach(handler=>{try{handler(tickPayload);}catch(error){debug.error('Tick handler error for session ' + sessionId,error instanceof Error ? error : new Error(String(error)));}});}}registerTickHandler(sessionId:string,handler:TickHandler):()=>void{const handlers = this.tickHandlers.get(sessionId); if(!handlers){debug.warn('No timer running for session %s, handler not registered',sessionId); return()=>{};}handlers.add(handler); return()=>{handlers.delete(handler);};}calculateProgress(session:SessionState):{elapsed:number;remaining:number;percentage:number;isComplete:boolean;shouldWarn:boolean;warningThreshold:number|null;}{const now = Date.now(); const startTime = session.startedAt || session.createdAt; const pausedDuration = session.totalPausedTime || 0; const rawElapsed = Math.floor((now - startTime) / 1000); const elapsed = Math.max(0,rawElapsed - pausedDuration); const duration = session.config.duration; const remaining = Math.max(0,duration - elapsed); const percentage = TimeCalculator.calculateCompletionPercentage(elapsed,duration); const isComplete = remaining === 0; const previousRemaining = session.remainingTime || duration; const warningThreshold = TimeCalculator.shouldTriggerWarning(remaining,previousRemaining,this.config.warningThresholds); return{elapsed,remaining,percentage,isComplete,shouldWarn:warningThreshold !== null,warningThreshold};}shouldTransitionPhase(session:SessionState):{shouldTransition:boolean;nextPhase?:string;reason?:string;}{const{elapsed,remaining} = this.calculateProgress(session); const intervalDuration = session.config.duration / (session.totalIntervals || 1); const currentIntervalElapsed = elapsed % intervalDuration; const isIntervalComplete = currentIntervalElapsed >= intervalDuration - 1; if(isIntervalComplete && (session.currentInterval || 0) < (session.totalIntervals || 1)){return{shouldTransition:true,nextPhase:this.getNextPhase(session.phase),reason:'interval_complete'};}if(remaining === 0){return{shouldTransition:true,nextPhase:'COMPLETED',reason:'duration_complete'};}return{shouldTransition:false};}private getNextPhase(currentPhase:string):string{const transitions:Record<string,string> = {'FOCUS':'SHORT_BREAK','SHORT_BREAK':'FOCUS','LONG_BREAK':'FOCUS','PREPARATION':'FOCUS'}; return transitions[currentPhase] || 'FOCUS';}cleanup():void{debug.info('Cleaning up all timers'); for(const[sessionId,intervalId]of this.intervals){clearInterval(intervalId); debug.debug('Stopped timer for session %s',sessionId);}this.intervals.clear(); this.lastTick.clear(); this.tickHandlers.clear();}}let timerService:SessionTimerService|null = null; export function getSessionTimerService(config?:Partial<TimerConfig>):SessionTimerService{if(!timerService){timerService = new SessionTimerService(config);}return timerService;}export function resetSessionTimerService():void{timerService?.cleanup(); timerService = null;}export default SessionTimerService;

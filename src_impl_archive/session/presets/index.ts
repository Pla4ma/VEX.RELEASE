import{getMMKVStorageAdapter}from'../../persistence/MMKVStorageAdapter'; import{v4 as uuidv4}from'../../utils/uuid'; import type{SessionPreset}from'../types'; import{ValidateSessionPresetSchema}from'../validation/schemas'; import{createDebugger}from'../../utils/debug'; const debug = createDebugger('session:presets'); export const DEFAULT_PRESETS:Omit<SessionPreset,'id'|'createdAt'|'updatedAt'>[] = [{name:'Quick Focus',description:'Short burst of focused work - perfect for quick tasks',duration:15 * 60,breakDuration:2 * 60,longBreakDuration:5 * 60,intervals:1,longBreakInterval:4,isDefault:true,category:'quick',tags:['quick','focus'],soundEnabled:true,vibrationEnabled:true,dndEnabled:false,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Pomodoro Classic',description:'Traditional Pomodoro technique - 25 min work, 5 min break',duration:25 * 60,breakDuration:5 * 60,longBreakDuration:15 * 60,intervals:4,longBreakInterval:4,isDefault:true,category:'work',tags:['pomodoro','classic','work'],soundEnabled:true,vibrationEnabled:true,dndEnabled:true,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Deep Work',description:'Extended focus session for complex tasks',duration:50 * 60,breakDuration:10 * 60,longBreakDuration:30 * 60,intervals:2,longBreakInterval:2,isDefault:true,category:'deep',tags:['deep','focus','complex'],soundEnabled:true,vibrationEnabled:true,dndEnabled:true,strictMode:true,autoStartBreaks:false,autoStartNextInterval:false},{name:'Study Sprint',description:'Intense study session with regular breaks',duration:45 * 60,breakDuration:8 * 60,longBreakDuration:20 * 60,intervals:3,longBreakInterval:3,isDefault:true,category:'study',tags:['study','learning','education'],soundEnabled:true,vibrationEnabled:true,dndEnabled:true,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Creative Flow',description:'Uninterrupted creative work time',duration:90 * 60,breakDuration:15 * 60,longBreakDuration:30 * 60,intervals:1,longBreakInterval:1,isDefault:true,category:'creative',tags:['creative','flow','art','design'],soundEnabled:true,vibrationEnabled:false,dndEnabled:true,strictMode:true,autoStartBreaks:false,autoStartNextInterval:false},{name:'Meeting Prep',description:'Quick preparation before important meetings',duration:10 * 60,breakDuration:2 * 60,longBreakDuration:5 * 60,intervals:1,longBreakInterval:1,isDefault:true,category:'work',tags:['meeting','prep','quick'],soundEnabled:true,vibrationEnabled:true,dndEnabled:false,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Coding Block',description:'Focused development time with strategic breaks',duration:55 * 60,breakDuration:10 * 60,longBreakDuration:25 * 60,intervals:3,longBreakInterval:3,isDefault:true,category:'development',tags:['coding','development','engineering'],soundEnabled:true,vibrationEnabled:true,dndEnabled:true,strictMode:true,autoStartBreaks:false,autoStartNextInterval:false},{name:'Reading Session',description:'Focused reading and comprehension time',duration:30 * 60,breakDuration:5 * 60,longBreakDuration:15 * 60,intervals:2,longBreakInterval:2,isDefault:true,category:'learning',tags:['reading','learning','books'],soundEnabled:true,vibrationEnabled:false,dndEnabled:true,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Writing Sprint',description:'Intense writing focus with recovery breaks',duration:40 * 60,breakDuration:8 * 60,longBreakDuration:20 * 60,intervals:3,longBreakInterval:3,isDefault:true,category:'creative',tags:['writing','content','creation'],soundEnabled:true,vibrationEnabled:true,dndEnabled:true,strictMode:false,autoStartBreaks:false,autoStartNextInterval:false},{name:'Meditation',description:'Mindfulness and meditation practice',duration:20 * 60,breakDuration:5 * 60,longBreakDuration:10 * 60,intervals:1,longBreakInterval:1,isDefault:true,category:'wellness',tags:['meditation','mindfulness','wellness'],soundEnabled:true,vibrationEnabled:false,dndEnabled:true,strictMode:true,autoStartBreaks:false,autoStartNextInterval:false}]; export class PresetService{private userId:string|null = null; private userPresets:Map<string,SessionPreset> = new Map(); private static initialized = false; private static systemPresets:SessionPreset[] = []; constructor(userId?:string){this.userId = userId ?? null; this.initializeSystemPresets(); this.loadUserPresets();}setUserId(userId:string):void{this.userId = userId; this.loadUserPresets(); debug.info('PresetService user set: %s',userId);}private initializeSystemPresets():void{if(PresetService.initialized){return;}const now = Date.now(); PresetService.systemPresets = DEFAULT_PRESETS.map(preset=>({...preset,id:`system-${preset.name.toLowerCase().replace(/\s+/g,'-')}`,createdAt:now,updatedAt:now})); PresetService.initialized = true; debug.info('Initialized %d system presets',PresetService.systemPresets.length);}async createPreset(presetData:Omit<SessionPreset,'id'|'createdAt'|'updatedAt'|'userId'>):Promise<SessionPreset>{if(!this.userId){throw new Error('PresetService: No user set');}const now = Date.now(); const preset:SessionPreset = {...presetData,id:uuidv4(),userId:this.userId,createdAt:now,updatedAt:now}; const validation = ValidateSessionPresetSchema.safeParse(preset); if(!validation.success){throw new Error(`Invalid preset data: ${validation.error.message}`);}this.userPresets.set(preset.id,preset); await this.saveUserPresets(); debug.info('Created preset: %s (%s)',preset.name,preset.id); return preset;}async updatePreset(presetId:string,updates:Partial<Omit<SessionPreset,'id'|'createdAt'|'userId'>>):Promise<SessionPreset>{if(!this.userId){throw new Error('PresetService: No user set');}const existing = this.userPresets.get(presetId); if(!existing){throw new Error(`Preset not found: ${presetId}`);}const updated:SessionPreset = {...existing,...updates,updatedAt:Date.now()}; const validation = ValidateSessionPresetSchema.safeParse(updated); if(!validation.success){throw new Error(`Invalid preset data: ${validation.error.message}`);}this.userPresets.set(presetId,updated); await this.saveUserPresets(); debug.info('Updated preset: %s',presetId); return updated;}async deletePreset(presetId:string):Promise<void>{if(!this.userId){throw new Error('PresetService: No user set');}const existing = this.userPresets.get(presetId); if(!existing){throw new Error(`Preset not found: ${presetId}`);}this.userPresets.delete(presetId); await this.saveUserPresets(); debug.info('Deleted preset: %s',presetId);}getPresetById(presetId:string):SessionPreset|undefined{const userPreset = this.userPresets.get(presetId); if(userPreset){return userPreset;}return PresetService.systemPresets.find(p=>p.id === presetId);}getAllPresets():SessionPreset[]{return[...PresetService.systemPresets,...Array.from(this.userPresets.values())];}getSystemPresets():SessionPreset[]{return[...PresetService.systemPresets];}getUserPresets():SessionPreset[]{return Array.from(this.userPresets.values());}getPresetsByCategory(category:string):SessionPreset[]{return this.getAllPresets().filter(p=>p.category === category);}getDefaultPreset():SessionPreset{return PresetService.systemPresets[1]!;}getCategories():string[]{const allPresets = this.getAllPresets(); const categories = new Set(allPresets.map(p=>p.category).filter(Boolean)); return Array.from(categories)as string[];}createCustomPreset(config:{name:string;duration:number;breakDuration?:number;intervals?:number;category?:string;strictMode?:boolean;dndEnabled?:boolean;description?:string}):Promise<SessionPreset>{const presetData = {name:config.name,description:config.description || `Custom ${config.duration} minute session`,duration:config.duration * 60,breakDuration:(config.breakDuration || 5) * 60,longBreakDuration:15 * 60,intervals:config.intervals || 1,longBreakInterval:4,isDefault:false,category:config.category || 'custom',tags:['custom'],soundEnabled:true,vibrationEnabled:true,dndEnabled:config.dndEnabled ?? false,strictMode:config.strictMode ?? false,autoStartBreaks:false,autoStartNextInterval:false}; return this.createPreset(presetData);}private async loadUserPresets():Promise<void>{if(!this.userId){return;}try{const key = `session:presets:${this.userId}`; const storage = getMMKVStorageAdapter(); const data = await storage.getItem(key); if(data){const parsed = JSON.parse(data); this.userPresets = new Map(Object.entries(parsed)); debug.info('Loaded %d user presets',this.userPresets.size);}}catch(error){debug.error('Failed to load user presets',error as Error);}}private async saveUserPresets():Promise<void>{if(!this.userId){return;}try{const key = `session:presets:${this.userId}`; const data = Object.fromEntries(this.userPresets); const storage = getMMKVStorageAdapter(); await storage.setItem(key,JSON.stringify(data));}catch(error){debug.error('Failed to save user presets',error as Error);}}exportPresets():string{const presets = Array.from(this.userPresets.values()); return JSON.stringify(presets,null,2);}async importPresets(jsonData:string):Promise<number>{if(!this.userId){throw new Error('PresetService: No user set');}try{const presets = JSON.parse(jsonData)as SessionPreset[]; let imported = 0; for(const preset of presets){try{const validation = ValidateSessionPresetSchema.safeParse(preset); if(!validation.success){continue;}const existing = Array.from(this.userPresets.values()).find(p=>p.name === preset.name); if(existing){await this.updatePreset(existing.id,preset);}else{await this.createPreset({name:preset.name,description:preset.description,duration:preset.duration,breakDuration:preset.breakDuration,longBreakDuration:preset.longBreakDuration,intervals:preset.intervals,longBreakInterval:preset.longBreakInterval,isDefault:false,category:preset.category,tags:preset.tags,soundEnabled:preset.soundEnabled,vibrationEnabled:preset.vibrationEnabled,dndEnabled:preset.dndEnabled,strictMode:preset.strictMode,autoStartBreaks:preset.autoStartBreaks,autoStartNextInterval:preset.autoStartNextInterval});}imported++;}catch(error){debug.error('Failed to import preset',error as Error);}}debug.info('Imported %d presets',imported); return imported;}catch(error){throw new Error(`Failed to import presets: ${(error as Error).message}`);}}}let presetServiceInstance:PresetService|null = null; export function getPresetService(userId?:string):PresetService{if(!presetServiceInstance){presetServiceInstance = new PresetService(userId);}else if(userId){presetServiceInstance.setUserId(userId);}return presetServiceInstance;}

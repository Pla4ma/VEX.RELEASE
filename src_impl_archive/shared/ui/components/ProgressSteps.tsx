import React, { useCallback } from "react"; import { View, ViewStyle, Pressable } from "react-native"; import Animated, { useSharedValue, useAnimatedStyle, withSpring, withTiming, interpolate, Extrapolation } from "react-native-reanimated"; import { Text } from "../../../components/primitives/Text"; import { Icon } from "../../../icons"; import { useTheme, type Theme } from "../../../theme"; import { createSheet } from "@/shared/ui/create-sheet"; export type StepStatus = "pending" | "active" | "completed" | "error" | "disabled"; export interface Step { id: string; title: string; description?: string; icon?: string; status: StepStatus; errorMessage?: string; disabled?: boolean; } export interface ProgressStepsProps { steps: Step[]; currentStep: number; orientation?: "horizontal" | "vertical"; size?: "sm" | "md" | "lg"; variant?: "default" | "numbers" | "dots"; showDescriptions?: boolean; allowClick?: boolean; onStepPress?: (stepIndex: number, step: Step) => void; style?: ViewStyle; } export interface StepIndicatorProps { status: StepStatus; index: number; size?: "sm" | "md" | "lg"; variant?: "default" | "numbers" | "dots"; icon?: string; title?: string; description?: string; showDescription?: boolean; onPress?: () => void; disabled?: boolean; } export const StepIndicator: React.FC<StepIndicatorProps> = ({ status, index, size = "md", variant = "default", icon, title, description, showDescription = true, onPress, disabled = false }) => { const { theme } = useTheme(); const progress = useSharedValue(status === "completed" ? 1 : status === "active" ? 0.5 : 0); React.useEffect(() => { progress.value = withSpring(status === "completed" ? 1 : status === "active" ? 0.5 : 0, { damping: 15, stiffness: 150 }); }, [status, progress]); const animatedStyle = useAnimatedStyle(() => ({ transform: [ { scale: interpolate(progress.value, [0, 0.5, 1], [1, 1.1, 1], Extrapolation.CLAMP), }, ], })); const sizeConfig = { sm: { width: 24, fontSize: 12, iconSize: "sm" as const }, md: { width: 32, fontSize: 14, iconSize: "md" as const }, lg: { width: 40, fontSize: 16, iconSize: "lg" as const }, }; const { width, fontSize, iconSize } = sizeConfig[size]; const getStatusColors = () => { switch (status) { case "completed": return { background: theme.colors.success.DEFAULT, border: theme.colors.success.DEFAULT, text: theme.colors.text.inverse, }; case "active": return { background: theme.colors.primary[500], border: theme.colors.primary[500], text: theme.colors.text.inverse, }; case "error": return { background: theme.colors.error[50], border: theme.colors.error.DEFAULT, text: theme.colors.error.DEFAULT, }; case "disabled": return { background: theme.colors.surface.button, border: theme.colors.border.DEFAULT, text: theme.colors.text.disabled, }; case "pending": default: return { background: "transparent", border: theme.colors.border.DEFAULT, text: theme.colors.text.tertiary, }; } }; const colors = getStatusColors(); const renderContent = () => { if (status === "completed") { return <Icon name={icon || "check"} size={iconSize} color={theme.colors.text.inverse} />; } if (status === "error") { return <Text style={[styles.stepNumber, { fontSize, color: colors.text }]}>!</Text>; } if (status === "active" && variant === "numbers") { return <Text style={[styles.stepNumber, { fontSize, color: colors.text }]}>{index + 1}</Text>; } if (variant === "dots") { return null; } return <Text style={[styles.stepNumber, { fontSize, color: colors.text }]}>{index + 1}</Text>; }; const indicatorContent = ( <Animated.View style={[ styles.indicator, { width, height: width, borderRadius: width / 2, backgroundColor: colors.background, borderColor: colors.border, }, variant === "dots" && status === "pending" && styles.dotIndicator, animatedStyle, ]} > {renderContent()} </Animated.View> ); if (variant === "dots" && !title) { return indicatorContent; } return ( <Pressable onPress={onPress} disabled={disabled || !onPress} style={styles.stepContainer} accessibilityLabel="Interactive control" accessibilityRole="button" accessibilityHint="Activates this control"> {indicatorContent}
{(title || (description && showDescription)) && ( <View style={styles.stepTextContainer}> {title && ( <Text variant="body" style={[ styles.stepTitle, { color: status === "active" ? theme.colors.text.primary : status === "disabled" || status === "pending" ? theme.colors.text.tertiary : theme.colors.text.secondary, }, ]} > {title} </Text> )} {description && showDescription && ( <Text variant="caption" color={status === "error" ? "error.DEFAULT" : "text.tertiary"} style={styles.stepDescription}> {description} </Text> )} </View> )} </Pressable> ); }; const Connector: React.FC<{ completed: boolean; orientation: "horizontal" | "vertical"; theme: Theme; }> = ({ completed, orientation, theme }) => { const progress = useSharedValue(completed ? 1 : 0); React.useEffect(() => { progress.value = withTiming(completed ? 1 : 0, { duration: 300 }); }, [completed, progress]); const animatedStyle = useAnimatedStyle(() => ({ backgroundColor: interpolateColor(progress.value, [0, 1], [theme.colors.border.DEFAULT, theme.colors.success.DEFAULT]), })); return <Animated.View style={[styles.connector, orientation === "horizontal" ? styles.connectorHorizontal : styles.connectorVertical, animatedStyle]} />; }; export const ProgressSteps: React.FC<ProgressStepsProps> = ({ steps, currentStep, orientation = "horizontal", size = "md", variant = "default", showDescriptions = true, allowClick = false, onStepPress, style }) => { const { theme } = useTheme(); const handleStepPress = useCallback( (index: number) => { if (allowClick && onStepPress) { onStepPress(index, steps[index]!); } }, [allowClick, onStepPress, steps], ); const containerStyle = [styles.container, orientation === "horizontal" ? styles.containerHorizontal : styles.containerVertical, style]; return ( <View style={containerStyle}> {steps.map((step, index) => { const isLast = index === steps.length - 1; const isCompleted = index < currentStep || step.status === "completed"; const isActive = index === currentStep || step.status === "active"; let effectiveStatus: StepStatus = step.status; if (!effectiveStatus || effectiveStatus === "pending") { if (isCompleted) { effectiveStatus = "completed"; } else if (isActive) { effectiveStatus = "active"; } else { effectiveStatus = "pending"; } } return ( <React.Fragment key={step.id}> <StepIndicator status={effectiveStatus} index={index} size={size} variant={variant} icon={step.icon} title={step.title} description={step.description} showDescription={showDescriptions} onPress={allowClick ? () => handleStepPress(index) : undefined} disabled={step.disabled} />
{!isLast && <Connector completed={isCompleted} orientation={orientation} theme={theme} />} </React.Fragment> ); })} </View> ); }; function interpolateColor(progress: number, _inputRange: [number, number], [start, end]: [string, string]): string { "worklet"; return progress > 0.5 ? end : start; } const styles = createSheet({ container: { flexDirection: "row" }, containerHorizontal: { flexDirection: "row", alignItems: "flex-start", justifyContent: "space-between", }, containerVertical: { flexDirection: "column", alignItems: "flex-start" }, stepContainer: { alignItems: "center" }, indicator: { justifyContent: "center", alignItems: "center", borderWidth: 2 }, dotIndicator: { borderWidth: 0, width: 8, height: 8 }, stepNumber: { fontWeight: "700" }, stepTextContainer: { marginTop: 8, alignItems: "center", maxWidth: 100 }, stepTitle: { fontWeight: "600", textAlign: "center" }, stepDescription: { marginTop: 4, textAlign: "center" }, connector: {}, connectorHorizontal: { flex: 1, height: 2, marginHorizontal: 8, marginTop: 15, }, connectorVertical: { width: 2, height: 24, marginVertical: 4, marginLeft: 15, }, }); export default ProgressSteps;

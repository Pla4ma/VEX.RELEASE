import React, { useCallback, useRef, useState } from "react"; import { View, ViewStyle, RefreshControl, ActivityIndicator, Pressable } from "react-native"; import { FlashList, type ListRenderItem } from "@shopify/flash-list"; import { Text } from "../../../components/primitives/Text"; import { EmptyState, ErrorState, Skeleton } from "../state-components"; import { useTheme, type Theme } from "../../../theme"; import { createSheet } from "@/shared/ui/create-sheet"; import { launchColors } from "@theme/tokens/launch-colors";
export interface DataListItem<T> { id: string; data: T; disabled?: boolean; loading?: boolean; } export interface DataListSection<T> { title: string; data: DataListItem<T>[]; key?: string; } export type DataListProps<T> = { items: DataListItem<T>[]; sections?: DataListSection<T>[]; renderItem: (item: T, index: number, context: { isSelected: boolean; isDragging: boolean; onSelect: () => void }) => React.ReactElement; renderSectionHeader?: (section: DataListSection<T>) => React.ReactElement; renderEmpty?: () => React.ReactElement; renderFooter?: () => React.ReactElement; keyExtractor?: (item: T, index: number) => string; loading?: boolean; loadingMore?: boolean; error?: Error | null; refreshing?: boolean; emptyTitle?: string; emptySubtitle?: string; emptyIcon?: string; selectionMode?: "none" | "single" | "multiple"; selectedIds?: Set<string>; onSelectionChange?: (ids: Set<string>) => void; onRefresh?: () => void | Promise<void>; onLoadMore?: () => void | Promise<void>; onRetry?: () => void; onItemPress?: (item: T, index: number) => void; estimatedItemSize: number; stickySectionHeadersEnabled?: boolean; showsVerticalScrollIndicator?: boolean; contentContainerStyle?: ViewStyle; style?: ViewStyle; accessibilityLabel?: string; getItemAccessibilityLabel?: (item: T) => string; }; const ListFooter: React.FC<{ loading: boolean; hasMore?: boolean; theme: Theme; }> = ({ loading, hasMore = true, theme }) => { if (!loading && !hasMore) { return ( <View style={styles.footerContainer}> <Text variant="caption" color="text.tertiary"> No more items </Text> </View> ); } if (loading) { return ( <View style={styles.footerContainer}> <ActivityIndicator size="small" color={theme.colors.primary[500]} /> <Text variant="caption" color="text.secondary" style={styles.footerText}> Loading more... </Text> </View> ); } return null; }; export function DataList<T extends Record<string, unknown>>({ items, sections, renderItem, renderSectionHeader: _renderSectionHeader, renderEmpty, renderFooter, keyExtractor, loading = false, loadingMore = false, error = null, refreshing = false, emptyTitle = "No items found", emptySubtitle = "Try adjusting your filters or check back later", emptyIcon = "📭", selectionMode = "none", selectedIds = new Set(), onSelectionChange, onRefresh, onLoadMore, onRetry, onItemPress, estimatedItemSize, stickySectionHeadersEnabled = false, showsVerticalScrollIndicator = true, contentContainerStyle, style, accessibilityLabel, getItemAccessibilityLabel }: DataListProps<T>) { const { theme } = useTheme(); const listRef = useRef<unknown>(null); const [internalSelectedIds, setInternalSelectedIds] = useState<Set<string>>(new Set()); const effectiveSelectedIds = onSelectionChange ? selectedIds : internalSelectedIds; const handleSelect = useCallback( (id: string) => { const newSet = new Set(effectiveSelectedIds); if (selectionMode === "single") { if (newSet.has(id)) { newSet.clear(); } else { newSet.clear(); newSet.add(id); } } else if (selectionMode === "multiple") { if (newSet.has(id)) { newSet.delete(id); } else { newSet.add(id); } } if (onSelectionChange) { onSelectionChange(newSet); } else { setInternalSelectedIds(newSet); } }, [effectiveSelectedIds, selectionMode, onSelectionChange], ); const handleItemPress = useCallback( (item: T, index: number, id: string) => { if (selectionMode !== "none") { handleSelect(id); } else if (onItemPress) { onItemPress(item, index); } }, [selectionMode, handleSelect, onItemPress], ); const renderItemWrapper: ListRenderItem<DataListItem<T>> = useCallback( ({ item, index }) => { const isSelected = effectiveSelectedIds.has(item.id); const context = { isSelected, isDragging: false, onSelect: () => handleSelect(item.id), }; return ( <Pressable onPress={() => handleItemPress(item.data, index, item.id)} disabled={item.disabled} accessibilityLabel={getItemAccessibilityLabel?.(item.data)} accessibilityState={{ selected: isSelected, disabled: item.disabled }} accessibilityRole="button" accessibilityHint="Activates this control"> <View style={[item.loading && styles.itemLoading]}> {renderItem(item.data, index, context)} {item.loading && ( <View style={styles.itemLoadingOverlay}> <ActivityIndicator size="small" color={theme.colors.primary[500]} /> </View> )} </View> </Pressable> ); }, [renderItem, effectiveSelectedIds, handleSelect, handleItemPress, getItemAccessibilityLabel, theme], ); const resolvedKeyExtractor = useCallback( (item: DataListItem<T>, index: number) => { return keyExtractor ? keyExtractor(item.data, index) : item.id; }, [keyExtractor], ); if (loading && items.length === 0) { return ( <View style={[styles.container, style]}> <Skeleton variant="list" count={6} height={estimatedItemSize} /> </View> ); } if (error && items.length === 0) { return ( <View style={[styles.container, style]}> <ErrorState error={error} onRetry={onRetry} title="Failed to load" /> </View> ); } if (items.length === 0 && !loading) { if (renderEmpty) {
return <View style={[styles.container, style]}>{renderEmpty()}</View>; } return ( <View style={[styles.container, style]}> <EmptyState icon={emptyIcon} title={emptyTitle} subtitle={emptySubtitle} actionLabel={onRetry ? "Try Again" : undefined} onAction={onRetry} /> </View> ); } const refreshControl = onRefresh ? <RefreshControl refreshing={refreshing} onRefresh={onRefresh} tintColor={theme.colors.primary[500]} colors={[theme.colors.primary[500]]} /> : undefined; return ( <View style={[styles.container, style]} accessibilityLabel={accessibilityLabel}> <FlashList ref={listRef} data={items} renderItem={renderItemWrapper} keyExtractor={resolvedKeyExtractor} estimatedItemSize={estimatedItemSize} refreshControl={refreshControl} onEndReached={onLoadMore} onEndReachedThreshold={0.5} ListFooterComponent={() => ( <> <ListFooter loading={loadingMore} theme={theme} /> {renderFooter?.()} </> )} showsVerticalScrollIndicator={showsVerticalScrollIndicator} contentContainerStyle={contentContainerStyle} stickyHeaderIndices={ stickySectionHeadersEnabled && sections ? sections.reduce((indices: number[], section, index) => { const sectionStart = sections.slice(0, index).reduce((sum, s) => sum + s.data.length, 0) + index; indices.push(sectionStart); return indices; }, []) : undefined } disableAutoLayout={false} removeClippedSubviews={true} /> </View> ); } export const SelectionToolbar: React.FC<{ selectedCount: number; totalCount: number; onClear: () => void; onSelectAll: () => void; actions?: Array<{ icon: string; label: string; onPress: () => void; destructive?: boolean; }>; }> = ({ selectedCount, totalCount, onClear, onSelectAll, actions }) => { const { theme } = useTheme(); return ( <View style={[styles.toolbar, { backgroundColor: theme.colors.background.secondary }]}> <View style={styles.toolbarLeft}> <Text variant="body" color="text.primary"> {selectedCount} selected </Text> <Pressable onPress={selectedCount === totalCount ? onClear : onSelectAll} accessibilityLabel="Interactive control" accessibilityRole="button" accessibilityHint="Activates this control"> <Text variant="caption" color="primary.500"> {selectedCount === totalCount ? "Clear" : "Select All"} </Text> </Pressable> </View>
{actions && ( <View style={styles.toolbarActions}> {actions.map((action, index) => ( <Pressable key={index} onPress={action.onPress} style={[ styles.toolbarAction, action.destructive && { backgroundColor: theme.colors.error.DEFAULT + "20", }, ]} accessibilityLabel="Interactive control" accessibilityRole="button" accessibilityHint="Activates this control" > <Text variant="caption" color={action.destructive ? "error.DEFAULT" : "primary.500"}> {action.label} </Text> </Pressable> ))} </View> )} </View> ); }; const styles = createSheet({ container: { flex: 1 }, itemLoading: { opacity: 0.7 }, itemLoadingOverlay: { position: "absolute", top: 0, right: 0, bottom: 0, left: 0, justifyContent: "center", alignItems: "center", backgroundColor: launchColors.rgb_0_0_0_0_3, }, refreshIndicator: { height: 60, justifyContent: "center", alignItems: "center", }, refreshArrow: { width: 20, height: 20, borderWidth: 2, borderRadius: 10, borderTopColor: "transparent", borderLeftColor: "transparent", }, footerContainer: { padding: 16, alignItems: "center", flexDirection: "row", justifyContent: "center", gap: 8, }, footerText: { marginLeft: 8 }, toolbar: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", padding: 12, borderTopWidth: 1, borderTopColor: launchColors.rgb_0_0_0_0_1, }, toolbarLeft: { flexDirection: "row", alignItems: "center", gap: 12 }, toolbarActions: { flexDirection: "row", gap: 8 }, toolbarAction: { paddingHorizontal: 12, paddingVertical: 6, borderRadius: 6 }, }); export default DataList;

import{z}from'zod'; import{getSupabaseClient}from'../../config/supabase'; import{createDebugger}from'../../utils/debug'; const debug = createDebugger('notifications:smart-scheduler'); const ANALYSIS_WINDOW_DAYS = 14; const DEFAULT_PEAK_HOUR = 19; const MAX_NOTIFICATIONS_PER_DAY = 1; export const PeakFocusWindowSchema = z.object({userId:z.string(),peakHour:z.number().min(0).max(23),confidence:z.number().min(0).max(1),sessionCount:z.number(),pattern:z.enum(['CONSISTENT','VARIABLE','ERRATIC','NEW']),hourDistribution:z.record(z.number(),z.number())}); export type PeakFocusWindow=z.infer<typeof PeakFocusWindowSchema>; export const NotificationContentTypeSchema = z.enum(['STREAK','BOSS','SOCIAL','POSITIVE','COMEBACK','RANK_REPORT']); export type NotificationContentType=z.infer<typeof NotificationContentTypeSchema>; export const SmartNotificationConfigSchema = z.object({userId:z.string(),peakWindow:PeakFocusWindowSchema,lastNotificationSent:z.number().optional(),notificationCountToday:z.number().default(0),preferredContentTypes:z.array(NotificationContentTypeSchema).default(['STREAK','BOSS','SOCIAL','POSITIVE'])}); export type SmartNotificationConfig=z.infer<typeof SmartNotificationConfigSchema>; export async function analyzePeakFocusWindow(userId:string):Promise<PeakFocusWindow>{try{const fromDate = new Date(); fromDate.setDate(fromDate.getDate() - ANALYSIS_WINDOW_DAYS); const{data:sessions,error} = await getSupabaseClient().from('sessions').select('started_at, timezone').eq('user_id',userId).eq('status','COMPLETED').gte('started_at',fromDate.toISOString()).order('started_at',{ascending:false}); if(error || !sessions || sessions.length === 0){return{userId,peakHour:DEFAULT_PEAK_HOUR,confidence:0,sessionCount:0,pattern:'NEW',hourDistribution:{}};}const hourDistribution:Record<number,number> = {}; for(const session of sessions){const timezone = session.timezone || 'UTC'; const sessionDate = new Date(session.started_at); const hourString = sessionDate.toLocaleString('en-US',{timeZone:timezone,hour:'numeric',hour12:false}); const hour = parseInt(hourString,10); hourDistribution[hour] = (hourDistribution[hour] || 0) + 1;}let peakHour = DEFAULT_PEAK_HOUR; let maxCount = 0; for(const[hour,count]of Object.entries(hourDistribution)){if(count > maxCount){maxCount = count; peakHour = parseInt(hour,10);}}const totalSessions = sessions.length; const peakRatio = maxCount / totalSessions; const confidence = Math.min(peakRatio,1); let pattern:PeakFocusWindow['pattern'] = 'CONSISTENT'; if(totalSessions < 5){pattern = 'NEW';}else if(confidence < 0.3){pattern = 'ERRATIC';}else if(confidence < 0.6){pattern = 'VARIABLE';}return{userId,peakHour,confidence,sessionCount:totalSessions,pattern,hourDistribution};}catch(error){debug.error('Error analyzing peak focus window',error instanceof Error ? error : undefined); return{userId,peakHour:DEFAULT_PEAK_HOUR,confidence:0,sessionCount:0,pattern:'NEW',hourDistribution:{}};}}export function isInPeakWindow(peakHour:number,windowSize = 2):boolean{const now = new Date(); const currentHour = now.getHours(); const diff = Math.abs(currentHour - peakHour); return diff <= windowSize;}interface NotificationContent{title:string;body:string;data:Record<string,unknown>;}async function generateStreakNotification(userId:string):Promise<NotificationContent|null>{try{const{data:streak,error} = await getSupabaseClient().from('user_streaks').select('current_streak').eq('user_id',userId).single(); if(error || !streak){return null;}const streakDays = streak.current_streak; if(streakDays === 0){return{title:'Start your streak today 🔥',body:'Day 1 is the most important. Complete a session to begin!',data:{type:'STREAK_START'}};}return{title:`Your ${streakDays}-day streak continues 🔥`,body:"Complete today's session to keep it alive!",data:{type:'STREAK_CONTINUE',streakDays}};}catch(error){return null;}}async function generateBossNotification(userId:string):Promise<NotificationContent|null>{try{const{data:encounter,error} = await getSupabaseClient().from('boss_encounters').select('boss_name, current_health, max_health').eq('user_id',userId).eq('status','ACTIVE').order('created_at',{ascending:false}).limit(1).maybeSingle(); if(error || !encounter){return null;}const healthPercent = encounter.current_health / encounter.max_health * 100; if(healthPercent <= 25){return{title:'👹 Boss almost defeated!',body:`${encounter.boss_name} has ${healthPercent.toFixed(0)}% health. One more session could finish it!`,data:{type:'BOSS_KILLING_BLOW',bossName:encounter.boss_name}};}return{title:'Boss needs your attention 👹',body:`${encounter.boss_name} has ${healthPercent.toFixed(0)}% health remaining. Deal some damage!`,data:{type:'BOSS_DAMAGE',bossName:encounter.boss_name,healthPercent}};}catch(error){return null;}}async function generateSocialNotification(userId:string):Promise<NotificationContent|null>{try{const{data:rival,error} = await getSupabaseClient().from('rivals').select('rival_name, rival_minutes, my_minutes').eq('user_id',userId).eq('is_active',true).maybeSingle(); if(error || !rival){return null;}const gap = rival.rival_minutes - rival.my_minutes; if(gap > 0){return{title:`⚔️ ${rival.rival_name} is ahead!`,body:`They're ${gap} minutes ahead this week. Time to focus and close the gap!`,data:{type:'RIVAL_AHEAD',rivalName:rival.rival_name,gap}};}return{title:`⚔️ You're beating ${rival.rival_name}!`,body:`You're ${Math.abs(gap)} minutes ahead. Keep the lead!`,data:{type:'RIVAL_LEADING',rivalName:rival.rival_name}};}catch(error){return null;}}async function generatePositiveNotification(userId:string):Promise<NotificationContent|null>{try{const weekStart = new Date(); weekStart.setDate(weekStart.getDate() - 7); const{data:sessions,error} = await getSupabaseClient().from('sessions').select('duration_seconds').eq('user_id',userId).eq('status','COMPLETED').gte('completed_at',weekStart.toISOString()); if(error || !sessions || sessions.length === 0){return null;}const totalMinutes = sessions.reduce((sum,s)=>sum + (s.duration_seconds || 0) / 60,0); return{title:'Great momentum this week 📈',body:`You've focused for ${Math.round(totalMinutes)} minutes this week. Today's session keeps it going!`,data:{type:'MOMENTUM',totalMinutes}};}catch(error){return null;}}async function generateRankReportNotification(userId:string):Promise<NotificationContent|null>{try{const now = new Date(); const dayOfWeek = now.getDay(); const hour = now.getHours(); if(dayOfWeek !== 0 || hour < 19 || hour >= 20){return null;}const weekStart = new Date(); weekStart.setDate(weekStart.getDate() - 7); const{data:sessions,error} = await getSupabaseClient().from('sessions').select('duration_seconds').eq('user_id',userId).eq('status','COMPLETED').gte('completed_at',weekStart.toISOString()); if(error){return null;}const weeklyMinutes = (sessions || []).reduce((sum,s)=>sum + (s.duration_seconds || 0) / 60,0); const{data:leaderboard,error:lbError} = await getSupabaseClient().from('weekly_leaderboard').select('user_id, focus_minutes').order('focus_minutes',{ascending:false}); if(lbError || !leaderboard){return null;}const userIndex = leaderboard.findIndex(entry=>entry.user_id === userId); const totalUsers = leaderboard.length; const rankPosition = userIndex >= 0 ? userIndex + 1 : totalUsers; const percentile = totalUsers > 0 ? (totalUsers - rankPosition) / totalUsers * 100 : 0; let tier:string; let tierIcon:string; let sessionsToNext:number|null = null; if(percentile >= 99){tier = 'LEGEND'; tierIcon = '👑';}else if(percentile >= 95){tier = 'DIAMOND'; tierIcon = '💎'; sessionsToNext = Math.ceil((0.99 * totalUsers - (totalUsers - rankPosition)) * 25);}else if(percentile >= 90){tier = 'PLATINUM'; tierIcon = '⭐'; sessionsToNext = Math.ceil((0.95 * totalUsers - (totalUsers - rankPosition)) * 25);}else if(percentile >= 75){tier = 'GOLD'; tierIcon = '🥇'; sessionsToNext = Math.ceil((0.9 * totalUsers - (totalUsers - rankPosition)) * 25);}else if(percentile >= 50){tier = 'SILVER'; tierIcon = '🥈'; sessionsToNext = Math.ceil((0.75 * totalUsers - (totalUsers - rankPosition)) * 25);}else{tier = 'BRONZE'; tierIcon = '🥉'; sessionsToNext = Math.ceil((0.5 * totalUsers - (totalUsers - rankPosition)) * 25);}let body = `You're in ${tier} tier this week — top ${Math.round(percentile)}% of VEX users. Keep the focus Sunday!`; if(sessionsToNext && sessionsToNext > 0){body += ` About ${sessionsToNext} more sessions to reach the next tier.`;}return{title:`Weekly Rank: ${tierIcon} ${tier}`,body,data:{type:'RANK_REPORT',tier,percentile:Math.round(percentile),weeklyMinutes:Math.round(weeklyMinutes),rankPosition,totalUsers}};}catch(error){return null;}}async function generateComebackNotification(userId:string):Promise<NotificationContent|null>{try{const{data:quest,error} = await getSupabaseClient().from('comeback_quests').select('stage, days_absent').eq('user_id',userId).eq('all_quests_completed',false).order('created_at',{ascending:false}).limit(1).maybeSingle(); if(error || !quest){return null;}const stageNum = quest.stage === 'QUEST_1' ? 1 : quest.stage === 'QUEST_2' ? 2 : 3; return{title:'🔥 Your comeback continues!',body:`You're on Quest ${stageNum}/3. Complete today's session to progress!`,data:{type:'COMEBACK_QUEST',stage:stageNum}};}catch(error){return null;}}async function selectNotificationType(userId:string,preferredTypes:NotificationContentType[]):Promise<NotificationContent|null>{const typeGenerators:Record<NotificationContentType,()=>Promise<NotificationContent|null>> = {COMEBACK:()=>generateComebackNotification(userId),BOSS:()=>generateBossNotification(userId),STREAK:()=>generateStreakNotification(userId),SOCIAL:()=>generateSocialNotification(userId),POSITIVE:()=>generatePositiveNotification(userId),RANK_REPORT:()=>generateRankReportNotification(userId)}; for(const type of preferredTypes){const generator = typeGenerators[type]; if(generator){const content = await generator(); if(content){return content;}}}return null;}async function checkRateLimit(userId:string):Promise<boolean>{try{const today = new Date(); today.setHours(0,0,0,0); const{count,error} = await getSupabaseClient().from('notifications_sent').select('*',{count:'exact',head:true}).eq('user_id',userId).eq('notification_type','SMART_REMINDER').gte('sent_at',today.toISOString()); if(error){debug.warn('Error checking rate limit',error); return false;}return(count ?? 0) < MAX_NOTIFICATIONS_PER_DAY;}catch(error){return false;}}async function recordNotificationSent(userId:string):Promise<void>{try{await getSupabaseClient().from('notifications_sent').insert({user_id:userId,notification_type:'SMART_REMINDER',sent_at:new Date().toISOString()});}catch(error){debug.warn('Failed to record notification',error instanceof Error ? error : undefined);}}export async function processSmartNotifications():Promise<void>{try{const{data:users,error} = await getSupabaseClient().from('users').select('id, timezone').eq('notifications_enabled',true); if(error || !users){debug.error('Failed to fetch users',error instanceof Error ? error : undefined); return;}for(const user of users){await processUserSmartNotification(user.id,user.timezone || 'UTC');}}catch(error){debug.error('Error processing smart notifications',error instanceof Error ? error : undefined);}}export async function processUserSmartNotification(userId:string,timezone:string):Promise<void>{try{const canSend = await checkRateLimit(userId); if(!canSend){debug.info('Rate limit reached for user',{userId}); return;}const peakWindow = await analyzePeakFocusWindow(userId); if(!isInPeakWindow(peakWindow.peakHour)){debug.info('Not in peak window',{userId,peakHour:peakWindow.peakHour}); return;}const content = await selectNotificationType(userId,['COMEBACK','BOSS','STREAK','SOCIAL','POSITIVE']); if(!content){debug.info('No notification content generated',{userId}); return;}debug.info('Would send smart notification',{userId,title:content.title}); await recordNotificationSent(userId);}catch(error){debug.error('Error processing user notification',error instanceof Error ? error : undefined);}}import{useState,useEffect,useCallback}from'react'; interface UseSmartNotificationsResult{peakWindow:PeakFocusWindow|null;isLoading:boolean;canSendNotification:boolean;refresh:()=>void;}export function useSmartNotifications(userId:string|undefined):UseSmartNotificationsResult{const[peakWindow,setPeakWindow] = useState<PeakFocusWindow|null>(null); const[isLoading,setIsLoading] = useState(true); const[canSend,setCanSend] = useState(false); const refresh = useCallback(async()=>{if(!userId){setIsLoading(false); return;}setIsLoading(true); const window = await analyzePeakFocusWindow(userId); setPeakWindow(window); const inWindow = isInPeakWindow(window.peakHour); const underLimit = await checkRateLimit(userId); setCanSend(inWindow && underLimit); setIsLoading(false);},[userId]); useEffect(()=>{refresh();},[refresh]); return{peakWindow,isLoading,canSendNotification:canSend,refresh};}export default{analyzePeakFocusWindow,isInPeakWindow,selectNotificationType,processSmartNotifications,processUserSmartNotification,useSmartNotifications,MAX_NOTIFICATIONS_PER_DAY};

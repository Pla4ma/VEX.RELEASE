/**
 * Currency Boundaries Configuration - Barrel File
 *
 * Phase 6.03 - Currency And Monetization Boundaries
 * Re-exports configuration from split modules
 * Maintains backward compatibility while complying with file size limits
 */

export {
  DEFAULT_CURRENCY_LIMITS,
} from './limits-config';

export {
  DEFAULT_PROTECTION_RULES,
  DEFAULT_MONETIZATION_BOUNDARIES,
  DEFAULT_CURRENCY_BOUNDARIES_CONFIG,
} from './rules-config';

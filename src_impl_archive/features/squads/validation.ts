import{captureSilentFailure}from'../../utils/silent-failure'; import{z}from'zod'; import{CreateSquadInputSchema,type CreateSquadInput,type InviteToSquadInput,type Squad,type SquadRole,type UpdateSquadInput}from'./schemas'; const ROLE_HIERARCHY:SquadRole[] = ['GUEST','MEMBER','MODERATOR','ADMIN','FOUNDER']; export const SquadValidationRules = {createSquad:(input:CreateSquadInput)=>{const extendedSchema = CreateSquadInputSchema.extend({name:z.string().min(3,'Squad name must be at least 3 characters').max(50,'Squad name must be at most 50 characters').regex(/^[a-zA-Z0-9\s-_]+$/,'Squad name can only contain letters, numbers, spaces, hyphens and underscores').trim(),description:z.string().max(500,'Description must be at most 500 characters').nullable().optional(),maxMembers:z.number().int().min(2,'Squad must allow at least 2 members').max(20,'Squad cannot exceed 20 members').default(5),minLevelRequirement:z.number().int().min(1,'Minimum level must be at least 1').max(100,'Minimum level cannot exceed 100').nullable().optional()}); return extendedSchema.safeParse(input);},updateSquad:(current:Squad,updates:UpdateSquadInput,memberCount:number):{valid:boolean;errors:string[];warnings:string[];}=>{const errors:string[] = []; const warnings:string[] = []; if(typeof updates.maxMembers === 'number'){if(updates.maxMembers < memberCount){errors.push(`Cannot reduce max members below current count (${memberCount})`);}if(updates.maxMembers > 20){errors.push('Max members cannot exceed 20');}}if(updates.joinRequirements === 'OPEN' && current.memberCount > 10){warnings.push('Opening squad to public with many members may attract unwanted join requests');}if(typeof updates.description === 'string' && updates.description.length > 500){errors.push('Description cannot exceed 500 characters');}if(typeof updates.avatarUrl === 'string' && updates.avatarUrl.length > 0){try{new URL(updates.avatarUrl);}catch(error){captureSilentFailure(error,{feature:'squads',operation:'safe-fallback',type:'data'}); errors.push('Invalid avatar URL format');}}return{valid:errors.length === 0,errors,warnings};},validateInvite:(input:InviteToSquadInput,context:{existingPendingInvite?:{createdAt:number;}|null;inviterRole:SquadRole;offeredRole:SquadRole;squadMemberCount:number;squadMaxMembers:number;isSelfInvite:boolean;targetAlreadyMember:boolean;}):{valid:boolean;errors:string[];}=>{const errors:string[] = []; if(context.squadMemberCount >= context.squadMaxMembers){errors.push('Squad is at maximum capacity');}if(context.isSelfInvite){errors.push('Cannot invite yourself');}if(context.targetAlreadyMember){errors.push('User is already a member of this squad');}if(ROLE_HIERARCHY.indexOf(context.offeredRole) > ROLE_HIERARCHY.indexOf(context.inviterRole)){errors.push('Cannot offer a role higher than your own');}if(context.existingPendingInvite){const hoursSinceLastInvite = (Date.now() - context.existingPendingInvite.createdAt) / (1000 * 60 * 60); if(hoursSinceLastInvite < 1){errors.push('Please wait at least 1 hour before sending another invite to the same user');}}if(input.expiresInHours !== undefined){if(input.expiresInHours < 1){errors.push('Invite must be valid for at least 1 hour');}if(input.expiresInHours > 168){errors.push('Invite cannot be valid for more than 7 days');}}return{valid:errors.length === 0,errors};},validateRoleTransfer:(currentRole:SquadRole,targetRole:SquadRole,isFounderTransfer:boolean):{valid:boolean;error?:string;}=>{const currentIndex = ROLE_HIERARCHY.indexOf(currentRole); const targetIndex = ROLE_HIERARCHY.indexOf(targetRole); if(isFounderTransfer){return{valid:true};}if(targetIndex > currentIndex){return{valid:false,error:`Cannot assign ${targetRole} role - higher than your ${currentRole} role`};}return{valid:true};},validateSessionConfig:(config:{type:string;duration:number;maxParticipants?:number;}):{valid:boolean;errors:string[];}=>{const errors:string[] = []; if(!['focus','break','collaboration'].includes(config.type)){errors.push('Invalid session type');}if(config.duration < 60 || config.duration > 7200){errors.push('Session duration must be between 1 minute and 2 hours');}if(config.maxParticipants !== undefined && (config.maxParticipants < 2 || config.maxParticipants > 20)){errors.push('Session participants must be between 2 and 20');}return{valid:errors.length === 0,errors};},validateContribution:(points:number,member:{role:string;isActive:boolean;}):boolean=>{if(!member.isActive){return false;}if(points <= 0){return false;}if(points > 10000){return false;}return true;}}; export const SquadBusinessRules = {shouldAutoArchive:(squad:Squad,members:Array<{lastActiveAt:number;}>):boolean=>{const INACTIVE_THRESHOLD_DAYS = 30; const MINIMUM_MEMBERS_FOR_ACTIVE = 2; if(members.length < MINIMUM_MEMBERS_FOR_ACTIVE){return true;}const now = Date.now(); const activeMembers = members.filter(member=>now - member.lastActiveAt < INACTIVE_THRESHOLD_DAYS * 24 * 60 * 60 * 1000); return activeMembers.length < MINIMUM_MEMBERS_FOR_ACTIVE;},calculateOptimalSessionTime:(_memberTimezones:string[]):Date=>{const now = new Date(); const optimalHour = 14; const optimal = new Date(now); optimal.setUTCHours(optimalHour,0,0,0); if(optimal < now){optimal.setDate(optimal.getDate() + 1);}return optimal;},shouldAutoKick:(member:{joinedAt:number;lastContributionAt?:number|null;role:string;}):{shouldKick:boolean;reason?:string;}=>{const now = Date.now(); const DAYS_14 = 14 * 24 * 60 * 60 * 1000; const DAYS_7 = 7 * 24 * 60 * 60 * 1000; if(['FOUNDER','ADMIN'].includes(member.role)){return{shouldKick:false};}if(!member.lastContributionAt && now - member.joinedAt > DAYS_7){return{shouldKick:true,reason:'No activity for 7 days after joining'};}if(member.lastContributionAt && now - member.lastContributionAt > DAYS_14){return{shouldKick:true,reason:'No activity for 14 days'};}return{shouldKick:false};},isSquadNameValid:(name:string,existingNames:string[]):boolean=>{const normalized = name.toLowerCase().trim(); const normalizedExisting = existingNames.map(existing=>existing.toLowerCase().trim()); if(normalizedExisting.includes(normalized)){return false;}const profanityList = ['badword1','badword2']; if(profanityList.some(word=>normalized.includes(word))){return false;}return true;}};
